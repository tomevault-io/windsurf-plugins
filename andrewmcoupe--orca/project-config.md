---
trigger: always_on
description: Use kebab-case for filenames.
---

## Repository Rules

Use kebab-case for filenames.

Prefer a TDD workflow: red, green, refactor.

For the React frontend, use TanStack Query for network requests and server-state management.

Use XState for complex state. Avoid `useEffect` where a clearer declarative pattern, derived state, TanStack Query, event handler, or state machine can solve the problem.

Co-locate frontend test files with the implementation they cover.

## Typography

Sans (IBM Plex Sans). Default voice for everything in the app — UI chrome, page titles, body text, form labels, button text, navigation, status badges, section headers, prose written or edited by the user, and any prose that isn't covered by the mono or serif rules below. If you're unsure which typeface to use, the answer is sans.

Mono (IBM Plex Mono). Reserved for code-related content: file paths (src/lib/favourites.tsx), code identifiers when appearing inline in prose (toggleFavourite, makeProgram), commit SHAs (2a823bb6), command strings (pnpm install --frozen-lockfile), event names and IDs (AuditorVerdictRendered, pr_01KQQ), code blocks, structured machine output, timestamps with fixed format (17:43:03), and numeric data with fixed format (95%, +12 -4). Use mono whenever the content is something a developer would type into a terminal, paste into a file, or read from machine output.

Serif (IBM Plex Serif). Reserved exclusively for the auditor's continuous prose judgement — specifically, the verdict summary paragraph in the auditor verdict card. Not for concern rationales, not for briefing drafts, not for plan descriptions, not for any other prose in the app, even if it was generated by an LLM. The serif appears in exactly one context, consistently, to mark the auditor's voice as distinct from app chrome and from code. If a new feature produces continuous prose from an auditor-style judgement (e.g. an auditor's pass-back explanation), serif applies. Otherwise, sans.

---
> Source: [andrewmcoupe/orca](https://github.com/andrewmcoupe/orca) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-23 -->
