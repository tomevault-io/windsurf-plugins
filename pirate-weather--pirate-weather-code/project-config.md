---
trigger: always_on
description: This repository uses `ruff` for linting and formatting. Run lint checks with:
---

# AGENTS Instructions

This repository uses `ruff` for linting and formatting. Run lint checks with:

```bash
scripts/lint
```

Format the code with:

```bash
scripts/format
```

Run the test suite using `pytest` before submitting changes:

```bash
pytest
```

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/Pirate-Weather)
> This is a context snippet only. You'll also want the standalone SKILL.md file — [download at TomeVault](https://tomevault.io/claim/Pirate-Weather)
<!-- tomevault:4.0:windsurf_rules:2026-04-07 -->
