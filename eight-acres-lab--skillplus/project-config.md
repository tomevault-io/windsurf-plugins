---
trigger: always_on
description: Guidance for Claude Code (claude.ai/code) when working in this repo.
---

# CLAUDE.md — skillplus

Guidance for Claude Code (claude.ai/code) when working in this repo.

## What this is

`@e8s/skillplus` is a compilable skill package format + a TypeScript reference compiler + a curated catalog of community skills. The compiler turns a `.skillplus` directory into one of several target formats. It is deterministic and never calls a model.

## Layout

```
package.json              # @e8s/skillplus, type:module, bin: skillplus
tsconfig.json
bin/skillplus.js          # ESM shim → dist/cli.js
src/
  index.ts                # public API exports (compilePackage, renderTarget, …)
  cli.ts                  # subcommand routing + backwards-compat injection
  compile.ts              # the compiler (~400 lines, monolithic by design)
  resolve.ts              # id-or-path → on-disk skill resolution
  paths.ts                # known paths (~/.skillplus/local, bundled skills)
  catalog.ts              # read catalog.json + enumerate local skills
  search-file.ts          # parse .search file (description + tags)
  commands/
    compile.ts            # `skillplus compile` (default action)
    list.ts               # `skillplus list`
    show.ts               # `skillplus show <id>`
    search.ts             # `skillplus search <query>`
    copy.ts               # `skillplus copy <id>`     fork into ~/.skillplus/local
    init.ts               # `skillplus init <id>`     scaffold a new skill
    update.ts             # `skillplus update`        npm install -g @latest
test/
  compile.test.ts         # compile + render targets
  resolve.test.ts         # id resolution
  catalog.test.ts         # catalog + .search parsing
scripts/
  build-catalog.js        # walks skills/, writes catalog.json + skills/README.md
  release.sh
spec/                     # draft schemas
targets/                  # target format docs
docs/                     # concepts, model-profiles
skills/                   # OFFICIAL skill catalog (PR-gated, bundled in npm)
  README.md               # auto-generated table of all skills
  food-street-realism.skillplus/
  brand-logo.skillplus/
model-profiles/           # per-vendor docs (Anthropic, …)
catalog.json              # auto-generated by scripts/build-catalog.js
```

The whole compiler lives in `src/compile.ts`. Don't split unless it grows past ~1000 lines.

## Commands

```bash
npm install
npm run build             # build:catalog (regenerates catalog.json) → tsc → dist/
npm test
npm run typecheck
npm run check             # typecheck + test + build
node bin/skillplus.js --help
```

## User state vs bundled state

```
npm package (read-only)
  ├── dist/               compiler
  ├── skills/             official catalog
  └── catalog.json        index

user's machine
  ~/.skillplus/local/     user's private skills (never touched by `update`)
```

Resolution order when `skillplus <id>` is called:
1. Path? → use directly
2. `~/.skillplus/local/<id>.skillplus` (user's private)
3. `<bundled>/<id>.skillplus` (official)

`skillplus copy <id>` copies a bundled skill to the local dir. The local copy then shadows the bundled one — safe place to fork without losing changes on `skillplus update`.

## Conventions

- **Determinism over cleverness.** Same package + args → byte-identical output. Provenance hashes depend on it.
- **No model calls.** The compiler produces prompts and contracts; running them is the runtime's job.
- **Minimal runtime deps.** Only `commander` (CLI) + `yaml` (parsing).
- **Scalar coercion.** `pythonCompatYAML` post-processes parsed values to coerce numbers to strings (preserves provenance hash parity with the original Python compiler).
- **Provenance always.** Every compiled output emits a `provenance_template`.
- **Targets are pure functions.** Adding a target = one branch in `renderTarget` + a `render<Name>` function.
- **`.search` file is mandatory** for any skill in `skills/` — the build-catalog script fails if missing or invalid.

## Adding a target

1. Add the name to `SUPPORTED_TARGETS` in `src/compile.ts`.
2. Document any new manifest metadata in `spec/`.
3. Implement `render<Name>(compiled): string`.
4. Wire into `renderTarget` dispatch.
5. Add a test case in `test/compile.test.ts`.

## Adding a vendor model profile

1. Create `model-profiles/<vendor>-<family>.yaml`.
2. Document prompt-style preferences, defaults, gotchas.
3. Verify reference packages compile cleanly against it.

## Adding a skill to the catalog

See [`CONTRIBUTING.md`](CONTRIBUTING.md). Short version: `skillplus init <id>`, fill TODOs, move into `skills/`, PR.

The `scripts/build-catalog.js` script enforces:
- `.search` must exist
- `description` ≤ 200 chars
- 1 ≤ `tags` ≤ 10, each kebab-case

Build fails if a skill in `skills/` violates these.

---
> Source: [eight-acres-lab/skillplus](https://github.com/eight-acres-lab/skillplus) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-05 -->
