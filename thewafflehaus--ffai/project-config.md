---
trigger: always_on
description: Apple Silicon LLM inference built on pre-compiled Metal kernels generated by the sibling [`ffai-kernels`](https://github.com/thewafflehaus/ffai-kernels) Rust DSL.
---

# FFAI

Apple Silicon LLM inference built on pre-compiled Metal kernels generated by the sibling [`ffai-kernels`](https://github.com/thewafflehaus/ffai-kernels) Rust DSL.

Global dev conventions (planning, issues, git workflow, testing, performance, code quality) live in `~/.agents/AGENTS.md`. This file covers FFAI / Apple-platform / ffai-kernels specifics only. For anything user-facing, the FFAI [`documentation/`](documentation/README.md) tree is the source of truth — DO read it progressively, DO NOT re-derive it.

## Build / test / bench / profile

- Local loop: `make regenerate-kernels` then `make build`.
- **DO go through `make` for tests; DO NOT run bare `swift test`.** Each `Tests/ModelTests/*` suite loads multi-GB HuggingFace snapshots; parallel runs pin the GPU and OOM the box.
  - `make test-unit` — `FFAITests` + `MetalTileSwiftTests` (fast, parallel-safe).
  - `make test-integration` — `ModelTests`, serialized (`--num-workers 1`).
  - `make test` — both in sequence; the full local CI gate.
  - `make coverage` — unit-suite coverage. Line coverage SHOULD stay ≥ 80 %.
  - `make test-stress` — production cap, uncapped parallelism; DO run it after touching dispatch code.
- After `make regenerate-kernels`, DO run the empty-body MSL detector (documented in ffai-kernels' `developing.md`) over `Sources/MetalTileSwift/Resources/kernels/*.metal`. A hit ships all-zeros output and is a release blocker.
- Longer-form testing story: [`documentation/developing/testing.md`](documentation/developing/testing.md).

## Branch base

DO branch off `dev` (not `main`). DO PR back to `dev`.

## Swift / Apple platform

- DO use Swift 6 strict concurrency — `Sendable`, `actor`, structured concurrency. DO NOT reach for `@unchecked Sendable` without a defensible, commented reason.
- DO target the latest macOS / iOS SDKs and prefer new APIs.
- DO prefer value types (`struct` / `enum`); use reference types only when identity or shared state is genuinely needed.
- DO use `MTLBuffer` directly — DO NOT add `MLXArray` middleware.
- `// swiftlint:disable` and `@available` SHOULD carry a one-line reason.

## ffai-kernels work

Sibling repo: `~/Development/personal/ai/ffai-kernels`. DO branch off its `dev` and PR back to its `dev`.

**DO read these once, up front. They are the source of truth — DO NOT re-derive what they cover:**

- [`CONTRIBUTING.md`](../ffai-kernels/CONTRIBUTING.md) — issue / PR process, agentic-contribution disclosure. **Required before any ffai-kernels PR.**
- [`docs/developing.md`](../ffai-kernels/docs/developing.md) — repo layout, the `make` dev loop, commits, and the **kernel-authoring hazards** (⚠️ empty-body MSL, machine-freezing dispatch shapes, macro traps, the `## DISPATCH INVARIANTS` convention). **Required before you write or touch a kernel.**
- [`docs/testing.md`](../ffai-kernels/docs/testing.md) — test layers, GPU correctness tests, MSL snapshots, coverage.
- [`docs/cli.md`](../ffai-kernels/docs/cli.md) — the `tile` CLI reference.

The rest of this section is FFAI-specific.

### Porting kernels from our `ekryski/mlx` fork

Custom kernels (turbo / gated-delta / SSM / …) live on **`ekryski/mlx@alpha`** under `mlx/backend/metal/kernels/` (local checkout `~/Development/personal/ai/mlx`, branch `alpha`). ffai-kernels' `MLX_COMMIT` is pinned there so `tile bench` has a side-by-side reference.

- DO try existing ffai-kernels DSL primitives + `#[kernel]` macros first.
- Missing a primitive? DO extend the DSL (body parser → IR → MSL codegen). DO NOT hand-write raw MSL.
- New AURA / GDN / SSM / vision / audio kernels DO start under `crates/ffai-kernels-std/src/ffai/`; graduate to `mlx/` once an MLX counterpart ships at the pinned commit.
- After each port, DO diff the ffai-kernels-generated MSL against the `alpha` reference kernel.

### Wrapping a kernel in a Swift `Ops` function

An `Ops` wrapper is the boundary between caller-supplied dimensions and the GPU. For **reduction-mode kernels** (`simd_*` / `threadgroup_alloc` / `KernelMode::Reduction` in the `.rs`, or a fixed `tpg=` in the `BenchSpec`) it is the only thing enforcing the geometry contract the kernel can't check at runtime:

1. DO read the kernel's `## DISPATCH INVARIANTS` block. If it has none, DO add one in the same commit.
2. DO `precondition` every invariant at the top of the wrapper, before any allocation or dispatch. Cite the kernel `.rs` in the message.
3. DO derive dispatch geometry from the invariants. DO NOT use `elementwiseGrid` for a reduction kernel — that is the canonical freeze bug; it is for genuine one-thread-per-output Grid3D kernels only.
4. DO add a `Tests/FFAITests/OpsTests.swift` test exercising the wrapper at a production-realistic shape.

### Testing — FFAI side

DO ship a paired GPU correctness test in the same commit for every non-trivial `ffai/` or `mlx/` kernel (test layers: `docs/testing.md`). What FFAI tests in Swift:

- **Per-Op** — `Tests/FFAITests/Ops*Tests.swift`: every `Ops.<fn>` against a small asserted output. New Ops DO land with a test.
- **Per-cache** — `Tests/FFAITests/*StateCacheTests.swift`: append / slice / serialize round-trip + multi-layer working-buffer sharing.

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [thewafflehaus/FFAI](https://github.com/thewafflehaus/FFAI) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-11 -->
