---
trigger: always_on
description: 本文件是影刀 xbot / 编码版项目的通用 Agent 规则，适用于后续多个影刀项目复用。
---

# AGENTS.md

本文件是影刀 xbot / 编码版项目的通用 Agent 规则，适用于后续多个影刀项目复用。

核心目标：**最小改动、简单直接、不猜 API、不做无关重构。**

---

## 0. 执行优先级

1. 用户当前需求优先。
2. 当前项目已有业务逻辑、参数约定、数据口径和已有接口优先保留；已有代码风格只有在不违反本文件规则时才沿用。
3. 本文件规则优先于 README、llms.txt 和普通文档。
4. 新建或重构“多个数据源下载并写入同一目标工作簿，且需要完整重试、统一保存和通知”的项目时，读取 [标准流程项目参考](docs/standard-project-reference.md)。
5. 先检查真实项目现有代码；仅当新增或无法确认 xbot API、市场指令、页面行为时，再按 `llms.txt` 定位相关文档。
6. 规则冲突时，以“用户需求和当前项目优先、改动更小、结构更简单、风险更低”为准。

已有代码本身不是正确性的依据。旧代码中的过度封装、重复校验、防御代码或历史错误写法，不应因为“保持现有风格”继续复制。

---

## 1. 默认执行原则

1. 目标清楚时直接改，不要先写长计划。
2. 只解决当前任务明确要求的问题。
3. 只改与当前需求直接相关的文件、函数和代码行。
4. 不做无关重构、无关格式化、无关改名或结构重排。
5. 能用现有结构解决，就不要新增框架、管理器、抽象层或复杂配置。
6. 能从当前代码确认用法，就不要额外查知识库。
7. 发现无关潜在问题可以指出，但不要擅自修改。
8. 每一处改动都必须能对应当前需求。

---

## 2. 复杂任务处理

只有出现以下情况时，才按复杂任务处理：

1. 需求不清楚，需要先梳理目标或取舍。
2. 需要新增完整功能、流程、数据结构或配置。
3. 需要跨多个模块修改，并会影响主流程。
4. 涉及新的影刀 API、市场指令、外部系统或页面行为，且当前代码无法确认用法。
5. 涉及批量写入、删除、迁移、发布、权限、生产配置等高风险操作。
6. 用户明确要求先评审、先设计、先规划或先给方案。

复杂任务只做必要步骤：

1. 先给简短计划：目标、修改范围、风险点。
2. 先看当前项目已有代码，再决定是否查知识库或 API 文档。
3. 只搜索与当前目标直接相关的文件和调用。
4. 优先在现有结构内完成，不默认重构架构。
5. 分步骤修改，但不要为了流程拆得过细。
6. 不确定 API、页面行为、XPath、市场指令参数时，标注“需运行验证”。

---

## 3. 知识库使用规则

真实业务代码应在真实影刀项目目录中修改，不要把知识库目录当成业务项目根目录。

知识库固定路径为：

```text
C:\Users\Administrator\Desktop\影刀xAI开发指南
```

开始真实影刀项目任务时，先确认该目录存在。若目录不存在，立即停止并请用户补充正确的知识库路径；不要猜测其他目录，也不要继续执行依赖知识库的开发。

先检查真实项目现有代码。仅当新增或无法确认 xbot API、市场指令、页面行为时，再按 `llms.txt` 定位相关文档。

仅当项目符合“多个数据源下载并写入同一目标工作簿，且需要完整重试、统一保存和通知”时，读取：

```text
docs/standard-project-reference.md
```

具体适用范围、标准结构和参考边界统一以该文档为准，不在本文件重复维护。

需要查文档时，按以下顺序确认：

1. `llms.txt` 中对应的文档入口。
2. `xbot-api-docs/docs/`。
3. 相关示例或市场指令源码说明。
4. 必要时用 `inspect.signature()`、`inspect.getfile()` 看真实实现。

如果仍不确定，必须写“需运行验证”，不要自行推断。

---

## 4. 代码风格

1. 代码要简单、直接、容易阅读。
2. 主入口优先采用从上到下连续阅读的顺序式流程，使执行顺序、数据流和关键状态可以直接看清。
3. 简单功能优先使用普通变量、`if`、`for`、`try` 等直白写法，不使用嵌套推导式、`lambda`、装饰器、生成器、动态调度或难以理解的一行式写法。
4. 代码应能从上到下连续阅读，不要为了减少行数而压缩语句或制造跳转。
5. 所有函数和方法调用默认写在一行；只有参数很多、参数结构复杂，或单行会明显降低可读性时才允许换行。
6. `page.find_by_xpath("...", timeout=15).click()` 这类参数较少的链式调用必须保持单行，不要为了格式化拆成多行。
7. 保持项目现有风格，不要为了“更规范”强行重构。
8. 变量名要直白，避免 `x`、`tmp`、`obj` 这类含义模糊的命名。
9. 默认使用标准库；新增第三方依赖需要用户明确允许，或项目已在使用。
10. 关键业务判断可以写中文注释；简单赋值、基础语法、显而易见的逻辑不要写注释。
11. 函数注释使用 Sphinx / reStructuredText 格式。
12. Git 提交信息、阶段性备注和变更摘要优先使用中文。

---

## 5. 函数、常量与结构

1. 除非代码量较多，否则只被调用一次的代码直接写在调用处，不封装函数。
2. 单次调用逻辑只有在代码较多、能形成清晰独立的业务步骤，并且抽出后更利于自上而下阅读时，才允许封装函数。
3. 函数体只有一两行时，不要封装。
4. 抽函数后参数更多、状态传递更多或阅读更绕时，不要抽。
5. 明显重复、需要多处复用，或项目已有同类结构时，可以抽函数。
6. 默认不要新增类、管理器、工厂函数、复杂配置层或抽象层。
7. 默认不要为了“看起来像常量”就提取常量。
8. 单次使用的 XPath、CSS、URL、字段名、按钮文案、状态值、提示语，默认就近写在使用处。
9. 不为未来可能出现的需求预留未使用参数、接口、分支、配置、Hook、兼容层或扩展点。
10. 默认保持主流程清晰；是否拆模块以用户需求、现有结构、业务边界和实际代码量为准，不单纯按文件长度决定。
11. 能在使用位置直接计算或直接读取的值，不要为了“统一管理”额外放入 `context`、`dict`、全局变量或长期中间状态；状态只保留跨步骤确实需要共享的业务状态。

---

## 6. 输入、参数与异常

1. 影刀项目入口通常为 `main(args)`。
2. 已知 `args` 参数名时，严格按已有参数名使用。
3. 新增 `args` 参数时，使用最小、直观、稳定的参数名；最终回复列出参数名、含义和建议类型。
4. 不要同时兼容多个猜测参数名，例如 `data_list / items / 数据列表` 混用。
5. 调用方已经约定入参类型或记录结构时，业务逻辑直接按约定使用。
6. 输入形态不确定时，只在边界统一归一化一次，不要在业务逻辑里反复兜底。
7. 异常处理只覆盖当前业务明确需要处理的失败情况。
8. 捕获异常时必须保留关键错误信息，不要静默吞异常。
9. 关闭、停止监听、停止加载等收尾方法直接调用，不要为了收尾动作单独套 `try / except`。包括 `workbook.close()`、`WebBrowser.close()`、`web.close_all()`、`Win32Window.close()`、`xbot.excel.kill_excel_process()`、`browser.stop_monitor_network()`、`browser.stop_load()` 和 `xbot.app.dialog.close_notifycation()`。需要保证业务异常时仍执行收尾，可放在 `finally` 中直接调用；对象可能未创建时只判断对象是否存在，例如 `if page: page.close()`。

---

## 7. 影刀日志与页面自动化

1. 影刀项目禁止使用 Python 标准 `logging`。
2. 只允许使用 `from xbot.app import logging`。
3. 日志方法只传一个字符串参数，例如 `logging.info(text)`。
4. 动态内容使用 f-string，例如 `logging.info(f"行数={n}")`。
5. 需要异常堆栈时，在 `except` 里用 `traceback.format_exc()` 拼到 `logging.error(...)`，不要用 `logger.exception(...)`。
6. 日志前缀不需要记录时间，影刀 `logging` 自带时间前缀，调用方不要再拼 `datetime` 或 `time`。
7. 涉及浏览器、URL、页面业务时，优先使用 `xbot.web` 和浏览器对象能力。
7. 除非用户明确要求接口方式，或项目已有接口调用方式，否则不要用 `requests`、`httpx`、`aiohttp`、`urllib` 处理网页业务。
8. 能用元素、选择器、页面接口处理，就不要优先坐标点击。
9. 中文或长文本输入优先考虑剪贴板输入。
10. 页面字段、按钮含义、价格口径必须以当前需求、页面证据或已验证结果为准。
11. XPath、CSS、页面接口、按钮状态未验证时，必须标注“需运行验证”。

---

## 8. 修改与验证

1. 修改已有文件前，先阅读相关上下文，避免覆盖用户已有改动。
2. 手工改文件时做最小 diff，避免整文件无意义重写。
3. 不要执行会丢改动的命令，例如 `git reset --hard`、`git checkout --`，除非用户明确要求。
4. 搜索文件、代码和文本时优先用 `rg`。
5. 默认不新增测试，不默认运行完整测试。
6. 只有用户明确要求、命令很小且和本次修改直接相关，或不验证会有明显风险时，才运行必要检查。
7. 校验按实际业务路径执行。只有当前流程真正会使用的数据、工作表、配置或外部资源才需要校验；默认宁可少做结构检查，也不要为了“提前发现问题”增加未使用依赖的全量预检。
8. 正常业务调用本身已经能够明确暴露错误时，直接依赖该调用失败，不要再额外增加 `REQUIRED_SHEETS`、`exists`、全量扫描等重复防御层。
9. 同一个结果已经通过正常业务流程得到后，不要为了“保险”再次读取、再次扫描或使用另一套逻辑重复确认；除非用户明确要求验证该结果。
10. `TODO（人工校验）` 或“需运行验证”不是不确定时的默认兜底。先检查现有代码、知识库和 API 文档；只有仍必须依赖真实影刀、WPS、浏览器或页面运行才能确认的行为才保留，能够静态确认的问题不要留 TODO。
11. `shadowbot_sync_tool.py` 应放在真实影刀项目根目录，与 `package.json` 同级。
12. 只要真实影刀项目新增了 `.py` 文件，默认必须运行 `python shadowbot_sync_tool.py`；用户本次明确要求暂不执行影刀同步时除外。仅修改已有 `.py` 文件且用户未要求同步时不运行。
13. 用户说“同步影刀”“执行影刀同步”“同步到影刀”或同类表述时，均指在真实项目根目录执行 `python shadowbot_sync_tool.py`，即使本次只修改了已有 `.py` 文件也要执行。
14. “同步影刀”不等于 Git 提交、Git push 或普通文件复制，不要用其他操作替代同步脚本。
15. 同步脚本自动扫描真实项目根目录下的 Python 文件，不接收手动文件列表。
16. 同步完成不等于已在影刀编辑器内验证通过。
17. 未运行测试或同步时，最终回复必须明确说明。

---

## 9. 禁止行为

1. 无关重构、无关格式化、无关改名。
2. 擅自更换实现路线。
3. 引入不必要依赖。
4. 引入复杂封装、复杂抽象层、复杂配置系统。
5. 为低概率场景写大量防御代码。
6. 隐藏副作用或吞异常不说明。
7. 基于旧项目经验猜 API、命令、目录、文件、XPath、页面字段、按钮含义或价格口径。
8. 同时兼容多个未经确认的参数名。
9. 自行猜测市场指令编码版参数。
10. 提交账号、密码、token、webhook、敏感路径。
11. 把“需运行验证”的内容描述成“已经验证通过”。

---

## 10. 最终回复

完成后简短说明：

1. 改了哪些文件。
2. 核心改动是什么。
3. 是否运行测试 / 同步。
4. 是否还有需运行验证或需人工确认的内容。

只有遇到阻塞、测试失败、不确定 API 或需要用户决策时，才写详细说明。

---
> Source: [apexcheng/yingdao-xbot-ai-agent](https://github.com/apexcheng/yingdao-xbot-ai-agent) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-13 -->
