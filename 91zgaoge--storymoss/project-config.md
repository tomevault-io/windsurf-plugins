---
trigger: always_on
description: 实现后必须对照功能与设计核验，持续迭代直到可上线
---


# 实现后核验直到可上线（强制）

任何功能/修复**写完代码不等于完成**。必须对照设计与用户可感知功能核验，发现缺口就改，再核验，循环直到达到可上线水准。用户明确要求永久记住。

## 强制流程

1. **对照设计**：把设计文档的不变量、契约表、验收指标逐条对代码（路由、落库、IPC、UI、失败降级），标记 executed / inspected / 缺口。
2. **对照功能**：用户能点到的路径都要能走通（含漏网入口：第二处 invoke、设置项、改写 vs 续写分流）。契约测试必须保护用户结果，不是只保护实现细节。
3. **持续迭代**：缺口 → 修 → 再跑验证（`cargo test --lib` / `tsc` / `vitest` / 相关契约），直到设计验收与功能路径闭合。
4. **可上线才停**：测试绿、设计契约有测试或等价探针、已知债务已登记且不阻断主路径。未跑通设计验收探针，不得宣称「症状已修复」或「可上线」。

## 反例（禁止）

- 计划任务勾完、提交/发版后不再对照设计。
- 只跑单元测试、不核对「用户点续写会怎样」。
- 把「已知债务」当完成；债务可登记，但不能冒充验收通过。
- 核验中发现缺口却停在报告，不迭代修复（除非真被人类闸门挡住，须写入 `humanpending.md` 或明确告知用户）。

---
> Source: [91zgaoge/StoryMoss](https://github.com/91zgaoge/StoryMoss) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-13 -->
