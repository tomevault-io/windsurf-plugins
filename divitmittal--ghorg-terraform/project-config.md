---
trigger: always_on
description: **Generated:** 2026-04-02 IST
---

# PROJECT KNOWLEDGE BASE

**Generated:** 2026-04-02 IST
**Branch:** main

## OVERVIEW
Terraform + Terragrunt project that declaratively manages multiple GitHub organisations
(DivitMittal, Qezta) with the GitHub provider, plus a Nix flake for tooling and CI generation.
Terragrunt DRYs up the backend configuration and provider generation across orgs.

## STRUCTURE
```
./
├── root.hcl                     # root: remote_state (local backend) + generate "versions"
├── orgs/
│   ├── DivitMittal/             # Terragrunt unit for github.com/DivitMittal
│   │   ├── terragrunt.hcl       # include root + generate "provider"/"locals" + inputs (token)
│   │   ├── main.tf              # import blocks + module calls
│   │   ├── variables.tf         # github_token variable
│   │   ├── outputs.tf
│   │   └── terraform.tfstate    # local state (do not edit manually)
│   └── Qezta/                   # Terragrunt unit for github.com/Qezta
│       ├── terragrunt.hcl       # include root + generate "provider" + inputs (token)
│       ├── locals.tf            # repos map + protected_branches (add Qezta repos here)
│       ├── main.tf              # import blocks + module calls
│       ├── variables.tf         # github_token variable
│       └── outputs.tf
├── terranix/
│   └── orgs/
│       └── DivitMittal.nix      # canonical repos map for DivitMittal (Nix → JSON)
├── modules/
│   ├── repositories/            # github_repository resources + validation
│   └── branch-protection/       # github_branch_protection; accepts protected_branches var
├── flake/                       # devshell, formatters, pre-commit, CI generation, terranix
└── .github/workflows/           # generated by flake/actions
```

## WHERE TO LOOK
| Task | Location | Notes |
|------|----------|-------|
| Add/modify DivitMittal repos | `terranix/orgs/DivitMittal.nix` | Edit `locals.repos` (Nix attrset). Compiles to `locals.tf.json` at terragrunt init/plan/apply. |
| Add/modify Qezta repos | `orgs/Qezta/locals.tf` | Edit `repos` map only (HCL — not yet migrated to terranix) |
| Add branch protection rules | Same `locals.tf` as repos | Edit `protected_branches` map |
| Repo resource behaviour | `modules/repositories/main.tf` | prevent-destroy, auto-init ignores |
| Branch protection rules | `modules/branch-protection/main.tf` | Uniform rule set |
| Terragrunt backend/versions | `root.hcl` | Inherited by all org units |
| Org provider + token | `orgs/<org>/terragrunt.hcl` | generate "provider" + inputs |
| Terranix flake plumbing | `flake/terranix.nix` | Exposes `packages.<system>.<org>-locals` derivations |
| State migration/imports | `orgs/<org>/main.tf` | `import` blocks |
| Nix devshell/formatters | `flake/devshells.nix`, `flake/formatters.nix` | Terraform + Terragrunt + Nix tooling |
| CI workflow sources | `flake/actions/*.nix` | Generates `.github/workflows/*.yml` |

## TOKENS
Each org uses a distinct PAT (Personal Access Token):
- **DivitMittal**: `GITHUB_TOKEN_DIVITMITTAL` env var (falls back to `GITHUB_TOKEN`)
- **Qezta**: `GITHUB_TOKEN_QEZTA` env var (falls back to `GITHUB_TOKEN`)

Tokens must have `repo` + `admin:org` scopes on the target org.

## CONVENTIONS
- Terraform >=1.7 is required for `import` blocks.
- For DivitMittal, `terranix/orgs/DivitMittal.nix` is the canonical source for the repo list — compiled to `locals.tf.json` by Terragrunt's `generate "locals"` block.
- For Qezta, `orgs/Qezta/locals.tf` remains canonical (HCL — terranix migration pending).
- The `modules/branch-protection` module receives `protected_branches` as a variable (not hardcoded).
- `prevent_destroy = true` guards every repository resource.
- Workflows in `.github/workflows/` are generated from `flake/actions/`.
- `.pre-commit-config.yaml` is generated by `flake/checks.nix`.

## ANTI-PATTERNS (THIS PROJECT)
- Do not edit `.github/workflows/*.yml` by hand (generated).
- Do not modify `.pre-commit-config.yaml` manually (generated).
- Do not edit `orgs/DivitMittal/locals.tf.json` by hand — it's a build artefact of `terranix/orgs/DivitMittal.nix`.
- Do not remove `prevent_destroy` from repo resources.
- Do not recreate existing repos; use `import` blocks to adopt them.
- Do not run `terraform` directly — always use `terragrunt` from inside an org directory.
- Do not use `terragrunt run-all` (deprecated) — use `terragrunt run --all` instead.
- Do not edit `orgs/*/terraform.tfstate` manually.

## COMMANDS
```bash
# Enter devshell (provides terraform, terragrunt, gh, nix tooling)
nix develop

# Per-org workflow
cd orgs/DivitMittal   # or orgs/Qezta
terragrunt init
terragrunt plan
terragrunt apply

# Run across all orgs at once
terragrunt run --all plan
terragrunt run --all apply

# Nix
nix fmt
nix flake check
nix run .#render-workflows
```

## NOTES
- State files live at `orgs/<org>/terraform.tfstate` (local backend).
- Terragrunt generates `backend.tf`, `versions.tf`, `provider.tf`, and (DivitMittal only) `locals.tf.json` in each org directory at init/plan time. These are git-ignored.
- The DivitMittal `locals.tf.json` is materialised by `run_cmd("nix build .#divitmittal-locals")` inside `orgs/DivitMittal/terragrunt.hcl`. To inspect manually: `nix build .#divitmittal-locals --print-out-paths | xargs cat | jq`.
- To switch to a remote backend (e.g. GCS), update `remote_state` in `root.hcl` only — all orgs inherit it.

---
> Source: [DivitMittal/ghOrg-terraform](https://github.com/DivitMittal/ghOrg-terraform) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-06-16 -->
