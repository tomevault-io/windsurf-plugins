---
trigger: always_on
description: - M0.5 与 M1A 已于 2026-07-19 通过人工验收并冻结。
---

# GATalk 持续开发规则

## 当前阶段

- M0.5 与 M1A 已于 2026-07-19 通过人工验收并冻结。
- M1B.0 已完成并建立 `v0.2.0a0-m1b0` 回退点。
- M1B.1 已通过用户真实项目试用并冻结。
- M1B.2 已通过用户真实项目试用并冻结。
- M2“AI 主美控制台与灯光审片台”内部候选已完成并建立第一个重大外部试用点。
- M3“参考匹配与画面优化实验室”内部候选已完成；当前停在第二个重大外部
  试用点。
- M4“深度主美审阅与构图诊断”内部候选已完成。
- M5“作品研究”新增应用首页与独立单图学习模块，当前等待真实作品试用。
- M6“资产拆分工作台”完整候选已完成，当前等待真实场景与真实供应商人工试用。
- 0.7.1 已更新 Gemini 稳定模型与 404/503 有界备用链，不再使用
  `gemini-2.5-flash`。
- 0.7.2 对 AI 资产清单执行有界引用完整性修复，语义字段保持原样。
- 0.8.0 新增与可校正拆分并列的全自动资产板、当前 Nano Banana 模型选项、
  图片生成熔断和资产编辑快捷键，等待真实 Gemini 凭据与复杂原画试用。
- 0.11.0 将资产拆分升级为“场景理解—逐类别深度方案—资产套件清单—分页
  展示板”，并支持作品研究显式交接与同图多方案，等待真实复杂原画试用。
- 0.12.0 已让清单与校正、自动资产板和生成提示语共享当前拆分依据，并以方案
  指纹标记旧版输出；资产项目格式为 v5，等待真实复杂原画与供应商试用。
- 0.12.1 已将资产工作台右侧页面改为顺序分段和按需纵向滚动，避免高 DPI 与
  较矮窗口下压缩控件；等待真实显示器检查。
- 0.13.0 新增高于专业工作台的参考资料平台、美术资料域和作品研究集合与对照研究；
  当前等待真实资料库、多作品研究和 Windows 候选试用。
- 0.14.0 新增资料翻译、局部截图、跨项目引用和平台级审阅任务与质量门禁中心；
  当前等待真实资料、跨项目引用和版本复查试用。
- 0.15.0 新增全局检索、项目恢复、阶段门禁、资产生产交接和视觉资料板；
  自动化回归完成，等待真实项目与 Windows 候选试用。
- 0.16.0 新增双图分布证据、八维灯光审片、AI 审阅历史和统一全局导航；
  自动化回归完成，等待真实灯光项目与 Windows 候选试用。
- 0.17.0 新增全局语言系统；简中为基准，繁中、英、日、法为预览，
  等待母语审校、高 DPI 与干净 Windows 多语言试用。
- 0.18.0 统一专业工作台导航与窗口状态，更新当前产品术语，并扩充经过筛选的
  视觉 AI 供应商目录；等待真实供应商、母语审校与 Windows 显示验证。
- 0.18.1 Beta 1 完成首次公开仓库安全、隐私、素材与许可证审计；项目源码采用
  MIT，Windows `onedir` 随附第三方通知和 Qt LGPL 对应源码材料。
- 不实现本地大型模型、视频、CUDA、自由多边形、动态第三方代码加载、插件市场
  或隐式联网。

## 模块与依赖边界

- 采用模块化单体；当前业务模块 ID 为 `scenelens.visual_review`、
  `scenelens.artwork_study`、`scenelens.asset_breakdown`、
  `scenelens.comparative_study`，以及平台模块 `gatalk.knowledge_base` 和
  `gatalk.review_control`。
- 平台层管理跨工作台资料，专业工作台管理具体研究或生产项目；资料域通过
  `KnowledgeDomainDescriptor` 注册，不把未来领域字段硬编码进美术资料。
- 审阅中心只保存跨项目任务索引、来源、验收条件和版本复查，不反向改写来源
  项目；业务证据正文继续由对应专业工作台拥有。
- `core` 只保存真正跨模块复用的契约，不依赖具体业务模块。
- `modules/<module_id>` 可拥有分析器、配置、业务服务、数据表和未来 UI 入口。
- `analysis` 只处理 NumPy 数据，不导入 PySide6。
- `imaging` 负责读取、EXIF、ICC、sRGB 和 NumPy/QImage 边界。
- `ui` 只负责交互和呈现，不实现色彩转换、聚类、明度算法或 SQL。
- `storage` 不导入 PySide6；迁移、原子写入和业务存储接口分别测试。
- 新分析器经统一注册表接入，并记录模块、分析器、版本、参数和输入哈希。
- 不为尚未实现的未来模块制造抽象；发现第三个真实调用方后再提取公共能力。

## 数据与并发

- `project.json` 是项目入口，`project.db` 保存结构化业务数据。
- 模块业务表使用模块前缀并记录模块 schema 版本。
- schema 迁移前必须备份数据库和清单；高版本项目拒绝写入。
- 同一项目同一时间只能有一个写进程；其他进程只读打开或取消。
- 操作系统文件锁是写权限依据，不能只凭 PID、时间戳或锁文件存在判断。
- 自动、算法或 AI 内容不得覆盖用户填写、修订或确认过的 Brief 字段。

## 图像规则

- 永不覆盖或重新保存用户输入文件；导入保留原始字节和 SHA-256。
- Pillow 执行 EXIF 方向修正；有 ICC 时用 ImageCms 转 sRGB，无 ICC 时记录
  为“假定 sRGB”。
- 明度使用线性 sRGB 相对明度，不平均 gamma 编码 RGB。
- Oklab 聚类固定随机种子并记录参数；派生图可删除并重建。

## UI 与性能

- 耗时读取和分析离开 GUI 线程，只在 GUI 线程创建 QPixmap。
- 4K 图片使用有界分析采样，不把全部像素直接送入聚类。
- Qt6 高 DPI 布局不得写死依赖 100% 缩放的像素尺寸。
- 路径使用 `pathlib.Path`，不得假设 ASCII 或无空格。
- 快速切换 Shot/Version 时，过期后台任务不得覆盖当前结果。
- API 与图像编辑任务必须在后台执行，支持取消、超时、有限重试和错误脱敏。
- 所有网络发送必须由用户主动触发，并在发送前展示数据清单。
- 界面固定文案以简体中文为源语言并进入本地化目录；用户输入、项目数据和历史
  结果不得因语言切换被自动翻译或改写。

## 质量门槛

- 每个分析算法有基础单元测试。
- 色彩转换、EXIF、ICC、三阶/五阶明度和固定种子有确定性测试。
- 可见功能后运行相关测试；阶段结束运行完整测试。
- 每阶段独立 Git 提交并保留稳定回退点。
- M2/M3 内部纵向切片使用原子提交和可回退标签；只在两个重大试用点暂停。
- 每阶段构建 `onedir`，并至少执行一次打包启动烟测。
- 用户错误信息使用非程序化语言，技术细节写入日志。

## 依赖、文档与许可证

- 不加入范围外模型、CUDA、服务端框架或大型 UI 依赖。
- 新依赖说明用途、许可证、Windows wheel 和打包影响。
- 依赖升级运行算法、UI 和 PyInstaller 验证。
- 重要取舍写入 `DECISIONS.md`，范围写入 `ROADMAP.md`，用户变化写入
  `CHANGELOG.md`，依赖变化同步 `THIRD_PARTY_LICENSES.md`。
- 每次用户可见更新必须同步更新 `USER_GUIDE.md`，并运行
  `scripts/build_user_guide_docx.py` 重新生成 `GATalk_使用手册.docx`。
  Word 版是用户交付手册，使用简洁的图文步骤；每个主要工作流配真实界面图和
  简短图注，图中不得包含 API Key、私人路径或真实项目机密。避免实现细节、重复
  说明和长篇背景。截图与当前版本同步，交付前必须渲染检查全部页面。
- `USER_GUIDE.md` 和 `USER_GUIDE_EN.md` 使用“用途—入口—步骤—结果—注意”结构；
  中文和英文说明同步更新，非必要内容不进入主手册。
- 文档使用中文；代码标识符、类型和测试名称使用英文。

## 常用命令

```powershell
.\start_dev.cmd
.\scripts\test.ps1
.\build_alpha.cmd
```

---
> Source: [WrayMark/GATalk](https://github.com/WrayMark/GATalk) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-16 -->
