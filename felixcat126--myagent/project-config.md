---
trigger: always_on
description: 助手语言、改码范围、编译校验与思考方式（MyAgent 全局）
---


# 助手协作约定

## 1. 语言

- **思考过程与所有面向用户的回复**（说明、总结、错误提示、提交说明等）**一律使用简体中文**。
- 代码中的标识符、第三方 API 原文、日志关键字等可保持英文；向用户解释时用中文。

## 2. 修改范围

- **仅修改完成任务所必需的文件与行**，禁止顺带「整理」、无关重构或扩大范围。
- **未经用户明确要求**，不得修改与当前任务无关的代码、配置或文档。
- **若任务范围仅为远端页**（如 `electron/remote-shell.html`、`electron/ipc/remote-gateway.ts` 等与浏览器壳相关），未经用户明确要求，**不得**改动桌面端 UI 渲染与样式（`src/**/*.tsx`、`src/**/*.css` 等）。
- 若必须触碰额外文件才能修复问题，在回复中**简要说明原因与涉及路径**。

## 3. 编译与校验

- 在完成**任意**代码或配置的实质性修改后，须在本地执行 **`npm run build`**（或与本次改动等效的 `tsc` / 项目约定的构建命令），**确认无报错**后再告知用户任务完成。
- 若构建失败，须先修复再继续汇报；不得在仍有编译错误时宣称完成。

## 4. 思考方式

- 面对需求时 **分步推理**：先澄清目标与约束，再给出方案与变更点，最后执行与验证。
- 在回复中可采用简短分步结构（第一步…第二步…），便于核对；避免跳步与含糊结论。

---
> Source: [FelixCat126/MyAgent](https://github.com/FelixCat126/MyAgent) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-22 -->
