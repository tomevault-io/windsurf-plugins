---
trigger: always_on
description: Kotlin multiplatform date & time library extending kotlinx-datetime.
---

# fluid-time

Kotlin multiplatform date & time library extending kotlinx-datetime.

## Build

```shell
./gradlew build
```

## Conventions

- Tab indentation in Kotlin and Gradle files
- Tags have no `v` prefix (e.g. `0.18.0`)
- Source layout: `sources/` (common), `sources-jvm/` (JVM), `tests/` (common tests), `tests-jvm/` (JVM tests), `tests-js/` (JS tests)
- Package: `io.fluidsonic.time`

---
> Source: [fluidsonic/fluid-time](https://github.com/fluidsonic/fluid-time) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-06-29 -->
