---
trigger: always_on
description: Read `README.md`, then `goal.md`. The coverage table in `README.md` is the work list. Run `python3 scripts/build.py --check` after changing journeys, screens, verification items, triage, or visual artifacts, then run `python3 scripts/check-links.py .`.
---

Read `README.md`, then `goal.md`. The coverage table in `README.md` is the work list. Run `python3 scripts/build.py --check` after changing journeys, screens, verification items, triage, or visual artifacts, then run `python3 scripts/check-links.py .`.

---
> Source: [tonk-labs/tonk](https://github.com/tonk-labs/tonk) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-09 -->
