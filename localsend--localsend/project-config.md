---
trigger: always_on
description: Always use **`fvm flutter`** / **`fvm dart`**, never the bare `flutter` / `dart` binaries — this repo pins its
---

# CLAUDE.md

Always use **`fvm flutter`** / **`fvm dart`**, never the bare `flutter` / `dart` binaries — this repo pins its
Flutter version in `.fvmrc` and the system-wide toolchain will not match it.

Full guidance for this repository lives in AGENTS.md:

@AGENTS.md

---
> Source: [localsend/localsend](https://github.com/localsend/localsend) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-09 -->
