---
trigger: always_on
description: <!-- Modified by AI on 04/22/2026. Edit #1. -->
---

# Copilot Instructions — AITrail.ExampleApp

<!-- Modified by AI on 04/22/2026. Edit #1. -->

## Repository Overview

**AITrail.ExampleApp** is a greenfield C# / .NET Web API application scaffolded with the `@paycor/ai-sdlc-toolkit`. As of this file's date, the repository contains **only** `.github/` AI workflow scaffolding — no application source code yet. Code is added via the `spec-and-execute` agent workflow.

Trust these instructions. Only search the codebase if information here is incomplete or appears outdated.

---

## Mandatory Instruction Files

All five files in `.github/instructions/` are **always active**. Never violate them. Read the relevant file for full rules before making changes.

| File | Applies To | Key Rule |
|---|---|---|
| `ai-attribution.instructions.md` | All files | Add `// Generated by AI on MM/DD/YYYY` (or `// Modified by AI on MM/DD/YYYY. Edit #N.`) to every function/class/file created or modified. |
| `critical-rules.instructions.md` | All files | No `.Result`/`.Wait()`; all endpoints need `[Authorize]`; use `DateTimeOffset`; `StringBuilder` in loops; feature-based folders. |
| `clean-code.instructions.md` | All files | SOLID, DRY, SRP; max ~20-line methods; max 3–4 nesting levels; no god classes/components. |
| `code-modification-policy.instructions.md` | All files | Minimal, targeted changes only. Never refactor unrelated code. |
| `api-standards.instructions.md` | `**/*.cs` | Versioned routes; standard 4xx error shape; no caching; GUIDs for IDs; HATEOAS links; `pageSize` max 100. |

---

## Repository Layout

```
.github/
  copilot-instructions.md          ← this file
  agents/
    spec-and-execute.agent.md      ← primary workflow for all feature development
    generate-repo-summary.agent.md
    create-copilot-instructions.agent.md
  instructions/                    ← mandatory coding standards (see table above)
  prompts/
    scripts/
      universal-docs.ps1           ← deep repo documentation generator (-Action deep)
```

No `.sln`, `*.csproj`, source files, CI pipelines, or README exist yet.

---

## Build, Test & Run

No build commands exist until application code is created. Once a solution exists:

```powershell
dotnet restore                                                      # always run first
dotnet build
dotnet test
dotnet run --project src/<Feature>/<Project>.csproj
```

Always run `dotnet restore` before `dotnet build`. If a `package.json` appears, always run `npm install` before any `npm` command. No CI/CD pipelines exist yet.

---

## Project Organization

- **Feature-based (vertical slice) folder structure** — group controller, service, models, and tests for a feature together. Never use a flat `/Controllers` / `/Services` / `/Models` layout.
- Place new features under `src/<FeatureName>/` alongside their own unit tests.
- Separate business logic from infrastructure (DB, file system, HTTP clients) using interfaces.

---

## Clean Code Principles

- Each class has **one reason to change** (SRP). Each method does **one thing** (≤ 20 lines).
- No duplicate logic — extract to a shared method or class when code appears more than twice.
- Max **3–4 levels of nesting**; use guard clauses / early returns to stay flat.
- Depend on **abstractions**, not concretions (DIP). Use dependency injection throughout.
- Avoid god classes, long parameter lists (>3–4 params), and primitive obsession.
- Names must reveal intent; comments explain *why*, not *what*.

---

## Coding Style

- Follow existing patterns and formatting in the file being edited.
- Use C# record types for immutable DTOs/request/response models.
- Prefer expression-bodied members for single-line implementations.
- Use `var` when the type is obvious from the right-hand side.
- One class per file; file name matches class name.
- Use string interpolation for simple concatenation; `StringBuilder` for loop concatenation.

---

## Security Rules

- Every controller and action must carry `[Authorize]`. Anonymous access requires explicit `[AllowAnonymous]` with a documented justification.
- Add specific privilege checks (e.g. `[RequirePrivilege("Resource.Action")]`) beyond bare authentication.
- **Never return PII in list/collection endpoints.** Single-resource endpoints may return PII only when `?includeSensitiveData=true` is passed, and the access must be audit-logged.
- Never expose stack traces, exception messages, or internal details in 5xx responses.
- Validate and sanitize all inputs at controller boundaries using Data Annotations (`[Required]`, `[StringLength]`, `[RegularExpression]`, etc.).
- Never accept sensitive filter data (e.g. SSN) as query string parameters — use a POST body instead.

---

## Data Access Layer Rules

- Return **business objects**, never raw database entities, from API responses.
- Use async repository methods exclusively (`await _repo.GetAsync(...)`) — no blocking calls.
- Always paginate collection queries; enforce `pageSize` ≤ 100.
- Avoid N+1 queries — use `.Include()` / projection queries or batched calls.
- Use `HttpClientFactory` for all outbound HTTP calls (connection pooling).
- Implement the circuit-breaker pattern for calls to external services.

---

## Language Features


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [ldodlapaycor/AITrail.ExampleApp](https://github.com/ldodlapaycor/AITrail.ExampleApp) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-05 -->
