---
trigger: always_on
description: 适用于本目录及全部子目录。用户要求优先；下列约定随源码交付。
---

# 青竹剑阵项目维护约定

适用于本目录及全部子目录。用户要求优先；下列约定随源码交付。

## 当前发布与数据版本

- 当前游戏 **1.0**，篇名 **虚天殿篇**。显示版本、篇名和标题分别来自 `game/gameplay/version.js` 的 GAME_VERSION、GAME_ARC、GAME_TITLE；包版本为 1.0.0，发布标签 v1.0。
- 新局：秘境 3、平衡 4、修持 3、内容 3、成长 5、遭遇 8、经济 4、叙事 3、集火 3、输入 2、成绩计时 1、章末分段 1；无尽挑战 2，其他模式挑战 0。各版本以所属源码常量为准，不共用游戏显示版本。
- 存档格式 1、键 `fanren-qingzhu-v1`、records schema 1 与原十项 RULE_KEYS 保持。升级显示版本不能清档、补造成绩或转换旧局。
- 历史引擎及所有仍有效的细节完整保存在 [兼容规则与维护沿革](docs/compatibility.md)，涉及对应系统前必须阅读相应条目。旧规则的 ID、价格、效果、生成器和随机抽样须保留，不可按当前数值改写旧分支。

## 同步说明与交付

- 任何玩法、入口、数值、资源、解锁、保存流程或界面提示变更，同次更新根游玩说明、日志和验收证据，不把计划写成已实现。
- 用户指定本次更新日志正文仅为“1.0正式发布”；本次修复详情记于 `docs/release-1.0.md`。后续发布仍按用户指定版本维护，不擅自升版。
- 数值表通过 `npm run docs:tables` 显式更新，须检查正文；构建只读校验说明并嵌入游戏帮助，不能顺带修改源文档。
- 可执行内容变更后重建 WebP 与 PNG 两份离线 HTML；兼容版结束后恢复默认资源。原 PNG 和 encoded manifest 均保留，嵌入前核对原图与副本哈希，不静默沿用失效编码。
- `npm run build:release` 将两份 HTML、源码 ZIP、免安装玩家 ZIP、SHA256SUMS.txt 和 release-manifest.json 集中写入 `release/<GAME_VERSION>/`；根目录仍更新两份 HTML，保留原游玩路径。玩家包入口为「开始游戏.html」，两种入口与源码须和本次构建逐字节一致，解压后可离线直接运行。源码包依据公开白名单，不收依赖、个人存档、缓存、历史证据或旧交付。
- 验收实际操作与数字裁切，不只检查元素边框；记录自动测试、合成夹具和真人／真机的区别。

## 目录维护

- `game/` 保留工程结构与当前依赖；`docs/` 为精选公开文档；历史源码、完整测试原文、方案和参考图统一放 `local-archive/`，不删原文、不加入源码包。
- 视频介绍成品、口播稿与录制说明放 `release/video/`；源模板仍在 `docs/presentation/`。输出位置统一由 `game/scripts/delivery-paths.mjs` 管理，新增构建和检查不得重新把 ZIP 或视频成品散落根目录。
- 游戏根目录两个 HTML 和根游玩说明／更新日志保留名称，避免改变既有直接游玩路径。发布副本须和根入口逐字节一致。

## 剧情、素材与许可证

- 主篇乱星海炼剑后至虚天殿，第六章“虚天夺鼎”，不混入后期小极宫人物。关键事件、取得顺序、人物结局按有据内容处理，无法核实之处见改编说明。
- 波次、数值、路线与战术是游戏化规则，不能称作原著事实。持有器物、携行配置和战场协战分开，不按账号历经章节给新局补发器物。
- 首页完整署名：`制作人：bilibili@卡布奇诺ultra`。噬金虫使用金色友方素材，友方与敌方甲虫区分。
- 原创代码 GPL-3.0-only，保留 SPDX 与版权说明；第三方声明及图片／IP 授权范围单列。发布成品必须同时提供对应源码、构建脚本和许可证。

## 存档安全

- 所有可能覆盖记录或回退未保存进度的入口，先说明具体影响并确认；确认弹窗暂停战斗，取消不修改当前局和磁盘记录。
- 覆盖前保留可恢复前态、旧备份、异常日志与导入原文；恢复先只读预览，再显式采用。损坏的续玩或成绩必须说明后隔离，不静默重置。
- 隔离全文去重，上限 12。满额停止覆盖，不自动淘汰；清理可在恢复期间使用，但须显式确认、比较主档／备份／日志／隔离四类快照。清理只影响已确认的隔离原文，正常备份轮换不能丢受保护记录。
- 写入失败停下并保留前态，保存成功后才显示已保存；另一窗口变化阻止旧页覆盖。恢复重试只接受本事务确实写入的精确状态。
- 机缘／核心出发前可改选、提交一次结算；购物和补养即时生效。经验、研究、经济与成绩凭据不能重复结算或截断遗忘。
- 洗点全额返还，确认前列明影响并备份；当前续玩保留出发配置。旧修持切树须确认全额返还。

## 战斗与兼容边界

- 保留冻结旧规则分支，缺标记按各系统既有默认恢复。规则 3 routePlan、机关状态、追兵截止、精英延后及目标计时按快照继续，不补兵、不重计时。
- 1.0 明确接受的边界：经济 4 在途局未来新整备使用 cashOptions 分档，已保存且缺字段的整备保留单一数额；遭遇 8＋平衡 4 在途局未来幻境使用独立种子 gateOrder，已有缺字段快照仍走原序。不得声称两项仅影响全新历练。
- 绘制与输入使用同一相机；集火只改新出击剑首目标，不改在途飞剑、射程、穿透或冷却。触屏锁敌曾开启标志不得回退，成绩分别分组。
- 暂停、悟道、整备、帮助、确认、设置、失焦与离开战斗清除输入和闪避缓冲。键位只能在设置绑定，Esc 取消重绑，不静默修正旧键。
- 表现与声音不消费战斗随机数。72 剑演示纯表现；首领死亡特效不重复发奖、不跳过终局。
- 分步练习仅在试剑台按真实操作推进，使用实际冷却，不发正式收益。观察包与正式持久化完全隔离，观察期间设置只读。
- 成绩使用真实完成时刻，旧局不倒推分段。无尽按完成圈和当时锁敌状态／挑战选择分组；原种子分组语义不改。

---
> Source: [jindx1020-crypto/qingzhu-sword-array](https://github.com/jindx1020-crypto/qingzhu-sword-array) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-17 -->
