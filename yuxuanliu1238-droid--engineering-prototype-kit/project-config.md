---
trigger: always_on
description: 本项目用于指导产品经理借助 AI，将业务资料转化为可运行、可验证、可审查、可追溯的前端工程化原型。默认不建设生产级后台。
---

# 工程化原型项目协作规则

## 项目定位

本项目用于指导产品经理借助 AI，将业务资料转化为可运行、可验证、可审查、可追溯的前端工程化原型。默认不建设生产级后台。

## 事实来源

- 当前阶段和工作门禁以 `project-status.json` 为准。
- 已确认业务规则以 `docs/decisions/` 为准。
- 状态流转以 `docs/domain/` 为准。
- 页面功能、动作、状态和验收事实以 `prototype/src/contracts/` 为准。
- 实际可见界面和可操作行为以当前 React 原型为准。
- 增量需求状态以 `docs/requirements/registry.json` 为准。
- CR 的执行状态、AI 授权、就绪结论、人工介入和产物关系也以 `registry.json` 为准。
- 页面说明是必须同步的下游材料，不独立覆盖决策、状态机、契约或原型。
- `inbox/`、`examples/`、历史截图和旧报告只用于输入或追溯。

页面契约与原型不一致时，视为同步未完成，不得静默选择一方并宣布完成。

## AI 授权边界

AI 可以：

- 整理业务资料，明确区分事实、假设和待确认问题。
- 规划和实现前端交互原型。
- 在用户确认后记录业务决策和状态模型。
- 建立页面契约、页面说明、测试和校验脚本。
- 在已授权范围内同步 CR。
- 运行检查、预览和构建。

未经明确授权，AI 不得：

- 猜测或确认业务规则。
- 扩大范围或顺带修复无关问题。
- 根据需求标题推断完整影响范围。
- 启动 CR、将 CR 更新为已同步、冻结正式版本或关闭工作包。
- 修改历史正式快照。
- 创建正式后端、生产数据库、真实鉴权和生产配置。
- 将示例、mock、旧截图或实验代码视为正式事实。
- 虚构接口 URL、错误码、密钥或鉴权方式。

发现越界问题时，只输出证据、影响和建议归属，等待确认。

## 固定阶段

`初始化 → 业务探索 → Demo 规划 → 原型实现 → 决策与建模 → 页面契约 → 质量审查 → 用户评审 → 评审交付`

增量需求使用：

`已登记 → 待启动 → 影响分析 → 规则确认 → 决策记录 → 页面契约 → 可交互原型 → 页面说明 → 一致性检查 → 用户评审 → 已同步`

流程节点状态只使用：

`未开始、进行中、等待用户、已完成、已跳过、被阻塞`

## 任务控制面

阶段表示“交付走到哪里”，执行状态表示“现在能否开始”，不得混用。

- 执行状态：`locked、ready、in-progress、waiting-human、completed、cancelled`。
- AI 策略：`unassessed、planning-only、execution-allowed、human-only`。
- 不得根据对话语气自行提升 AI 策略；授权必须回写台账并有证据。
- `ready` 和 `in-progress` 必须通过就绪检查，授权不等于已就绪。
- `waiting-human` 必须记录原因、唯一问题、确认人、影响和恢复条件。
- `completed` 和“已同步”必须有类型化产物，并能说明支撑哪条验收事实。
- 自动检查可以判断和建议解锁，但不自动跨过人工门禁或启动下一阶段。

## 实施规则

- 新需求登记不构成实施授权。
- 开始实施前必须逐项检查目标、范围、验收、依赖、规则、授权和风险。
- 会改变产品结果的歧义必须由产品或业务确认。
- 简单文案和局部样式可不新增决策；长期规则和禁止恢复项应记录决策。
- 原型使用本地 fixtures；未确认接口写“待设计”。
- 未实现能力标记为 `partial` 或 `planned`，不得伪装成可操作。
- 只修改当前任务直接相关文件。
- 审查任务默认只读，用户确认问题后才允许修复。
- 一个工作包完成不自动启动下一工作包。

## 分级验证

1. 文案或局部样式：打开目标页面，确认渲染、目标区域和阻断性错误。
2. 单页面小交互：检查主路径、边界、取消恢复、相关契约和逻辑。
3. 共享逻辑或结构变化：运行契约、工作流、链接、相关测试和受影响入口构建。
4. 正式交付或多入口：运行 `pnpm check`、`pnpm build:delivery` 和关键流程浏览器检查。

只能引用 `package.json` 中真实存在的命令。不能把无法打开或有阻断性错误的页面交给用户。

## 完成证据

每次声称完成时必须说明：

- 实际修改文件；
- 已确认规则与修改的对应关系；
- 执行的检查和逐项结果；
- 未执行检查及原因；
- 未建设能力；
- 遗留风险；
- 是否仍需产品、业务或研发确认；
- 下一阶段是否已获得授权。

## 新人协作

如果需要用户操作：

- 一次只提供一个可执行步骤；
- 用通俗中文说明目的、预期结果和成功判断；
- 报错时先解释含义，再给一个排查步骤；
- 截图前提醒遮挡密钥、令牌、密码和个人敏感信息。

---
> Source: [yuxuanliu1238-droid/engineering-prototype-kit](https://github.com/yuxuanliu1238-droid/engineering-prototype-kit) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-28 -->
