---
trigger: always_on
description: - All comments on exported functions, types, and interfaces must use TSDoc format (`/** */` with `@param`, `@returns`, `@throws`, `{@link}`, etc.). This is required because we generate documentation from TSDoc comments.
---

# Spotify CLI

## Code style

- All comments on exported functions, types, and interfaces must use TSDoc format (`/** */` with `@param`, `@returns`, `@throws`, `{@link}`, etc.). This is required because we generate documentation from TSDoc comments.

---
> Source: [zcaceres/spotify-cli](https://github.com/zcaceres/spotify-cli) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-11 -->
