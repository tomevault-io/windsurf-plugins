---
trigger: always_on
description: - 英文名与项目名：QingTingMusic。
---

# 晴听音乐项目规则

## 项目信息

- 中文名：晴听音乐。
- 英文名与项目名：QingTingMusic。
- 项目目录：`D:\githubPro\IsleMusic`。
- 当前只维护 Windows 桌面端，不创建、恢复或维护 Web 版本。
- 默认启动页是“我的音乐”。
- 产品定位是搜索优先、播放顺手、推荐克制的个人音乐播放器。推荐页只提供每日推荐和私人 FM 等高价值入口，不堆叠瀑布流与无关运营内容。

## 开发前

- 先阅读 `docs/AI_DEVELOPMENT_GUIDE.md`。
- 运行 `git status --short`，保留用户和其他协作者尚未提交的改动。
- 一次只处理明确的一组相关需求，不顺手重构无关模块。
- 开始修改前先确认需求是否已经完成，避免重复实现。
- 如果 IDEA MCP 可用，优先用它定位文件、类、方法、符号和调用关系；定位后只读取相关小片段，减少无关上下文。
- 命令行主要用于 `rg` 批量文本搜索、Git 历史和差异、格式化、测试、构建、提交与推送。

## 架构边界

- `main.dart` 只负责应用初始化。
- 页面、组件、控制器、服务、模型和数据仓库分层维护。
- UI 不直接请求音乐 API，统一通过 `MusicRepository`。
- 播放状态统一通过 `PlayerController`，底层播放统一通过 `AudioPlayerService`。
- Windows 播放后端使用 `just_audio_media_kit` 和 `libmpv`，不得恢复为 `just_audio_windows`。
- 文件统一通过 `AppStorageService`，偏好设置统一通过 `AppPreferencesService`。
- 不把账号、Cookie、Token、设备密钥或其他凭证提交到仓库。

## 数据与缓存

- Windows 用户数据优先放在 `D:\QingTingMusic\userdata`。
- 无 D 盘或 D 盘不可写时，才回退到安装目录下的 `userdata`。
- 禁止把缓存和用户数据写入 `C:\Users\Public`、`LOCALAPPDATA` 或 `APPDATA`。
- 在线 URL 负责当前播放，音频缓存只能在后台进行；缓存失败不得中断播放。
- 覆盖升级必须保留用户数据，完整卸载必须删除程序和用户数据。

## UI 原则

- 默认主题保持晴空蓝、白色和冷浅灰；允许用户切换预设或自定义主题色，但品牌图标保持原色。
- 颜色统一从 `AppColors` 和主题控制器获取。用户选择的主题色应与实际主操作色一致，不再经过会使颜色灰暗的二次映射；收藏、危险、成功等状态使用独立语义色。
- 使用轻量玻璃感、连续浅色氛围和克制阴影增强层次，避免多层套卡、大面积高饱和渐变、夸张圆角和纯装饰动效。
- 标题栏、侧栏、内容区和播放器应形成连续背景；不要用明显横线或竖线把窗口机械切成几块。
- 左侧导航保持“我的音乐、搜索、推荐”三个主入口，账户与设置固定在底部。侧栏宽度、品牌区和菜单项应保持视觉居中，不挤压主内容。
- 明暗模式只保留品牌图标快捷入口和设置页完整入口，不重复放置状态图标；品牌文字不承担主题切换。
- 页面标签使用轻量文字、状态色和短指示线，避免与侧栏选中态重复使用大面积色块。
- 播放器控制保持同一基线和稳定分组，不为辅助时间或重复入口增加第二行。收藏按钮不应压过播放主操作，音量浮层必须锚定音量按钮。
- 只对真实可点击的控件使用手型指针；禁用项和普通歌曲行保持普通指针。
- 修改 UI 后检查 `1280x720` 和 `1536x900`，同时考虑未来 Android 端的信息结构复用。
- 页面跳转和返回必须有位置记忆。列表滚动位置、当前 tab、详情页来源和返回栈应由统一状态维护，不要返回后重置到“我的音乐/歌曲”默认页。
- 歌曲行信息保持稳定排版。悬浮按钮、收藏按钮、加入歌单、音量面板等交互不得挤动歌名、歌手、专辑和时长列。
- 多歌手显示必须使用可读文本，不允许显示一串空白或只有分隔符。每个可解析到主页的歌手应能进入对应主页；无法解析时显示纯文本。
- 从歌手页进入专辑或歌曲时，避免循环点击造成导航混乱。必要时将当前上下文中的歌手/专辑显示为不可点击文本。

## 开发策略

- 使用统一的开发流程，避免不同实现习惯在状态管理、UI 风格和验证方式上互相打架。
- 简单 UI、文案、间距和局部交互可以快速处理，但仍要遵守现有主题和组件风格。
- 播放状态、API、账号会话、缓存、分页、后台运行和跨模块重构必须整体分析后再修改。
- 涉及第三方官方接口、隐藏功能、下载功能或版权边界时，必须先分析方案和风险，不直接实现。
- 需要临时隐藏的功能只隐藏入口，不删除底层能力；恢复入口前要确认接口条件、凭证风险和 UI 状态都可控。
- 后端 API、更新代理、日志和诊断信息属于开发者管理，不放在普通设置入口。开发者口令只用于轻量隐藏入口，不作为安全边界；不要把真实密钥、Token 或平台 secret 写进客户端。
- 修复用户反馈的 bug 时，先定位“数据源、解析层、状态层、展示层、播放层”中的具体责任点，再改代码；不要只凭截图在页面层兜底。
- 一次修复不要同时改变多个排序入口。列表原始顺序、控制器派生顺序、页面展示顺序只能选一层负责反转或排序。
- 用户已经确认正常的行为不要顺手重写。比如收藏歌单详情不倒序、创建歌单详情倒序、我的音乐默认页等，除非用户明确要求。
- 新需求落地前先检查是否影响已确认正确的链路，尤其是歌手、云盘、收藏歌曲、歌单详情、专辑详情和搜索。不要为了修一个入口把另一个入口清空。
- 修改共享组件前先列出所有调用场景。歌曲行、详情页头部、播放栏、登录弹窗、播放队列这类组件被多处复用时，必须确认不同上下文的字段和点击行为。
- 对照 EchoMusic 时只读相关模块和 mapper，不全仓库大范围检索；优先查看 `server/module`、`src/renderer/api`、`src/renderer/utils/mappers`、相关 view 文件。
- 对照参考项目时只提取当前问题需要的接口、字段语义或交互思路，不照搬 UI、架构或无关实现；参考结论必须回到本项目现有分层中落地。

## API 与音乐库规则

- 真实 API 问题先用小范围命令确认接口路径、请求参数、返回字段和本地 mapper，再改 UI。
- 官方直连、登录态、云盘、歌手、收藏、搜索和播放 URL 这类问题，必须先用临时本地探针或脚本自动验证；不要让用户反复打开软件截图当主要验证方式。
- 临时探针可以读取当前构建目录的 `userdata/session.dat`，但不得打印 token、cookie、dfid、mid、guid 等敏感值；只输出登录状态、接口名称、数量、错误码和必要的截断结构。
- 修复官方接口前后必须用同一登录态验证关键入口，至少覆盖：用户歌单、默认收藏歌曲、关注歌手、云盘歌曲、搜索结果。目标是每项返回合理数量后再构建交付。
- 官方直连不得偷偷兜底到用户自定义 API 或反代接口；除非用户明确要求，否则设置留空时必须只请求官方服务器。
- 概念版官方接口是一套完整参数体系，不要只改 `appid/clientver`。涉及签名时同时检查 Android salt、RSA 公钥、`signKey`、`signParamsKey`、`clientver/appid` 和接口是否需要 lite 或标准参数。
- 微信登录这类需要 `secret` 的官方开放平台能力必须走后端，不得把开发者密钥写进 Flutter 桌面端；如果暂时没有可用后端，只隐藏微信入口并保留实现代码。
- 登录 UI 默认保留“扫码”和“手机验证码”两个入口；切换登录方式时保持弹窗尺寸稳定，使用轻量过渡，避免高度跳变。
- API 字段解析要兼容常见别名，例如 `hash/FileHash/file_hash`、`audio_id/audioid/mixsongid/album_audio_id`、`filename/file_name/songname/audio_name`。
- 多歌手字段必须同时检查名字数组、id 数组、字符串分隔形式和列表对象形式；不能只按一个 singername 字符串渲染。
- 云盘歌曲可能没有 `hash`，列表展示可以用 `audio_id/mixsongid/album_audio_id` 作为身份；播放时再按可用字段解析 URL。
- 歌手收藏接口只展示歌手，不混入用户；字段判断要参考 `singerid`、`iden_type`、`jumptype`，但不要因为某个字段缺失就把全部数据过滤为空。
- 分页接口优先做有限分页和去重，避免只加载第一页；同时设置上限，防止接口异常导致无限请求。
- 收藏歌曲、专辑、歌单和云盘的分页加载要分别验证数量，不要用一个入口的成功推断其他入口也成功。
- 音乐库实体要区分来源 ID、用户侧 ID、展示 ID 和删除/操作 ID。涉及收藏、取消收藏、添加歌曲、删除歌曲时，先确认接口真正需要哪个 ID，再改状态或 UI。
- 我的音乐的信息架构要稳定：歌曲、歌单、专辑、歌手、云盘、最近播放各自归位；歌单内继续区分创建与收藏。修一个入口时不得把实体跨栏目合并或临时隐藏入口。
- 我的收藏歌曲应按最近收藏在前展示；收藏歌单详情保持原歌单顺序；创建歌单详情按用户确认的最新加入逻辑展示。不要在多层重复 reverse。
- 缓存只作为首屏加速。刷新失败时保留旧数据并提示，不要把空响应直接覆盖掉已有可用数据。
- 调试缓存问题时要区分“缓存展示成功”和“当前接口刷新成功”。切换 API 模式、清空 API 地址或重新登录后，应先失效内存加载状态，再用探针或强制刷新确认真实接口结果。
- 播放 URL 获取失败时按固定链路处理：官方 URL、收藏/爱心相关可用路径、云盘精准匹配；仍失败时提示“该歌曲无版权或需付费”，不要显示泛化网络错误。
- 云盘替代播放必须精准匹配歌曲名和歌手，替代成功时给轻量提示，例如“已替换为云盘歌曲”，提示后自动消失。

## 播放与队列

- 双击歌曲时，用当前歌曲所在列表替换播放队列并立即播放；播放队列、当前高亮、底部播放栏和播放模式必须保持一致。
- 播放模式至少包含顺序播放、列表循环、单曲循环和随机播放；播放结束、手动切歌、删除队列项和替换队列后都要验证。
- 解决“进度到最后不跳下一首”时，要检查播放器状态流、duration/position 误差、processingState 和兜底定时判断，不只依赖单一 completed 事件。
- 选中一首歌后底部播放栏应立即显示该歌曲标题和封面；URL 解析和缓存下载可以异步完成，失败不得让旧歌继续看起来像正在播放。
- 音量调节使用真实播放器音量；弹出层必须可操作、不遮挡主体、不因鼠标移动过快立刻消失。
- 随机播放必须记录真实听歌历史；“上一首”返回实际播放过的歌曲，不重新随机抽取。
- 私人 FM 使用独立连续队列并保持顺序播放。频道或偏好变化时重新请求；接近队尾时自动补充，不能只循环首批歌曲；离开 FM 后恢复进入前的播放模式。
- 每日推荐与私人 FM 入队前必须补齐可获得的歌曲时长、专辑 ID 和专辑名称，避免队列显示 `00:00` 或专辑入口失效。

## 验证

- 完成代码修改后默认运行 `.\scripts\verify.ps1`。
- `.\scripts\verify.ps1` 默认执行格式化、静态检查、核心单测和 Windows 构建，不默认跑全量 Widget 测试，避免持续动画或插件初始化拖住开发。
- 需要完整 UI 回归时运行 `.\scripts\verify.ps1 -FullTests`；如需限制等待时间，可加 `-FullTestTimeoutSeconds 180`。
- 只想快速检查代码时可以运行 `.\scripts\verify.ps1 -SkipBuild`。
- 不要把 `test\widget_test.dart` 或全量 widget 测试作为小 UI 改动的默认验证。当前应用初始化包含窗口管理、音频后端、定时器、登录/更新轮询和持续动画，测试环境容易因为未静止的异步任务卡住。小 UI 改动优先跑 `flutter analyze --no-pub`、相关服务/控制器单测和 `flutter build windows --no-pub`。

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [bingjiu-lei/QingTingMusic](https://github.com/bingjiu-lei/QingTingMusic) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-10 -->
