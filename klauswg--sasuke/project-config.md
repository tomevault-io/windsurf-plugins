---
trigger: always_on
description: 每次实现需求时，都应该先思考，当前的需求，是否已经有现有的开源组件可以满足需求，是否已经有行业内最佳实践，都没有的话，才考虑自己实现。
---

当前项目的全量设计方案位于：
docs\sasuke\产品设计文档
当前项目的MVP版本开发计划位于：
docs\sasuke\开发计划
每次代码修改，必须同步维护以上两类文档

每次实现需求时，都应该先思考，当前的需求，是否已经有现有的开源组件可以满足需求，是否已经有行业内最佳实践，都没有的话，才考虑自己实现。
每次出方案时，必须先告诉用户当前的方案才开始实现，方案设计时对问题的解决思路是：1.这个问题是否来源于根本性的设计缺陷？如果是则需要把设计缺陷一起修复掉，宁愿多付出一些成本 2.这个问题是否是来源于好的设计但是实现不够完善？如果是则基于好设计，继续完善实现。必须杜绝针对特定问题的补丁式修复。
修复 Bug 前必须回溯原始设计意图和缺陷形成路径，判断根因属于原有设计缺陷、正确设计但实现不完整，还是业务或实现逻辑错误，并向用户说明判断依据及对应修复方向。
修复 Bug 时，完成根因分析后应优先编写最小失败测试稳定复现问题，确认测试失败原因与分析一致后再修改实现；无法可靠自动复现时，必须说明原因并记录可审计的替代证据。
每次实现功能后并验证无误后，必须进行单元测试，从接口层面把正确验收固化下来，这样方便后续改动时进行回归。

优秀的设计思路：
1.分析哪些数据结构
2.不同数据结构分属于哪些不同领域
3.生命周期相关联的数据结构，状态应该统一管理
4.先定数据、再定接口、再补实现
5.杜绝硬编码，应该统一设置成变量，设计时要考虑到变量的应用范围，可复用的应该有单独的类管理，类中单独的变量则可以放在类中
6.错误码等不要使用简单的string，而是用结构体管理，至少包含code、msg等字段，后端只返回错误码，前端根据错误码进行处理，后端不应该包含任何对客文案的处理
7.使用现代开发的最佳实践，积极使用现成的库、框架、工具、组件等，严禁无效地重复造轮子

当前项目前端使用tailwindcss+shadcn-ui, chat ui界面使用prompt-kit。必须优先根据页面需要使用组件库生成copy-in代码，不能自己手写代码，可以在生成的copy-in代码中，根据需要进行修改

当前项目所有内置提示词（profile、runtime、repair、AI-DYNAMIC 等）必须统一放在 `src/prompts/` 下管理，不允许把长 prompt 文本散落硬编码在实现代码里。
提示词目录必须按语言组织为 `src/prompts/zh-CN/...` 和 `src/prompts/en/...`，语言目录下的子目录结构必须保持一致；新增或修改提示词时，必须同步维护中英文版本。

system prompt / user prompt 的固定区分标准：
1. 由 runtime 决定、用户不需要直接操心、且需要稳定执行的上下文，放 system prompt，例如角色、历史、目录路径、文件规则、能力边界、剩余预算、输出协议、repair 指令。
2. 与本次执行目标直接相关、会随任务变化的内容，放 user prompt，例如 requirement、goal、当前 task。
3. 任何需要模板变量、条件分支或修复重试的提示词，也必须继续放在 `src/prompts/` 下统一管理和渲染，不能回退成代码内硬编码字符串。

# 团队共享协作规则

## 经验沉淀机制

- 用户修正设计或要求修复 Bug 后，应在完成并验证操作后复盘根因，判断该问题是否能提炼为可预防同类问题的通用经验。
- 提议沉淀前必须先检索 `docs/sasuke/rules/`；已有相同或等价规则时不得重复添加，必要时只建议收紧现有规则。
- 规则缺失且确有复用价值时，应先向用户说明拟沉淀的精炼原则、它解决的根因和预期收益，并询问是否需要写入。
- 只有用户明确同意后，才可以新增或修改规则及其 `AGENTS.md` 路由；未获得明确同意时不得自行沉淀。
- 经验表述必须精简、直接、可执行且可验收，聚焦普适约束，不得复述具体 Bug、截图或冗长实现过程。

## 桌面端产品心智

- sasuke 是 EXE 桌面产品，主路径不要保留 command bar / terminal 心智。
- 优先使用菜单、工具栏、按钮、上下文菜单、对话框、设置页等原生可视化交互。

## 前端组件策略

- 前端样式和交互优先使用 shadcn/ui、Tailwind CSS utilities 和成熟依赖。
- AI chat / ACP / agent session UI 优先使用 prompt-kit copy-in 组件。
- 不要自研消息容器、输入框、工具调用卡片、Markdown 渲染等基础控件；只在项目 i18n、数据映射或样式 token 需要时修改 copy-in 组件。

## UI 交互详细规则

涉及前端 UI、交互、页面布局、弹窗、抽屉、右侧工作区、响应式设计、选择器、按钮状态、导航状态或用户偏好记忆时，开始设计或修改前必须完整读取并遵守：

- `docs/sasuke/rules/ui-interaction.md`

修改上述交互原则时，必须同步维护该规则文件。不得把详细规则复制回本文件形成两份真源。

## 数据加载与渐进加载详细规则

涉及应用启动、首屏、页面或 Tab 初始化、列表、选择器、摘要/详情/正文接口、目录或历史读取、分页、搜索、数据刷新、Tauri IPC、数据库查询、文件系统扫描、后台诊断或任何可能随数据规模增长的加载路径时，开始分析、设计或修改前必须完整读取并遵守：

- `docs/sasuke/rules/data-loading.md`

实现必须先明确数据规模、增长上限、消费字段和关键路径，再按数据重量拆分接口并优先渐进加载；不得让全量历史、逐项详情、诊断或维护工作阻塞基础身份与首批可见数据。修改上述原则时，必须同步维护该规则文件，不得在本文件复制第二份详细规则。

## 前端性能详细规则

涉及 React 状态边界、组件重渲染、Context、渐进数据的 UI 投影、分页 DOM 合并、流式更新、Markdown 渲染、Resize/Scroll 热路径或任何前端性能优化时，开始分析、设计或修改前必须完整读取并遵守：

- `docs/sasuke/rules/frontend-performance.md`

性能优化必须先确认瓶颈和可验收目标，优先修复数据量、刷新范围和状态边界等根因，不得用零散 `memo` 或局部节流掩盖设计缺陷。修改上述性能原则时，必须同步维护该规则文件，不得在本文件复制第二份详细规则。

## 状态、生命周期与数据完整性详细规则

涉及实体身份与作用域、canonical state、生命周期状态机、authoring definition 与 executable snapshot 分层校验、异步竞态、持久化、原子写入、幂等、并发冲突、结构化错误、能力发现或资源信任边界时，开始分析、设计或修改前必须完整读取并遵守：

- `docs/sasuke/rules/state-lifecycle-and-data-integrity.md`

实现必须先明确数据归属、权威事实源和状态转换，再定义接口与写入顺序；不得用名称反查、旁路状态、迟到响应覆盖或补丁式兼容掩盖边界缺陷。修改上述原则时，必须同步维护该规则文件，不得在本文件复制第二份详细规则。

## Bug 修复验证详细规则

涉及 Bug 诊断与修复时，开始修改实现前必须完整读取并遵守：

- `docs/sasuke/rules/bug-fix-verification.md`

Bug 修复必须先验证根因，优先通过最小失败测试建立可重复证据，再实施根因修复并使用同一测试确认转绿。修改上述原则时，必须同步维护该规则文件，不得在本文件复制第二份详细规则。

## 前端验证规则

- 涉及 UI、交互、页面布局、样式或客户端流程时，必须启动前端并验证。
- 优先 deep link 到目标页面，避免从任务列表反复手动点入。
- 验证结束后清理自己启动的测试资源，避免长期会话堆积。

## 开发阶段破坏式更新

- 当前项目仍处于开发阶段，明确替换旧方案时优先删除旧入口、旧字段、旧消费路径。
- 不为旧 UI 或旧数据结构增加兼容层、灰度逻辑和 fallback。

## UI 文案与视觉约束

- UI 只展示帮助用户决策或完成操作的文案。
- 不展示解释实现方式、布局原因、未来扩展性的说明。
- 深色主题减少浅黑色方块、嵌套卡片和强边框。
- 通过留白、层级、少量边界和重点状态表达，而不是堆叠面板。

## Chat / ACP 展示约束

- Chat / ACP / agent message 中 Markdown 标题不要使用文档页大标题样式。
- 标题字号接近正文，通过字重、轻量间距、细分隔表达层级。
- ACP 工具/思考结构化行不显示头像，但保留横向位置。
- ACP 工具卡片高度压缩，标题左对齐，展示操作名和关键参数。
- ACP 的“发送中 / 处理中 / 计时”状态放 composer 内，不作为消息流卡片。

## 命令 / 终端 调用约束
在 sasuke 桌面端中，所有不需要用户交互的外部命令或终端调用都必须通过 process::background_command() 创建进程，不能直接使用 std::process::Command::new()。该 helper 会在 Windows 上统一应用隐藏控制台窗口的启动参数，避免 cmd.exe、git.exe、taskkill、mklink、MCP/ACP helper 等后台子进程弹出黑色终端窗口，保证产品保持桌面应用心智而不是 terminal 心智。


## 验证工具使用规则

- 涉及页面、路由、交互、布局或样式时，除非用户明确指定浏览器或工具，否则必须优先使用当前 agent 的内置浏览器；只有内置浏览器不可用或无法完成任务时，才能使用项目内 `agent-browser` skill 和 CLI。验证时优先 deep link 到目标页面。
- Codex 特例：必须先通过 `mcp__cua_repl.js` 尝试 `iab`；`iab` 不可用时再尝试已连接的 Chrome；两者均不可用或无法完成任务时，才能使用 `agent-browser`。
- 只有在必须验证 EXE 桌面客户端、Windows 原生窗口或客户端级交互时，才使用桌面控制工具操控本地客户端。
- 验证结束后关闭本次页面、浏览器会话、测试进程和临时资源。

# 方案自评审

用户向你寻求解决方案时，你再产出方案前，必须按下面的规则进行方案审视并保证符合要求，并在向用户说明方案的同时说明各项审视结果。

## 方案过度设计评审

每次出方案和完成验收时都必须评审方案是否过度设计，确认新增抽象、状态、依赖、缓存、队列或并发机制与实际问题、数据规模和风险相匹配；现有机制足以满足需求时优先复用，不得为假设性需求增加复杂度。

新增 aggregate、状态机、持久字段或跨层 identity 前，必须明确现有 canonical identity、sequence/revision 和 lifecycle 无法表达的具体不变量。现有事实已经足够时，应修复消费端投影或状态转换契约，不得为单一 UI 竞态复制 canonical 数据模型。

## 代码性能影响评审

每次代码修改，无论需求是否以性能优化为目标，都必须在方案设计和完成验收时评审性能影响。

- 评审数据规模与增长上限、算法复杂度、I/O 与数据加载量、调用频率、刷新与渲染范围、内存与缓存、队列与背压、并发锁范围及阻塞行为。
- 主动检查是否引入全量扫描、全量加载、N+1 请求、重复解析、无界缓存或队列、过大状态订阅、事件线程阻塞、长时间持锁或无意义串行等待。
- 存在性能风险时，必须先定义数据规模、基线和验收目标，再使用生产构建、Profiler、benchmark 或性能时间线验证，并按风险增加回归测试。
- 无明显性能风险时，可以记录结论及判断依据，不要求为了形式增加无意义的 benchmark。
- 性能优化不得破坏正确性、数据完整性和可维护性；优先解决算法、数据量、刷新范围和状态边界等根因，不使用盲目缓存、并行化或零散微优化掩盖设计缺陷。
- 涉及前端性能时，还必须完整遵守 `docs/sasuke/rules/frontend-performance.md`。

---
> Source: [klauswg/sasuke](https://github.com/klauswg/sasuke) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-23 -->
