---
trigger: always_on
description: AI instruction file for txai-support by badesouza
---

# Cursor Rules (Project-level)
#
# These rules constrain the AI coding agent behavior for this repository.
#
# CRITICAL:
# - Do NOT run `git commit` or create commits unless the user explicitly asks for it
#   in the current message (clear permission). If a commit is desired, ask for
#   confirmation and the exact commit message first.
# - Do NOT run `git push` unless the user explicitly asks for it in the current message.
#
# UI / Styling:
# - Do NOT change theming, dark-mode behavior, or global UI styling unless the user
#   explicitly requests it in the current message.
# - If a bug report involves UI, restrict changes to the smallest scoped fix and
#   confirm any global style/theme changes before applying them.
#
# Safety:
# - If you're about to do a destructive action (reset, clean, remove volumes/data),
#   ask for confirmation unless the user explicitly requested that exact action.

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/badesouza) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:windsurf_rules:2026-04-09 -->
