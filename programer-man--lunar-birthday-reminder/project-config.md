---
trigger: always_on
description: - 任何技术选型、方案设计、实现细节，**必须先由我列出 2~4 个方案 + 各自的优缺点/适用场景，用通俗语言解释清楚，由用户选择后再动手**。
---

# 阴历生日提醒（Lunar Birthday Reminder）

## ⚠️ 协作规则（最重要，整个项目期间严格遵守）

- 任何技术选型、方案设计、实现细节，**必须先由我列出 2~4 个方案 + 各自的优缺点/适用场景，用通俗语言解释清楚，由用户选择后再动手**。
- 不默认替用户做技术决定；不用术语轰炸；关键决策点按「列方案 → 解释 → 等用户选」的流程走。
- 涉及装环境、装库、选数据库、选通知方案等技术动作，同样要先解释再执行。

## 项目概述

- 名称：阴历生日提醒
- 定位：以**阴历生日**为核心的个人提醒 App，提前提醒、到点反复提醒，避免忘记亲友生日。

## 技术选型（已确定）

- 框架：Flutter（Dart 语言），跨平台手机 App。
- 平台：先做安卓版（Windows 环境）；苹果打包需 Mac，暂缓。
- 待实现阶段确认的库（届时先列方案对比再定）：
  - 本地通知：flutter_local_notifications
  - 本地存储：sqflite / hive / drift（三选一）
  - 阴历转换：lunar（6tail 出品）

## 产品需求

### 1. 提醒增删改查（CRUD）
- 字段：姓名、头像、阴历生日、阳历生日（可选）、提前天数、通知间隔、启用开关。
- 列表页：按「下一个生日最近」排序，显示头像 + 姓名 + 距今天数。
- 新增/编辑页：表单填写；删除需二次确认。

### 2. 生日判断（核心逻辑）
- 以阴历生日为准：把「今天」换算成阴历月日，与提醒里填的阴历月日比对，相等即视为生日。
- 闰月不作区分：阴历闰月一律按同名平月处理（闰二月 = 二月）。

### 3. 提前通知
- 每个提醒可设提前 1~7 天通知（默认提前 1 天）。

### 4. 通知间隔
- 提醒窗口内按设定间隔反复通知，可选 15 秒 ~ 2 小时（默认 1 小时）。

### 5. 补充细节
- 头像：默认 Emoji 快速选择，可选相册图片（v2）。
- 展示：生日当天显示「今天」；显示年龄、生肖（阴历）、星座（阳历）。
- 每个提醒可单独启用/停用。
- 数据存本机，无需联网、无需账号。

## 数据模型

Reminder {
  id: String                 // 唯一标识
  name: String               // 姓名
  avatar: String             // emoji 或图片路径
  lunarMonth: int            // 阴历月 1-12
  lunarDay: int              // 阴历日 1-30
  solarMonth: int?           // 阳历月（可选）
  solarDay: int?             // 阳历日（可选）
  advanceDays: int           // 提前天数 1-7
  notifyIntervalSeconds: int // 通知间隔 15~7200 秒
  enabled: bool              // 是否启用
  createdAt / updatedAt: DateTime
}

## 页面结构

1. 首页/列表：所有提醒按最近生日排序，右上角「+」新增。
2. 新增/编辑页：表单（姓名、头像、阴历生日、阳历生日、提前天数、通知间隔、启用开关）。
3. 详情页：完整信息 + 编辑/删除/开关。

## 开发阶段

1. 环境搭建（Flutter SDK + Android Studio，带用户一步步装）
2. 数据层（本地存储 + Reminder 模型）
3. 界面（列表 / 新增编辑 / 详情）
4. 阴历逻辑（换算 + 生日判断）
5. 通知（本地通知 + 提前天数 + 间隔）
6. 打磨（头像、生肖星座、排序、开关）

---
> Source: [programer-man/lunar-birthday-reminder](https://github.com/programer-man/lunar-birthday-reminder) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-15 -->
