---
trigger: always_on
description: This file provides guidance to Claude Code (claude.ai/code) when working in this repository.
---

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working in this repository.

## Commands

```bash
uv pip install -e ".[dev,ws,explorer,mcp]"
uv run pytest tests/test_unit.py tests/test_async_unit.py -v
uv run ruff check pyth_pandas tests
uv run ruff format pyth_pandas tests
uv run mypy pyth_pandas
uv run python -c "from pyth_pandas import PythPandas, AsyncPythPandas"
```

## Package structure

```
pyth_pandas/
  __init__.py        # public exports
  client.py          # PythPandas dataclass — request helpers, preprocessing
  async_client.py    # AsyncPythPandas via composition + ThreadPoolExecutor
  exceptions.py      # PythError → PythAPIError → AuthError / RateLimitError
  utils.py           # preprocess_dataframe, filter_params, snake_to_camel, ...
  schemas.py         # pandera DataFrameModels (one per resource)
  types.py           # TypedDicts (JsonUpdate, SignedGuardianSetUpgrade, ...)
  ws.py              # sync WebSocket (extra: ws)
  async_ws.py        # async WebSocket (extra: ws)
  mcp_server.py      # FastMCP server (extra: mcp)
  py.typed
  mixins/
    _prices.py       # /latest_price, /price, /reduce_price
    _governance.py   # /guardian_set_upgrade
```

## Architecture (mirrors polymarket-pandas conventions)

- **Sync first, async via composition.** `AsyncPythPandas` wraps a sync
  `PythPandas` and runs each method in a `ThreadPoolExecutor`. Wrappers are
  auto-generated by `_populate_async_methods()` using `callable()` so
  `cachedmethod` descriptors are caught.
- **Every DataFrame return has a pandera schema** (`ParsedFeedSchema`) and a
  `DataFrame[Schema]` annotation. `_Lenient` base: `strict=False`,
  `coerce=True`.
- **Every dict return has a `TypedDict`** in `types.py`.
- **Auth guards** raise `PythAuthError` *before* any HTTP call when the
  bearer token is missing.
- **Single error mapping**: `_handle_response` is the only place HTTP status
  codes turn into exceptions.
- **`preprocess_dataframe` pipeline**: snake→camel, drop columns, coerce
  numeric/datetime/bool/json by name. Pyth uses microsecond timestamps
  pervasively, so this fork adds a dedicated `us_int_datetime_columns` axis
  beyond the four standard ones.

## Adding endpoints

1. Add a method to the appropriate mixin in `mixins/_<tag>.py`.
2. If it returns a list, define / extend a pandera schema in `schemas.py`
   and use `DataFrame[Schema]` as the return type.
3. If it returns a dict, define a `TypedDict` in `types.py`.
4. Re-export new schemas/TypedDicts from `__init__.py`.
5. Add a mocked unit test in `tests/test_unit.py` (and async mirror).

## Generated by

Scaffolded by the `api-pandas` Claude Code skill on 2026-04-14.

---
> Source: [sigma-quantiphi/pyth-pandas](https://github.com/sigma-quantiphi/pyth-pandas) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-04-26 -->
