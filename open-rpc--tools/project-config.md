---
trigger: always_on
description: Before committing any changes, contributors MUST run `npm test` and `npm run lint` from the repository root and ensure both pass.
---

# AGENT Instructions

Before committing any changes, contributors MUST run `npm test` and `npm run lint` from the repository root and ensure both pass.

Each pull request MUST include a new changeset generated with `npm run changeset` describing the changes being made.

---
> Source: [open-rpc/tools](https://github.com/open-rpc/tools) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-08 -->
