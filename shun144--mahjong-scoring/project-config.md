---
trigger: always_on
description: このファイルは、将来の Claude セッションが本リポジトリで作業する際の道標。
---

# CLAUDE.md — プロジェクトガイド

このファイルは、将来の Claude セッションが本リポジトリで作業する際の道標。
**詳細な仕様は `SPEC.md`、実装計画は `TASKS.md` を正典とする。** 迷ったら両者を参照。

---

## プロジェクト概要

麻雀の**点数計算**をスマホWebで反復練習するドリルアプリ。ランダム出題された和了形（手牌・上がり牌・ドラ・裏ドラ・自風・場風・親子・リーチ有無）を見て、正しい答えを4択から選ぶ。出題は2モード:**最終点数モード**（最終点数を答える）と**符計算モード**（符を答える）。回答後に内訳（役・翻・符・計算式）と高点法の別解を解説し、苦手分野を記録・復習する。

## 技術スタック

- **React + TypeScript + Vite**（静的ホスティング）
- テスト: **Vitest**（エンジン）／ **Playwright**（E2E）
- 状態の永続化: **localStorage**
- 牌は**画像スプライト**（PNG/SVG。鳴きは横向き、赤ドラは赤色）

## ディレクトリ構成

bulletproof-react（機能で縦割り）＋オニオン（機能内を層で整理）のハイブリッド構成。
フォルダ構成・層分割・依存ルールの詳細は **`ARCHITECTURE.md`を正典**とする。

```
src/
  core/
    scoring/              # 純粋TS: 牌モデル, 和了分解, 役判定, 符/翻, 点数, ドラ, 高点法（唯一のコア・ドメイン。旧engine/→core-domain/）
      domain/
  features/
    practice/             # 出題（4モード: 最終点数/符計算/符分解/点数換算）＋成績・復習
      domain/  application/  infrastructure/  presentation/
    settings/             # 設定（切り上げ満貫トグル等）
      domain/  application/  infrastructure/  presentation/
    articles/             # 学習ガイド記事
      domain/  infrastructure/  presentation/
  shared/                 # 複数featureが使う共有UI（牌描画一式・PageHeader/Sidebar/ErrorBoundary/ChoiceGrid等）
  app/                    # ルーティング・静的ページ・画面組み立て（App.tsx, HomePage, About/Contact/Privacy, RouteMeta, site.ts）
  main.tsx
ARCHITECTURE.md     # フォルダ構成・依存ルール（正典）
SPEC.md             # 仕様書（正典）
TASKS.md            # 実装計画
STYLE-TRANSFER.md   # CSS→Tailwind移行のルール（T-014/T-015より抽出）
```

## 開発コマンド

```bash
npm run dev      # 開発サーバ
npm run build    # 本番ビルド
npm run preview  # ビルドのプレビュー
npm test         # ユニットテスト（Vitest）
npm run lint     # Lint
```

※ P0 セットアップ時に確定。実際のスクリプトは package.json を参照。

---

## 設計原則

- **エンジンは純粋関数＋型安全**。UIから独立させ、テストで正確性を担保する。
- **教材のため誤答提示は致命的**。点数ルール（`SPEC.md` §5）はユニットテストで網羅する。
- **問題バンクはエンジンの回帰テストフィクスチャ**を兼ねる（バンクの正解＝エンジン出力）。
- **高点法**: 複数解釈は最終点数が最大の解釈を正とする。
- 出題は**必ず最低1役成立**する正当な和了形のみ（ドラのみの手は出さない）。
- 問題には**符の型／役の種類**のタグを付与し、苦手復習に使う。

## スコープ境界

**含む**: 四人麻雀・標準リーチ麻雀／門前・鳴き（喰い下がり）／七対子・特殊符／役満・数え役満／赤・裏・ドラ（ドラ表示牌からの読み替え）／リーチ・門前ツモ・自風・場風・親子／高点法／**符計算モード（符を答える練習）**。

**含まない**:

- 状況役（一発・海底・河底・嶺上・槍槓・ダブリー・天地人和・流し満貫）
- ハウス/ローカルルール（切り上げ満貫・ダブル役満・役満の複合・青天井）
- 三人麻雀、両対応
- 持ち点・ウマ・オカ・順位・供託加算
- タイマー／タイムアタック、PWA/オフライン、多言語（日本語のみ）

## 前提（デフォルト）

- 局条件: 場風=東・南、自風=東南西北（西・北場は出題しない）。**自風=東の家は必ず親**（自風=東 ⟺ 親。SPEC §4.1）
- 赤ドラ: 各色5に赤1枚（計3枚）
- ドラ表示牌: 枚数は「1＋槓の数」（基本1枚＋槓ごとに1枚、上限5枚。麻雀ルール上「なし」はあり得ない。SPEC §5.4）
- 標準ルール（役満1倍・複合なし・数え役満は13翻以上→役満扱い）
- 符計算モードは満貫以上（符が点数に影響しない区分）を出題しない（SPEC §4.0）

---

## 麻雀用語の要点

点数 = **符**（手の形の加算値、10符切り上げ）と**翻**（役・ドラの合計）から算出。
基本点 = 符 × 2^(2+翻)。**親/子・ツモ/ロン**で支払いが変わる（`SPEC.md` §5.3）。
用語の詳細と対象役リスト・符/点数テーブルは **`SPEC.md` §3・§5** を参照。

## スタイル方針

- CSSをTailwind化する作業（既存 `.css` の置換・新画面の移行）を行う際は **`STYLE-TRANSFER.md`** のルールに従う。

## ドキュメント方針

- すべてのドキュメント（README・仕様・設計）は**日本語**で作成・維持する。
- 仕様変更時は **`SPEC.md` を先に更新**し、実装とテストを追随させる。

---
> Source: [shun144/mahjong_scoring](https://github.com/shun144/mahjong_scoring) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-31 -->
