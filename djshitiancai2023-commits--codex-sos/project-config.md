---
trigger: always_on
description: 这是一份 clean-room 公开项目规则。不要读取、复制或借用任何私有仓库、真实会话、真实项目路径、生产数据、内部提示词或私有工作流。
---

# Codex SOS 项目约定

这是一份 clean-room 公开项目规则。不要读取、复制或借用任何私有仓库、真实会话、真实项目路径、生产数据、内部提示词或私有工作流。

## 产品底线

- 普通用户只需要给一张报错截图，或随便写一句发生了什么。其余步骤自动完成。
- 正常流程不追加表单。确实缺少决定性信息时，最多追问一次，只能使用大白话选择题，并标出推荐项和推荐原因。
- 结果页先回答四件事。大概是什么、是不是只有我、现在最安全怎么做、还不行怎么办。
- 结论来自透明、可测试的固定规则。证据不足就写“不确定”，不得声称已经确定根因。
- 官方体检全绿不等于没有故障。没有搜到相似 issue 不等于没人遇到。
- 安全建议必须低风险、可逆。不得自动删除、重置、重装、改网络、改 Codex 配置或发布 issue。

## 数据边界

- Codex SOS 本身不得直接打开或枚举 `auth.json`、会话目录、聊天、提示词、用户项目、Git 私有远程或整个 `.codex` 目录。
- 官方 `codex doctor --json` 作为独立的官方黑箱自动运行。官方体检可能读取它需要的 Codex 状态、联网检查，并在必要时维护登录状态。这些是官方进程自身的行为。
- SOS 只接收官方体检返回的内容，保留少量允许字段；原始输出不落盘，展示和导出前再次遮盖。
- SOS 等待官方体检最多 25 秒。到时界面先继续，本次不再次启动。真实官方体检允许自然结束，避免在它维护登录状态时强行中断。
- 截图只在内存中用于本机识字。默认不保存、不上传，也不进入公开报告。
- 自动联网限于只读的 OpenAI 状态页和 `openai/codex` 公共 issue 搜索。发送搜索前先遮盖，只发送少量稳定错误词。不得调用 OpenAI API，不得要求 API Key。
- 自动遮盖不能宣传为 100% 安全。保存材料前必须给用户一个简短、能看懂的预览。

完整数据说明见 [`PRIVACY.md`](PRIVACY.md)，可执行验收规则见 [`docs/ACCEPTANCE_MATRIX.md`](docs/ACCEPTANCE_MATRIX.md)。

## 工程边界

- v0.1 只做 Windows x64 桌面应用。主栈为 .NET 10 和 WPF。
- 不增加服务器、数据库、账号系统、大模型调用、自由框选、语音、自动修复或自动提交 GitHub issue。
- 平台相关能力放在 App 层，通过 Core 接口接入。固定判断、遮盖、相似度和公开报告留在 Core 层。
- 所有外部进程都要有等待上限和输出上限。失败、不支持、超时和格式变化必须降级到结果页。
- 所有网络请求都必须走受控客户端，只允许公开只读 GET。不得把截图、原始故障记录或完整用户描述放进请求。
- 新依赖必须说明直接收益、许可证、发布体积和维护成本。优先使用成熟且能随安装包离线运行的组件。

## 修改与验证

- 先读离改动最近的文件和测试，不重复扫描整个仓库。
- 只做完成当前目标所需的最小改动；不顺手扩功能。
- 修复必须带防回归测试。正式测试数据只能是明确标注的虚构资料。
- 不得在测试中运行用户机器上的真实官方体检。使用受控的假进程覆盖正常、警告、失败、不支持、超时和损坏输出。
- 不得把真实截图、真实日志、账号、用户名、路径或会话写入 fixture、截图证据、提交历史或发布包。
- 修改者不能只凭自己的说明判定 Gate 通过。以可重跑测试、UI 截图、运行回执、文件清单和独立复核为准。

常用入口如下。

```powershell
pwsh ./scripts/test.ps1
pwsh ./scripts/build-release.ps1 -Version 0.1.0
```

构建脚本不得在测试或打包时调用真实 `codex doctor`。构建结果不能自动上传或发布。

## 文案规则

- 面向用户默认使用简体中文和大白话，少用缩写与开发术语。
- 原始证据、程序推断和最终建议分开写。
- 只使用“很可能有关”“可能有关”“暂未发现”“这份检查无法解释当前故障”等保守表达。
- 明确标注“非官方社区工具，与 OpenAI 无隶属关系”。不得使用 OpenAI 官方 Logo 或暗示授权。

---
> Source: [djshitiancai2023-commits/codex-sos](https://github.com/djshitiancai2023-commits/codex-sos) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-04 -->
