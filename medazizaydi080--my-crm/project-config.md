---
trigger: always_on
description: This document provides essential context for AI models interacting with this project. Adhering to these guidelines will ensure consistency and maintain code quality.
---

# GEMINI.MD: AI Collaboration Guide

This document provides essential context for AI models interacting with this project. Adhering to these guidelines will ensure consistency and maintain code quality.

## 1. User-Provided Instructions & Overrides

_This section is for human developers to provide explicit instructions, overrides, or clarifications that might not be obvious from the codebase. AI collaborators should treat this section as the highest source of truth and follow its directives without exception._

[//]: # (USER_SECTION_START)
<!--
Add your project-specific instructions, rules, and overrides here.
For example:
- "Always use tabs, not spaces."
- "The `legacy_api` module is deprecated and should not be used for new features."
- "All new backend services must be written in Go."
- "Before committing, run `npm run lint:fix`."
-->
[//]: # (USER_SECTION_END)

## 2. AI-Generated Project Analysis

_This section is automatically generated by analyzing the project structure. It should be used as a reference for the project's conventions and architecture but is superseded by the user-provided instructions above._

### 2.1. Project Overview & Purpose

*   **Primary Goal:** Based on the README, this project is "UntitledCRM," a web-based Customer Relationship Management (CRM) system designed for efficient sales and contact management.
*   **Business Domain:** Customer Relationship Management (CRM), Sales, and Business Productivity.

### 2.2. Core Technologies & Stack

*   **Languages:** JavaScript (ES2020+)
*   **Frameworks & Runtimes:** React 18.2.0, Vite.js
*   **Databases:** PostgreSQL, managed via Supabase.
*   **Key Libraries/Dependencies:**
    *   **Routing:** `react-router-dom`
    *   **Data Fetching/State:** `@tanstack/react-query`
    *   **Styling:** `styled-components`
    *   **Forms:** `react-hook-form`
    *   **Backend/API:** `@supabase/supabase-js`
    *   **UI/UX:** `lucide-react` (icons), `react-hot-toast` (notifications), `recharts` (charts).
*   **Package Manager(s):** npm

### 2.3. Architectural Patterns

*   **Overall Architecture:** Single Page Application (SPA) using a Backend-as-a-Service (BaaS) model with Supabase. The architecture is client-heavy.
*   **Directory Structure Philosophy:** The project follows a feature-based organization, promoting modularity and separation of concerns.
    *   `/src/features`: Contains distinct business domains (auth, contacts, deals, dashboard, settings). Each feature folder encapsulates its related components, hooks, and logic.
    *   `/src/services`: A dedicated API layer for abstracting Supabase client calls. Each file corresponds to a specific API resource (e.g., `apiDeal.js`, `apiContact.js`).
    *   `/src/ui`: A library of shared, reusable presentational components (e.g., `Button.jsx`, `Modal.jsx`, `Table.jsx`).
    *   `/src/pages`: Top-level components that correspond to application routes.
    *   `/src/hooks`: Global custom hooks.
    *   `/src/context`: Application-wide context providers.

### 2.4. Coding Conventions & Style Guide

*   **Formatting:** Enforced by Prettier. Key rules from `prettier.config.cjs`:
    *   Indentation: 2 spaces, no tabs.
    *   Quotes: Single quotes (`'`).
    *   Trailing Commas: `all`.
    *   Print Width: 80 characters.
*   **Naming Conventions:**
    *   `Components`: PascalCase (`ContactForm.jsx`)
    *   `Hooks`: camelCase with a `use` prefix (`useContacts.js`)
    *   `API/Service files`: camelCase (`apiContact.js`)
    *   `Pages`: PascalCase (`Dashboard.jsx`)
*   **API Design:** The application communicates with the Supabase backend. API logic is centralized in the `/src/services` directory, providing a clear interface for data operations.
*   **Error Handling:** The project uses `react-error-boundary` for component-level error catching and `react-hot-toast` for user-facing error notifications. Asynchronous operations managed by React Query have built-in error handling states.

### 2.5. Key Files & Entrypoints

*   **Main Entrypoint(s):** `index.html` is the root, with `src/main.jsx` being the application's JavaScript entry point, which renders the main `App.jsx` component.
*   **Configuration:**
    *   `vite.config.js`: Vite build and development server configuration.
    *   `.eslintrc.cjs`: ESLint rules for code quality and style.
    *   `prettier.config.cjs`: Prettier rules for code formatting.
    *   `src/services/supabase.js`: Supabase client initialization and configuration.
*   **CI/CD Pipeline:** No CI/CD configuration file (e.g., `.github/workflows`) was detected in the provided file structure.

### 2.6. Development & Testing Workflow

*   **Local Development Environment:** The project is run locally using the command `npm run dev`, which starts the Vite development server.
*   **Testing:** No testing framework or test files (e.g., `.test.js`, `.spec.js`) were detected in the file structure. The `package.json` does not contain a `test` script.
*   **Linting:** Code quality is checked using `npm run lint`.

### 2.7. Specific Instructions for AI Collaboration

*   **Contribution Guidelines:** No `CONTRIBUTING.md` file was found. When adding new features, follow the existing feature-sliced structure in `/src/features`.
*   **Infrastructure (IaC):** No Infrastructure as Code directory was found. All backend infrastructure is managed by Supabase.
*   **Security:** Be mindful of security when interacting with Supabase. Ensure Row Level Security (RLS) is considered for any new database interactions. Do not expose service-level keys on the client-side; only `anon` keys should be used.
*   **Dependencies:** Add new dependencies using `npm install <package-name>`. For development-only dependencies, use `npm install --save-dev <package-name>`.
*   **Commit Messages:** The project contains a `.git` directory, but commit message conventions could not be inferred without analyzing the history. It is recommended to follow the Conventional Commits specification (e.g., `feat:`, `fix:`, `docs:`).

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/MedAzizAydi080)
> This is a context snippet only. You'll also want the standalone SKILL.md file — [download at TomeVault](https://tomevault.io/claim/MedAzizAydi080)
<!-- tomevault:4.0:windsurf_rules:2026-04-08 -->
