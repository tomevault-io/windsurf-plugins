---
trigger: always_on
description: 本项目允许代理实现和运行仓库内的确定性自动化场景，用于复现、取证和回归验证。原先
---

# CBE_EMU 项目协作规范

## 仓库内自动化测试规范

本项目允许代理实现和运行仓库内的确定性自动化场景，用于复现、取证和回归验证。原先
“客户端操作只能由用户手动完成、修改后只能构建而不得运行”的限制不再适用。自动化的
目标是重放真实客户端输入和服务端协议流程，不能成为绕过客户端逻辑的修复手段。

### 自动化实现边界

- 不得使用 Codex 自动化、Computer Use、浏览器自动化、操作系统级鼠标键盘工具、窗口
  控制 API、剪贴板脚本或桌面截图来操控和观察客户端。自动化必须由本仓库代码、命令行
  测试运行器和可审查的场景定义完成。
- 点击、按键和触摸必须进入模拟器已有的硬件输入/事件队列，完整产生按下、释放及必要的
  帧间隔，走与用户实际操作相同的客户端分支。不得直接调用游戏按钮回调、界面跳转函数
  或业务处理函数来冒充点击成功。
- 允许在指定 PC 到达时触发预先声明的输入或测试事件，但该 PC 只能是只读观察条件。
  触发器必须包含二进制/模块名、本地规范化 PC、命中次数及必要的状态指纹；动态模块应
  根据当前装载基址计算本地 PC，不能硬编码某次运行的内存池地址。
- PC 触发器不得修改 PC/LR、寄存器、CBE 全局变量、客户内存、返回值或指令字节，不得
  跳过等待、parser、资源加载、战斗或场景状态机。触发后的动作仍须通过正常输入队列、
  网络请求或模拟器公开事件路径发生。
- 每个自动化场景必须有稳定的场景 ID、最大步骤数、总超时、单步超时、一次性/重复触发
  规则和失败条件。默认触发一次；重复触发必须显式配置并设定上限，防止无限点击、重复
  请求或动画重播。
- 场景不得依靠固定延时判断业务完成。优先等待可证明的 PC、screen 生命周期、网络包、
  parser 分支、状态只读快照或渲染帧条件；确需延时只能作为输入节奏，并说明依据。

### 定时与代码驱动的自动化许可

- 允许自动化场景声明并发送**一次性、可审计的定时事件**。事件仍应优先经模拟器既有
  键盘/触摸事件队列发送，包含按下与释放；每项必须在场景定义、运行日志和产物中记录
  计划时刻、实际发出时刻、输入内容、最大触发次数及后续断言。
- 允许为自动化目的编写测试专用的宿主侧代码操作，例如准备隔离夹具、启动自有进程、
  调度事件、读取状态、导出帧、收集包与调用已有的测试接口。此类操作必须有稳定场景 ID
  和独立产物目录，且不得影响用户正在使用的进程、端口、账号或数据库。
- 定时事件和测试代码可以用于**到达待测路径**，但不能单独作为业务成功证据。协议、
  parser、screen/场景/战斗状态的完成仍须由真实客户端后续行为证明；失败时应保留第一处
  未满足断言的证据。
- 即使采用代码驱动自动化，仍不得修改 CBE/CBM 指令、客户内存、PC/LR、寄存器、网络
  响应字节或生产业务状态来伪造通过。若确需直接调用某个现有测试接口，必须在场景文档
  说明其边界、为何不绕过待验证客户端分支，以及对应的独立断言。

### 屏幕内容与证据导出

- 画面验证必须直接从模拟器 LCD/帧缓冲在渲染完成边界复制像素并导出 PNG；必要时可额外
  保留无损原始像素。不得启动桌面自动化去截取客户端窗口，也不得依赖窗口是否位于前台、
  是否被遮挡或桌面缩放比例。
- 每张图片应同时记录场景 ID、运行 ID、帧号、分辨率、像素格式、导出触发条件、活动
  screen、模块名/本地 PC 和时间戳。导出操作只读取帧缓冲，不能反向写入画面或改变渲染
  时序。
- 自动化日志、请求/响应包、状态快照和图片统一写入独立运行目录，例如
  `artifacts/automation/<run-id>/`。失败时保留第一次偏离前后的证据；成功场景可按保留策略
  清理，但不得覆盖其他运行或用户日志。

### 进程、账号与数据隔离

- 代理可以通过项目提供的自动化命令构建并运行客户端测试。自动化运行器必须记录自己
  启动的 PID，并且只能停止和清理这些 PID；不得终止、替换或重启用户手动启动的客户端、
  服务端或其他同名进程。
- 自动化需要服务端时，优先连接用户明确指定的测试服务；需要自启服务时必须使用隔离的
  测试配置、端口、运行目录和数据库/schema，并在启动前检查端口及数据目标。不得对线上、
  生产或用户正在使用的 `jh_online` 数据执行自动化写入。
- 自动化账号必须是明确标记的测试账号，其创建、初始状态和清理过程应可重复。不得修改
  用户真实角色来制造前置条件，也不得把测试账号结果当成用户账号已经验收通过。
- 若目标二进制或端口被用户进程占用，自动化应停止并报告冲突；不得强制终止进程、换用
  未声明的端口或静默连接到另一个服务实例。

### 自动化验证与人工验收

- 修改代码后仍须先执行 `make -j2`。构建成功后，可运行与本次问题直接相关的仓库内自动化
  场景，并报告场景命令、输入序列、服务端目标、证据目录及通过/失败边界。
- 自动化通过必须证明客户端走过预期 parser、screen、场景或战斗状态，而不只是“没有
  闪退”或图片看起来正常。协议问题应同时核对请求签名、响应对象、事件类型和客户端分支。
- 用户手动测试仍可作为最终体验和主观画面验收，但不再是获取所有运行时证据的唯一方式。
  自动化结果与用户复现冲突时，以原始日志、包字节和首次偏离位置重新调查，不能以自动化
  结果否定用户现象。
- 自动化设施本身的实现和重要场景应记录到 `docs/re/`，说明输入注入点、PC/状态触发依据、
  图像导出边界、隔离方式及已知限制。

## 故障处理必须根因优先

本规范适用于客户端闪退、断言、解包错误、界面卡住、状态错乱，以及由模拟服务端响应、事件投递、资源更新或持久化逻辑引起的其他异常。

### 修改代码前的准入条件

处理故障时，必须先理顺完整业务链路：

1. 固定可重复的触发步骤，保留原始客户端日志、服务端日志、请求和响应字节、崩溃寄存器或调用位置。
2. 串起客户端请求、服务端 detector/handler/builder、响应对象与字段顺序、网络事件投递、客户端 parser/callback 以及相关状态生命周期。
3. 明确预期行为、实际行为和二者第一次发生偏离的位置。崩溃 PC 通常只是最终症状，不能自动视为根因。
4. 使用运行时证据、真实包结构、IDA 反编译/汇编和资源数据交叉验证字段语义与时序；同时记录已排除的假设。
5. 在调查文档中写出可检验的根因陈述：触发条件、被违反的契约、首个错误状态、证据和计划修改点。

如果还无法确定第一次偏离发生在哪里，只允许增加窄范围、可删除的取证日志或更新调查文档，不得先改变业务行为来“试着不崩”。

### 修复要求

- 修复最早被违反的协议、数据来源、对象生命周期、状态归属或事件顺序；修改应位于真正拥有该契约的层级。
- 不得以“客户端不再闪退”作为唯一完成标准。客户端必须走过预期 parser 分支并产生正确的后续 UI、状态或请求。
- 不得盲目丢弃请求或响应对象、吞掉错误、返回伪造成功、硬编码默认场景/坐标/角色/资源、重复投递、自动重试、强制重置状态，或按崩溃地址添加特判。
- 不得通过修改客户端内存、全局变量、寄存器、PC/LR 或 CBE/CBM 代码绕过真实流程。
- 权威数据缺失或语义未确认时，应显式记录 `unresolved` 并继续取证，不能用看似可用的默认值掩盖缺口。

抑制或兜底逻辑只有同时满足以下条件才能使用：

1. 有客户端 parser、真实协议包、资源格式或既有服务端行为证明它属于正确契约或明确的兼容边界。
2. 作用范围足够窄，不会改变正常路径，也不会隐藏新的未知请求或数据错误。
3. 有可观察日志、调查文档和针对性回归测试，并说明为何根因修复后仍然需要它。

### 验证与交付

- 复测原始失败路径，并覆盖相邻路径、重复进入/退出、重新登录和多客户端状态隔离等与问题相关的场景。
- 检查没有引入加载停滞、重复回调、重复动画、状态泄漏、错误默认值或新的高频延迟。
- 修改代码后执行 `make -j2`；文档修改应检查链接、术语和规范间没有冲突。
- 将触发条件、证据、根因、修改点、验证结果和仍未知的风险记录到 `docs/re/`，便于后续继续取证，而不是只在代码里留下特判。

江湖 OL 协议和客户端行为的详细取证流程继续遵循 `docs/re/jianghu-ol-server-agent.md`。

---
> Source: [1144822034/CBE_EMU](https://github.com/1144822034/CBE_EMU) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-11 -->
