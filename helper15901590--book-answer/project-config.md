---
trigger: always_on
description: **本文档作为系统开发规范与 AGENTS.md 准则**
---

# book_answer — 可靠多模块项目代码规范与开发指令

**本文档作为系统开发规范与 AGENTS.md 准则**

## 项目概述

本项目是高质量、高可靠性的多模块系统，遵循极简、整洁、高内聚、低耦合的分层架构与开发规范。

## 核心通用原则 (Core Principles)

1. **YAGNI** — 只实现需求中明确的功能，不添加猜测性的“可能以后需要”的代码。
2. **最少代码 (Minimal Code)** — 能用 50 行清晰解决的不用 200 行，避免臃肿和冗余代码。
3. **精准修改 (Targeted Edits)** — 只修改任务涉及的文件，不重构无关代码，UI风格保持现状不动。
4. **代码复用与封装** — 尽量将通用逻辑封装成公用方法/工具，优先代码复用，避免重复代码。
5. **清晰命名与模型传参** — 使用具有业务意义的变量名和方法名；当方法参数超过两个时，使用 Model/Interface 对象传参，避免超长参数签名。
6. **减少硬编码** — 业务常量、枚举值统一管理，配置项集中规范，避免在业务代码中硬编码。
7. **完整注释与类型安全** — 关键逻辑做好精炼注释，保持完整的 TypeScript 强类型定义。

## 编码规范

### 数据流与接口规范
- 接口交互与返回格式统一规范处理（包含成功状态、数据载荷与清晰的业务错误提示）。
- 模块间采用清晰的数据对象（DTO / VO / BO 理念）进行传递，不直接暴露混乱的内部数据结构。
- 金额与数值保持精度统一（保留两位小数或标准数额类型）。

### 前端与组件规范
- 组件结构清晰：按功能职责划分子组件，禁止单个组件膨胀。
- 状态与接口封装：接口请求统一由 API/服务层处理，状态管理逻辑清晰。
- UI与交互体验：UI 风格保持现状统一，保持优雅的响应式布局与明确的交互反馈。

---
> Source: [helper15901590/book-answer](https://github.com/helper15901590/book-answer) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-17 -->
