---
trigger: always_on
description: This is a Python + Django port of the Solid Queue task processing library for Ruby on Rails. We strive to maintain 1-1 structure and feature parity, while conforming to Django's conventions.
---

# Steady Queue Agent documentation

This is a Python + Django port of the Solid Queue task processing library for Ruby on Rails. We strive to maintain 1-1 structure and feature parity, while conforming to Django's conventions.

## Workflow reminders

- Always update `CHANGELOG.md` when opening a PR. Add an entry under `## Unreleased` following the existing format (Fixed / Added / etc.).

## Useful commands

- Use `uv run python ...` to ensure the Python environment is loaded properly before running a command.
- Run tests with `make test`
- Lint with `make lint`

---
> Source: [knifecake/steady-queue](https://github.com/knifecake/steady-queue) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-06 -->
