---
trigger: always_on
description: 一本关于大模型应用的工程哲学著作，也是一个结构化的知识库。不是教程，不是文档汇编，不是框架推荐指南，而是一个人对"软件工程在大模型时代应该是什么样子"的系统性回答。
---

# CLAUDE.md -- 项目指引

## 项目定位

一本关于大模型应用的工程哲学著作，也是一个结构化的知识库。不是教程，不是文档汇编，不是框架推荐指南，而是一个人对"软件工程在大模型时代应该是什么样子"的系统性回答。

双重读者：人和大模型。如果一份文档不能同时对人和机器保持清晰，那它的结构本身就有问题。

## 写作语气校准

- 让观点自己说话。不通过罗列作者背景来为观点增加权威性。
- 避免自证式叙事：不用"因为我做过 X，所以我有资格说 Y"的结构。如果一个观点需要作者简历来支撑，说明观点本身的论证不够有力。
- 跨领域经验（数学、工程、投资）可以作为类比和思维工具的来源，但以抽象原则的形式出现，不以个人经历的形式出现。
- 锋利但不傲慢。明确的判断和明确的偏好是本书的特色，但"我比别人看得清"不是。
- 自省要真诚：承认局限性时，指向真实的盲区，而非变相的自夸。
- 警惕 AI 写作的典型味道：面面俱到的平衡术、每段末尾的自我对冲、过于工整的平行结构、"综合以上分析"式的过渡词、把简单的事情用复杂的术语重新包装。宁可有锋利的偏见，不要滴水不漏的正确。

## 核心原则

### 写作立场

- 表达个人观点与哲学观，不追求从众，不依赖权威背书。
- 偏重对未来的构想和对问题结构的分析，而非对既有事实的罗列。
- 概念边界清晰，观点有论据支撑。
- 允许跨领域类比（数学、物理），但类比必须指向共享的深层结构和具体的工程启示，而非表面相似。类比是调味料，不是主菜——同一种类比在全书中不应反复出现。

### 写作风格

- 中文为主。不使用 emoji、icon 或任何装饰性符号。
- 行文严肃但不刻板，允许锋利的判断和明确的偏好。
- 标题层级不超过二级（h1-h2）。
- 代码块仅用于：（1）架构蓝图（Agent 循环、RAG 流程等结构模式）；（2）Prompt 的代码化表达（如 Pydantic 模型）；（3）好写法与坏写法的代码层面对比。概念分类、决策框架、思维模型、简单算术用散文、表格或列表表达。

### 技术倾向

声明式优于命令式，类型系统优于运行时检查，组合优于继承，显式依赖优于隐式约定，结构驱动优于流程驱动，简单方案优于"看起来高级"的方案。对"框架崇拜"持批判态度。

### 思维习惯

架构决策先于实现细节。迭代过程应有明确的终止条件。理论等价的路径在实际执行中可能产生不同的结果。约束越精确，系统行为越可预测。这些思维习惯应该隐性地影响写作，而非被当作命名概念反复标注在文中。

## 项目结构

```
awesome-llm-practice/
  CLAUDE.md                  -- 本文件
  README.md                  -- 项目入口，书籍目录（GitHub 仓库首页）
  index.md                   -- VitePress 站点首页
  chapters/                  -- 各章节内容
    00-序章/                  -- 为什么写这本书
    01-认识论/                -- 大模型的本质与边界
    02-不确定性与决策/         -- 不确定性环境下的工程决策框架
    03-人机协作的软件过程/      -- 开发方法论的重构（工作方式先于技术细节）
    04-声明式提示与类型契约/    -- 从命令式到声明式的范式跃迁，类型系统作为约束工具
    05-架构与编排/             -- LLM 应用的结构设计与动态执行
    06-测试评估与可观测性/      -- 不确定性系统的质量保障
    07-反模式与陷阱/           -- 值得警惕的实践
    08-终章/                  -- 软件工程的下一个形态
  docs/.vitepress/           -- VitePress 配置与自定义主题
  build.sh                   -- PDF 构建脚本（Pandoc + Typst）
  metadata.yaml              -- 电子书元数据
  pdf-filter.lua             -- Pandoc Lua 过滤器（PDF 构建用）
  book-conf.typst            -- Typst 排版模板
  .github/workflows/         -- GitHub Actions（自动部署 GitHub Pages）
```

章节目录下包含 `00-概述.md`（章节概述与文章索引）和按阅读顺序编号的独立 `.md` 文章。Markdown 文件命名格式：`序号-中文名.md`，序号两位数字。

## 全书结构

前三章是地基（认识论、决策框架、工作方式），第四五章是技术构建（声明式约束、架构编排），第六章是质量保障，第七章是反面教材，第八章收束。章节之间有逻辑递进关系，但写作时不需要在文中反复交代这种关系——让内容自然衔接，不要写"本章的结论是下一章的前提"这种导游词。

## 协作规范

- 理解本项目的立场和品味后再动笔。不写空话、套话、正确的废话。
- 不使用"让我们"、"大家都知道"之类的口语化表达。
- 不回避争议性判断。作者观点与主流不同时，忠实表达，不做对冲。
- 论证靠逻辑和证据，不靠作者背景。"这个判断是对的因为 X"，而非"这个判断是对的因为作者做过 Y"。
- 每篇文章应有清晰的论点，而非话题的泛泛介绍。
- 跨领域类比要能指向具体的工程启示，不堆砌类比，同一种类比不反复使用。
- 概念首次出现时给出精确定义。
- 观点要有论据，但不要机械地在每个观点后面都附加"局限性"段落。局限性在真正需要时讨论，不作为固定格式。
- 数学概念的引用要准确，不为类比牺牲数学含义的严谨性。
- 引用其他章节时使用相对路径。

---
> Source: [Onenightcarnival/llm-engineering-philosophy](https://github.com/Onenightcarnival/llm-engineering-philosophy) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-12 -->
