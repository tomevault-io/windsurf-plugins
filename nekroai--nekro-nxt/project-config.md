---
trigger: always_on
description: 本文件是开发者、AI 编码工具和自动化任务的统一入口。规则使用中文沉淀；项目早期只保留必要约束，不提前引入复杂门禁。
---

# NekroNXT 开发与知识约束

本文件是开发者、AI 编码工具和自动化任务的统一入口。规则使用中文沉淀；项目早期只保留必要约束，不提前引入复杂门禁。

## 1. 产品身份

- 产品名：`NekroNXT`，受限空间简称 `NXT`；
- 根包名与仓库名：`nekro-nxt`；
- 定位：以 DSH 为核心引擎、原生群聊、兼具娱乐性和生产力、可由智能体参与扩展开发的高扩展聊天系统；
- Nekro Agent 仅作为历史经验来源，不做数据迁移、插件兼容或旧架构复刻。

## 2. 术语铁律

- 面向用户的唯一实体称呼是“智能体”；
- UI、帮助文档、通知和错误信息不得用 Agent、AI 成员、Bot、机器人或助手指代智能体；
- “助手”只可作为具体智能体的名称或人设角色；
- 外部聊天平台的发送账号可以称“机器人账号”，但不得与智能体混为一个对象；
- 产品界面、通知、空态、错误和 Composer 不得硬编码 QQ、Discord 等具体平台名；平台名称只来自已安装 Adapter 的 `displayName`，频道来源使用该连接自己的展示名；
- 内部 TypeScript 类型允许使用 `AgentDefinition`、`AgentRevision`、`AgentSession`、`AgentActivation`，不得直接泄漏到用户界面；
- DSH 原生概念如 Agent Loop 可在开发文档中使用，首次出现时必须解释其产品含义。

详细映射见 `docs/01-术语与文案规范.md`。

## 3. 开发方式

- 纯 TypeScript 主栈，ESM 优先；
- 使用 pnpm workspace 的单一仓库；
- 默认直接在 `main` 分支开发和提交；除非用户明确要求，否则禁止为普通功能、修复或临时工作擅自创建新分支；
- 提交说明使用英文类型前缀和中文主题，格式为 `type(scope): 中文动词短语`；类型使用 `feat`、`fix`、`refactor`、`docs`、`test`、`merge`，细则见 `docs/06-开发与测试规范.md` §1.2；
- Desktop、Server、Web UI、社区后端和共享包最终都位于本仓库；
- 先形成少量清晰包，禁止为了“未来可能复用”过早拆出大量包；
- 根命令应逐步收敛为 `pnpm dev`、`pnpm check`、`pnpm test`、`pnpm build`；
- 可公开、可重建的品牌母版位于 `assets/brand/`；公开构建不得依赖 `.local`。Logo、角色和宣传素材不属于代码许可证，修改时同时遵循 `docs/BRAND.md`；
- 文档查阅从本文件进入[公开知识路由](docs/README.md)；本机存在[本地私有知识路由](.local/README.md)时，本地服务、环境、私有参考和接管任务再从该私有索引进入，禁止绕过索引凭文件名猜测权威文档；
- 重要架构决策和踩坑结论使用中文写入 `docs/`，不要只留在聊天记录中。
- 抽象、包和配置项必须有当前所有者与消费者；无法说明删除它会破坏什么行为或测试时，不提前引入；
- 安装代价小、风险低且主流的能力直接使用成熟依赖，禁止手写同等复杂度的交互或算法内核；选型写入对应现行文档；
- 状态只在真实提交点发布，注册必须可撤销，dispose 必须等待资源静止；详细规范见 `docs/06-开发与测试规范.md`。
- `/data` 是部署与本地运行的统一数据根；新增顶层文件或目录必须有唯一所有者、恢复语义和文档依据，禁止为单个功能或调试方便把运行产物散落到根层。智能体开发工作区固定归入 `workspaces/<agentId>/`，除非宿主显式覆盖工作区根。
- 仓库内测试、原型、文档和注释只使用虚构样本。禁止把真实聊天、个人昵称、群标识、平台临时 URL 或文件哈希写进 Git；覆盖平台拍扁协议时合成标记，不粘贴一次真实入站。细则见 `docs/00-文档公开边界.md` 与 `docs/06-开发与测试规范.md` §5。

## 4. DSH 与扩展体系

- DSH 是外部核心智能体引擎，优先通过公开 API、插件、Bundle、Preset 和组合层扩展；
- 不维护深度 Fork，不复制 DSH 内核；必须修改上游行为时，先记录兼容层边界和升级成本；
- NekroNXT 自身尽量以 Cordis 插件/Bundle 组合接入，不把自己设计成不可替换的特权模块；
- 面向 DSH 本身能力扩展的插件以零 NekroNXT 适配可用为默认目标；NekroNXT 专用元数据、文案和界面只增强产品体验，不能成为加载、配置或调用 DSH 能力的必要条件；
- 不承诺无差别兼容所有 DSH 官方应用界面扩展，但必须尽可能兼容使用 DSH 公开 Plugin、Service、Tool、Provider、Settings、Scope、Bundle 或 Preset 接缝的能力插件；发现缺口时优先补通用兼容层，不逐插件制造特例；
- 插件支持情况必须区分已验证、可加载但未验证、部分支持、不兼容和未评估，并记录插件版本、DSH 版本、已覆盖能力面与可复现证据；不得仅凭安装成功、类型可解析或模型声称成功标记为支持；
- Adapter、Tool、Preset、MCP 接入和 UI Slot 都是统一 Extension 的不同 Contribution；
- “添加连接”必须先消费已安装 `AdapterContribution` 的平台目录，再按其版本化配置 schema 创建 Connection；不得让 QQ 或其他单个平台占用通用入口，系统托管 Adapter 不出现在用户创建列表；
- “NekroNXT 扩展”只是一种元数据标记，表示支持创造工作台、热重载和产品集成，不是第二套插件系统；
- 首版不建设社区市场、签名、SBOM 和复杂供应链，只保留未来所需的数据边界。

## 5. 产品与运行时边界

- 用户显式创建智能体，再为其设置人设、模型、能力、频道和扩展；
- 一个智能体可以同时绑定多个频道；一个频道同一时间只由一个智能体响应；
- 一个频道只包含自己的消息事实流，不把多个频道的短期上下文直接混合；
- 工具执行期间的新消息可靠入库，在安全间隙注入下一步思考；普通消息不得隐式取消当前工具；
- 智能体自己的用户可见发言必须调用通信工具产生，包括内置频道回复；管理员可通过客户端以机器人账号向外部频道发言，该消息进入频道事实流，并以系统事实注入会话，不记为模型发送；内部兼容字段 `kind: web` 不得泄漏成“网页频道”等用户文案；
- DSH 模型原始 `text/reasoning` 只作为内部运行轨迹和后台观察，不自动投递到频道；
- Mention、图片、文件等内容统一使用通信工具的结构化内容块，不通过提示词标记或宿主路径传递；
- 智能体配置和扩展均通过不可变版本演进；运行中的会话不在工具执行中热替换；
- 动态运行、保存扩展版本、给智能体启用扩展是三个独立动作；
- 产品界面说明操作对象、作用范围和结果，不用含糊的“确定”“能力”“查看”替代真实动作。
- 用户可见操作控件不得是空处理器或无结果占位；未实现能力必须永久禁用并明确标注“尚未开放”，不能呈现为可点击按钮。
- 未实现能力不得长期占据一级导航、页面主操作或大面积正文；只有用户必须了解前置条件时才保留产品化说明，开发阶段和内部实现进度进入文档与诊断日志。

## 6. 动态创造

- 首版优先打通本地闭环：描述需求 → 生成动态包 → 运行 → 验证 → 修改 → 保存本地扩展 → 给指定智能体启用；
- 动态创造本身不隐式获得开发 Shell 或完整文件访问；三种能力分别由用户配置；
- 验证展示真实构建、调用、渲染和停止结果，不使用“智能体声称成功”作为唯一证据；
- 不在项目尚未验证前引入重型审批、安全分级和社区审核流程。

## 7. 交付形态

- Desktop：Windows/macOS 安装即用，不要求用户预装 Node、pnpm、Python 或 Docker；
- Server：面向 7×24 小时运行，目标是单容器、一个主要 `/data`、少量必要配置；
- 两种宿主复用核心、数据模型、Web UI 和扩展体系；
- 升级必须可理解、可验证、失败可恢复，不在运行容器中执行 `git pull`。
- 正式版的可变更新内容只写入 `docs/releases/current.md`，按 `docs/releases/template.md` 和 `docs/09-正式发布与更新日志规范.md` 维护；版本号、下载地址、镜像、Release ID、Commit 和许可证由发布 CI 生成，禁止手工复制进更新内容；
- 正式版更新内容必须由用户审查并明确同意。AI 复核、测试通过、Preview 发布或交互确认都不能代替用户审查；用户要求改文案后必须重新审查。未获得当前全文的明确同意时，禁止运行 `pnpm release <版本>`、创建正式版 Tag 或触发正式发布；
- 正式发布只使用 `pnpm release <X.Y.Z>`。命令显示指定版本与完整更新内容，验证根版本、main、干净 worktree、远端 HEAD、已验证 Preview、Tag 和 Release 可用性，并在交互确认后推送不可移动 Tag；Tag 触发在线正式发布。

## 8. 修改完成条件

- 与任务相关的类型检查、测试或手工验证已经执行；
- 本机已有 `pnpm dev` 时，改完会触发 Server / Web / `tsx watch` 重载的代码后，必须确认开发服务仍可访问；细则见 `docs/06-开发与测试规范.md` §6.1。watch 进程还在、单测通过或 Vite 首页 200 都不能代替；
- 测试验证真实入口和外部结果，不把智能体或被测代码自称成功当作证据；
- 用户可见 Web 行为发生变化时，必须运行生产构建的产品旅程测试；SSR 静态标记、API 测试和构建成功都不能单独证明页面可用；
- 产品旅程必须把 `pageerror`、React `console.error`、未处理拒绝、空白根节点和关键页面不可见视为硬失败；没有任何真实页面驱动手段时不得降级为“未验证但已完成”；
- UI/UX 改动必须同时完成源码审查与真实渲染画面的视觉验收，权重与类型检查、相关测试和产品旅程相同；标准见 `docs/06-开发与测试规范.md` §5.1。布局、可见功能、美观、合理性或可读性任一明显不合格，不得以功能可用、Console 无错或元素可见替代。只要实现者能理解图像，就必须亲自阅读真实画面，不得只根据 DOM、样式计算或测试日志声称视觉通过；
- 用户可见文案符合“智能体”术语规则；
- 仓库内新增或改动的测试、原型、文档和注释不含真实入站样本、个人标识或平台临时 URL；
- 新行为没有破坏频道隔离、版本边界和扩展生命周期；
- 同一改动必须更新该事实的唯一现行文档；发现现行文档与代码不一致时，即使任务不是文档工作也要当场改掉；
- 不在子目录创建嵌套 Git 仓库。

## 9. 文档自维护

现行文档是实现的一部分，不是事后补记。详细规则见 `docs/06-开发与测试规范.md` §1.1。

- 行为、契约、文案、进度或包职责变化时，在同一改动里重写对应现行文档，使用现在时；
- 一个事实只改一个权威源，别处改成链接，不复制第二份长描述；
- 禁止用“后续说明 / 曾错误 / 以前是”给现行文档打补丁；过时正文整段重写，过程与否决进入 `docs/archive/`；
- 不要为普通功能写里程碑日记。只有公共协议、跨包职责、持久格式或测试策略变化才新增或更新 Decision，落地后把 Decision 改成现行契约；
- 完成前自问：只读现行文档的人，能否得到与代码一致的规则，且不会读到已否决的模型。
- `AGENTS.md` 是文档引用图的唯一根节点，`docs/README.md` 是公开知识索引，`.local/README.md` 是存在时的本机私有索引；专题索引再路由到协议、Decision、包 README、操作文档和归档，不把全部文件平铺回根文件；

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [NekroAI/nekro-nxt](https://github.com/NekroAI/nekro-nxt) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-26 -->
