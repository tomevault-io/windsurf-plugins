---
trigger: always_on
description: > 修改权限：仅当用户在当前任务中明确点名本文件并授权修改时可改；本次创建授权不延续到未来，普通“更新文档”不构成授权。
---

# 小橙子桌宠代理开发规则

> 文档级别：`LOCKED`  
> 修改权限：仅当用户在当前任务中明确点名本文件并授权修改时可改；本次创建授权不延续到未来，普通“更新文档”不构成授权。  
> 适用版本：`1.1.0` 起，直至用户明确替换。  
> 最后核对日期：`2026-08-15`  
> 权威来源：用户明确决策；`docs/governance/` 下的受保护治理文档；仓库中的实际源码、配置和测试。  
> 更新触发：用户明确改变开发治理、产品边界、架构安全或发布兼容规则时。

## 1. 适用范围与目标

本文件适用于整个仓库，是所有主代理、子代理和后续开发者进入项目后的第一份必读文件。目标是让每次开发都基于同一套产品边界、架构边界、兼容承诺和工程习惯进行。

## 2. 权威顺序

遇到冲突时按以下顺序判断：

1. 用户在当前任务中的明确指令；但修改 `LOCKED` 文档仍须明确点名文件或具体受保护规则，笼统的“更新文档”不算授权。
2. 本文件与 `docs/governance/` 中的 `LOCKED` 治理文档。
3. 源码、配置、测试和已核验发布产物所反映的当前实现事实。
4. `LIVING` 开发参考文档与当前版本说明。
5. 明确标为未实现、候选或待批准的规划内容。

“目标规则”和“当前事实”必须分开判断：治理文档定义项目必须遵守的目标；源码、配置和测试定义当前实现事实。若两者不一致，不得静默改写治理文档来迎合代码，也不得擅自扩大任务去改代码。

## 3. 冲突停机规则

出现以下任一情况时，停止相关修改，保留已完成的安全检查，并向用户说明冲突、影响范围和可选处理方式：

- 请求会违反受保护治理规则，但用户没有明确授权改变该规则。
- 需要修改 `LOCKED` 文件，却只有笼统的文档更新指令。
- 代码与治理规则不一致，而当前任务没有授权实施对齐改动。
- 存档结构、稳定标识符或发布身份将发生不兼容变化，却没有迁移和验证方案。
- 无法确认真实入口、配置、数据流或目标文件，继续操作将依赖猜测。

## 4. 每次任务的必读顺序

1. `AGENTS.md`。
2. [文档控制规则](docs/governance/DOCUMENT_CONTROL.md)。
3. [Git 与 GitHub 版本控制准则](docs/governance/GIT_VERSION_CONTROL.md)。
4. 与任务相关的[产品底线](docs/governance/PRODUCT_GUARDRAILS.md)、[发布与兼容准则](docs/governance/RELEASE_AND_COMPATIBILITY.md)、[架构安全边界](docs/governance/ARCHITECTURE_SECURITY.md)。
5. [开发文档地图](docs/README.md)及其中对应的 `LIVING` 参考文档。
6. 对应源码、配置、测试和当前工作区差异。

禁止仅依据旧版说明或构建产物推断当前代码。若 Git 元数据不可用，必须明确报告无法识别未提交改动，不得假称工作区干净。

## 5. 开发纪律

- 编辑前先定位真实文件和符号，并用 3–6 条说明计划、修改边界和验证方式。
- 优先小而可审查的差异；未经明确要求，不做扫荡式重构、无关格式化或目录搬迁。
- 不臆造 API、配置、路径、存档字段或物品 ID；不确定时先搜索仓库。
- 保持现有 TypeScript 严格类型、显式错误处理和模块边界；只在意图不明显时添加注释。
- 不写入或输出密钥、令牌、私钥、`.env` 值或凭据。
- 未经用户明确授权，不增加联网、遥测、分析、广告、账号、付费、远程配置或自动更新能力。
- 保留用户已有改动；发现重叠修改或来源不明的工作区变化时，先停机确认。
- 任何产生可跟踪文件变更的任务，都必须按 [Git 与 GitHub 版本控制准则](docs/governance/GIT_VERSION_CONTROL.md)完成检查、验证、提交、推送和远端核对闭环；用户当前明确要求暂不提交或暂不推送时除外。

## 6. 文档同步纪律

- 行为、接口、数据结构、变量、资源目录或构建流程发生变化时，必须在同一任务中更新对应的 `LIVING` 参考文档和必要测试。
- 数值参数只能有一个明确的文档权威位置；其他文档应链接引用，不得复制出多个互相漂移的版本。
- 用户可见变化进入 `CHANGELOG.md`；正式发布后创建或补全对应版本快照，历史记录只追加，不覆写旧事实。
- 不得为了“保持同步”自动修改 `LOCKED` 文档。若实现与治理规则冲突，按第 3 节停机。
- 每份开发参考文档须标明文档级别、修改权限、适用版本、最后核对日期、权威来源和更新触发。

## 7. 测试与交付纪律

- 行为变化必须新增或更新测试；先运行最快、最相关的检查，再按风险扩大范围。
- 共享领域规则至少运行相关 Vitest；影响整体 TypeScript 或渲染层时运行构建；影响安装、更新或存档兼容时执行[发布与兼容准则](docs/governance/RELEASE_AND_COMPATIBILITY.md)规定的实装验证。
- 不运行会重写仓库文件的格式化、代码生成或迁移命令，除非当前任务明确需要并已说明影响。
- 交付时列出修改摘要、实际变更文件、已运行检查和未验证风险。未运行的检查必须如实说明，不能用“应当通过”代替结果。
- 产生文件变更时，交付还必须列出分支、提交哈希、GitHub 推送结果和最终工作区状态。

## 8. 受保护文件清单

以下文件均为 `LOCKED`：

- `AGENTS.md`
- `docs/governance/DOCUMENT_CONTROL.md`
- `docs/governance/PRODUCT_GUARDRAILS.md`
- `docs/governance/RELEASE_AND_COMPATIBILITY.md`
- `docs/governance/ARCHITECTURE_SECURITY.md`
- `docs/governance/GIT_VERSION_CONTROL.md`

本次任务对这些文件的创建授权仅在本次任务内有效；创建完成后，后续修改仍须重新获得用户的明确、具体授权。

---
> Source: [RizardSKs/little-orange-desktop-pet](https://github.com/RizardSKs/little-orange-desktop-pet) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-27 -->
