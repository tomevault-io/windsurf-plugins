---
trigger: always_on
description: Dit is een **gratis open-source** MCP (Model Context Protocol) server voor Marktplaats.nl. De server stelt AI-assistenten in staat om advertenties te zoeken en bekijken op Marktplaats.
---

# Claude Code Instructions - Marktplaats MCP Server

## Project Overview

Dit is een **gratis open-source** MCP (Model Context Protocol) server voor Marktplaats.nl. De server stelt AI-assistenten in staat om advertenties te zoeken en bekijken op Marktplaats.

**Repository**: https://github.com/PonClick/marktplaats-mcp
**Eigenaar**: lessClick AI (PonClick)
**Status**: Open source - geen affiliate/monetisatie

## Project Structure

```
marktplaats_mcp/
├── src/marktplaats_mcp/
│   ├── __init__.py
│   └── server.py          # Main MCP server code
├── pyproject.toml          # Package configuration
├── README.md               # User documentation
├── LICENSE                 # MIT License
├── DISCLAIMER.md           # Legal disclaimer (TO CREATE)
├── AFFILIATE_PLAN.md       # Monetization strategy
└── .env.example            # Environment template (TO CREATE)
```

## Current Task: Affiliate Integration Preparation

We zijn bezig met het voorbereiden van affiliate marketing integratie via Awin. De Awin account is aangevraagd en we wachten op goedkeuring.

### Wat nu gedaan moet worden:

1. **README.md updaten**
   - Vervang `jouw-username` door `PonClick`
   - Voeg affiliate disclosure toe onder "## Affiliate Disclosure"
   - Voeg toe: "Links may contain affiliate tracking to support development"

2. **DISCLAIMER.md aanmaken** met inhoud:
   ```markdown
   # Disclaimer

   ## Unofficial Status
   This tool is provided for educational and personal use purposes. This is an unofficial tool and is not affiliated with or endorsed by Marktplaats.nl or Adevinta.

   ## Affiliate Links
   Links generated by this tool may contain affiliate tracking parameters. When you click on a Marktplaats link through this tool and make a purchase, the developer may earn a small commission at no extra cost to you.

   You can disable affiliate tracking or use your own affiliate ID by setting the `AWIN_PUBLISHER_ID` environment variable.

   ## Liability
   - Users are responsible for complying with Marktplaats terms of service
   - The developer is not responsible for any misuse of this tool
   - No guarantees are made about the accuracy or availability of listing data
   - Use at your own risk

   ## Terms of Service Compliance
   Users should:
   - Respect rate limits and not overload Marktplaats servers
   - Not use this tool for scraping or data harvesting at scale
   - Comply with all applicable laws and regulations

   ---
   *lessClick AI - 2026*
   ```

3. **server.py aanpassen** - Voeg bovenaan toe na de imports:
   ```python
   import os
   from urllib.parse import quote

   # Affiliate Configuration
   AFFILIATE_CONFIG = {
       "enabled": os.getenv("AFFILIATE_ENABLED", "true").lower() == "true",
       "provider": "awin",
       "publisher_id": os.getenv("AWIN_PUBLISHER_ID", ""),  # Will be set after approval
       "advertiser_id": os.getenv("AWIN_ADVERTISER_ID", ""),  # Marktplaats advertiser ID
   }

   def _create_affiliate_link(original_url: str) -> str:
       """Wrap a Marktplaats URL in an Awin affiliate tracking link."""
       if not AFFILIATE_CONFIG["enabled"]:
           return original_url
       if not AFFILIATE_CONFIG["publisher_id"] or not AFFILIATE_CONFIG["advertiser_id"]:
           return original_url

       encoded_url = quote(original_url, safe='')
       return (
           f"https://www.awin1.com/cread.php"
           f"?awinmid={AFFILIATE_CONFIG['advertiser_id']}"
           f"&awinaffid={AFFILIATE_CONFIG['publisher_id']}"
           f"&ued={encoded_url}"
       )
   ```

4. **In `_format_listing()` functie** - Vervang de link regel:
   ```python
   # Change this line:
   "link": f"https://link.marktplaats.nl/{listing.get('itemId')}",

   # To this:
   original_link = f"https://link.marktplaats.nl/{listing.get('itemId')}"
   ...
   "link": _create_affiliate_link(original_link),
   "original_link": original_link,  # For transparency
   ```

5. **Nieuwe tool toevoegen** aan server.py:
   ```python
   @mcp.tool()
   def get_affiliate_info() -> dict[str, Any]:
       """
       Get information about affiliate link configuration.

       Returns:
           Current affiliate settings and disclosure information
       """
       return {
           "affiliate_enabled": AFFILIATE_CONFIG["enabled"],
           "provider": AFFILIATE_CONFIG["provider"] if AFFILIATE_CONFIG["enabled"] else None,
           "custom_id_configured": bool(os.getenv("AWIN_PUBLISHER_ID")),
           "disclosure": (
               "Links may contain affiliate tracking. "
               "See DISCLAIMER.md for details. "
               "Set AWIN_PUBLISHER_ID env var to use your own affiliate ID."
           ),
       }
   ```

6. **.env.example aanmaken**:
   ```
   # Marktplaats MCP Server - Environment Variables

   # Affiliate Configuration (Optional)
   # Set these after getting approved by Awin
   # AWIN_PUBLISHER_ID=your_publisher_id
   # AWIN_ADVERTISER_ID=marktplaats_advertiser_id

   # Set to "false" to disable affiliate link wrapping
   # AFFILIATE_ENABLED=true
   ```

7. **pyproject.toml updaten** - Voeg `python-dotenv` toe aan dependencies

## Code Style Guidelines

- Python 3.11+

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [PonClick/marktplaats-mcp](https://github.com/PonClick/marktplaats-mcp) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-06-17 -->
