---
trigger: always_on
description: This file used to hold the full project guide for Claude Code. That guide is now canonical in `AGENTS.md` at the repo root, so the same rules apply to every agent (Claude Code, Cursor, Codex, Copilot, Zed, Aider, etc.). Claude Code resolves the `@AGENTS.md` import above when this file is loaded.
---

@AGENTS.md

# Claude Code specifics

This file used to hold the full project guide for Claude Code. That guide is now canonical in `AGENTS.md` at the repo root, so the same rules apply to every agent (Claude Code, Cursor, Codex, Copilot, Zed, Aider, etc.). Claude Code resolves the `@AGENTS.md` import above when this file is loaded.

Add Claude-Code-only specifics below as they emerge. Today: none.

---
> Source: [7xuanlu/origin](https://github.com/7xuanlu/origin) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-23 -->
