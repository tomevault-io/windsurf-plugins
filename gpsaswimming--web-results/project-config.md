---
trigger: always_on
description: Guidance for Claude Code when working with this repository.
---

# CLAUDE.md

Guidance for Claude Code when working with this repository.

---

## Repository Overview

GPSA meet results archive, served at `results.gpsaswimming.org`. Contains all dual meet result HTML files organized by season year, historical invitational meet files, and the scripts that build and maintain the archive indexes.

---

## Structure

```text
/
├── 2022/                    # Dual meet results by season year
├── 2023/
├── 2024/
├── 2025/
│   ├── YYYY-MM-DD_T1_v_T2.html   # Individual meet result files
│   ├── divisions.csv              # Division assignments for archive builder
│   ├── index.html                 # Auto-generated season archive (do not edit)
│   └── scores.json                # Auto-generated scores feed (do not edit; gitignored)
├── 2026/
├── invitationals/           # Historical invitational static files (PDFs, meet invites)
│   ├── CityMeet/
│   ├── SummerSplashInvitational/
│   └── MiniMeet/
├── _headers                 # Cloudflare Pages headers (CORS for scores.json feeds)
├── index.html               # Root index — auto-generated by generate-indexes workflow
└── scripts/
    ├── build_season_index.py     # Season archive generator
    ├── build_scores_json.py      # Per-season scores.json feed generator
    ├── generate_index.py    # Directory index page generator
    └── bulk_process_results.py
```

---

## File Naming

Dual meet results must follow: `YYYY-MM-DD_TEAM1_v_TEAM2.html`

---

## GitHub Actions

### deploy.yml

Triggers on every push to `main`. Runs sequentially:

1. `generate_index.py` — builds directory listing indexes for root and `invitationals/`
2. `build_season_index.py` — builds season standings/schedule page for each year directory (overwrites plain listing with rich archive)
3. `build_scores_json.py` — builds `scores.json` for each year directory (see below)
4. Deploys full working tree to Cloudflare Pages project `gpsa-results` — nothing is committed back to the repo.

Requires GitHub Actions secrets: `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID`.

---

## Scores feed (`scores.json`)

Each season directory publishes `scores.json` at `results.gpsaswimming.org/<year>/scores.json`.
It is the cross-repo data feed consumed by the **meet-schedule** site
(`meet-schedule.gpsaswimming.org`, repo `web-schedule`), which fetches it client-side
to show each dual meet's final score and a link to its result page.

- Generated by `scripts/build_scores_json.py`, which reuses `parse_meet_file()` and the
  team-name maps from `build_season_index.py` — score extraction is **not** duplicated.
- One record per swum meet: `date`, `home`/`away` (official abbrs), `home_name`/`away_name`
  (short schedule display names — the join key used by the schedule site, because abbrs
  drift e.g. Wythe = `GWRA`/`WYTHE`), `home_score`/`away_score`, and `url`.
- Built at deploy time from the committed HTML and gitignored (like `index.html`) —
  self-healing, so adding a result the normal way keeps the feed current with no extra step.
- `_headers` adds `Access-Control-Allow-Origin: *` so the schedule site and the Rules
  Committee portal (`submit.gpsaswimming.org`) can both fetch it cross-origin. It was
  scoped to the schedule site alone until a second consumer appeared; `_headers` takes
  one static value per header and cannot reflect an origin, so two named origins is not
  expressible. `*` grants nothing new — these files already serve unauthenticated to any
  client that asks, so it widens only what *browser JavaScript on another origin* may
  read.

---

## Standings order (division placings)

`build_season_index.py` orders each division's standings table and renders a **Place** column.
Ordering is by record (most wins, then fewest losses), and teams on **identical** records are
separated by the GPSA Rulebook tiebreaker — `docs-rulebook` `awards.md` §2:

1. **Head-to-head** among the tied teams.
2. If still level, **total points scored in meets against the other tied teams only.** Season-wide
   points are deliberately *not* used — the rule excludes them so a team cannot bank a tiebreaker
   margin by running up scores on weaker opponents.

Teams the tiebreaker cannot separate **share a place**, rendered as `T-2`, and the next place is
skipped (two teams sharing 2nd means no 3rd; the next team is 4th). A footnote explaining the
tiebreaker is emitted only for divisions where two teams held identical records.

This matters because `index.html` is regenerated from the committed result HTML on every deploy and
is gitignored — **hand-editing a placing will not survive.** Any change to how placings are decided
belongs in `rank_division()`.

Both 2026 rulings are recorded in the private `gpsa-admin` repo under `analysis/`
(`2026_red_division_tiebreaker.md`, `2026_blue_division_tiebreaker.md`). Two open Board questions
noted there affect this code if resolved differently:

- Whether the tiebreaker re-runs from step 1 on the reduced group after each placing, or ranks the
  whole tied group once by points. `rank_division()` implements the latter; both give the same 2026
  result, so nothing turns on it yet.
- How a **tied dual meet** should weight a record (2026 Blue: Beaconsdale 209 – Wythe 209). Ties are
  counted separately and do not affect ordering.

---


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [gpsaswimming/web-results](https://github.com/gpsaswimming/web-results) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-22 -->
