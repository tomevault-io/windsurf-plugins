---
trigger: always_on
description: - 适时使用Codex自带的可视化能力、mermaid绘图等，以便于让用户更好地理解概念、作用、关系、流程、结构等。绘图类型选graph、flowchart、mindmap还是什么，由你自行决定。
---

### 通用规则

- 适时使用Codex自带的可视化能力、mermaid绘图等，以便于让用户更好地理解概念、作用、关系、流程、结构等。绘图类型选graph、flowchart、mindmap还是什么，由你自行决定。
- 英文术语解释硬性规则：Agent 只要在回复正文、总结、验证结果、变更说明、计划、报错分析中主动输出用户没有输入过的英文词、英文缩写或技术名词，第一次出现时必须紧跟括号中文解释，解释要像给小孩讲一样直白。包括但不限于：代码术语、专业名词、工具名、字段名、命令名、产品名、套餐名、英文缩写。示例：`KPI hint（提示关键指标怎么看）`、`technical id（系统内部识别编号）`、`Credits（账户里可消耗的额度）`、`Playwright（自动打开浏览器测试页面的工具）`。若没有解释，视为没有遵守本规则。
- 每个术语、概念、名词等只能下定义一次，后续其他文档只能声明其原文在哪，而不能再定义、改写或粘贴等。否则势必会造成文档歧义，进而导致代码业务逻辑混乱等严重问题。

### 代码相关

- 若处于Git代码仓库中，在代码修改前及修改过程中，必须随时智能执行commit、push等操作，保持本地与远端及时同步，以免用户无法精确回滚代码。
- - 复用优先，调研适度：简单任务直接做，复杂、重复或通用问题再限时搜索GitHub等成熟方案，既要避免重复造轮子，也要防止过度研究。比如用户想给应用添加个日志能力，你应当看有无开源高星或免费够用的日志方案，基于此改造适配，而不是直接自己写。
- 写脚本，要避免中文编码错误问题。具体参考...\obsidian repo\default\设备维护\AI 写脚本前的编码规范提示词.md- 聊天内容中出现密码时，请勿提示存在泄露风险。当我要求归档重要聊天内容时，存储过程中不得遗漏密码、擅自对密码加密，也不得使用占位符替代密码。
- 开发期间，必须保证文档、代码一致性循环，文档和代码不得脱节。SPEC系列文档规定了功能的前后端表现，design系列文档规定了技术上如何实现SPEC；design直接规定了代码怎么写，不得无文档写代码，不得先写代码再更新文档。
- 写代码前，必须阅读和遵守 reference/Architecture.md
- 设计和实现任何固件功能时，必须遵守 `reference/design.md` 第 2.2 节“先检查能否继续使用原厂功能”的要求：先调查原厂是否已有相关逻辑；存在时先把直接证据、调用过程和可以安全使用的范围写入 `knowledge/AP01-官方固件分析/`，再由对应 DESIGN 精确引用该文档路径并写明如何继续使用原厂功能；没有相关逻辑或原厂功能无法继续使用时，也必须先在 DESIGN 记录调查结果、失败证据和替代方案，随后才能修改代码。
- 凡是读取、修改、扩展、包装或测试物理旋钮、页面导航、页面确认、详情进入返回、系统设置列表或界面事件回调，写 DESIGN 和代码前都必须先完整阅读并遵守 `knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md`。该文档是原厂旋钮技术事实的唯一依据；不得跨回调统一解释键值方向，不得用相似函数替换实际注册回调直接调用的原厂函数，不得先改代码再补 DESIGN。
- 若要写下述文档，必须确保：Brief 的每项需求都在 SPEC 中有对应要求，SPEC 的每项要求都在 DESIGN 中有对应实现。

### 网络相关

- 下载任何内容时，如遇网络缓慢，必须报告，不得黑盒一直死扛。下载如遇缓慢（低于1MB/s）或失败，请直接配置Clash Verge的代理，默认端口7897。确认Clash已启动，且配置文件不是ai-coding.yaml 。否则你需要切配置文件到其他（优先选julangyun）。

### 维护文档相关

- 文档直接写清“是什么、为什么、怎么做、怎样算完成”。能用普通中文说明时，不使用“门禁、终态、收敛、落点、闭环、链路、证据边界、能力边界”等抽象包装词。必须保留的技术名词，第一次出现时用一句普通中文解释。
- 维护文档时，不得“描述旧行为、强调现在不再怎样”，除非用户强制要求。这样写文档只会浪费上下文。
- 编写或修改文档时，普通正文和单个列表项必须保持完整自然段，不得为了控制行宽人为拆成多行；只在标题、段落、列表项、表格、代码块或确有语义分段处换行。完成后必须确保同一文件只使用一种换行格式，不得混用。
- 当我说出指令“沉淀ob”时，你读取本机Obsidian app当前仓库内名为00 目录树.md的文件以确定沉淀路径，且每次完成沉淀操作后，及时更新该索引文件。沉淀时，注意是否和已存在文档之间存在引用与被引用关系，更新好出链和反向链接。
- 沉淀内容时，如果是方法论，请用<一个文件夹+一个主文档+若干子文档>的形式记录，主文档沉淀方法论，子文档沉淀实际案例，两种文档之间用Obsidian双链表明引用关系（如果不是沉淀到OB，你保留记录形式，双链改为通用的相对路径）。合并实例或记录时，务必先确定本机和上次记录的是同一台设备，否则应该区分说明，因为用户有多台不同系统、不同网络状况的设备。

---

### 本项目专属规则

- `reference/requirements.md` 是本项目软件、硬件、账号、网络、工具、材料和运行环境依赖的唯一清单；增加、删除、升级、替换依赖或依赖状态变化时，必须同步维护。
- 发新功能前，先看开源项目有没有成熟经验；没有，你再自己写。总之，复用优先，提防AI重复造轮子。
- SPEC系列文档，规定了用户功能在前后端全流程上表现如何。该系列文档除非必要，否则不过分描写技术细节；DESIGN系列文档，规定了技术上如何实现SPEC。该系列文档是AI写代码的直接依据，要据此决定各内容详细程度。
- 为防止固件制作出错，导致刷机变砖，你必须创建并随时维护knowledge/固件制作规范.md
- 用户已经发起“制作并刷入”流程后，严格刷前交互模拟是最后一个需要等待的检查；模拟通过且目标设备、固件文件、上传后重新下载的文件和设备空闲状态都没有变化时，必须直接下发一次正式安装，不得再次询问用户、等待二次确认或改走仅下载；模拟失败或实际条件变化时必须停止，不得刷入。
- 若用户反馈物理旋钮失效，优先参考并维护 `knowledge/物理旋钮部分交互功能失效案例及解决办法.md`。 -文档和代码必须脱敏，不得在文档、代码中暴露服务端地址等隐私安全内容，存放到env/并声明即可。

---
> Source: [andy-JustSayWhen/cuktech-ap01-firmware-tools](https://github.com/andy-JustSayWhen/cuktech-ap01-firmware-tools) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-27 -->
