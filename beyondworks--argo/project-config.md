---
trigger: always_on
description: > **정본은 [`CLAUDE.md`](CLAUDE.md) 한 곳이다. 작업 시작 전 반드시 읽어라.**
---

# Argo — 에이전트 지침 (Codex 등 AGENTS.md를 읽는 도구용)

> **정본은 [`CLAUDE.md`](CLAUDE.md) 한 곳이다. 작업 시작 전 반드시 읽어라.**
> 이 파일은 그 사본이 아니라 포인터다 — 사본을 두면 반드시 갈라지고, 실제로 갈라졌었다
> (2026-07-29: 사본이 `Claude`→`Codex` 일괄 치환으로 "Codex Agent SDK 코어" 같은 거짓을
> 담은 채, 절대 규칙 한 절이 빠진 상태로 방치돼 있었다).

아래는 **읽지 않고 지나가면 안 되는 것만** 인라인으로 둔다. 나머지 전부(도메인 파이프라인,
추천 에이전트·스킬, 검증 기준)는 `CLAUDE.md`에 있다.

## North Star (고정값)

> 일반 사용자가 가입해 프롬프트 한 줄로 AI 직원 회사를 만들고, 폴더 단위 기억으로 일 시키는 SaaS

파일 3개 / 단계 1개마다 자문한다: "방금 만든 게 North Star에 직접 기여하나, 만들기 쉬운
곁가지로 샜나?" 표류를 감지하면 한 걸음도 더 가지 말고 정지 → 재정렬.

## 절대 규칙 (위반 금지)

- **시크릿 평문 금지.** 코드·주석·커밋 메시지·PR·문서 어디에도 API 키·토큰·DB 접속 문자열을
  적지 않는다. 환경변수 **이름과 저장 위치만** 기록한다. `git add .` / `git add -A` 금지 —
  파일을 명시해 add하고, 커밋 전 `git diff --cached`를 `key|token|secret|DATABASE_URL`로 훑는다.
- **빌드 통과 ≠ 동작.** UI 변경은 dev 서버에 띄워 눈으로 확인해야 완료다. 배포 직후엔
  엔드포인트를 `curl`로 검증한다.
- **자기 승인 금지.** 검수는 구현한 컨텍스트가 아닌 다른 컨텍스트가 한다. 정렬 검수(목적 일치)가
  품질 검수보다 먼저다.
- **못 한 검증은 정직하게** — "추론(코드 동일성) — 라이브 아님"처럼 명시한다.
  "완료 / 무결 / 검증 완료" 도장을 결과물에 붙이지 않는다.
- **다국어**: 모든 UI 문자열은 `app/i18n.jsx` 사전(`t`)을 거친다. 새 화면·기능은 지시가 없어도
  ko·en 둘 다 등록한다. 한국어 모드는 고유명사(Argo, Claude 등)를 빼고 전부 한글.
- **파괴적 액션**: 크루 해고·회사 보관 등은 `ui.jsx`의 `DangerModal`을 쓴다.
  `window.confirm` / `window.prompt`는 Tauri에서 무동작이라 금지.
- **사용자 데이터 커밋 금지**: `workspaces/`는 gitignore. 워크스페이스 루트는 env `ARGO_ROOT`.

## 변경 안전 절차

`CLAUDE.md`의 "변경 안전 절차" 절을 그대로 따른다. 요약하면 세 방어선이다 —
**올바른 베이스에서 시작**(stale 브랜치 위 작업 금지, 한 변경 = 한 워크트리, 테스트는 임시
`ARGO_ROOT` + 별도 포트), **읽고 나서 쓰기**(추측한 줄번호로 고치지 않고 기존 패턴 재사용),
**수준에 맞는 실제 검증 + 분리 검수**. 변경 크기별 체크리스트는 정본을 볼 것.

---
> Source: [beyondworks/argo](https://github.com/beyondworks/argo) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-22 -->
