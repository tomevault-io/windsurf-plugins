---
trigger: always_on
description: Automatic video transcoding service for Jellyfin that monitors media libraries and transcodes to optimized 720p HEVC, H.264, or AV1 with NVIDIA NVENC and Intel QSV hardware acceleration.
---

# CLAUDE.md — Jellyfin Encoder

## Overview
Automatic video transcoding service for Jellyfin that monitors media libraries and transcodes to optimized 720p HEVC, H.264, or AV1 with NVIDIA NVENC and Intel QSV hardware acceleration.

## Tech Stack
- Python
- FFmpeg (HEVC/H.264/AV1 encoding via NVENC, QSV, or software fallback)
- Docker (drumsergio/jellyfin-encoder)
- Pytest (testing)
- GitHub Actions CI

## Development
```bash
# Run tests
cd tests && pytest

# Run locally
python app/monitor.py

# Docker
docker compose up
```

## Architecture
- `app/monitor.py` — main monitoring and transcoding logic
- `app/requirements.txt` — Python dependencies
- `tests/` — test suite
- `scripts/` — helper scripts
- `Dockerfile` — container build with FFmpeg and GPU drivers
- `docker-compose.yml` — example deployment with device passthrough
- `setup.cfg` — Python package configuration

## Key Rules
- Never hardcode media paths; use environment variables and volume mounts
- Docker images use semver tags on Docker Hub (drumsergio/jellyfin-encoder)
- Always support hardware acceleration fallback to software encoding (libx265/libx264/libsvtav1)
- Temp-file workflow (`.tmp` extension) with atomic rename to prevent indexing incomplete files
- Mount-health checks must pass before any cleanup/deletion operations
- An existing output is done whatever container it uses. Locate encodes by filename stem across every supported extension, never by a hardcoded `.mkv`, so a codec or container switch never re-encodes an existing library

*Generated by [LynxPrompt](https://lynxprompt.com) CLI*

---
> Source: [GeiserX/jellyfin-encoder](https://github.com/GeiserX/jellyfin-encoder) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-09 -->
