---
trigger: always_on
description: > 5,226-year structured chronicle of human civilization. Layer 1 (year-level JSON corpus) shipped 2026-04-13. Layer 2 (scholarly evidence layer) in progress on `evidence-layer` branch.
---

# Human History According to AI — Project Rules

> 5,226-year structured chronicle of human civilization. Layer 1 (year-level JSON corpus) shipped 2026-04-13. Layer 2 (scholarly evidence layer) in progress on `evidence-layer` branch.

## TOP-PRIORITY RULES (read first)

1. **Never modify `RESEARCH_PROMPT.md`.** It is the canonical ICCRA prompt and the contract behind all 5,226 JSON files. Any change invalidates the corpus. (See `CONTRIBUTING.md:43`.)
2. **Never touch `outputs/json/*.json` by hand.** These are daemon outputs. Fixes flow through `scripts/fix_categories.py`, `scripts/backfill_meta.py`, or a new re-research run — never sed/jq edits in place.
3. **Schema is enforced.** `scripts/validate_corpus.py` is the gate. After any corpus change, run it and confirm 100% valid before committing.
4. **Never swap models without an archived quality comparison.** Haiku and Gemini Flash experiments both failed quality checks; archived under `outputs/haiku_experiment/` and `outputs/gemini_experiment/`. Sonnet 4.6 is the chosen model; changing it requires a side-by-side run.
5. **Frontend must never crash on unexpected AI enum values.** Categories/certainties occasionally drift outside the schema. Any lookup against `CATEGORY_CONFIG` / `CERTAINTY_CONFIG` / docLevel maps must guard for unknowns (see commits `fix: guard ...`).
6. **Next.js 16 is NOT the Next.js in your training data.** Before writing frontend code, read `frontend/node_modules/next/dist/docs/` for the current API surface. Heed deprecation notices. (See `frontend/AGENTS.md`.)
7. **Priority order:** correctness > honesty-about-uncertainty > clarity > maintainability > performance. The project's whole value proposition is "every gap is declared, nothing is fabricated" — do not paper over uncertainty.
8. **Ask before committing.** Daemon-style auto-commits (`Progress: …`, `Auto-refresh: …`) are written by `scripts/git_sync.sh` and `scripts/refresh_frontend.sh`. Human commits should use the observed lowercase-prefix style (`docs:`, `fix:`, `feat:`, `redesign:`) with em-dash elaboration.

## PROJECT IDENTITY

- **Layer 1 (main branch, complete):** 5,226 JSON files (one per year, 2025 CE → ~3200 BCE) in `outputs/json/`. 17,991 events, 13,130 graph edges, 0 failed years, ~$15.68 total cost. Generated by async Python daemon against the Anthropic API using Claude Sonnet 4.6.
- **Layer 2 (evidence-layer branch, in progress):** Scholarly-evidence deep-dives per era via Scite MCP; new top-level `evidence-layer/` directory. Staged but uncommitted as of 2026-04-17.
- **Frontend:** Next.js 16 + React 19 interactive timeline at `frontend/`. Served from timeline.sumarhus.com (Hetzner edge; the old Vercel host redirects). Virtualized timeline, progressive disclosure, year-jump search.
- **Daemon:** Python 3.11 async (`scripts/orchestrator.sh` + `scripts/api_client.py`) running in Docker (`docker/docker-compose.yml`). Idle now — Phase 1 done.

## FILE / DIRECTORY MAP

| Path | Role | Touch policy |
|---|---|---|
| `RESEARCH_PROMPT.md` | Canonical ICCRA prompt | **Locked — never modify** |
| `outputs/json/*.json` | Corpus (5,226 files) | **Never hand-edit; use scripts** |
| `outputs/haiku_experiment/`, `outputs/gemini_experiment/` | Failed-quality archives | Read-only reference |
| `outputs/failed/` | Per-attempt failed outputs | Read-only diagnostic |
| `scripts/orchestrator.sh`, `scripts/run_year.sh`, `scripts/api_client.py`, `scripts/batch_processor.py` | Daemon core | Modify with care; daemon is idle but scripts still referenced |
| `scripts/validate_corpus.py` | QA gate | Treat as source of truth for schema |
| `scripts/fix_categories.py`, `scripts/backfill_meta.py` | Corpus migrators | Use as templates when writing new migrators |
| `scripts/git_sync.sh`, `scripts/refresh_frontend.sh` | Automation | Run on cron in prod; don't invoke casually |
| `state/progress.json` | Daemon progress | Source of truth for `completed`/`failed`/`in_progress` |
| `state/cache/` | SHA256-keyed API response cache | Large; safe to ignore |
| `frontend/src/` | Next.js 16 app | React 19; shadcn/ui; tailwind v4 |
| `frontend/public/data/` | Aggregated chunks + manifest | Regenerate via `aggregate-data.mjs` |
| `frontend/scripts/aggregate-data.mjs` | JSON → 100-year chunks | Run from repo root |
| `evidence-layer/` | Layer 2 scholarly deep-dives | Branch `evidence-layer`; uncommitted |
| `docker/` | Daemon container | Reboot-persistent |
| `LEDGER.md` | Append-only daemon progress log | Auto-maintained |

## CORPUS SCHEMA (ICCRA)


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [Magnussmari/chronograph](https://github.com/Magnussmari/chronograph) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-13 -->
