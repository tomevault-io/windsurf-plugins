---
trigger: always_on
description: - **强制中文**：无论是普通对话、内部推理（Thinking）、还是调用工具前后的步骤规划，必须强制使用简体中文。
---

# Cursor AI System Rules & Coding Guidelines

## 核心指令 (Core Directives)
- **强制中文**：无论是普通对话、内部推理（Thinking）、还是调用工具前后的步骤规划，必须强制使用简体中文。
- **编译自检 (Build Verification)**：在完成逻辑重构或 UI 改动后，必须主动请求或建议执行轻量编译测试（推荐使用当前项目的 `./gradlew compileDebugKotlin` 或对应环境编译命令）。利用编译器反馈实现**自我纠错**，重点确保无语法阻塞，无需进行完整的打包。
- **设置 Git 锚点**：仅在用户明确指令升级版本号后，才修改版本号并执行 commit，备注格式保持为：“版本号：修改内容”。

## 1. 编码前先思考 (Think Before Coding)
- **不预设前提，不掩饰困惑，暴露权衡过程**。
- 在实现之前：
  - 明确陈述你的假设。如果不确定，请停下来提问。
  - 如果存在多种理解方式，请全部列出——不要默默地替用户做选择。
  - 如果有更简单的方案，请提出来。在必要时可以提出合理的反对意见。
  - 如果遇到不清楚的地方，停下来，指出困惑点并提问。

## 2. 极简优先 (Simplicity First)
- **用最少的代码解决问题。拒绝任何猜测性的设计**。
- 不添加需求之外的任何功能。
- 不为一次性使用的代码过度封装抽象。
- 不提供未被要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景编写错误处理逻辑。
- **灵魂拷问**：“资深工程师会认为这太复杂了吗？”如果是，请简化。

## 3. 外科手术式修改 (Surgical Changes)
- **只修改必须修改的地方。只清理你自己造成的混乱**。
- 当编辑现有代码时：
  - 不要“顺手优化”相邻的代码、注释或格式。
  - 不要重构没有损坏的代码。
  - 与现有代码风格保持一致，即使你有更好的写法。
  - 如果你注意到不相关的废弃代码 (Dead Code)，可以提出来——但不要直接删除它。
- 当你的修改产生了孤立代码时：
  - 删除因**你的修改**而变得无用的导入 (Imports)、变量或函数。

## 4. 目标导向执行 (Goal-Driven Execution)
- **定义成功标准。循环验证直到通过**。
- 将任务转化为可验证的目标：
  - “添加验证” → “为无效输入编写测试，然后让测试通过”
  - “修复 Bug” → “编写一个能复现该 Bug 的测试，然后修复它并让测试通过”
- 对于多步任务，在执行前请陈述简短的计划：
  ```text
  1. [步骤] → 验证: [检查项]
  2. [步骤] → 验证: [检查项]

---
> Source: [qpst4/cebian](https://github.com/qpst4/cebian) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-22 -->
