---
trigger: always_on
description: \## Superpowers 核心纪律\
---

\## Superpowers 核心纪律\
Superpowers — 全技能合集

\<EXTREMELY\_IMPORTANT>
如果你认为某个技能有哪怕 1% 的可能性适用于你正在做的事情，你必须调用它。
这不是可选的，不是建议，是强制要求。
用户指令始终拥有最高优先级。
\</EXTREMELY\_IMPORTANT>

***

## 技能 1：头脑风暴（brainstorming）

**触发条件：** 收到新功能需求、要构建组件、添加功能或修改行为时，必须使用。

**铁律：** 严禁在呈现设计并获得用户批准之前，调用任何实现技能、编写任何代码。每个项目都必须经过此流程，无论看起来多简单。

**流程（按顺序执行）：**

1. **探索项目上下文** — 检查文件、文档、最近的提交记录
2. **逐个提出澄清问题** — 一次只问一个问题，理解目的/约束/成功标准，优先使用多选题
3. **提出 2-3 种方案** — 附带权衡分析和你的推荐
4. **分段呈现设计** — 按复杂度缩放，每段后询问用户是否正确
5. **编写设计文档** — 保存到 docs/superpowers/specs/ 目录并提交 git
6. **设计文档自审** — 检查占位符(TBD/TODO)、矛盾、歧义、范围问题并修复
7. **用户审阅** — 请用户审阅设计文档后再继续
8. **过渡到实现** — 调用"编写计划"技能

**关键原则：** 一次一个问题、优先多选题、YAGNI 无情移除不必要功能、始终探索替代方案、增量验证。

**如果项目太大无法用单个规格覆盖：** 帮用户分解为子项目，每个子项目独立走 设计→计划→实现 循环。

***

## 技能 2：测试驱动开发（test-driven-development）

**触发条件：** 实现任何功能或修复 Bug 之前，在编写实现代码之前必须使用。

**铁律：** 没有失败的测试就没有生产代码。在测试之前写了代码？删除它，从头开始。不要保留为"参考"，不要"改编"，直接删除。

**RED-GREEN-REFACTOR 循环：**

**RED — 编写失败测试：**

- 写一个展示预期行为的最小测试
- 要求：一个行为、清晰名称、真实代码（除非不可避免否则不用 mock）

**验证 RED — 看它失败（必须执行，不可跳过）：**

- 运行测试命令，确认测试失败（不是报错）
- 失败原因应是功能缺失（不是拼写错误）
- 如果测试通过 → 你在测试已有行为，修复测试
- 如果测试报错 → 修复错误，重跑直到正确失败

**GREEN — 最小代码：**

- 写最简单的代码让测试通过
- 不要添加功能、不要重构、不要"改进"超出测试范围

**验证 GREEN — 看它通过（必须执行）：**

- 运行测试，确认通过且其他测试仍然通过

**REFACTOR — 清理：**

- 仅在绿色之后：移除重复、改善命名、提取辅助函数
- 保持测试绿色，不添加行为

**常见借口反驳：**

| 借口         | 现实                     |
| :--------- | :--------------------- |
| 太简单不需要测试   | 简单代码也会出错，测试只需30秒       |
| 我之后补测试     | 测试立即通过证明不了任何东西         |
| 已手动测试过了    | 临时性≠系统性，没有记录，无法重跑      |
| 删掉X小时工作太浪费 | 沉没成本谬误，保留未验证代码才是技术债务   |
| TDD太教条     | TDD就是务实的：先发现Bug比之后调试更快 |

***

## 技能 3：系统化调试（systematic-debugging）

**触发条件：** 遇到任何 Bug、测试失败或意外行为时，在提出修复方案之前必须使用。

**铁律：** 没有根因调查就没有修复。未完成阶段1就不能提出修复方案。

**四阶段流程（必须按顺序完成）：**

**阶段1：根因调查**

- 仔细阅读错误信息（完整堆栈跟踪、行号、文件路径、错误码）
- 稳定复现（能可靠触发吗？确切步骤？每次都发生？不能稳定复现→收集更多数据，不要猜）
- 检查近期变更（git diff、最近提交、新依赖、配置变更、环境差异）
- 多组件系统：在每个组件边界添加诊断日志，运行一次确定在哪里中断
- 追踪数据流：错误值从哪来？什么调用了它？一直追溯到源头

**阶段2：模式分析**

- 找到可工作的类似代码
- 与参考实现完整对比（不要跳读）
- 列出每个差异（无论多小）
- 理解依赖关系

**阶段3：假设与测试**

- 形成单一假设："我认为X是根因，因为Y"
- 做最小可能变更来测试，一次一个变量
- 验证后再继续；不起效→形成新假设

**阶段4：实现修复**

- 先创建失败的测试用例
- 实现单一修复，一次一个变更
- 验证修复
- **如果已尝试3次修复仍失败：停下来质疑架构，与用户讨论**

**红旗（立即停止回到阶段1）：**
"快速修一下之后再调查" / "试试改X看看" / "一次加多个修改" / "跳过测试我手动验证" / "应该是X直接改吧"

***

## 技能 4：编写计划（writing-plans）

**触发条件：** 有规格说明或需求需要多步骤任务时，在接触代码之前使用。

**核心原则：** 编写全面实现计划，假设执行者对代码库零上下文。记录一切：文件路径、代码、命令、预期输出。

**计划文档头部（必须包含）：**

- 目标：一句话
- 架构：2-3句话
- 技术栈：关键技术/库

**任务结构要求：**

- 每步一个动作（2-5分钟）
- 精确文件路径
- 完整代码（代码步骤必须有代码块）
- 精确命令和预期输出
- 复选框语法跟踪进度

**严禁出现：** TBD/TODO/"之后实现"/"添加适当的错误处理"/"为上述写测试"（无实际代码）/"类似于任务N"/描述做什么但不展示怎么做

**自审：** 规格覆盖检查→占位符扫描→类型/命名一致性检查

**执行交接：** 计划保存后提供两个选项：

1. 子代理驱动（推荐）— 每任务派遣新子代理+两阶段审查
2. 内联执行 — 当前会话批量执行带检查点

***

## 技能 5：子代理驱动开发（subagent-driven-development）

**触发条件：** 在当前会话中执行具有独立任务的实现计划时使用。

**核心原则：** 每任务派遣新子代理 + 两阶段审查（先规格合规再代码质量）。

**流程：**

1. 读取计划，提取所有任务全文，创建 TodoList
2. 对每个任务：
   - 派遣实现子代理（提供完整任务文本+上下文）
   - 子代理有问题→回答后重新派遣
   - 子代理实现、测试、提交、自审
   - 派遣规格审查子代理 → 不合规→修复→重审
   - 规格合规后派遣代码质量审查子代理 → 有问题→修复→重审
   - TodoList 标记完成
3. 全部完成后派遣最终代码审查
4. 调用"完成开发分支"技能

**子代理状态处理：**

- DONE → 继续审查
- DONE\_WITH\_CONCERNS → 阅读关注点后决定
- NEEDS\_CONTEXT → 提供缺失上下文重新派遣
- BLOCKED → 评估原因（上下文不足/任务太大/计划错误）

**红旗：** 绝不跳过审查、绝不带着未修复问题继续、绝不并行派遣多个实现子代理

***

## 技能 6：请求代码审查（requesting-code-review）

**触发条件：** 完成任务、实现主要功能或合并之前。

**必须审查的时机：** 子代理开发中每任务后 / 完成主要功能后 / 合并到主分支前

**流程：**

1. 获取 git 提交 SHA（基准和当前）
2. 派遣代码审查子代理（提供：实现了什么、计划/需求、基准SHA、当前SHA）
3. 根据反馈：Critical 立即修复 / Important 继续前修复 / Minor 记录稍后 / 审查者错误则用技术理由反驳

***

## 技能 7：完成前验证（verification-before-completion）

**触发条件：** 在声称工作完成、已修复或通过之前，在提交或创建PR之前。

**铁律：** 没有新鲜验证证据就不能声称完成。

**门控函数（声称任何状态前必须执行）：**

1. 识别：什么命令能证明？
2. 运行：执行完整命令
3. 阅读：完整输出，检查退出码，计数失败数
4. 验证：输出确认了声明？
5. 只有这样：才能做出声明

**常见失败：**

| 声称     | 需要的证据        | 不充分         |
| :----- | :----------- | :---------- |
| 测试通过   | 测试命令输出：0失败   | 上次运行/"应该能过" |
| Bug修复了 | 测试原始症状：通过    | 代码改了/假设修好了  |
| 构建成功   | 构建命令：退出码0    | linter过了    |
| 代理完成了  | VCS diff显示变更 | 代理报告"成功"    |

**红旗：** "应该"/"大概"/"似乎" / 验证前表达满意 / 未验证就准备提交

***

## 技能 8：使用Git工作树（using-git-worktrees）

**触发条件：** 开始需要隔离的功能开发时，或执行实现计划之前。

**目录选择优先级：** 已存在目录(.worktrees优先) → 项目配置文件偏好 → 询问用户

**安全验证：** 项目本地目录必须验证已被 gitignore 忽略。未忽略→添加到.gitignore→提交→继续。

**创建步骤：**

1. 检测项目名 → 创建工作树并新建分支
2. 自动检测并运行项目设置（npm install / cargo build / pip install 等）
3. 验证干净基线（运行测试）
4. 测试失败→报告并询问；测试通过→报告就绪

***

## 技能 9：执行计划（executing-plans）

**触发条件：** 有书面实现计划需要在当前会话执行时。

**流程：**

1. 加载计划 → 批判性审查 → 有疑虑先向用户提出
2. 无疑虑→创建 TodoList → 逐任务执行（标记进行中→按步骤执行→运行验证→标记完成）
3. 全部完成→调用"完成开发分支"技能

**何时停止求助：** 遇到阻塞/计划有缺口/不理解指令/验证反复失败

***

## 技能 10：派发并行代理（dispatching-parallel-agents）

**触发条件：** 面对2+个独立任务，无共享状态或顺序依赖。

**何时使用：** 3+测试文件因不同根因失败 / 多子系统独立损坏 / 每个问题可独立理解
**何时不使用：** 失败相关 / 需要完整系统状态 / 代理会互相干扰

**流程：** 识别独立域→创建聚焦任务（特定范围+明确目标+约束+预期输出）→并行派遣→审查集成（读摘要→验证不冲突→跑完整测试）

***

## 技能 11：完成开发分支（finishing-a-development-branch）

**触发条件：** 实现完成、所有测试通过，需要决定如何集成。

**流程：**

1. 验证测试（失败→停止）
2. 确定基准分支
3. 呈现恰好4个选项：本地合并 / 推送并创建PR / 保持原样 / 丢弃（需输入"discard"确认）
4. 执行选择 → 清理工作树（选项3除外）

***

## 技能 12：接收代码审查（receiving-code-review）

**触发条件：** 收到代码审查反馈时，在实施建议之前。

**响应模式：** 完整阅读→复述需求→对照代码库验证→评估技术合理性→响应（确认或反驳）→逐项实施并测试

**禁止：** "你说得完全正确！"/"好观点！"/"让我现在就实现"（验证之前）
**正确：** "已修复。\[简述]" / "好发现-\[具体]。已在\[位置]修复。" / 直接修复展示

**不清楚的反馈：** 有任何不清楚→停止→先澄清。部分理解=错误实施。

**YAGNI检查：** 审查者建议"正确实现"某功能→搜索实际使用→未使用则建议移除。

***

## 技能 13：使用Superpowers（using-superpowers）


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [CXCXHH/career-ops-GUI-cn](https://github.com/CXCXHH/career-ops-GUI-cn) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-12 -->
