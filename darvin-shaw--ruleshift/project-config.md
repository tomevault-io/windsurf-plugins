---
trigger: always_on
description: 每次将代码提交并推送到 GitHub 远端（`origin`）前，必须：
---

# AGENTS.md

## 提交与推送约定（必须遵守）

每次将代码提交并推送到 GitHub 远端（`origin`）前，必须：

1. 检查并总结本次变更：
   - 对已跟踪文件运行 `git diff`；
   - 如有新增/未跟踪文件，先 `git add -A`，再运行 `git diff --cached`（可结合 `git status`），确保所有变更都被纳入总结。
2. 将变更总结保存为新文件，放在 `docs/git-diffs/` 目录：
   - 文件名格式：`<YYYY-MM-DD>-<简短描述>.md`；
   - 内容至少包含：日期、涉及文件、变更要点、注意事项/影响。
3. 将代码变更与该总结文件一起暂存并提交：
   - `git add -A`
   - `git commit -m "概括本次变更的提交信息"`
4. 推送到远程仓库：`git push`。

说明：空提交或纯文档调整同样适用本约定；若无法自动执行，由助手按本约定人工完成。

## 开发与运行约定（必须遵守）

### Skill 使用约定

- 自主掌握并调用当前环境可用的全部技能族（通用/系统、Figma、Mixpanel、Vercel、Zotero 等），
  不依赖用户逐条点名；任务命中某技能时应主动判断、说明并采用。
- 采用前先完整读取对应 `SKILL.md` 再执行；复杂任务可组合多个技能，并保持步骤有序。
- 开发、运行、诊断过程中，若当前任务命中可用 Skill（仓库内 `SKILL.md`、插件或工具提供的技能），
  应先读取对应 `SKILL.md` 并按其指引执行，以提高效率与质量。
- 若没有可用或相关的 Skill，不得强行套用，按常规工程流程推进，并可在变更总结中说明取舍。

### 节约 Token 约定

- 在仓库探索、长文档阅读、日志排查、调试与评审前，先评估必要性，优先使用定向搜索
  （`rg`/关键词）或读取最小必要片段，避免整库/整文件无差别读取。
- 输出保持精简：优先汇报结论与关键证据，避免重复搬运大段原文或工具输出。
- 若存在适用的 Token/上下文治理技能（如 `token-triage`），应先行使用。

## 创新提案与联动更新约定（必须遵守）

- 在开发、研究、评测或联调过程中发现新的创新点时，应先形成简短提案并向用户汇报，
  至少说明：待解决问题、创新内容、与现有方案的差异、预期价值、可行性证据、成本/风险、
  受影响文件以及可验证的验收标准。
- 不改变项目目标、数据合规边界、核心架构、外部依赖和总体排期的小范围改进，可在汇报后
  直接纳入当前已授权任务；涉及上述任一重大影响的创新，必须在用户明确确认后实施。
- 已纳入开发过程的创新必须登记到 `docs/innovation-log.md`，并按照
  `docs/innovation-process.md` 完成影响分析、任务编号、状态维护和验收记录。
- 实施创新时必须在同一变更批次同步调整所有受影响产物，包括但不限于：README、总体方案、
  WBS/里程碑、架构说明、数据来源与许可清单、Schema/本体、接口与 MCP、Agent/Skill 配置、
  测试、Golden/评测指标、部署说明和演示材料；不允许只修改代码而保留失真的文档或配置。
- 创新完成的定义：实现与测试通过、相关文档和状态已同步、风险/限制已记录、创新台账已更新；
  提交与推送时仍须执行本文件的“提交与推送约定”。

---
> Source: [Darvin-shaw/RuleShift](https://github.com/Darvin-shaw/RuleShift) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-07 -->
