---
trigger: always_on
description: - 用户是资深 iOS / macOS 工程师；涉及 Electron、React、Node.js 或 Web 前端概念时，优先给出与 Xcode、Swift、AppKit / SwiftUI 的对应关系。
---

# CodeDoc 项目协作规范

## 用户与沟通

- 默认使用中文交流。
- 用户是资深 iOS / macOS 工程师；涉及 Electron、React、Node.js 或 Web 前端概念时，优先给出与 Xcode、Swift、AppKit / SwiftUI 的对应关系。
- 沟通应务实、简洁、专业。复杂问题先给专业结论，再解释关键点。
- 不要只说明框架名称；应给出准确文件路径、修改边界、可复制的运行命令和验证结果。

## 项目背景

本项目是一个本地优先的桌面工具，用于把用户主动选择的本地代码项目整理为软件著作权登记所需的源程序鉴别材料准备稿。核心流程包括项目扫描、文件筛选、代码清洗、敏感信息脱敏、规范化分页、风险校验以及 docx / txt 导出。

CodeDoc 是当前正式品牌。产品保持完全离线、本地优先，并以可验证、可维护、可发布的桌面应用为长期目标。

## 品牌基准

品牌信息的唯一基准是 [`docs/BRAND.md`](docs/BRAND.md)。未经产品负责人明确确认，不得自行改写、缩写或翻译正式品牌文案。

- 中文正式名：软著代码整理器
- 英文正式名：CodeDoc Generator
- 英文简称：CodeDoc
- 中文副标题：一键生成软著代码审核材料
- 英文副标题：Generate source code documents for software copyright registration.

应用、npm workspace、项目配置和发布产物统一使用 CodeDoc 技术标识。图标、截图、仓库目录或远程地址如需调整，必须单独确认范围并完成对应验收。

## 产品定位

- App 类型：开发者工具 / 文档整理工具。
- 主要语言：简体中文。
- 目标平台：macOS 与 Windows。
- 核心路径：导入本地项目 -> 选择代码文件 -> 清洗与排版 -> 分页预览 -> 校验并导出。
- 核心价值：减少人工整理源程序鉴别材料时的格式错误和重复劳动。
- 隐私定位：产品功能完全离线，用户代码不离开本机。

## 产品与合规边界

- CodeDoc 生成的是材料准备稿，不替代中国版权保护中心或其他登记机构的正式审查。
- 不得宣传“官方认证”“保证通过”“直接提交必过”“自动代办”等无法证明的结论。
- 不得把工具输出描述为法律意见、行政审批结论或专业法律建议。
- 最终提交前，用户仍需按登记机构的最新要求复核软件名称、版本号、页数、行数、页眉、署名、权属和材料真实性。
- 代码扫描、清洗、脱敏、分页、校验与导出必须保持本地执行；未经明确允许，不得上传、缓存或远程处理用户源码。
- 工具不得修改、删除或覆盖用户导入的源代码文件。

## 硬性约束

- 未经明确允许，不新增网络请求、账号系统、登录、服务端、云同步、在线 AI、分析 SDK、广告追踪、自动上传或敏感权限申请。
- 未经明确允许，不新增第三方依赖。
- 未经明确允许，不修改业务逻辑、工程配置、应用签名、App ID、Version、Build、发布仓库或自动发布配置。
- 不要把实验功能接入正式默认路径，除非用户明确要求。
- 不要保存、打印或提交任何密钥、token、证书、签名资产或私钥。
- 不要提交安装包、构建产物、二进制大文件、本地环境文件或临时文件，除非用户明确允许。
- 必须保留适用的 Apache-2.0 `LICENSE`、`NOTICE`、第三方许可证及历史归属信息，并按许可证要求标明修改。

## 工程结构与职责边界

- 根目录是 npm workspaces 工作区，不是 Xcode 工程；项目与脚本入口以根目录及各 workspace 的 `package.json` 为准。
- `packages/core/`：零 Electron 依赖的 TypeScript 业务流水线。扫描、清洗、截取、分页、校验和导出规则应优先放在这里，并配套核心测试。
- `packages/app/`：Electron 43 + React 18 桌面应用，负责窗口、文件交互、IPC、状态管理和界面展示。
- `packages/app/src/main/`：Electron Main Process。负责受控的系统能力、文件访问、Worker 调度和 IPC 参数校验。
- `packages/app/src/preload/`：Preload 与 `contextBridge` 安全桥。不得向 Renderer 暴露 `fs`、`path` 或完整 `ipcRenderer`。
- `packages/app/src/renderer/`：React Renderer。界面不得绕过 Preload 直接获得 Node.js 或文件系统能力。
- `design/prototype/`：历史界面原型与实现参考。除非任务明确要求，不得将原型文件当作生产入口修改。
- `docs/`：产品、品牌、安全、维护、版本和验收文档。不得存放构建产物或临时文件。
- `handoff/`：交接材料和辅助流程。涉及品牌迁移时必须单独确认是否继续维护，不能默认视为生产应用的一部分。

新增桌面或文件系统能力时，必须遵循：Renderer 类型声明 -> Preload 白名单 API -> `contextBridge` -> Main IPC handler -> 参数校验 -> Core / Worker。不得为图方便扩大 Renderer 权限。

## 品牌迁移纪律

- 品牌迁移必须作为独立任务先做只读审计，再列出替换清单、兼容策略和验收标准。
- 品牌文字、图标、安装包名称、可执行文件名、App ID、npm 包名、配置文件名、用户数据目录、仓库名和更新地址是不同层级，不得一次性默认全部改名。
- App ID、npm 包名、`.codedoc.json`、既有用户配置目录等技术标识可能影响升级兼容；未经专项评估和明确授权不得修改。
- `LICENSE`、`NOTICE` 和第三方许可证属于法律归属文件，不得机械替换或删除依法需要保留的内容。
- 新图标必须先确认设计方案和适用平台，再生成和接入；不得未经确认覆盖现有图标源文件或二进制图标资源。

## 开发环境与验证基线

- Node.js 基线：`24.19.0`，仓库约束为 `>=24.19.0 <25`。
- 安装依赖：`npm ci`。
- 开发运行：`npm run dev`。
- Main Process 调试：`npm run dev -w @codedoc/app -- --inspect=5858 --sourcemap`。
- 完整验证：`npm run verify`。
- 性能基线：`npm run perf`。
- Apple Silicon DMG 开发验证：`CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist:mac:arm64`。未签名、未公证产物不能作为正式发布包。

必须区分以下结论：

- 开发环境能够启动，不等于 Debug / Release 基线通过。
- 单项测试通过，不等于 `npm run verify` 通过。
- 本地未签名安装包能够运行，不等于完成签名、公证和公开分发验收。

## 开发推进方式

- 不要一上来直接写代码。
- 每次开发任务开始前必须明确：
  1. 产品目标
  2. 技术方案
  3. 任务边界
  4. 允许修改的文件
  5. 不允许修改的文件
  6. 验收标准
- 按阶段小步执行；每完成一个阶段，先汇报结果并等待用户确认，再进入下一阶段。
- 所有修改必须聚焦、可回滚，不得顺手修改无关文件。
- 开始修改前检查 `git status`。发现已有未提交改动时必须保留并避让，不得覆盖、回退或归因到当前任务。
- 结束任务前再次检查 `git status`，说明本阶段文件和异常文件。
- 纯文档修改至少执行 `git diff --check` 和针对目标字段的内容检索。
- 代码、配置或资源修改应运行与风险相匹配的测试，并原则上执行 `npm run verify`；若环境或既有改动阻塞，必须报告原始错误和未验证范围。
- 涉及界面或运行时行为时，除自动化检查外，还应启动开发版并检查实际界面；不得仅凭静态代码宣称视觉验收完成。

## 阶段汇报格式

每个阶段完成后必须汇报：

- 本阶段是否完成
- 新增了哪些文件
- 修改了哪些文件
- 明确哪些重要区域未修改
- 运行了哪些验证命令
- Debug / Release 构建结果
- `git status` 是否存在既有或异常文件
- 是否有未解决问题
- 是否建议进入下一阶段

---
> Source: [littleben803/CodeSucker](https://github.com/littleben803/CodeSucker) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-30 -->
