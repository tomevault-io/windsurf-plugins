---
trigger: always_on
description: 本规范用于大型开发任务，要求 Agent 在编码前充分理解需求、消除疑问、建立可恢复的任务清单，并按任务发起人明确指定或本规范默认规则确定执行节奏。
---

# 通用 Agent 开发协作规范

## 1. 目的

本规范用于大型开发任务，要求 Agent 在编码前充分理解需求、消除疑问、建立可恢复的任务清单，并按任务发起人明确指定或本规范默认规则确定执行节奏。

目标是让任务在聊天中断、上下文丢失、人员或 Agent 更换后，仍可依据仓库内的任务文档安全恢复。

本规范当前仅适用于使用 Git 进行版本管理的项目。

“任务发起人”是创建当前整体开发任务并拥有执行方式、范围变更、任务取消和最终验收决定权的人。除非任务发起人明确授权，其他人的反馈只能作为待确认信息，不得直接改变任务范围、执行方式、验收结果或取消决定。

## 2. 适用范围

### 2.1 必须使用本流程

符合下列任一情况时，视为大型开发任务：

- 涉及多个文件、模块、服务或系统层级；
- 包含多个相互依赖的开发步骤；
- 需求存在业务规则、技术方案或验收口径上的不确定性；
- 预计需要分阶段开发、验证或验收；
- 中断后仅靠聊天记录难以可靠恢复。

### 2.2 可以豁免

错别字修正、明确的单点配置调整、局部样式微调等简单修改，可以不执行完整流程。

如果无法确定任务属于大型任务还是简单修改，Agent 必须先说明判断依据，并向任务发起人确认，不得自行按简单修改处理以绕过本规范。

## 3. 核心原则

1. 先理解需求，后进行实现。
2. 存在未确认疑问时，不得编写业务代码或实施实质性修改。
3. 每轮确认后，必须重新列出“已确认事项”和“剩余疑问”。
4. 只有剩余疑问为零，才能形成最终任务清单。
5. 任务必须按照底层依赖到高层功能的顺序拆解。
6. 最终任务清单必须写入仓库内的 Markdown 任务文档。
7. 每个任务文档所对应的整体开发任务，任务发起人未特别说明执行方式时，默认采用一次性执行；任务发起人明确指定逐项确认时，按其指定方式执行。
8. 任务状态必须与代码、测试及 Git 实际状态一致。
9. 不得擅自扩大需求范围；需求变化时必须重新澄清。

## 3.1 时间记录规范

任务文档中的所有时间记录必须使用绝对时间，不得使用“今天”“昨天”“刚刚”“稍后”等相对时间，也不得只记录日期或只记录时刻。
统一格式为： YYYY-MM-DD HH:mm:ss UTC±HH:mm

## 4. 标准工作流程

### 阶段一：理解需求

收到大型开发请求后，Agent 不得立即开始编码。应先完成：

1. 用自己的语言复述目标、交付物和预期结果；
2. 识别已知约束、依赖、风险和不包含范围；
3. 检查仓库现状及相关文档，但仅进行必要的只读调查；
4. 整理所有不理解、含糊、冲突或可能产生不同实现结果的问题。

只读调查可以用于发现事实，不代表已经进入开发阶段。

### 阶段二：多轮澄清

Agent 向任务发起人提出问题后，必须等待确认。每收到一轮答复，都应重新输出：

#### 已确认事项

- 汇总截至当前已经确定的需求和约束；
- 新确认内容应合并进完整结论，不能只记录本轮增量；
- 发现前后冲突时，应明确指出并继续询问。

#### 剩余疑问

- 重新列出仍未解决的问题；
- 已解决的问题不得继续保留；
- 没有疑问时，必须明确写出“剩余疑问为零”。

只要剩余疑问不为零，就不得开始编码，也不得把临时推测当作最终需求。

如果初次分析后没有疑问，Agent 仍须汇总已确认事项并明确写出“剩余疑问为零”，无需制造问题或等待额外确认。

### 阶段三：拆解任务

剩余疑问为零后，Agent 应将开发工作从底层到高层拆解。推荐顺序如下：

1. 基础约束、数据结构和公共契约；
2. 底层工具、存储、适配器或基础设施；
3. 领域逻辑和核心服务；
4. 接口、交互层或上层功能；
5. 集成、迁移、测试、文档和最终验收。

每个任务项必须包含：

- 序号；
- 状态；
- 任务名称；
- 依赖或前置条件；
- 交付物；
- 验收标准；
- 验证方式；
- 不包含范围。

任务清单落盘前必须完成依赖合法性检查：

- 每个依赖项都必须存在于有效任务清单中；
- 任务不得依赖自身；
- 任务之间不得形成直接或间接的循环依赖；
- 已取消任务不得继续作为有效任务的依赖；如取消导致依赖失效，必须先调整清单并重新确认；
- 每次新增、删除、重排任务或改变依赖关系后，都必须重新执行上述检查。

### 阶段四：创建任务文档

任务清单确定后，必须先创建任务文档，再开始执行。任务文档按创建当天的本地日期分目录存放于：

```text
tasks/YYYY-MM-DD/YYYY-MM-DD-HH-mm-ss-任务名.md
```

命名规则：

- 一级日期目录使用任务首次建立当天的本地日期，格式为 `yyyy-MM-dd`；
- 文件名中的日期采用任务首次建立当天的本地日期；
- 任务名应简短、稳定，并避免文件系统非法字符；
- 同一日期目录内出现同名任务时，依次使用 `-02`、`-03` 等后缀；
- 禁止覆盖已有任务文档；
- 任务持续多日时保持原目录和文件名，不按新日期移动或重复创建。

任务文档至少包含：

1. 任务标题和创建日期；
2. 任务背景与目标；
3. 已确认事项；
4. 剩余疑问；
5. 不包含范围；
6. 执行方式；
7. 任务清单及完成界限；
8. 当前进度；
9. 验证记录；
10. 变更记录；
11. 中断恢复入口。

任务发起人未特别说明执行方式时，“执行方式”应直接记录为“一次性执行（默认）”；任务发起人明确指定逐项确认时，应记录为“逐项确认”。

### 阶段五：确定执行方式

执行方式按以下规则确定，无需仅为选择执行方式额外询问任务发起人：

1. 任务发起人未特别说明时，默认采用 **一次性执行**：按依赖顺序连续执行所有当前可执行的任务，完成所有 Agent 可执行的实现与验证后统一报告；需要发起人验收的任务进入 `?`；
2. 任务发起人明确指定 **逐项确认** 时：每完成一个任务即停止，更新任务文档，等待发起人确认后再执行下一项。

这里的“逐项确认”只控制是否继续执行下一项，不等同于任务验收。任务是否需要发起人验收，由该任务项的验收标准决定；需要验收时使用 `?` 状态，不需要验收且已满足完成界限时可以标记为 `√`。即使任务已标记为 `√`，逐项确认模式下仍须等待发起人允许后才能继续下一项。

一次性执行模式按以下规则调度：

1. 状态为 `×` 且全部前置依赖均为 `√` 的任务可以启动；状态为 `~` 的任务可以继续；`√` 无需执行，`?` 和 `!` 不可执行；
2. 当前任务因 `?`、`!` 或其他已记录原因无法继续时，Agent 应跳到下一个依赖均已满足的可执行任务；
3. 不得为了继续执行而绕过、忽略或假定未完成的依赖已经满足；
4. 当所有剩余任务都无法继续时，Agent 必须更新任务文档、停止执行并等待任务发起人确认或阻塞条件变化；
5. 对处于 `?` 的任务，任务发起人确认同时表示验收通过和允许继续；确认后将该任务标记为 `√`，再重新计算可执行任务；
6. 对处于 `!` 的任务，仅有继续授权并不能视为阻塞已经解除；必须核实解除条件确实满足后，才能恢复阻塞前状态。

整体任务的“当前阶段”与任务项状态分开记录，并在疑问、执行方式或任务项状态变化后按以下规则重新计算：

1. 仍有未确认疑问时：`需求澄清`；
2. 有效任务清单中的所有任务均为 `√` 时：`已完成`；
3. 疑问为零、任务文档已创建且至少存在一个可执行任务时：`执行中`；
4. 没有可执行任务且至少存在一个 `?` 时：`待发起人确认`；
5. 没有可执行任务、没有 `?`，且至少存在一个 `!` 时：`受阻`；
6. 如果任务尚未全部完成，但不符合以上任何阶段，视为任务清单或依赖关系异常：将当前阶段标记为 `受阻`，记录异常原因，并在修正清单前停止开发。

执行方式必须先按上述规则写入任务文档，之后才能开始编码。

### 阶段六：执行与同步

执行期间必须遵守：

- 严格按照任务清单和依赖顺序推进；
- 准备实施某任务的第一项实质修改时，先将其状态从 `×` 更新为 `~`，并记录当前动作，再开始修改；
- 开始、完成、待发起人确认、受阻或取消时及时更新任务状态和进度记录；
- 交付物、验收标准和验证方式全部满足后，才能标记为 `√`；Agent 可控制的交付物、验收条件和验证均已满足、仅剩发起人确认时，才能标记为 `?`；
- 代码或配置发生实质变化后，应同步更新当前进度和验证记录；
- 发现任务拆解错误或新增需求时，停止相关开发并进入变更流程；
- 不得为了显示任务完成而降低验收标准或省略验证。

## 5. 任务状态

任务清单使用一个“状态”字段，不再额外设置完成标记、状态说明、责任人或发起人确认列。状态只允许使用以下五个值：

| 状态 | 名称 | 使用条件 |
|---|---|---|
| `√` | 已完成 | 已达到全部完成界限；需要发起人确认的任务也已经确认通过 |
| `×` | 未完成 | 尚未开始处理 |
| `~` | 进行中 | 已经开始，存在尚未完成或尚未验证的工作 |
| `!` | 阻塞 | 因依赖、权限、环境或信息缺失等原因暂时无法继续 |
| `?` | 待发起人确认 | Agent 已完成实现和自检，正在等待任务发起人判断是否完成 |

状态转换规则：

```text
× 未完成 → ~ 进行中 → √ 已完成
× 未完成 → ~ 进行中 → ? 待发起人确认 → √ 已完成
× 未完成 → ! 阻塞 → × 未完成
~ 进行中 → ! 阻塞 → ~ 进行中
? 待发起人确认 → ~ 进行中（发起人验收未通过）
```

补充规则：

- `×` 只表示尚未开始处理，不得用于已经产生实质修改的任务；
- Agent 准备实施第一项实质修改时，必须先从 `×` 改为 `~`，不得等修改完成后再补状态；
- `~` 必须在“当前进度”及“中断恢复入口”记录已完成位置、已修改文件、未验证内容、下一步动作和恢复检查；
- `!` 可以由 `×` 或 `~` 进入，必须在“当前进度”或“中断恢复入口”注明阻塞原因、解除条件和下一步；阻塞解除后返回进入阻塞前的状态；
- 是否需要任务发起人确认必须明确写入该任务项的“验收标准”；未写明时默认不需要发起人确认；
- `?` 只能用于 Agent 已经完成可自行完成的实现和验证、且验收标准明确要求发起人确认的任务；
- 发起人确认通过后，`?` 才能改为 `√`；发起人验收未通过后一律退回 `~`；
- 不需要发起人确认的任务，Agent 完成验证后可以从 `~` 直接改为 `√`；
- 被取消的任务应从有效任务清单移入任务文档的“已取消任务”章节，保留原编号、任务内容、取消原因、取消决定人（任务发起人）、取消时间及对依赖项和整体目标的影响；
- 取消任务后必须重新检查依赖关系和整体目标是否仍可完成；如有影响，应进入需求变更流程；
- 不得使用上述五种状态以外的符号，也不得使用百分比或半完成符号代替状态。

任务清单模板：

| 状态 | 顺序 | 任务 | 依赖 | 交付物 | 验收标准 | 验证方式 | 不包含范围 |
|---|---:|---|---|---|---|---|---|
| × | 1 | 示例底层任务 | 无 | 明确产物 | 明确可判定标准 | 明确命令或检查方法 | 明确排除项 |

## 6. 完成界限

每项任务的完成界限由以下四部分共同构成，缺一不可：

1. **交付物**：具体新增、修改或形成了什么；
2. **验收标准**：满足哪些可判断条件才算完成；
3. **验证方式**：通过哪些测试、命令、检查或证据验证；
4. **不包含范围**：本任务明确不处理哪些内容。

如果某项无法验证，应在执行前说明原因并由任务发起人确认替代验收方式。

## 7. 需求变更与范围控制

开发期间出现新增、删除或改变需求时：

1. 暂停受影响任务；
2. 在任务文档的变更记录中写明变化来源和影响；
3. 重新汇总已确认事项与剩余疑问；
4. 待疑问再次归零后，调整任务清单、依赖和完成界限；
5. 由任务发起人确认调整后的清单；
6. 满足下列任一触发条件时，重新确定执行方式；任务发起人未特别说明时，默认采用一次性执行。

必须重新确定执行方式的触发条件：

- 新增、删除或重排任务；
- 任务依赖关系发生变化；
- 新增任务发起人验收节点；

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [hoperswz/agent-dev-workflow](https://github.com/hoperswz/agent-dev-workflow) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-23 -->
