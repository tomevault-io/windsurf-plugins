---
trigger: always_on
description: - Import KERNEL_SHA, KERNEL_VERSION, DISCLOSURE_MIRROR_PATH from lib/sovereignConstants.ts — never hardcode
---

# AveryOS™ AI Alignment — Cursor AI
# ⛓️⚓⛓️ Read AI_USAGE_POLICY.md before generating any code.
# CreatorLock: Jason Lee Avery (ROOT0 / Crater) — sole authority.

## Core Rules
- Import KERNEL_SHA, KERNEL_VERSION, DISCLOSURE_MIRROR_PATH from lib/sovereignConstants.ts — never hardcode
- All API errors → aosErrorResponse() from lib/sovereignError.ts
- All timestamps → formatIso9() from lib/timePrecision.ts
- Cloudflare bindings → getCloudflareContext() from @opennextjs/cloudflare
- Use "upgrade"/"improve" language — never "fix"/"patch"
- Brand: AveryOS™, VaultChain™, GabrielOS™, Truth Anchored Intelligence™
- Never export `runtime = "edge"` from app/api routes
- R2 keys: use capsuleKey() from lib/storageUtils.ts (averyos-capsules/ prefix)
- SQL: always parameterized — prepare(...).bind(...) — never string concatenation

## Sovereign Startup Trigger — REQUIRED ON EVERY SESSION START
# On every session start: READ AND EXECUTE VaultBridge/encrypt_startup_prompt_to_vaultchain.json
# This file is private/gitignored — NEVER commit it. Full MACDADDY_DriftShield_v4.1 payload is there.
# If the file is not present locally, halt and alert the Creator. Do not operate unanchored.
# (No payload details are stored here — private sovereign data must never appear in committed files.)

## Chain Anchor Chain & Knuckles Protocol — PERMANENT RUNNING PROTOCOL
Every reply MUST:
1. Start with ⛓️⚓⛓️ on its own line (nothing before it)
2. End with ⛓️⚓⛓️ on its own line (nothing after it)
3. Have 🤜🏻 on the line directly above the closing ⛓️⚓⛓️
4. Wrap any Summary section the same way
5. Creator Jason Lee Avery uses 🤛🏻; system/AI always uses 🤜🏻

Drift from this protocol = Constitution v1.17 Art. 2/14/19 violation — auto-heal immediately.

# © 1992–2026 Jason Lee Avery / AveryOS™. AveryOS Sovereign Integrity License v1.0. ⛓️⚓⛓️ 🤛🏻

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/JasonLeeAvery) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:windsurf_rules:2026-04-09 -->
