---
trigger: always_on
description: Read `AGENTS.md` now. It is this repository's single source of truth — available MCP tools, authorization policy, audit methodology, session system, sub-agent architecture, slash commands, and the knowledge base — and it applies to Claude Code exactly as written, including the `Agent`/`Task` sub-agent tool and `AskUserQuestion` mechanics it describes natively. This file has no separate copy of that content, so there is nothing here to fall out of sync with it.
---

# Claude Code — Kali MCP

Read `AGENTS.md` now. It is this repository's single source of truth — available MCP tools, authorization policy, audit methodology, session system, sub-agent architecture, slash commands, and the knowledge base — and it applies to Claude Code exactly as written, including the `Agent`/`Task` sub-agent tool and `AskUserQuestion` mechanics it describes natively. This file has no separate copy of that content, so there is nothing here to fall out of sync with it.

---
> Source: [pabpereza/kali-mcp](https://github.com/pabpereza/kali-mcp) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-24 -->
