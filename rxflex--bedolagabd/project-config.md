---
trigger: always_on
description: [⌂ База знаний](README.md) · [🗺 Карта разделов](MAP.md) · [📖 Термины](indexes/термины.md)
---

# 🤖 AGENTS.md — карта базы для ИИ-агента

[⌂ База знаний](README.md) · [🗺 Карта разделов](MAP.md) · [📖 Термины](indexes/термины.md)

Это справочник-архив по стеку **Remnawave + Bedolaga + Xray** и обходу блокировок в РФ, собранный из Telegram-чата за 23.08.2025 – 23.08.2026. Только markdown, никакого кода проекта: репозиторий не собирается и не запускается.

## Что тут есть

| Где | Что | Правится руками? |
|---|---|---|
| `docs/01..08/**.md` | 108 документов со знанием | **да** — это единственный источник истины |
| `docs/*/README.md` | хаб раздела: таблица документов + разделы | нет, `build_nav.py` |
| `MAP.md` | все разделы всех документов | нет, `build_nav.py` |
| `indexes/`: релизы, ошибки, env, ссылки | сводные индексы | нет, `build_indexes.py` |
| `indexes/термины.md` | глоссарий | **да** |
| `source/notes/note_001..172.md` | заметки-первоисточники с дословными цитатами | нет, `build_source.py` |
| `assets/**` | SVG-баннеры и схемы | нет, `build_assets.py` / `build_timeline.py` |
| `_tools/data/threads.tsv` | id сообщения → форум-тред для пруф-ссылок | нет, `link_proofs.py` |
| `_tools/kb_lib.py` | спека структуры: разделы, файлы, названия, цвета | **да**, если меняется дерево |

Служебные блоки внутри документов ограничены маркерами `<!-- KB:NAV -->`, `<!-- KB:TOC -->`, `<!-- KB:FOOT -->` — их содержимое перезаписывается генератором, править бессмысленно.

## Соглашения в тексте

- `[id=123456](https://t.me/c/2941121338/123456)` — пруф: номер сообщения в чате и ссылка на него (6642 штуки).
  В таблицах пайпы внутри метки экранированы (`id=123\|автор\|дата`). Часть ссылок трёхсоставные — с id форум-треда.
  Если нужен только номер — регексп `\[id=([^\]]+)\]` по-прежнему работает.
- `note_045` — ссылка на первоисточник `source/notes/note_045.md`.
- Даты — только `ДД.ММ.ГГГГ`. Период документа/раздела пишется в заголовке (`## Период 16.05–05.06.2026 …`).
- Конфиги и команды приводятся **дословно**; пометка «дословно» означает «скопировано из сообщения без правок».
- Устаревшее не удаляется, а датируется: в теме обхода блокировок любой рецепт живёт 1–3 месяца.
- Противоречия оставлены обеими версиями — если в ответе используется спорный факт, стоит упомянуть обе.

## Как отвечать на типовые вопросы за один-два вызова

| Вопрос | Куда идти |
|---|---|
| «что делает переменная X» | `indexes/env.md`, затем ссылка на раздел |
| «в какой версии появилось Y» | `indexes/релизы.md` (222 записи, сгруппированы по продукту) |
| «падает с ошибкой Z» | `indexes/ошибки.md` (257 симптомов → фикс) |
| «что было в такую-то дату» | `docs/08-хронология/ГГГГ-ММ.md` |
| «какой транспорт сейчас живой» | `docs/03-обход-тспу/итоги.md` + `docs/02-транспорты/вехи.md` |
| «какой хостер выбрать» | `docs/05-хостинги/правила-выбора.md`, затем `рф-хостеры.md` / `зарубежные-хостеры.md` |
| «как настроить платёжку» | `docs/06-платёжки/env-вебхуки.md` + `профили-платёжек.md` |
| «дай рабочий конфиг для …» | `docs/02-транспорты/<протокол>.md` (в каждом — дословные инбаунды) |
| «как поднять/обновить бота» | `docs/01-панели/установка-обновление.md` + `docker-compose.md` |

Полезные грепы:

```bash
grep -rn "REMNAWAVE_AUTH_TYPE" docs/          # где описана переменная
grep -rln "hysteria" docs/                    # какие документы вообще про Hysteria2
grep -rn "^| 16.06.2026" docs/08-хронология/  # события конкретной даты
grep -rn "id=698034" docs/ source/            # факт и его первоисточник
rg -n "Ошибки → фиксы" docs/                  # все разделы с разборами поломок
```

## Чего не делать

- Не изобретать факты и не «дополнять» из общих знаний: всё в базе привязано к пруфу. Нет пруфа — нет записи.
- Не подавать рецепты как актуальные без даты: рядом с методом всегда указывать, на какой момент он работал.
- Не править сгенерированные файлы (`MAP.md`, `indexes/*` кроме `термины.md`, `docs/*/README.md`, `assets/*`) — правки затрутся; менять источник и перегенерировать:

```bash
python _tools/build_nav.py && python _tools/build_indexes.py && python _tools/check_links.py
```

- Не добавлять новый документ мимо спеки: сначала запись в `SECTIONS` в `_tools/kb_lib.py` (файл, человеческое название, описание), потом сам файл, потом `build_nav.py`.

## Контекст темы (коротко, чтобы не гадать)

- **Remnawave** — панель, **Bedolaga** — Telegram-бот-магазин к ней, **Cabinet** — веб-ЛК. Ноды — серверы с Xray.
- Год прошёл под ТСПУ: Reality (основа 2025) к июню 2026 выбита, главный рабочий транспорт — **XHTTP + selfsni**, запас — **Hysteria2**, лечение шейпинга — **мост RU→EU**.
- Деньги — «серые» платёжки с комиссией 3–12 %; главная боль — вебхуки и двойные зачисления.
- Хостинг — расходник: подсети банят, аккаунты CDN сносят, серверы изымают; правило чата — не держать всё у одного провайдера.

Если нужен обзор базы «человеческими словами» — есть внешний индекс: <https://deepwiki.com/Rxflex/BedolagaBD> (ИИ-агент прочитал репозиторий и отвечает со ссылками на документы). Первоисточник всё равно здесь, в `docs/`.

Расшифровка жаргона (сквад, сабка, БС, selfsteal, ТСПУ, рекуррент): [indexes/термины.md](indexes/термины.md).

---
> Source: [Rxflex/BedolagaBD](https://github.com/Rxflex/BedolagaBD) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-19 -->
