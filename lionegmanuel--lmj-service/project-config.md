---
trigger: always_on
description: Repo de una sola página estática (`index.html`): propuesta comercial de Velinex para el cliente LMJ Tecnología (Villa María). No es una app — es un documento de venta en HTML/CSS con JS inline, sin build ni dependencias (no hay `package.json`). Assets en `img/` (logo Velinex). Editar `index.html` directo con `Edit`, sin introducir tooling nuevo (bundler, framework) salvo pedido explícito.
---

# CLAUDE.md — lmj_service

## Contexto del proyecto

Repo de una sola página estática (`index.html`): propuesta comercial de Velinex para el cliente LMJ Tecnología (Villa María). No es una app — es un documento de venta en HTML/CSS con JS inline, sin build ni dependencias (no hay `package.json`). Assets en `img/` (logo Velinex). Editar `index.html` directo con `Edit`, sin introducir tooling nuevo (bundler, framework) salvo pedido explícito.

## Contexto de sesión — UPDATES.md

- Al iniciar cualquier conversación nueva, revisar `UPDATES.md` (raíz del repo) si existe, para confirmar contexto de la última sesión trabajada antes de responder o actuar.
- Nunca escribir ni actualizar `UPDATES.md` salvo que el usuario lo pida explícitamente.
- Retención por días, no por cantidad de sesiones: mantener las últimas 3 secciones de días. Al agregar un día nuevo que supere ese total, eliminar el día más antiguo completo.
- Dentro de un mismo día no hay límite de sesiones — cada sesión trabajada ese día se documenta, nunca se trunca por volumen.
- Cada sesión dentro de un día lleva su propio título corto.
- Sesiones del mismo día separadas por `---`.
- Orden cronológico dentro del día: de la más antigua a la más reciente.

---
> Source: [lionegmanuel/lmj_service](https://github.com/lionegmanuel/lmj_service) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-12 -->
