---
trigger: always_on
description: - Current version lives in `version.go` (`const Version`).
---

# Launch — project instructions

## Versioning
- Current version lives in `version.go` (`const Version`).
- Update `CHANGELOG.md` with a new entry each session where user-facing changes are made. Only include user-experience changes (new features, bug fixes, behavior changes visible to the user). Do not include internal refactoring, code reorganization, or technical implementation details.
- If the session introduces changes that warrant a version bump, ask the user which version to use before writing it.

---
> Source: [adamarutyunov/launch](https://github.com/adamarutyunov/launch) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-04 -->
