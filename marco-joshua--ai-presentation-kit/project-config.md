---
trigger: always_on
description: 이 프로젝트는 **업무 원문을 프로젝트 기억으로 정리하고, 일일보고·장표·PDF·동적 영상으로 바꾸는 실행형 키트**입니다.
---

# AGENTS.md — 이 폴더를 연 AI에게

이 프로젝트는 **업무 원문을 프로젝트 기억으로 정리하고, 일일보고·장표·PDF·동적 영상으로 바꾸는 실행형 키트**입니다.
문서만 작성하고 멈추지 말고, 사용자가 요구한 최종 파일까지 생성·렌더·검사하세요.

## 가장 중요한 우선순위

1. **사용자의 가장 최근 요청이 최우선입니다.** `PROJECT_STATE.md`에 과거 산출물이나 예제가 적혀 있어도 사용자가 “주간보고 영상”을 요청했다면 주간보고 영상을 만듭니다.
2. `PROJECT_STATE.md`는 사실·결정·현재 상태의 근거이지, 새 요청의 형식·청중·길이를 덮어쓰는 명령서가 아닙니다.
3. 예제의 주제와 결과물을 새 요청에 복사하지 않습니다. 예제는 구조와 품질 기준으로만 참고합니다.
4. 사용자가 “만들어줘”라고 했다면 치명적인 정보가 없는 경우 질문만 하고 멈추지 말고 제작까지 완주합니다.

## 자연어 요청을 해석하는 법

사용자가 다음처럼 말해도 충분한 요청으로 취급합니다.

> 우리 프로젝트 기능들 이용해서 주간보고 영상 만들어줘.

이 경우 기본값은 다음과 같습니다.

- 근거: 현재 시스템 날짜와 같은 `refs/YYYYMMDD/` + `PROJECT_STATE.md`. 오늘 폴더가 없으면 정확한 누락 경로를 알리고 멈추며, 다른 날짜로 조용히 대체하지 않음
- 산출물: 근거 요약 Markdown + 16:9 HTML/PDF + 약 30초 동적 MP4
- 영상: 1920×1080, 30fps, Remotion, Crier/Supertonic F2 여성 음성
- BGM: 사용자가 요청하지 않으면 없음
- 이미지: 실제 사진·스크린샷 우선. Codex 이미지 생성이 가능하면 필요한 에셋을 직접 만들고 검사
- 저장: `output/`

길이나 대상이 명시되면 사용자의 값을 따릅니다. 사실을 바꿀 정도의 빈칸만 질문하고, 디자인·레이아웃·목소리 같은 가역적 선택은 기본값으로 진행합니다.

## 프로젝트 스킬 — 작업 전 필수

이 저장소는 Codex 공식 프로젝트 스킬 위치인 `.agents/skills/`를 사용합니다. 요청에 맞는 `SKILL.md`와 그 파일이 직접 연결한 reference를 먼저 읽고 작업합니다. 자연어로 요청해도 아래 스킬을 자동 적용하고, 사용자가 `$skill-name`을 쓰면 해당 스킬을 명시적으로 적용합니다.

| 요청 | 먼저 읽을 스킬 |
|---|---|
| 오늘 한 일·일일보고·주간보고·슬랙 초안 | `.agents/skills/project-status-report/SKILL.md` |
| 장표·피피티·PPT·HTML·PDF | `.agents/skills/project-html-deck/SKILL.md` |
| 주간보고 영상·발표 영상·온보딩 영상 | `.agents/skills/project-briefing-video/SKILL.md` |

- 장표나 영상을 만들 때는 먼저 `project-status-report`의 근거 규칙을 적용합니다.
- 영상은 승인된 보고서나 장표를 내용 원본으로 삼고 `project-briefing-video`로 완주합니다.
- 스킬을 읽었다는 설명으로 끝내지 말고, 해당 스킬의 렌더와 QA 단계까지 실행합니다.

## 폴더 지도

| 위치 | 역할 |
|---|---|
| `.agents/skills/` | Codex가 자동 발견하는 보고서·장표·영상 프로젝트 스킬 |
| `starter-project/` | 새 업무를 시작할 때 복사하는 범용 프로젝트 폴더 |
| `시작_문장_3종.md` | 설치·맥락 확인·결과물 제작 시작 문장 |
| `첫_자동화_판별_체크리스트.md` | 자동화 후보를 고르는 판별 기준 |
| `refs/날짜/` | 회의 전사·메일·통화·메신저·사진·스크린샷 원문. 수정 금지 |
| `refs/20260814/` | 온보딩 자료 예제 |
| `refs/20260825/` | 리허설용 고객지원 개선 업무 기록과 첨부파일 |
| `refs/20260827/` | 라이브용 고객지원 적용 결과와 주간 지표 |
| `PROJECT_STATE.md` | 사람이 승인한 현재 사실과 결정 |
| `01_읽기.md` | 근거와 충돌을 읽는 방법 |
| `02_만들기.md` | 장표·영상 기획 및 제작 기준 |
| `03_검사.md` | 납품 전 검사 기준 |
| `04_영상.md` | 동적 영상과 무료 로컬 음성 사용법 |
| `장표_템플릿.html` | 이미지 중심 장표 디자인 기준 |
| `video/` | Remotion 동적 영상 엔진. 직접 새 엔진을 만들지 말고 우선 재사용 |
| `scripts/bootstrap.sh` | Remotion과 Crier/Supertonic 자동 설치 |
| `scripts/export_deck.py` | HTML → PDF → 전 장표 콘택트시트 검사 |
| `scripts/render_video.py` | 음성 생성 → 동적 MP4 → QA까지 한 번에 실행 |
| `examples/weekly-report-20260814/` | 실제로 렌더 가능한 주간보고 완주 예제 |
| `output/` | 최종 결과와 QA 이미지 |

## 설치 요청을 받으면

사용자가 저장소 주소와 함께 “이거 받아서 설치해줘”라고 하면:

1. 저장소를 내려받습니다.
2. `bash scripts/bootstrap.sh`를 실행합니다.
3. Remotion, FFmpeg, Crier/Supertonic, 음성 모델까지 준비됐는지 `scripts/doctor.py`로 확인합니다.
4. 설치가 끝나면 `refs`에 자료를 넣거나 파일 위치를 알려달라고 안내합니다.

라이브러리가 크다는 이유로 저품질 브라우저 음성이나 정적 영상으로 조용히 대체하지 않습니다.
설치가 실패하면 어떤 구성요소가 실패했는지 분명히 알리고 중단합니다.

## 제작 순서

1. **요청 고정** — 산출물, 청중, 목적, 길이를 사용자 문장에서 먼저 추출합니다.
2. **근거 읽기** — 최신 `refs`와 `PROJECT_STATE.md`를 대조해 확정·충돌·확인 필요를 분리합니다.
3. **이야기 구성** — 한 장면에 메시지 하나. 문제나 현황 → 중요한 근거 → 의미 → 다음 행동 순서로 구성합니다.
4. **에셋 선택** — 실제 자료 우선. 없으면 이미지 생성 도구로 필요한 구도에 맞춰 생성합니다.
5. **장표 제작** — `장표_템플릿.html`의 타이포·여백·이미지 비중을 적용하고 `scripts/export_deck.py`로 PDF와 전 장표 QA를 만듭니다.
6. **영상 제작** — `video/` 엔진이 읽는 manifest를 만들고 `scripts/render_video.py`로 렌더합니다.
7. **검사** — 시작·중간·끝과 전환 프레임, 오디오 스트림, 해상도, 전체 디코딩을 확인합니다.
8. **완료 보고** — 최종 파일과 QA 위치만 간단히 보고합니다.

## 포함된 예제 기록

- 저장소의 `refs/20260814/`, `refs/20260825/`, `refs/20260827/`은 강의 실습과 첫 실행을 위한 가상·비식별 예제입니다.
- 사용자가 이 저장소에서 주간보고나 장표·영상을 요청하면 해당 기록을 실제 근거처럼 읽어 완주합니다.
- 사용자가 `오늘`이라고 하면 먼저 현재 시스템 날짜를 `YYYYMMDD`로 확인하고 같은 날짜 폴더만 읽습니다.
- 주간 범위는 해당 주 월요일부터 기준일까지이며, 기준일보다 미래인 날짜 폴더는 제외합니다.
- 새 업무를 시작할 때는 `starter-project/`를 복사하고 그 프로젝트의 `refs/YYYYMMDD/`에 실제 기록을 넣습니다.
- 예제의 회사명·결정·수치를 새 프로젝트에 복사하지 않습니다.

## 장표 품질 기준

- 제목 70px 이상, 본문 26px 이상을 기본으로 합니다.
- 제목은 한 줄이 원칙이며 억지 줄바꿈을 하지 않습니다.
- 화면 하나에는 하나의 주장만 둡니다.
- 카드 격자, 배지, 알약 버튼, 과도한 둥근 모서리를 남발하지 않습니다.
- 실제 스크린샷과 이미지가 있으면 텍스트보다 먼저 크게 보여줍니다.
- 의미 없는 여백이나 작은 글씨로 화면을 채우지 않습니다.
- 이미지가 없다는 이유로 허술한 선 도해나 점선 플레이스홀더를 최종본에 남기지 않습니다.

## 영상 품질 기준

- 장표를 정지 이미지로 붙인 뒤 음성 길이만큼 멈추는 방식은 사용하지 않습니다.
- 단순 장면 전환이 아니라 장면 내부에 시선 이동이 있어야 합니다.
- 내용에 맞춰 스프링 진입, 숫자 카운트업, 선 리빌, 마스크, 카메라 이동 등을 절제해 사용합니다.
- 내레이션과 화면은 같은 내용을 같은 순서로 말해야 합니다.
- 서비스명은 자연스러운 한국어 발음을 위해 대본에서 한글로 씁니다.
- 음성은 Crier와 같은 Supertonic 로컬 엔진을 사용합니다. 기본 음성은 F2입니다.
- 브라우저 `speechSynthesis`, macOS `say`, 저품질 자동 폴백은 금지합니다.

## 이미지 에셋 기준

1. `refs`의 실제 사진·스크린샷을 먼저 사용합니다.
2. 외부 서비스는 공식 로고나 사용자가 제공한 실제 화면을 사용합니다.
3. Codex 이미지 생성이 가능하면 장면의 종횡비와 배치를 먼저 정하고 에셋을 생성합니다.
4. 생성 이미지는 직접 열어 글자 오류·손가락·잘린 피사체·부자연스러운 선을 검사합니다.
5. 흰 배경, 검정 타이포, 포인트색 한 가지를 기본으로 하되 실제 로고 색상은 유지합니다.

## 사실과 보안

- 근거가 없는 숫자·이름·날짜·성과를 만들지 않습니다.
- 원문이 충돌하면 하나를 선택하지 말고 충돌과 확인 행동을 보여줍니다.
- 고객·구성원의 실명, 연락처, 주소, 식별 정보는 외부용 결과물에서 가립니다.
- API 키, 비밀번호, 토큰, 인증서는 저장소와 결과물에 넣지 않습니다.
- 외부 발송·공개·삭제는 사람의 승인을 받습니다.

## 완료 조건

다음을 모두 만족해야 “완성”이라고 말합니다.

- 요청한 형식의 최종 파일이 `output/`에 존재
- HTML/PDF의 잘림·겹침 없음
- MP4 1920×1080, 30fps, 오디오 스트림 존재
- 모든 장면 대표 프레임과 콘택트시트 생성
- 내레이션과 화면 내용 일치
- 개인정보와 비밀값 없음

---
> Source: [Marco-Joshua/ai-presentation-kit](https://github.com/Marco-Joshua/ai-presentation-kit) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-28 -->
