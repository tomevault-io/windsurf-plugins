---
trigger: always_on
description: Python package extending sympy expressions with two methods for engineering calculations:
---

# SymEval — Agent Guide

## What it is

Python package extending sympy expressions with two methods for engineering calculations:

- `.sym_evalf()` — evaluates with `pint` quantities and renders the symbolic evaluation: symbolic form → substituted form → result. Returns a `SymbolicEvaluation` with `_repr_latex_`. See `CONTEXT.md` for the vocabulary (symbolic evaluation, symbolic/substituted form, result, chaining).
- `.quantity_evalf()` — same unit-aware evaluation, no LaTeX overhead. Useful inside `DataFrame.with_columns(...)` to compute a new column row-by-row. Returns a `pint.Quantity`.

Built on sympy + pint. Rendering via `_repr_latex_()` so it works in both marimo and Jupyter without a marimo runtime dependency.

## Source layout

Notebook-as-source using mobuild (like koaning/hastyplot):

- **`symeval_mo.py`** — marimo notebook; single source of truth for all library code, tests, and feature examples. Cells marked `## EXPORT` get extracted into the package.
- **`src/symeval/__init__.py`** — auto-generated by mobuild. **Do not edit.**
- **`examples/`** — standalone marimo demo notebooks. `getting_started.py` is generated from `symeval_mo.py` (do not edit); the rest are hand-authored.
- **`docs/`** — Quarto docs website; pages are generated from the notebooks. See `CONTRIBUTING.md`. `docs/public/` holds the README GIFs (copied verbatim to the site).
- **`README.md`** — generated from the docs by `uv run task docs_readme` (**do not edit**); `README.template.md` is the editable frame.
- **`scripts/`** — the generators behind those tasks: `extract_getting_started.py` (notebook → `examples/getting_started.py`), `examples_to_qmd.py` (notebooks → `docs/*.qmd`), `docs_to_readme.py` (docs → `README.md`).
- **`media/`** — Node tooling (Playwright + node-canvas) that records the README GIFs into `docs/public/`; see `media/README.md`.

## Conventions

- All library code and tests live in `symeval_mo.py`. Test functions defined inside cells (`def test_*`) are discovered by pytest running against the notebook.
- Use `## EXPORT` markers for cells that should be extracted into the package.
- No `__version__` attribute at runtime — use `importlib.metadata.version("symeval")`.
- Units use `pint`, not `sympy.physics.units`.
- No bundled `UnitRegistry` — derived from user's pint quantities (`q._REGISTRY`). Empty-subs falls back to `pint.get_application_registry()`. Users create quantities with `pint.Quantity` (or their own `UnitRegistry`); the result lands in their registry.
- Rendering via `_repr_latex_()` — works in marimo and Jupyter without a marimo runtime dependency.
- Keep the package minimal.

### Release workflow

See `RELEASING.md` for full details. Tests run inside `release` (before tagging), not in `pypi`. The tag is the trust boundary — once a tag exists, the code is tested. Version lives only in `pyproject.toml` (no `__version__` attribute).

## Commands

Tasks run via taskipy (a dev dependency). List them, with their command bodies:

```bash
uv run task -l               # all tasks (source: [tool.taskipy.tasks] in pyproject.toml)
```

Setup and gotchas the task bodies do not spell out:

- `build` runs a `pre_build` hook first: it executes the whole notebook (`marimo export session`) as an end-to-end smoke test before `mobuild` extracts the package.
- `docs_build` is what CI runs; `docs_readme` (which regenerates `README.md`) is deliberately **not** in CI — run it locally after changing the docs.

`CONTRIBUTING.md` has the full contributor workflow.

## TODO

@TODO.md

---
> Source: [bedrock-engineer/symeval](https://github.com/bedrock-engineer/symeval) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-13 -->
