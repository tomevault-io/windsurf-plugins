---
trigger: always_on
description: Go + SQLite single binary (`wppackages`) for CLI + HTTP. Server-rendered templates + Tailwind. Composer metadata served from R2/CDN. See `docs/` for details.
---

# WP Packages

Go + SQLite single binary (`wppackages`) for CLI + HTTP. Server-rendered templates + Tailwind. Composer metadata served from R2/CDN. See `docs/` for details.

## Dev

- `make dev` — build + bootstrap + serve
- `make test` — run all tests
- Templates are embedded (`//go:embed`), requires rebuild to see changes

---
> Source: [roots/wp-packages](https://github.com/roots/wp-packages) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-04-20 -->
