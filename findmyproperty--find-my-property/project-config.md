---
trigger: always_on
description: This repo keeps **project-scoped** skills under `.agents/skills/<name>/SKILL.md` (not `.cursor/skills/`). When a task matches a skill’s scope, read that `SKILL.md` before implementing. Each skill includes a **“This repository: find-my-property”** section so generic upstream guidance matches **pnpm**, `app/`, `views/`, `next.config.mjs`, shadcn **base-nova**, and current tooling.
---

## Project agent skills

This repo keeps **project-scoped** skills under `.agents/skills/<name>/SKILL.md` (not `.cursor/skills/`). When a task matches a skill’s scope, read that `SKILL.md` before implementing. Each skill includes a **“This repository: find-my-property”** section so generic upstream guidance matches **pnpm**, `app/`, `views/`, `next.config.mjs`, shadcn **base-nova**, and current tooling.

| Skill | Path | Use when |
|-------|------|----------|
| **shadcn** | `.agents/skills/shadcn/SKILL.md` | shadcn/ui, `components.json`, registries, CLI (`shadcn add`, presets) |
| **nextjs-seo** | `.agents/skills/nextjs-seo/SKILL.md` | Next.js–aligned SEO: robots/sitemap, Metadata API, OG, headings, images, JSON-LD, technical + on-page |
| **frontend-design** | `.agents/skills/frontend-design/SKILL.md` | Distinctive UI, pages, components—avoid generic “AI” styling |
| **vercel-react-best-practices** | `.agents/skills/vercel-react-best-practices/SKILL.md` | React/Next.js performance, data fetching, bundles, refactors |


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [findmyproperty/find-my-property](https://github.com/findmyproperty/find-my-property) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-08 -->
