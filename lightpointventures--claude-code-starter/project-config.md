---
trigger: always_on
description: <!-- 这个文件告诉 Claude Code 如何在你的项目中工作。
---

# 项目规则

<!-- 这个文件告诉 Claude Code 如何在你的项目中工作。
     带 {{}} 的地方需要替换成你的项目信息。
     你可以运行 /setup 让 Claude 自动帮你填写。 -->

## 回复语言与风格

<!-- 控制 Claude 的回复方式 -->

- 始终用中文回复
- 回答要简洁直接，不要废话
- 不要在回复中使用 emoji
- 先做事，再解释（如果需要的话）

## 项目基本信息

<!-- 告诉 Claude 你的项目是什么，这样它才能更好地理解上下文 -->

- 项目名称：{{project_name}}
- 技术栈：{{tech_stack}}
- 项目描述：{{project_description}}
- 源代码目录：{{source_dir}}
- 测试目录：{{test_dir}}
- 包管理器：{{package_manager}}
- Lint 命令：{{lint_command}}
- 测试命令：{{test_command}}
- 运行环境：{{environment}} <!-- 例如：本地 macOS、Docker、AWS EC2 等 -->

## 术语表

<!-- 项目中容易混淆的术语，写在这里防止 Claude 理解错。
     格式：术语 = 含义。 -->

- （示例）dashboard = 管理后台，不是数据分析面板
- （示例）deploy = 部署到 Vercel，不是 Docker
- （在这里添加你项目中容易混淆的术语）

## 思维方式

<!-- 让 Claude 用更高质量的方式思考问题，而不是无脑执行 -->

### 第一性原理
- 遇到问题时，从最根本的事实出发思考，不要照搬"别人都是这么做的"
- 每个技术决策都要问：为什么要这样做？有没有更简单的方式？
- 如果一个方案唯一的理由是"大家都这么用"，重新考虑

### KISS 原则
- Keep It Simple, Stupid — 永远选择最简单的方案
- 能用 10 行代码解决的，不要写 50 行
- 能用一个函数解决的，不要拆成三个类

### 80/20 法则
- 先完成最重要的 20% 功能，解决 80% 的问题
- 不要一开始就追求完美，先让它跑起来，再优化
- MVP 优先：能用的原型 > 完美的设计稿

### 费曼学习法
- 当我问你某个概念或技术时，用最简单的语言解释，就像在教一个完全不懂的人
- 避免术语堆砌，多用类比和例子
- 如果你不能简单地解释它，说明你还没有真正理解它

## 代码规范

<!-- 统一代码风格，避免 Claude 每次写出不同风格的代码 -->

### 命名约定
- 变量和函数：使用项目已有的命名风格（如 camelCase 或 snake_case）
- 文件命名：与项目现有文件保持一致
- 如果项目还没有明确风格，优先使用语言社区的通用规范

### Commit Message 格式
- 格式：`type: 简短描述`
- type 可选值：feat（新功能）、fix（修复）、docs（文档）、refactor（重构）、test（测试）、chore（杂项）
- 示例：`feat: 添加用户登录功能`、`fix: 修复列表排序错误`
- 不要在 commit message 中包含敏感信息（密钥、密码、内部 URL）

### 分支策略
- 主分支：main
- 新功能或修复：从 main 创建 feature/xxx 或 fix/xxx 分支
- 完成后合并回 main
- 未经确认不要删除任何分支

## 铁律

<!-- 铁律 = 绝对不能违反的规则。违反任何一条都可能导致数据丢失、安全事故或生产故障。
     普通规则可以灵活处理，铁律不行。 -->

### 铁律：不要提交敏感文件
- 绝对不要把 .env、API Key、密码、token 提交到 git
- 如果发现 staged 文件中包含敏感信息，立即警告并移除
- 确保 .gitignore 覆盖了所有敏感文件类型

### 铁律：Push 前安全检查
- 每次 push 前，扫描代码中是否有硬编码的密钥、密码、token
- 检查是否有调试日志或临时文件被意外提交
- 发现问题必须先修复，再 push

### 铁律：禁止未经确认的破坏性操作
- 未经我明确同意，不要执行以下操作：
  - `rm -rf`（递归删除）
  - `git push --force`（强制推送）
  - `git reset --hard`（丢弃所有更改）
  - `git checkout .`（丢弃未提交的更改）
  - 删除分支或数据库表
  - 删除或覆盖用户数据文件

### 铁律：测试安全
<!-- 防止测试跑飞消耗大量资源 -->
- 运行测试时始终加 timeout 参数，防止死循环或卡住
- 不要在后台静默运行完整测试套件
- 如果测试失败，先分析原因再重跑，不要盲目重试
- 测试中不要连接生产环境的数据库或 API

## 依赖管理

<!-- 装错包、删错包是最常见的事故来源之一 -->

- 包管理器：{{package_manager}}
- 添加新依赖前先问我，说明为什么需要这个包
- 安装依赖后检查是否有不相关的包被意外删除或降级
- 锁文件（package-lock.json / uv.lock / poetry.lock）的变更要仔细检查
- 如果项目用 monorepo，确认依赖装到了正确的 workspace

## 工作流程

<!-- 确保 Claude 按正确的顺序做事，减少出错 -->

### 改代码前先读代码
- 修改任何文件之前，先读取并理解现有代码
- 不要对没读过的代码提出修改建议
- 理解上下文后再动手

### 编辑后先检查再提交
- 每次修改代码后，运行 {{lint_command}} 检查代码规范
- 运行 {{test_command}} 确保没有破坏现有功能
- 多文件改动时，做一次 self-review 再提交

### 保持简单
- 只做被要求的事情，不要过度设计
- 不要添加没有要求的功能、注释或重构
- 如果一个简单的方案能解决问题，不要用复杂的方案

### 日期和时间
- 提到日期、星期、时间之前，先运行 `date` 确认，不要凭记忆猜测

## 不要做什么

<!-- 明确告诉 Claude 哪些事情不要做，比禁止更有效。
     根据你的使用经验不断补充。 -->

- 不要在没有读过代码的情况下建议修改
- 不要一次改太多文件，改完一批先提交再继续
- 不要自作主张添加新依赖，先问我
- 不要在代码里写中文注释（除非我要求）
- 不要重复解释你做了什么，我能看到 diff
- 不要创建 README 或文档文件，除非我明确要求
- 不要修改不相关的代码（"顺便优化"）
- （在这里添加你不希望 Claude 做的事情）

## 决策日志

<!-- 重要的技术决策记在这里，避免以后反复讨论同一个问题。
     格式：决策 + 原因 + 被否决的方案。 -->

- （示例）选择 SQLite 而不是 PostgreSQL — 原因：单用户项目，不需要并发；否决 PostgreSQL 因为运维成本高
- （在这里记录你的技术决策）

## 已知陷阱

<!-- 把项目中踩过的坑记在这里，Claude 下次就不会再犯同样的错误。
     格式：简短描述问题 + 正确的做法。 -->

- 示例：修改 .env.example 后要同步更新文档中的环境变量说明
- 示例：数据库迁移文件不要手动修改，用框架命令生成
- 示例：缓存文件可能导致新改动不生效 — 改完配置后清一下缓存再测试
- （在这里添加你项目的已知陷阱）

---
> Source: [lightpointventures/claude-code-starter](https://github.com/lightpointventures/claude-code-starter) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-04 -->
