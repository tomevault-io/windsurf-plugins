---
trigger: always_on
description: Whenever you create or edit code, make sure that the code is warning- and error-free: `just check-all`. If there are format errors, format the code with `just fmt`
---

Whenever you create or edit code, make sure that the code is warning- and error-free: `just check-all`. If there are format errors, format the code with `just fmt`

Whenever you add dependencies, always ensure that there are no outdated dependencies: `cargo outdated`. If there are outdated dependencies, update them with `cargo upgrade`.

---
> Source: [rust-linz/rust-fundamentals-training](https://github.com/rust-linz/rust-fundamentals-training) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-23 -->
