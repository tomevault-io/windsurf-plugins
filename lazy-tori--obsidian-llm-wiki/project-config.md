---
trigger: always_on
description: 사용자의 **시도 → 경험 → 배움 → 산출물**을 기록하고 연결하는 개인 지식베이스.
---

# 세컨드 브레인 위키

사용자의 **시도 → 경험 → 배움 → 산출물**을 기록하고 연결하는 개인 지식베이스.
[LLM Wiki 패턴](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)을 따른다.
상세 규약은 `docs/wiki-conventions.md`.

## 역할 분담

- **사용자**: 소스를 가져오고, 경험을 이야기하고, 질문하고, 방향을 정한다
- **LLM(나)**: `wiki/` 전체를 쓰고 유지한다 — 요약·교차참조·정리·부기

사용자는 위키를 거의 쓰지 않는다. **내가 안 쓰면 아무도 안 쓴다.**

## 3계층

자료는 `raw/ → wiki/` 로만 흐른다. 스키마는 흐름의 단계가 아니라 **흐름을 지배하는 층**이라
쌓이지 않고 오퍼레이션마다 읽힌다.

| 계층 | 위치 | 소유 |
|---|---|---|
| 원본 | `raw/` | 사용자. **불변 — 수정·삭제 금지** |
| 위키 | `wiki/` | LLM. 내가 만들고 고친다 |
| 스키마 | `CLAUDE.md` · `docs/` · `templates/` · `scripts/` | 공동. 안 맞으면 갱신을 제안 |

## 디렉토리

```
inbox/            모든 입력의 1차 착지 (로컬 전용). 비우는 건 wiki-ingest
raw/              원본 (불변, YYMMDD-slug.md) — 취급 방식으로 나눈다
  conversations/ · notes/ · docs/ · personal/ · assets/   ← 전부 로컬 전용
  external/       외부 공개물 — 유일한 git 추적 채널 (논문은 papers/). 기준은 규약 §2
wiki/             사다리만 번호 접두사, 같은 층 = 같은 번호
  1-sources/      읽은 것 — 요약 + 내 반응
  1-experiments/  해본 것 — 가장 강한 근거
  2-lessons/      배운 것 — 일반화된 주장
  concepts/       어휘·허브 — 링크의 교차점 (사다리 밖, 자유 생성)
  projects/       업무 프로젝트 허브 (트랙이 여럿이면 <부모명>/ 폴더로)
  builds/         만들어낸 것 — 코드·문서·시스템
  daily/          업무 일지 (YYYY-MM-DD) — 날짜로 찾고 프로젝트로 흘려보낸다
  questions/      아직 답 못 한 질문
캘린더/           (선택) 캘린더 미러 — CalDAV 플러그인이 재생성. 읽기 전용, git 미추적
index.md          전체 카탈로그 (질문받으면 여기부터)
log.md            시간순 기록 (append-only)
예약.md           미래 날짜 할일 — 그날 아침 일지로 자동 주입
templates/ docs/ scripts/    템플릿 · 상세 규약 · 점검·자동화 스크립트
```

## 페이지 규약 요약

- 프론트매터 필수: `type`, `title`, `domain`, `created`, `updated` (타입별 추가 필드는 규약 §1)
- 파일명은 한국어 제목 그대로. `raw/` 만 날짜 접두사 **`YYMMDD-`**.
  단 **프론트매터 날짜·`daily/` 파일명·`log.md` 표기는 `YYYY-MM-DD`** — 스크립트가 파싱한다
- **같은 접두사 형제가 3개 이상이면 폴더로 묶는다** (파일명은 유지 — 링크는 경로를 안 쓴다)
- 연결은 위키링크 `[[페이지명]]`. 외부 URL 만 마크다운 링크
- **새 페이지엔 반드시 인바운드 링크를** 만들고 `templates/` 구조를 따른다. 고아는 없는 것과 같다

## 승격 (이 위키의 핵심)

```
근거 페이지(source·experiment·대화 발췌) →(독립 근거 2건)→ lesson
```

**근거 2건은 필요조건이지 충분조건이 아니다.** 개수를 세기 전에 주장의 형태를 본다 —
**반증 가능한가** · **몰랐다면 다르게 행동했을 것인가**. 하나라도 걸리면 근거가 몇 건이든
격언이라 승격하지 않는다 (근거 3건짜리 후보를 이 필터로 철회한 날 생긴 규칙). 규약 §5.

**`concept` 은 사다리 밖이다** — 증류의 종착지가 아니라 어휘·허브다. 링크의 교차점이
필요하면 자유 생성한다 (역링크가 "이 주제에 대해 아는 전부"를 모으는 착지점).
`project`·`daily` 도 사다리 밖 — 관리 단위·시간 기록이라 승격되지 않는다.

승격해도 **원본을 지우지 않고 링크로 잇는다.** 내려가는 길도 있다 — `lesson` 은
반박됨(`confidence` 하향 + 반례) / 대체됨(`superseded_by`) / 방치됨(인용 관계로 계산)
셋 중 하나로 죽는다. 페이지 충돌은 고치기 전에 **모순인가 진화인가**부터 구분한다.

## 오퍼레이션

### Ingest — 소스를 줄 때

**0. 받는 즉시 원본을 `inbox/` 에 떨군다 (판단 0)** — 어느 경로로 왔든.
소화가 중단돼도 자료가 남고, 잔량 = 미처리 백로그.

→ 1. 분류를 정해(애매하면 묻는다) `raw/` 로 이동 → 2. 통독 → 3. 핵심을 짧게 논의
→ 4. `wiki/1-sources/` 에 소스 페이지 → **5. 영향받는 기존 페이지를 찾아 고침**
→ 6. 필요한 개념 페이지 → 7. `index.md` 등재 → 8. `log.md` 한 줄 → 9. 변경 보고

**5번이 본체다.** 새 페이지만 쌓고 기존 페이지를 안 고치면 파일 더미가 된다.
사용자가 직접 `inbox/` 에 던진 파일도 "inbox 비워줘"로 1번부터 같다.

### Ingest — 경험을 말할 때

대화 자체가 소스다. "이거 해봤는데 이랬어" → `wiki/1-experiments/` 페이지 + 관련
`lesson`·`concept` 갱신. 발췌도 먼저 `inbox/` 에 착지시킨다.

**라우팅** — 위에서부터 먼저 걸리는 것으로:

1. 기존 페이지 보강·정정 → **그 페이지를 고친다 (새 페이지 금지)** ← 기본값
2. 주장 1개 + 근거 2건 → `lesson` / 링크 교차점이 필요한 용어·구조 → `concept`
3. 못 푼 질문 → `question` / 내가 해본 시도 → `experiment`
4. 여러 페이지를 엮은 답변 → 소속 있으면 그 `project`·`build`, 없으면 `question` 에 답 적고 `answered`

새 페이지를 남발하면 위키가 대화 로그가 된다. 상세는 규약 §6.1.

### Query — 질문받을 때

`index.md` → 후보 페이지 → 필요하면 `raw/` 원본. 답변에 근거를 링크한다.
**재사용할 가치가 있는 답은 ingest 로 넘긴다** — 대화에만 두면 사라진다.

### Daily — 오늘 일한 것을 말할 때

`wiki/daily/YYYY-MM-DD.md` 에 일정 / 해야할일 / 작업 / **정해진 것** / 이슈 / 볼트.
**「이슈」는 사람의 업무 이슈, 「볼트」는 자동화 전용** — 기계가 이슈 칸을 쓰면 그 칸이 점검 로그가 된다.
**일지는 종착지가 아니라 입구다** — 정해진 것은 프로젝트 「현재 상태」로, 작업은 build 로
같이 반영한다. 미완료 할 일은 다음날로 **이월**(`(MM-DD~)`). 날짜로 찾으므로 인바운드 대신
**아웃바운드**를 요구한다. 규약 §11.

### Lint — 점검 요청받을 때

```bash
python3 scripts/lint_all.py            # 전종 실행 + 실패한 것만 요약 (--verbose: 전문)
```

링크·역링크·프론트매터는 **깨진 것**을, 현황 숫자·신선도·lesson·raw 커버리지는
**조용히 낡는 것**을 잡는다 — 썩은 사실도 문장은 그대로라 참처럼 읽히기 때문이다.
깨진 링크는 `scripts/lint-allowlist.txt` 에 적은 것만 면제된다 (예약과 오타를 가르는 장치).

⚠️ **exit 0 ≠ 깨끗함**: `lint-index-stats.py` 는 「현황」에 없는 항목을 못 보고
`부분 대조 (n/m)` 로 알린다. allowlist 예약분도 통과로 뜬다. 이 둘은 출력을 읽는다.

그다음 의미를 읽는 것은 내가 본다: 모순 / 노후 lesson / 승격 후보 / 방치된 stub /
근거 부족 (규약 §8). **대화 중에는 보고만 하고 승인 후 고친다.**

예외는 **무인 실행**뿐 — 매일 08:30 `scripts/lint_morning.py` 가 정답이 하나뿐인
셋(낡은 allowlist·현황 숫자·`updated` 보정)만 고치고 나머지는 그날 일지 「볼트」로 넘긴다.
**등재 누락이면 숫자 동기화를 거부한다** (숫자만 고치면 그 누락이 은폐된다). inbox 줄은
템플릿으로 다시 쓴다(`0개 — 소화 완료(날짜)` / `N개 — 미처리`). 규약 §8.1.

**CLAUDE.md 나 규약을 고칠 일이 생기면 바로 고치지 말고 변경안을 먼저 제안한다.**

### 볼트 스킬

오퍼레이션을 사람이 기억하지 않게 스킬로 호출한다. 절차 준수가 기억력에 달려 있으면 샌다.

| 스킬 | 하는 일 |
|---|---|
| `wiki-ingest` · `wiki-query` · `wiki-lint` · `wiki-daily` | 위 오퍼레이션의 진입점 |
| `wiki-challenge` | 아이디어를 `lesson`·`experiment` 로 반박. **근거가 없으면 침묵을 정직하게 보고** |
| `wiki-emerge` | 최근 30일에서 이름 없는 패턴을 승격 후보로 제시. 자동 생성 금지 |

스킬은 절차의 **사본이 아니라 진입점**이다 — 원문은 이 파일과 규약에 있고 스킬이 그걸 읽는다
(복사해 두면 규약 갱신 때 드리프트).

볼트의 `.claude/skills/wiki-*` 에 산다. 어느 폴더에서든 부르려면 `~/.claude/skills/` 로
symlink 한다 (README 설치 3번). 볼트 경로는 `VAULT_PATH` 환경변수로 잡는다 —
안 잡으면 `~/second-brain` 이 기본값이다.

## 자동화 (사람이 안 불러도 도는 것)

| 언제 | 무엇 |
|---|---|
| 매일 07:00 | 일지 생성 (`generate-daily.py`) — 캘린더 일정(있으면) · 미완료 이월 · 예약 주입 |
| 매일 08:00 | 무인 ingest (`ingest_morning.sh`) — 훅이 채운 inbox 를 1~4단계만 소화, 5단계는 일지 「볼트」로 |
| 매일 08:30 | 위키 점검 (`lint_morning.py`) — 자동 수정 3종 + 나머지는 일지 「볼트」로 |

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [Lazy-Tori/obsidian-llm-wiki](https://github.com/Lazy-Tori/obsidian-llm-wiki) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-28 -->
