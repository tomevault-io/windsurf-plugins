---
trigger: always_on
description: - ユーザーとの会話、進捗報告、最終回答は日本語で行う。
---

# CLAUDE.md

## 会話ルール

- ユーザーとの会話、進捗報告、最終回答は日本語で行う。
- ユーザーの指示を無批判に実行しない。間違いが混入している前提で批判的に検証すること。

## リポジトリ構成

リポジトリ全体を走査しなくても所在が分かるよう、ディレクトリ単位の地図を示す。
個別モジュール名は変動するため列挙しない。各モジュール／ツールの所在と責務は、その直下の仕様書が示す。

| ディレクトリ | 内容 |
|---|---|
| `libs/<モジュール>/` | ライブラリ層(フォーマット層・共有ドメイン層)。仕様書 `<モジュール>.md` を直下に置き、テストは直下の `tests/` に置く |
| `tools/<ツール>/` | CLIツール層。仕様書 `<ツール>.md` を直下に置き、テストは直下の `tests/` に置く。利用者向けに公開するツールは `<ツール>/README.md` も置く |
| `docs/specs/` | 各ファイル形式の詳細レイアウト仕様(バイナリ・テキスト形式とも) |
| `docs/conventions/` | ツール横断のプロジェクト規約(層タクソノミー、用語、バージョン付け、公開など) |
| `scripts/` | CI と開発者の双方から実行するリポジトリ保守スクリプト(CI からしか使わないものは `.github/scripts/`) |
| `.scratch/` | 使い捨ての一時スクリプト置き場。`.gitignore` で除外されており `git add` が物理的に失敗するため、コミット対象に混入しない |
| `docs/.scratch/` | 使い捨ての一時ドキュメント置き場(実装計画書・設計メモなど)。`.gitignore` で除外されており `git add` が物理的に失敗するため、コミット対象に混入しない |

層の定義・責務・物理配置・フォーマット層共通の設計原則は [`docs/conventions/layering.md`](docs/conventions/layering.md) が正本。
このファイルは層タクソノミーを再記述せず、上の地図と運用ルールだけを持つ。

## ドキュメントの正本

問いの種類ごとに、どの文書を正とするか。具体ファイル名はパターンで示し、文書の分割・追加で本節を更新せずに済むようにする。

- プロジェクト概要、利用者向け手順、開発環境: [`README.md`](README.md)
- 変更を確定させる前に通す検証(検査の一覧と合格条件): [`docs/conventions/verification.md`](docs/conventions/verification.md)
- 各ファイル形式の詳細レイアウト仕様: `docs/specs/`
- 各ライブラリ／CLIツールの仕様: そのモジュール／ツール直下の仕様書(`libs/<モジュール>/<モジュール>.md`, `tools/<ツール>/<ツール>.md`)。
  仕様書が機能別に分割されている場合も同じディレクトリ内に置く。
- 層タクソノミー・設計境界: [`docs/conventions/layering.md`](docs/conventions/layering.md)
- 上記以外のツール横断の規約: `docs/conventions/` 配下の該当文書

正本の指定は、その主題をどの文書が決めるかを定めるものであって、記述が現在のコードと一致していることも、
必要な事柄を漏れなく述べていることも保証しない。仕様書は常に改善の対象であり、記述をそのまま真として
扱わない。仕様書の記述を実装計画書の根拠として使うときに何を示すかは
[`docs/conventions/document-authoring.md` の実装計画書が示す根拠](docs/conventions/document-authoring.md#4-実装計画書が示す根拠) が正本。

## 運用ルール

- 新しい仕様や挙動を追加・変更する場合は、対応する正本ドキュメントを更新してから実装・テストと整合させる。
- このファイルには他ドキュメントの内容を重複して書かず、長期的に必要な運用ルールだけを書く。
- このファイルを更新する際は単純に追記せず、重複を避けるよう全体を再構成して書き直す。

---
> Source: [skyflash521/mmd-toolbox](https://github.com/skyflash521/mmd-toolbox) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-12 -->
