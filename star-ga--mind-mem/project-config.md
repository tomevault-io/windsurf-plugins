---
trigger: always_on
description: This repository uses mind-mem for persistent memory. Before answering, consult memory with `mm inject --agent copilot`. Respect ADR / DECISION blocks; route new decisions through `propose_update` rather than modifying them directly.
---

# mind-mem: GitHub Copilot workspace instructions

This repository uses mind-mem for persistent memory. Before answering, consult memory with `mm inject --agent copilot`. Respect ADR / DECISION blocks; route new decisions through `propose_update` rather than modifying them directly.

---
> Source: [star-ga/mind-mem](https://github.com/star-ga/mind-mem) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-06-16 -->
