---
trigger: always_on
description: Files in `docs/docs/command/`, `docs/docs/commands.md`, and
---

# docs/CLAUDE.md

Files in `docs/docs/command/`, `docs/docs/commands.md`, and
`docs/command-sidebar.json` are generated by `hack/gen-command-docs`
(triggered via `go generate ./cli/commands/`). Do not edit them by hand.
Instead, update the command source in `cli/commands/` (or the generator in
`hack/gen-command-docs`) and regenerate.

## Admonitions (callouts)

Use Docusaurus admonitions for constraints, gotchas, and asides — never inline
bold (`**NOTE:**`), blockquotes, or emoji. Consistent admonition markup lets
both readers and AI agents reliably identify "this is important" and retrieve
it with its context intact.

Use exactly these five types (`:::caution` is deprecated in Docusaurus 3 — do
not use it):

- `:::note` — neutral, supplementary "good to know" context that would clutter
  the main flow.
- `:::tip` — a recommendation, best practice, shortcut, or optional
  enhancement.
- `:::info` — notable contextual facts: version/availability, what a page does
  or doesn't cover, comparisons and definitions.
- `:::warning` — something that can break, surprise, or bite the reader, or
  behavior that is experimental / subject to change. Proceed with care.
- `:::danger` — destructive, irreversible, or security-critical actions (data
  loss, exposing secrets). Use sparingly.

Rules:

1. Every admonition has a title, in bracket syntax and sentence case:
   `:::warning[Review before sharing]` (capitalize only the first word and
   proper nouns). The legacy space form (`:::warning Review before sharing`) is
   not used here. A short, descriptive title makes callouts scannable and gives
   agents a stable anchor for retrieval.
2. One concept per admonition; keep the body concise — the type tag already
   carries the "importance" signal. Don't nest admonitions.
3. Always close with `:::` on its own line.
4. Don't stack them. More than two or three admonitions in a row and the
   "this is important" signal collapses into wallpaper — the reader (and the
   retrieval agent) can no longer tell the load-bearing gotcha from the aside.
   Reserve callouts for what actually breaks, surprises, or bites; explanatory
   "here's why this value" context belongs in prose or a code comment, not a
   box. When a section genuinely has many gotchas, one scannable summary (a
   roadblock checklist, a table) beats a column of boxes.

`hack/gen-command-docs` also emits admonitions; keep it consistent with the
above. The docs lint (`bun run lint`) enforces these rules.

---
> Source: [mirendev/runtime](https://github.com/mirendev/runtime) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-24 -->
