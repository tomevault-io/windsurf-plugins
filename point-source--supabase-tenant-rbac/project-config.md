---
trigger: always_on
description: A PostgreSQL extension that provides multi-tenant RBAC for Supabase projects. Installs as plain SQL (no pg_tle dependency). Current version: **5.2.1**.
---

# CLAUDE.md — Developer & Agent Quick Reference

## What This Project Is

A PostgreSQL extension that provides multi-tenant RBAC for Supabase projects. Installs as plain SQL (no pg_tle dependency). Current version: **5.2.1**.

## Repository Layout

```
supabase_rbac--5.2.1.sql       # Current extension full install script (READ THIS FIRST)
supabase_rbac--X.Y.Z.sql       # Prior version install scripts (keep for reference)
supabase_rbac.control          # Extension metadata (default_version lives here)
VERSIONS                       # Version manifest for tools/install.sh (latest + upgrade edges)
CHANGELOG.md                   # Version history

examples/
  policies/
    quickstart.sql             # Recommended starter RLS for rbac.* tables
    storage_rls.sql            # Storage RLS examples
    custom_table_isolation.sql # RLS for custom app tables with group_id FK
    hardened_setup.sql         # REVOKE ALL + targeted GRANT defense-in-depth
  triggers/
    sync_user_into_group_role.sql  # Keep user email/name synced into members.metadata
    on_delete_user_roles.sql   # Cascade deletes via user_roles view
  views/
    user_roles.sql             # Flattened view: one row per user-group-role
  setup/
    create_public_wrappers.sql      # Opt-in: create public.* wrappers for PostgREST discovery
    remove_public_wrappers.sql      # Drop previously created public.* wrappers
    create_service_role_wrapper.sql # Opt-in: public.add_member for service_role only

supabase/
  config.toml                  # Local dev config (Postgres 15, port 54321)
  migrations/
    20240502214828_install_rbac.sql       # Plain SQL install (generated by tools/install.sh)
    20240502214829_add_dummy_data.sql     # Test sensitive_data table
    20240512101038_add_rls_policies.sql   # RLS on rbac tables + sensitive_data
  seed.sql                     # 2 test users, 3 groups, role definitions, member assignments
  functions/invite/index.ts       # Deno edge function for invite acceptance
  functions/add-member/index.ts  # Deno edge function for service_role add_member

docs/
  CONCEPTUAL_MODEL.md          # Three-tier model, entities, escalation prevention
  ARCHITECTURE.md              # End-to-end system design
  SECURITY.md                  # Security model and known limitations
  API_REFERENCE.md             # All RPCs and helpers with signatures and examples
  MIGRATION_GUIDE.md           # Upgrading from v4.x
  DESIGN_GOALS.md              # Canonical spec (source of truth for v5 design)
  SECURITY_TEST.md             # Security test spec — coverage map for the 18-file test suite

tools/
  install.sh                   # Generates plain SQL migrations (local dev + curl|bash for users)
  get_jwt.sh                   # Script to get a JWT for local dev testing
```

## Local Development

```bash
# Start local Supabase (Docker required)
supabase start

# Applies migrations + seed.sql automatically
# Studio available at http://localhost:54323
# API at http://localhost:54321
# DB at postgresql://postgres:postgres@localhost:54322/postgres

# Stop
supabase stop

# Reset (re-runs migrations + seed)
supabase db reset

# Run tests
supabase test db

# Get a JWT for a test user (for curl testing)
./tools/get_jwt.sh
```

### Test Users (from seed.sql)
| Email | Password | Role in Alpha group |
|-------|----------|---------------------|
| `alice@example.local` | `password` | owner |
| `bob@example.local` | `password` | admin |
| `carol@example.local` | `password` | editor |
| `dave@example.local` | `password` | viewer |
| `eve@example.local` | `password` | (non-member) |

## Extension Versioning

When making changes to the core extension:

1. Edit `supabase_rbac--5.2.1.sql` (or create a new version file)
2. **CRITICAL: Run `tools/install.sh` immediately after any edit** — the install migration embeds the full extension SQL with `@extschema@` replaced by `rbac`. Without regeneration, `supabase db reset` uses stale code.
3. Create `supabase_rbac--<old>--<new>.sql` — upgrade path (ALTER/REPLACE only changed objects). Source files still use `@extschema@` for TLE compatibility; the tool handles substitution.
4. Update `default_version` in `supabase_rbac.control`
5. Update `VERSIONS` manifest (`latest=X.Y.Z`, add upgrade edge if applicable)
6. Add entry to `CHANGELOG.md`

**Upgrade migrations for users:**
```bash
tools/install.sh --from 5.0.0 -o path/to/upgrade.sql
```
This chains upgrade path files (e.g. 5.0.0→5.1.0→5.2.0) into a single plain SQL migration.

**Version bump rules:**
- `patch` (5.0.x): Bug fixes only
- `minor` (5.x.0): New features, backwards compatible
- `major` (x.0.0): Breaking changes

## Key Design Principles

- **Private schema**: Tables in `@extschema@` (e.g. `rbac`), NOT in PostgREST's exposed schemas. All access via RPCs.
- **Public wrappers (opt-in)**: NOT auto-created. Run `examples/setup/create_public_wrappers.sql` to add thin `public.*` pass-throughs for PostgREST RPC discovery or unqualified RLS policy calls. Default install has zero public surface.
- **Deny-all by default**: All tables have RLS enabled with zero policies. Consumers must add policies.

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [point-source/supabase-tenant-rbac](https://github.com/point-source/supabase-tenant-rbac) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-24 -->
