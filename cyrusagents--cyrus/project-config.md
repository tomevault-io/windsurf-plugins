---
trigger: always_on
description: Note, there is a need to maintain the use of '--print' when running the `claude` exec commands because that is what makes it non-interactive.
---



Note, there is a need to maintain the use of '--print' when running the `claude` exec commands because that is what makes it non-interactive. 

NEVER leave meta-commentary about your changes as code comments.

NEVER remove code comments that were still relevant.

---
> Source: [cyrusagents/cyrus](https://github.com/cyrusagents/cyrus) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-17 -->
