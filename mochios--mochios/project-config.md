---
trigger: always_on
description: mochiOSはマイクロカーネル指向の全く新しい、セキュアな商用レベルのデスクトップOSです。
---

# agents.md

## このリポジトリについて

mochiOSはマイクロカーネル指向の全く新しい、セキュアな商用レベルのデスクトップOSです。

OSの開発時にカーネル（mnu）へ変更を加えることがあると思います。
カーネルへ実装を追加する前に、その機能が本当にカーネルに必要かを必ず検討してください。

## レイヤーの責務

カーネル

- スケジューラ
- 仮想メモリ
- IPC
- Capability
- Syscall
- 割り込み
- 保護機構

ユーザーランド

- システムポリシー
- Service Manager
- ドライバー
- ファイルシステム
- アプリケーション

## 実装方針

以下は原則としてカーネルへ実装しません。

- 特定サービス向けの処理
- 特定ドライバー向けの処理
- 特定ファイルシステム向けの処理
- アプリケーション固有の処理
- サービス名やドライバー名のハードコード

必要であれば、汎用的なシステムコールやカーネルAPIとして設計してください。
カーネルはユーザーランドの実装を知りません。
特定のディレクトリやサービス、ドライバー、ファイルシステム、アプリケーションの名前をハードコードすることはしてはなりません。

例えば、ケーパビリティを付与する際も、ケーパビリティを付与できるケーパビリティを持つサービスなどがマニフェストをパースし、カーネルはそれを検証するだけです。

initfsは最小限にしてください。core.service、起動に必須のcext以外**載せてはなりません**

## syscall

syscallはアプリケーション機能ではなく、汎用的なプリミティブを提供してください。

マウスのデータを取得するsyscallや、特定のファイルシステムを読むsyscallは、カーネルに実装しないでください。

良い例

- memory_map
- ipc_call
- capability_transfer

悪い例

- ext2_read
- ps2_read
- appstore_launch

## コードを書くとき

大きなリファクタリングは行わず、必要最小限の変更を行ってください。
unsafeは最小限にしてください。
設計に迷った場合は実装せず、どのレイヤーに配置すべきか確認してください。

## セキュリティ方針

mochiOSはCapabilityセキュリティを採用します。

新しい機能を追加する場合は、最小権限の原則を守ってください。
権限確認を省略してはいけません。
一時的な実装であっても、Capabilityを迂回するコードを書いてはいけません。

グローバルな共有状態を追加する場合は、その状態へアクセスできる主体を明確にしてください。

## ドライバーとcext

デバイス固有の処理やファイルシステム固有の処理は、原則としてcextまたはPlugKitドライバとして実装してください。

カーネル側に追加してよいのは、以下のような汎用的な仕組みに限ります。

- IRQの配送
- MMIOのマッピング
- DMA用メモリの確保
- Capabilityの受け渡し
- IPCの補助

## 実装前の判断手順

カーネルへ変更を入れる前に、次の順で検討してください。

1. ユーザーランドサービスで実装できないか
2. cextまたはユーザーランドドライバーで実装できないか
3. 既存のsyscallやIPCで実現できないか
4. 新しいsyscallが必要な場合、それが汎用プリミティブか
5. 最後に、本当にカーネル本体へ入れる必要があるか

## 互換性とABI

既存のsyscall番号、構造体レイアウト、IPC メッセージ形式、Capabilityの意味を変更する場合は注意してください。

互換性を壊す変更を行う場合は、その理由を明確にしてください。
既存のユーザーランドやサービスが壊れる変更は、必要最小限にしてください。

## エラー処理

カーネル内では、回復可能な失敗をpanicにしないでください。

ユーザーランド由来の不正な入力、無効なポインタ、不正なCapability、存在しないハンドルは、カーネルクラッシュではなくエラーとして返してください。

## アプリケーションABI

mochiOSのアプリケーションは、将来的にLinux ABI互換で動作させる方針です。

アプリケーション向けの独自ABIを安易に増やしてはいけません。
アプリケーションから見える syscall、ELFローダー、動的リンク、ファイルディスクリプタ、プロセス、スレッド、シグナル、メモリマップなどは、可能な限りLinuxとの互換性を意識してください。

ただし、カーネル内部の設計をLinuxカーネル風に寄せる必要はありません。
mochiOSの内部構造は、Capability-based securityとマイクロカーネル指向を優先します。

Linux ABI互換は、アプリケーション境界で提供します。
カーネル内部やサービス設計をLinuxと同じにすることを意味しません。

---
> Source: [mochiOS/mochiOS](https://github.com/mochiOS/mochiOS) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-24 -->
