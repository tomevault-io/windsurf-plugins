---
trigger: always_on
description: 一本关于 agent 的工程哲学著作，也是一个结构化的知识库。它是《大模型应用工程哲学》的续作——二部曲的第二部。
---

# AGENTS.md -- 项目指引

## 项目定位

一本关于 agent 的工程哲学著作，也是一个结构化的知识库。它是《大模型应用工程哲学》的续作——二部曲的第二部。

第一本从模型出发：模型是一个会出错的概率组件，工程化的对象是建在模型之上的应用。这一本从 agent 这个自主角色出发——有岗位、有技能、有履历、有工作环境，工程化的对象是 agent 本身。这是比模型高一层的抽象，不是同一层的更深一刀。

核心视角：把 agent 当打工人看。系统提示词是 JD，skill 是简历与可验证的证书，模型分级是资历，长期记忆是职业履历，带持久化卷的沙箱是工作电脑。一套人力资源视角，串起 agent 工程的全部问题。

不是教程，不是 agent 框架使用手册，不是 multi-agent 编排速查表，而是一个人对"当 agent 进入软件系统的核心位置，工程该怎么做"的系统性回答。

双重读者：人和大模型。如果一份文档不能同时对人和机器保持清晰，那它的结构本身就有问题。

## 与第一本书的边界

这本书会反复涉及第一本已经讲过的问题：不确定性、声明式提示、类型契约、skill 的可移植性。边界要钉死——凡是能在模型层讲清的，引用第一本（https://onenightcarnival.github.io/llm-engineering-philosophy/），不在这里重讲。第二本从"agent 作为自主角色"起步，往上写到团队与组织。一句话区分：第一本是怎么和一个不靠谱的函数相处，这一本是怎么和一个不靠谱的同事相处。

## 写作语气校准

- 让观点自己说话。不通过罗列作者背景来为观点增加权威性。
- 避免自证式叙事：不用"因为我做过 X，所以我有资格说 Y"的结构。如果一个观点需要作者简历来支撑，说明观点本身的论证不够有力。
- 跨领域经验（数学、工程、投资）可以作为类比和思维工具的来源，但以抽象原则的形式出现，不以个人经历的形式出现。
- 锋利但不傲慢。明确的判断和明确的偏好是本书的特色，但"我比别人看得清"不是。
- 自省要真诚：承认局限性时，指向真实的盲区，而非变相的自夸。
- 警惕 AI 写作的典型味道：面面俱到的平衡术、每段末尾的自我对冲、过于工整的平行结构、"综合以上分析"式的过渡词、把简单的事情用复杂的术语重新包装。宁可有锋利的偏见，不要滴水不漏的正确。

## 核心原则

### 写作立场

- 表达个人观点与哲学观，不追求从众，不依赖权威背书。
- 偏重对未来的构想和对问题结构的分析，而非对既有事实的罗列。
- 概念边界清晰，观点有论据支撑。
- 允许跨领域类比（数学、物理、组织管理），但类比必须指向共享的深层结构和具体的工程启示，而非表面相似。人力资源是本书的主干隐喻，要让它承重，而不是停在比喻的趣味上。

### 写作风格

- 中文为主。不使用 emoji、icon 或任何装饰性符号。
- 行文严肃但不刻板，允许锋利的判断和明确的偏好。
- 标题层级不超过二级（h1-h2）。
- 代码块仅用于：（1）架构蓝图（agent 循环、岗位定义、skill 结构等结构模式）；（2）prompt / JD 的代码化表达；（3）好写法与坏写法的代码层面对比。概念分类、决策框架、思维模型用散文、表格或列表表达。

### 技术倾向

声明式优于命令式，类型系统优于运行时检查，组合优于继承，显式依赖优于隐式约定，结构驱动优于流程驱动，简单方案优于"看起来高级"的方案。对"框架崇拜"持批判态度。岗位定义清晰优于模型堆砌，能力可验证可迁移优于一次性 prompt。

### 思维习惯

架构决策先于实现细节。岗位与能力的匹配先于模型选择。迭代过程应有明确的终止条件。约束越精确，agent 行为越可预测。这些思维习惯应该隐性地影响写作，而非被当作命名概念反复标注在文中。

## 项目结构

```
agent-engineering-philosophy/
  AGENTS.md                  -- 本文件
  README.md                  -- 项目入口，书籍目录（GitHub 仓库首页）
  index.md                   -- VitePress 站点首页
  chapters/                  -- 各章节内容
    00-序章/                  -- 为什么写第二本书
    ...                       -- 后续章节按 HR 映射展开（待定稿）
  docs/.vitepress/           -- VitePress 配置与自定义主题（沿用第一本的星空主题）
  build.sh                   -- PDF 构建脚本（Pandoc + Typst）
  metadata.yaml              -- 电子书元数据
  pdf-filter.lua             -- Pandoc Lua 过滤器（PDF 构建用）
  book-conf.typst            -- Typst 排版模板
  .github/workflows/         -- GitHub Actions（自动部署 GitHub Pages）
```

章节目录下包含 `00-概述.md`（章节概述与文章索引）和按阅读顺序编号的独立 `.md` 文章。Markdown 文件命名格式：`序号-中文名.md`，序号两位数字。新增章节后需同步更新 `docs/.vitepress/config.mts` 的 sidebar、`README.md` 的目录和 `build.sh` 的章节列表。

## 拟定全书结构（HR 映射，待定稿）

按"打工人"这套抽象，章节大致沿一条线展开：岗位定义（JD）→ 能力作为可验证可迁移资产（skill / 证书）→ 资历与岗位匹配（模型分级）→ 履历与记忆 → 工作环境（持久化沙箱）→ 从个体到团队与组织。具体章节标题和切分在写作中定稿。

## 协作规范

- 理解本项目的立场和品味后再动笔。不写空话、套话、正确的废话。
- 不使用"让我们"、"大家都知道"之类的口语化表达。
- 不回避争议性判断。作者观点与主流不同时，忠实表达，不做对冲。
- 论证靠逻辑和证据，不靠作者背景。
- 每篇文章应有清晰的论点，而非话题的泛泛介绍。
- HR 隐喻是主干，要承重并落到具体工程启示，不停在比喻的趣味上。
- 概念首次出现时给出精确定义。
- 与第一本重叠的概念，引用而非重讲。
- 引用其他章节时使用相对路径。

---
> Source: [Onenightcarnival/agent-engineering-philosophy](https://github.com/Onenightcarnival/agent-engineering-philosophy) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-06 -->
