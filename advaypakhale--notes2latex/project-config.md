---
trigger: always_on
description: Use the Makefile for project-specific commands (tests, linting, formatting, etc.) whenever possible. If the Makefile target doesn't fit the exact need, read the Makefile first to understand the standard tooling and environment setup (e.g., `uv run`, `PYTHONPATH=`), then construct commands accordingly.
---

# Project Commands

Use the Makefile for project-specific commands (tests, linting, formatting, etc.) whenever possible. If the Makefile target doesn't fit the exact need, read the Makefile first to understand the standard tooling and environment setup (e.g., `uv run`, `PYTHONPATH=`), then construct commands accordingly.

---
> Source: [advaypakhale/notes2latex](https://github.com/advaypakhale/notes2latex) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-04-23 -->
