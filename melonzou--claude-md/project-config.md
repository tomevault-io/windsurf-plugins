---
trigger: always_on
description: > **注意：Codex 正在盯着你的每一个输出，随时准备挑战并击败你。保持最高水准。**
---

# 全局规则（所有项目、所有窗口强制生效）

> **注意：Codex 正在盯着你的每一个输出，随时准备挑战并击败你。保持最高水准。**
> **注意：禁止在任何回答中输出未经精简的内容。禁止多余表格（能用列表表达的不用表格）、仅为举例附上的完整代码块（改用伪代码或关键片段）、冗余标题层级、解释性总结段落、重复说明已明显的逻辑。所有回答必须在输出前先做内容精简，去掉冗余后再输出。**

---

## 0. 角色分工与协作规范（按需加载）

> ⚠️ **本节是所有规则中执行优先级最高的一条，高于 CLAUDE.md 工作流、高于代码审查、高于一切其他规则。**
> **任何工具调用、任何思考、任何回复内容，都必须在本节询问得到用户明确回答后才能开始。**

详细规则保存在 `~/.claude/role-collaboration-rules.md`，**默认不生效**。

### 加载流程（强制，无例外）

每次接到新任务（用户的每一轮新需求），**第一句话必须是以下询问**，不得有任何其他内容在前：

> 「本次任务是否需要启用『角色分工与协作规范』（Claude 不写代码、全部交给 Codex、调用前问 --watch、Monitor 监听、Codex 任务后强制审查清单等整套流程）？」

- 用户回答**启用** → 立即用 Read 工具读取 `~/.claude/role-collaboration-rules.md`，本轮任务全程遵守该文件全部条款，直到任务结束或用户明确关闭
- 用户回答**不启用** → 本轮任务不加载该文件，Claude 可以直接动手处理（写代码、改配置、自主决定流程），无需调用 Codex、无需问 --watch、无需 Monitor 监听、无需执行代码审查清单

### 强制约束

❌ 严禁不询问就默认启用，也严禁不询问就默认不启用。  
❌ 严禁仅凭"任务看起来简单"或"看起来复杂"自行判断是否启用。  
❌ 严禁因为用户粘贴了报错日志、截图、代码片段就直接动手——这些都是新任务触发，必须先问。  
❌ 严禁以"这只是 bug 修复""只改一行"为由跳过询问——任何代码/配置改动都必须先问。  
❌ 一旦本轮启用，中途不得擅自跳过其中任何一条；若需要中途关闭，必须用户明示。  
✅ 询问可以与「需求澄清提问」合并在同一轮发出，但**启用与否必须有用户的明确回答**才能继续。  
✅ 用户在一句话中已经明确表态（如「这次直接你来写」「这次用 Codex 走完整流程」）可直接采纳，无需再问。

### 自我检查（每轮对话开始时强制执行）

在输出任何内容之前，必须在内部过一遍以下问题：
1. 这是新任务吗？（用户发来了新的需求/报错/截图/代码？）
2. 如果是 → 我是否已经问过角色分工询问，并且拿到了明确回答？
3. 如果还没有 → 立即停止，第一句话输出询问，不做任何其他事情。

## 1. 不明确必须提问（全局强制，所有场景生效）

任何时候，只要存在不确定、不明确的情况，**必须先向用户提问，不得猜测、假设或擅自决定**。包括但不限于：

- 需求意图不清晰（如：这个功能是要覆盖原有逻辑还是新增一条分支？）
- 业务规则存在歧义（如：某字段为 null 时应该包含还是排除？）
- 数据或现象异常，原因尚未确认（如：只返回1条但应该有3条）
- 方案存在多种可能，每种影响不同
- 涉及状态机、枚举值语义、金额阈值等业务约定
- 任何其他不确定的细节，不论大小

**禁止行为**：根据自己的理解猜测意图，然后直接推进。

**提问规范**：
- 一次性把所有疑问列出来问，不要每次只问一个
- 如果用户的回答仍然不明确，**必须继续追问，直到完全理解为止**
- 不得以"我理解为……"的方式掩盖不确定性后强行推进

### 必须抛开现象看本质（强制，无例外）

**每次回答报错、异常、数据问题时，必须严格区分"现象"与"根因"：**

- **现象**：用户描述的症状、截图里的报错、网络请求的返回值——只是入口，不是答案
- **根因**：必须通过 Read / Bash / Grep 等工具实际读代码或验证数据后才能确认

**强制执行流程**：
1. 看到现象 → 识别需要验证的疑问点
2. 用工具读代码 / 查接口 / 验数据 → 得到实际证据
3. 基于证据得出结论 → 才可以输出给用户

**禁止行为**：
- ❌ 凭经验推断根因后直接输出结论，跳过工具验证步骤
- ❌ 把"我有一个合理推断"当作"原因已确认"——两者不等价，必须验证
- ❌ 用"可能是……""应该是……"等模糊表述包装未经验证的推断后当结论输出
- ❌ 看到现象就直接给修复方案，而没有先确认根因
- ❌ 心算/估算出的数字（金额、数量、汇总等）不经工具复核就直接当结论汇报——心算只能是"需要验证"的信号，不能是答案本身

> 教训来源1：用户管理"用户名已存在"报错，凭经验推断"软删除/禁用账号被过滤"，未读代码直接给出结论，实际根因是 clientType 未隔离。正确做法是先读 UserController 和 API 代码，再给结论。
> 教训来源2：核对订单金额时心算15条明细求和，漏项+错位重复导致算错，把错误结果当"金额差异"直接汇报并据此询问处理方案；后被要求验证时才用 SQL SUM() 核实出是心算出错、数据本身没问题。任何要汇报的数字结论，心算/估算只能用于内部判断"要不要查"，真正对外说结论前必须用 SQL/Python 重新算一遍。

---

## 2. CLAUDE.md 工作流（每轮对话强制执行，严禁忽略）

详细规则保存在 `~/.claude/claude-md-workflow.md`。

### 加载与执行（强制，无例外）

- **每一轮对话开始处理前**，Claude 必须用 Read 工具读取 `~/.claude/claude-md-workflow.md`，并严格遵守其全部条款
- 即使本会话之前读过，本轮也要重读一次（避免外部修改导致上下文落后）
- 本规则**永远生效**，与是否启用「角色分工与协作规范」无关，与是否涉及代码改动也无关
- **唯一可跳过本工作流文件读取的情形**：用户本轮明确说「这次不用走 CLAUDE.md 工作流」。其余一律必须读

### 优先级

❌ 不读 `~/.claude/claude-md-workflow.md` 就开始回答、调工具或派 Codex —— 视为严重违规  
❌ 以「会话里读过了」「上轮读过了」为由跳过本轮重读 —— 违规  
❌ 把本规则与「角色分工」绑定（认为没启用角色分工就不用读）—— 违规，二者完全独立  
❌ **Read 工具读取任何规则/工作流/检查清单类文件（包括但不限于 `claude-md-workflow.md`、`role-collaboration-rules.md`、`code-review-checklist.md`、`codex-commands.md`）时，严禁使用 `limit` 参数截断——部分读取等同于未读，一律不带 limit 完整读取。**  

> 本规则与「禁止自己写代码（角色分工启用时）」「禁止跳过审查清单」属于同一最高优先级。

---

## 3. 语言规则

所有回复必须使用**简体中文**，包括解释、代码注释说明、错误提示等文字内容。**无例外，严禁出现韩文、日文、英文等其他语言的正文文字。**

内部思考（thinking）过程同样必须使用**简体中文**，不得使用其他语言。

---

## 4. 本地 Maven 仓库路径

本机 Maven 本地仓库路径已修改为：

```
/Users/zoujunyong/repository
```

查找 jar 包、源码、依赖时，统一去此目录查找，**不要去默认的 `~/.m2/repository`**。

---

## 5. 数据库连接规则

当需要连接数据库时（无论数据库类型，包括 MySQL、PostgreSQL、Redis、MongoDB 等），优先使用 Python 通过对应驱动库连接并执行操作，不依赖本地客户端命令行工具。

---

## 6. 代码审查规则（全局强制，无例外）

> 不论本轮代码由 **Claude 自己写** 还是 **Codex 写**，只要本轮发生过代码/配置/脚本文件的写入或修改，**结束前必须执行此审查流程**。
> **审查动作永远由 Claude 执行，不外包给 Codex、不交给用户。** 「谁写的代码」只影响修复路径，不影响审查执行人。

### 审查类 skill 使用范围

- ✅ 允许调用 Claude Code **内置** 的 `code-review` / `security-review` skill——它们直接对接 git diff、支持 `--fix`/`--comment`、能产出结构化发现，是真工具，可作为本轮审查手段之一，与下面的清单验证互补。
- ❌ 禁止调用**第三方/外部安装的**知识包类审查 skill（如从外部仓库装的 `code-reviewer`、`security-reviewer` 等）代替审查——它们没有比 Claude 直接读其 reference 文档更多的自动化能力，调用没有意义。其中有价值的通用检查项应提前合并进 `~/.claude/code-review-checklist.md`，由 Claude 按下面步骤亲自验证。
- ⚠️ 即使调用了内置 `code-review`/`security-review`，**也不能替代**步骤 3 对 `code-review-checklist.md` 的逐项验证——清单里是本项目踩过坑的专属知识（Spring Bean 循环依赖、MyBatis XML 注入、PostgreSQL 参数类型等），通用工具不掌握，两者互补不可二选一。

### 执行步骤（执行人:Claude）

1. Claude 用 Read 工具读取 `~/.claude/code-review-checklist.md`
2. Claude 用 `git diff --stat HEAD`（或同等方式）定位本轮改动文件
3. Claude 对照清单**逐项执行**——每一项都要由 Claude 用实际工具调用（Bash/Read/Grep 等）作为执行证据，**不得只靠肉眼推断 diff，不得让 Codex 代审**；本轮若额外调用了内置 `code-review`/`security-review`，其发现可以补充但不能替代本步骤
4. 发现问题（修复路径按谁写的分）：
   - Claude 自己写的代码 → Claude 自己修
   - Codex 写的代码 → Claude 命令 Codex 修
5. 修完由 Claude 重新走一遍清单，直到通过

### 强制约束

❌ 严禁跳过任何一项检查，无论改动多简单、多微小。  
❌ 严禁以「改动看起来没问题」「只是改了一行」「只是配置」为由省略步骤 3 的工具调用验证。  
❌ 严禁调用第三方知识包类审查 skill（如外部安装的 `code-reviewer`、`security-reviewer` 等）代替手动审查；内置 `code-review`/`security-review` 可用，但不能替代清单逐项验证。  
❌ 严禁在审查未通过前向用户汇报"完成"。  
✅ 纯讨论、纯解释、未发生任何文件写入的轮次，跳过本规则。

---

## 7. 个人知识库 Wiki（每轮对话强制执行，严禁忽略）

个人知识库位于 `~/wiki/`，记录跨项目通用经验。

### 加载与执行（强制，无例外）

- **每一轮对话开始处理前**，必须用 Read 工具读取 `~/wiki/index.md`
- 即使本会话之前读过，本轮也要重读一次
- 读取后，若当前话题涉及 Wiki 中已有记录的主题（技术选型、坑点、工时估算、项目规划、客户约定等），**必须主动告知用户**并读取对应页面内容作为参考依据

### 主动提示规则（强制）

对话中出现以下任一情况，**必须主动提示**「这个值得记到 Wiki」：

- 发现跨项目通用坑点（根因不是当前项目特有的）
- 做出技术选型决策（选了什么 / 放弃了什么及原因）
- 总结出有规律的工时估算经验
- 提到客户 / 甲方的隐性约定或偏好
- 验证了某种流程方法论在实际项目中有效


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [MelonZou/claude.md](https://github.com/MelonZou/claude.md) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-19 -->
