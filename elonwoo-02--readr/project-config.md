---
trigger: always_on
description: Generates `library/_index.md` (reading-status counts, papers by direction, asset counts, validation summary). **Only runs if validation passes.** The generated file is auto-generated — do not edit manually.
---

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# ReadR — AI-Assisted Research Workflow

An Obsidian vault template for academic research, covering the full pipeline: **sources → library → annotations → reviews**.

> **Active direction:** `nlp/knowledge-graph/kg-augmented-llm` (6 papers). See [Further Reading](#further-reading) for the full README (759 lines) and onboarding details.

## Quick Reference

| Item | Detail |
|---|---|
| Status lifecycle | `to-read` → `browsed` → `close-read` |
| Validate | `pwsh scripts/ReadR.ps1 -Validate` |
| Update index | `pwsh scripts/ReadR.ps1 -UpdateIndex` (only after validation passes) |
| Plugin build | `cd .obsidian/plugins/readr-dashboard/ && npm install && npm run build` |
| Auto-generated | `library/_index.md` — **never edit manually** |
| Skills | `.claude/skills/` has 5 skills (see [Skills & Configuration](#skills--configuration)) |
| Placeholders | `library/entries/your_direction/` and `annotations/your_direction/` should be replaced with actual directions |

## Architecture

### Four-Layer Architecture

- `sources/` — Raw materials (read-only). Subdirectories: `papers/` `web/` `books/` `talks/` `misc/`
- `library/` — Paper catalog + knowledge distillation
  - `entries/` — Paper entries (organized by research direction, replace `your_direction/`)
  - `concepts/` — Core concepts
  - `authors/` — Researcher profiles
  - `datasets/` — Datasets **and** benchmarks (use `subtype: dataset` / `benchmark` / `both` to distinguish)
  - `comparisons/` — Method comparisons
  - `syntheses/` — Literature syntheses (write after 3+ papers in a sub-direction)
  - `projects/` — Active projects
  - `_index.md` — Auto-generated by `scripts/ReadR.ps1 -UpdateIndex`. **Do not edit manually.**
- `annotations/` — Close-reading notes (one **folder** per paper). **Mirrors `library/entries/` sub-direction structure exactly** (e.g., `annotations/nlp/knowledge-graph/kg-augmented-llm/` mirrors `library/entries/nlp/knowledge-graph/kg-augmented-llm/`).
- `reviews/` — Survey papers, with `templates/writing_constraints_template.md` for structured writing
- `docs/` — Project documentation (column series, specs, plan, demo, literature review)
- `scripts/` — Automation: `ReadR.ps1` (validate + index generation)
- `.claude/skills/` — 5 skills for AI workflow integration

### Design Principles

- **`sources/` is immutable** — never create, modify, move, or delete files under `sources/`
- **One source, one entry** — each paper has exactly one entry in `library/entries/`
- **Annotations only after close reading** — `annotations/` is the output of close reading, not casual browsing
- **Knowledge distillation alongside browsing** — concepts/authors/datasets are distilled while you browse
- **Synthesis bridges to review** — `syntheses/` is the intermediate state between reading and writing

## Templates

### Library Templates (`library/_template/`)

| Template | Purpose | Key YAML Fields |
|---|---|---|
| `library-entry.md` | Paper entry | type, title, authors, venue, method, task, keywords, status, direction, source, doi, url, annotation, concepts, datasets, metrics, github, generated, verified, created, updated |
| `concept.md` | Core concept | type, title, aliases, generated, verified, created, updated |
| `author.md` | Researcher profile | type, title, affiliation, position, lab, research_interests, homepage, orcid, generated, verified, created, updated |
| `dataset.md` | Dataset **or** benchmark (merged) | type, title, subtype, year, task, modality, size, license, homepage, paper, github, leaderboard, generated, verified, created, updated |
| `comparison.md` | Method comparison | type, title, methods, decision_criteria, related_concepts, last_compared, generated, verified, created, updated |
| `synthesis.md` | Literature synthesis | type, title, direction, paradigms, coverage_status, generated, verified, created, updated |
| `project.md` | Active project | type, title, status, start_date, datasets, generated, verified, created, updated |

### Annotation Template

- `annotations/_template/reading-note.md` — Close-reading note. **AI builds the skeleton** (identity block, sections 1–4 with guiding questions, figure/table/formula placeholders); **humans fill the content** (section 4 "Personal Evaluation & Reflection" answers, all actual figures/tables/formulas). Uses `generated`/`verified` trust signals.

## Naming & Conventions

### File Naming

- **Paper entries:** `Short Title (Venue Year).md` — e.g., `GFM-RAG (NeurIPS 2025).md`
- **Researcher profiles:** `Last, First.md` — e.g., `Vaswani, Ashish.md`
- **Concepts / datasets (merged):** descriptive name — e.g., `GraphRAG.md`, `HotpotQA.md`
- **All files and directories use English names**

### YAML Frontmatter

- **Required fields** (enforced by validator): `title`, `authors`, `venue`, `method`, `task`, `status`, `direction`, `source`
- **Relationship fields** (recommended, warned if absent): `annotation`, `concepts`, `datasets`, `github`, `generated`, `verified`
- **Status values** (enforced): `to-read`, `browsed`, `close-read`

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [elonwoo-02/ReadR](https://github.com/elonwoo-02/ReadR) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-23 -->
