---
trigger: always_on
description: <!-- Generated: 2026-02-13 | Updated: 2026-02-13 -->
---

<!-- Generated: 2026-02-13 | Updated: 2026-02-13 -->

# react-native-nitro-cookies

## Purpose
High-performance HTTP cookie management library for React Native using Nitro Modules JSI architecture. Provides both synchronous and asynchronous APIs for cookie CRUD operations on iOS (NSHTTPCookieStorage / WKHTTPCookieStore) and Android (CookieManager). Published as `react-native-nitro-cookies` on npm. Designed as a drop-in replacement for `@react-native-cookies/cookies` with 5x+ faster performance.

## Key Files

| File | Description |
|------|-------------|
| `package.json` | Monorepo root config (yarn workspaces: `package/`, `example/`) |
| `turbo.json` | Turborepo task definitions for iOS/Android builds |
| `eslint.config.mjs` | ESLint flat config for the monorepo |
| `babel.config.js` | Babel configuration |
| `lefthook.yml` | Git hooks (commitlint) |
| `CLAUDE.md` | AI assistant instructions and project guidelines |
| `CHANGELOG.md` | Release changelog (v1.0.0) |
| `CONTRIBUTING.md` | Contribution guidelines |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `package/` | The npm package source: TypeScript API, iOS Swift, Android Kotlin, Nitrogen codegen (see `package/AGENTS.md`) |
| `specs/` | Feature specifications and design documents (see `specs/AGENTS.md`) |
| `openspec/` | OpenSpec change proposal system (see `openspec/AGENTS.md`) |
| `.specify/` | SpecKit templates, scripts, and memory for feature planning (see `.specify/AGENTS.md`) |
| `example/` | React Native example app for testing the library |

## For AI Agents

### Working In This Directory
- This is a **yarn workspaces monorepo** with two workspaces: `package` and `example`
- Use `yarn` (v3.6.1 via Corepack) for all package management; the `.yarnrc.yml` configures it
- The library source lives in `package/`; treat `example/` as a consumer/test app
- Nitrogen code generator (`nitrogen`) creates C++/Swift/Kotlin bridge code from the TypeScript interface in `package/src/NitroCookies.nitro.ts`
- **Never edit files under `package/nitrogen/generated/`** -- they are auto-generated by running `yarn nitrogen`
- **Never edit files under `package/lib/`** -- they are build output from `react-native-builder-bob`

### Build & Test Commands
- `yarn test` -- run Jest tests
- `yarn lint` -- ESLint across the monorepo
- `yarn typecheck` -- TypeScript type checking
- `yarn nitrogen` -- regenerate Nitrogen bridge code
- `yarn prepare` -- full build (runs `bob build` which triggers nitrogen + module + typescript targets)
- `yarn clean` -- remove Android build and lib output directories

### Commit Conventions
- Follows [Conventional Commits](https://www.conventionalcommits.org/) enforced by commitlint
- Pre-commit hooks managed by lefthook

### Architecture Overview
```
TypeScript API (package/src/index.tsx)
    |
    v
Nitro HybridObject Interface (package/src/NitroCookies.nitro.ts)
    |
    v
Nitrogen Code Generator --> C++ specs + platform bridges
    |                   |
    v                   v
iOS (Swift)         Android (Kotlin)
NSHTTPCookieStorage  CookieManager
WKHTTPCookieStore    HttpURLConnection
```

## Dependencies

### External
- `react-native-nitro-modules` ^0.31.4 -- JSI-based native module framework (peer dep)
- `react` / `react-native` -- React Native framework (peer deps)
- `react-native-builder-bob` -- Build tool for React Native libraries
- `nitrogen` -- Code generator for Nitro Modules bridge code
- `turbo` -- Monorepo task runner
- `release-it` -- Release automation

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->

---
> Source: [l2hyunwoo/react-native-nitro-cookies](https://github.com/l2hyunwoo/react-native-nitro-cookies) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-04-22 -->
