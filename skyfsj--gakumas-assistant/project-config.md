---
trigger: always_on
description: * 所有方法必须带有文档注释和类型标注，按Python官方风格编写
---

## 基本要求
* 所有回复必须使用简体中文。
* 代码注释必须使用中文。
* 所有方法必须带有文档注释和类型标注，按Python官方风格编写
* 不理解需求、资料不足、样本不足、规则冲突时，必须停止并询问用户，禁止盲做。
* 不得伪造测试通过、删除失败样本、跳过失败用例或隐藏失败原因。

## 参考资料
实现游戏机制、流程判断、UI 识别、动作决策前，必须先查阅相关资料：
* 游戏手册：`train/gakumas_rl/docs/help_content_pages/categories`
* UI 数据采集规则：`docs/references/producer_plan.md`
* 其他玩法参考：`docs/references/*`
如果 UI 数据采集规则发生变化，必须同步更新 `docs/references/producer_plan.md`。

## 项目接入
* 决策逻辑需要以无状态方式对接到：`train/gakumas_rl`
* 更新决策逻辑时应同步更新src/core/tasks/producer_challenge/gameplay/strategy/llm_strategy.py和src/core/tasks/producer_challenge/gameplay/strategy/algo_strategy.py
* 禁止依赖不可复现的全局状态、隐藏缓存或历史残留状态。
* 必要历史信息必须通过显式输入传入。

## 常量与文本
* 游戏文本常量统一放到：`src/constants/game/text`
* 通用字符串常量统一放到：`src/constants`
* 禁止在业务逻辑中硬编码游戏文本。
* 重复使用的字符串必须提取为常量。
* 游戏数据在程序/API中传递必须使用游戏主数据库中的id，而不是使用OCR结果

## Python 语法与风格
* 新代码应尽量补全类型标注，函数参数和返回值必须标注类型。
* 复杂返回值应优先定义 dataclass，不要返回含义不明的 tuple 或 dict。
* 禁止使用裸 except:，必须捕获具体异常类型。
* 捕获异常后不得静默吞掉，至少需要记录原因或向上抛出。
* 禁止使用可变对象作为默认参数，例如 []、{}。
* 路径处理优先使用 pathlib.Path，不要手动拼接路径字符串。
* 导入顺序应保持：标准库、第三方库、项目内部模块。
* 禁止使用 from xxx import *。
* 函数应保持职责单一，避免超长函数；复杂流程需要拆分为可测试的小函数。
* 判断空值时优先使用明确语义，例如 is None、if not items。
* 日志和异常信息应包含足够上下文，方便定位失败样本和处理阶段。
* 测试代码不得依赖执行顺序、外部隐式状态或本机绝对路径。
* 任务步骤需有相应的注释
* 增加Python依赖时需要及时更新requirements.txt，更新requirements.txt时需要按分类，有运行时、开发时、打包时区分。不更新.cn.txt，该文件仅用于覆盖镜像源

## 代码风格
* 复杂数据优先使用 `dataclass`。
* 尽量少用 `dict` 传递复杂结构。
* 非必要不使用 `getattr`、`setattr`、`hasattr`。
* 新增第三方依赖、修改公共接口、调整目录结构、大规模重构前必须先询问用户。
* 能复用的工具方法尽量写到src/utils中
* 字符串匹配必须使用src/utils/string_tools.py
* OpenCV可复用方法在src/utils/opencv_tools.py
* 容易造成性能问题的方法需使用src/utils/performance_tools.py装饰
* 游戏主数据库读取需使用src/utils/game_database_tools.py
* 点击Yolo_Box/元素尽量使用device.click_element()非自行判断的坐标不允许使用device.click()
* 尽可能不使用全屏OCR，效率过低，可以使用HSV+蒙版提取参考点后再OCR
* 有新页面需及时更新src/utils/game_tools.py get_current_location()识别规则
* 禁止把无关功能全部塞进同一个文件，文件应按职责拆分。
* 项目目录不能过于扁平化，目录命名需要语义化，能从路径看出模块用途。
* 新增模块应放在符合语义的目录中，不要随手放到根目录或公共目录。
* 拆分文件时应以功能边界、领域边界或复用边界为依据，避免过度拆分。

## README 与文案约束
* README 应像真实项目维护者写的文档，禁止写成营销页、宣传稿或 AI 生成味很重的长文。
* README 只写用户真正需要的信息：项目用途、安装方式、基本用法、配置说明、常见问题、开发说明。
* 禁止把实现细节、内部调试过程、无关背景、过度优势描述、空泛愿景塞进 README。
* README 中的功能描述必须与当前代码真实能力一致，未完成能力必须明确标注状态，禁止夸大。
* 新增 README 内容前，应优先检查是否已有相同说明，避免重复段落和重复标题。
* UI 文案、设置项说明、提示文本必须简短、明确、可操作。
* UI 和设置提示只描述当前界面需要用户理解的内容，禁止塞入技术实现、开发备注、长篇解释或无关说明。
* UI 风格应尽可能符合 Google Material Design 规范，优先保证清晰、克制、一致和可用性。
* 禁止随意使用半透明卡片、毛玻璃、强渐变、复杂阴影、花哨动效等与整体风格不符的元素。
* 新增 UI 必须注意全局字号、间距、圆角、控件高度、颜色层级和交互状态的一致性。
* 不要为了“好看”堆叠无关装饰组件；UI 组件应服务于功能和信息表达。
* 新增页面或组件前应先参考现有 UI 风格，避免做出与项目整体风格割裂的界面。
* 错误提示应说明发生了什么、用户能做什么；不要输出堆栈、内部变量名或调试废话。
* 面向用户的文本应集中维护，重复文案需要提取为常量。

## 兼容性要求
- 修改公共接口、配置格式、数据结构、文件路径时，必须检查所有调用方并同步更新。
- 不得为了实现新功能随意破坏旧功能、旧配置或已有测试。
- 如果必须做破坏性变更，必须说明原因、影响范围和迁移方式。
- 不得随意删除看似暂时不用的代码、配置、文档或测试。

## 配置与路径
- 禁止在代码中写死本机绝对路径、用户名、设备路径、端口或私有环境信息。
- 可变配置应放入配置文件、环境变量或明确的参数中。
- 默认配置必须适合普通开发环境，特殊环境配置需要单独说明。
- 示例配置不得包含真实密钥、Token、Cookie 或私人地址。

## 日志要求
- 日志应用于定位问题，不得刷屏。
- Debug 日志必须可开关，默认不输出大量中间信息。
- 日志中不得输出密钥、Token、Cookie、账号密码等敏感信息。
- 识别、决策、数据采集失败时，日志应包含阶段、输入摘要、失败原因和可排查线索。

## 测试组织
- 测试文件应与被测模块保持清晰对应关系。
- 测试样本、期望结果、测试代码应分开管理。
- 不允许把大量测试数据直接硬编码进测试逻辑。
- 新增功能必须优先补充最小可复现测试，再扩展边界样本测试。

## 提交前自查
提交前必须确认：
- 没有无关格式化改动。
- 没有残留临时代码、调试文件、无用注释。
- 没有引入未使用的导入、变量、函数或依赖。
- 没有把本地路径、个人配置、测试截图误提交。
- 相关测试已运行并记录结果。

## 图像识别要求
* 禁止使用强模板匹配作为核心方案。
* 禁止写死绝对坐标。
* 禁止假定固定分辨率、固定截图尺寸或固定 UI 缩放。
* 识别逻辑应使用相对位置、检测框、轮廓、颜色、OCR、目标检测、多信号融合等方式。
* 识别流程需要具备一定 JPG 噪点、轻微颜色偏差、缩放误差和边缘锯齿容错能力。
* 涉及识别的代码必须添加 `Debug_Tools` 或等价调试输出，至少能展示关键中间结果、候选区域、最终结果和失败原因。

## 数据与测试
* 所有任务 / 流程都需要采集多张相同步骤、不同画面的样本进行测试。
* 样本需要先人工审查，确认标签和期望结果正确。
* 目标必须达到真实 100% 通过率。
* 初次不通过时，需要分析失败原因并修复；不得制造假通过。
* 如果样本不足或无法确认失败原因，必须停止并询问用户。
* 修改代码后必须运行相关测试，不能只做静态修改。

## 失败时必须说明
测试或识别失败时，必须记录：
* 失败样本
* 期望结果
* 实际结果
* 中间识别结果
* 失败原因
* 下一步修复方向

## 交付回复要求
完成任务后必须说明：
* 修改了哪些文件
* 实现了什么功能
* 参考了哪些资料
* 新增或更新了哪些常量
* 新增或更新了哪些测试
* 测试结果
* 是否还有未解决问题

不得只回复“已完成”。

---
> Source: [skyfsj/gakumas-assistant](https://github.com/skyfsj/gakumas-assistant) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-17 -->
