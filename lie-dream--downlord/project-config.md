---
trigger: always_on
description: > 本文件会被 Claude Code 自动载入每次会话。规则按优先级排列;与其他文档冲突时,**以本文件为准**。
---

# CLAUDE.md — DownLord 项目 AI 协作规则

> 本文件会被 Claude Code 自动载入每次会话。规则按优先级排列;与其他文档冲突时,**以本文件为准**。
> 本文件只放「红线 + 入口」,详细内容在 `docs/` 分层文档,按需阅读(避免每次会话灌入全部上下文)。

## 提交纪律（最高优先 · 红线 · 唯一权威处）

- **所有 `git commit` / `git push` 一律由用户亲自执行。AI（任何模型）严禁自动提交**——代码、测试、文档、配置、schema、`.gitignore` 等任何改动都不例外。
- 各 plan / prompt 中出现的「commit 节点」「`git commit …`」均为**给用户的提交检查点**:AI 到达该点应**停下、汇总本次改动清单交用户审查**,由用户决定并亲自提交。
- 一次同意提交**不延续到后续步骤**;每次提交都需用户当次明确执行。AI 可以**建议** commit message,但**不得运行** `git commit` / `git push`。
- **本条是唯一权威处,别处不必复述**:本文件每次会话自动载入,故其他文档与执行提示词**无需重复申明这条红线**,每段可单独复制执行的提示词末尾**保留一条**执行提醒即可。对保证在场的东西复述不叫自包含,叫抄。

## 文档分层（开发前按需阅读）

| 文件 | 职责 |
|---|---|
| `CONTEXT.md`(根) | **术语表**:任务 / 状态 / 落点 / BT / 工程流程的统一口径——**最轻、最先读** |
| `docs/PRD.md` | 产品定位 / 用户 / 痛点 / 旅程 / MVP / 功能黑名单 |
| `docs/ARCHITECTURE.md` | 技术栈 / 结构 / 数据模型 / **开发约束 / 禁止破坏的逻辑** |
| `docs/DESIGN.md` | 视觉与交互规范(也是 frontend-design 的输入缰绳) |
| `CHANGELOG.md`(根) | 各版本对外有影响的变更记录 |

**开始任何改动前必读**:本文件 + `CONTEXT.md`(术语)+ `docs/ARCHITECTURE.md` §7「禁止破坏的逻辑」。

**按需精读原则**:大文档(如 `ARCHITECTURE.md`)**只读指名的章节 / 条目**,不整份灌入;需要更多再回头取。`CONTEXT.md` 是例外——它短,整份读。

**文档工作必读**:涉及文档同步、交接或结论承接时,先读 `docs/ARCHITECTURE.md` §5.1,结束前按其逐项核对本轮产出。上下文已被注入不等于已阅读或遵守。

## 项目工作流（设计 / 计划 / 评审文档流）

- 每个功能单元(Task)先出架构设计 + 实施计划,**第一步只设计不写码**;收口评审只在该 Task 的最后一步写;中间步骤不单独写评审,进度与证据回填既有设计 / 计划。公开仓库不含内部的逐 Task 设计 / 计划 / 评审过程资料,贡献者在自己的分支或 PR 说明中沿用同一顺序即可。
- **术语随开发沉淀**:出现新概念或发现旧词有歧义时,当场更新 `CONTEXT.md`(一条 1–2 句),不攒到收口再补。

## 日期纪律（文档命名 / 记账 · 2026-07-30 立）

- **日期只准「读」,不准「推算」**。合法来源只有两个:①**系统当天真实日期** ②**文档里已由用户确认过的既有日期**。**严禁**从「A 发生在 B 之后」推出「A = B 的次日」——同一天里完全可以先收口再补测。
- **AI 发现系统日期与文档内日期冲突时,停下问用户,不自行裁决**。这类冲突一问就清,却会被文件名与交叉引用扩散到几十处。
- **待办类记录本身不需要日期**——它需要的是**成因 / 影响边界 / 落点**三件;日期只在末尾「来源:…」写**一次**,且必须是当天真实日期。
- **已做事项**(设计 / 计划 / 评审文件名、收口日期、交付标注)的日期 = **当天**。
- **由来(踩过两次,方向相反)**:一次把实际发生在当天的补测推算成「收口次日」,写进待办与评审共 30 处;随后又**因为看见仓库里那个推算值**而推翻系统给出的真实日期,把臆造值扩散进文件名。两次都是「日期被推理出来」而非被读出来。

## 指派纪律（双向指针 · 2026-08-03 立）

- **把某项工作指派给后续某个 Task 时,必须同时写进那个 Task 的执行说明**。只写在待办里是**单向指针**——待办知道该找谁,被指派的一方却不知道有人在等它。
- **回写形式 = 事实内联,不是给链接**:可单独复制执行的提示词必须自包含(块外的东西执行方一个字看不到),故把该事项的成因 / 边界 / 判据**抄进去**。
- **目标执行说明还不存在时**:不许当作「以后会想起来」。撰写任何 Task 的执行说明,第一件事是回查待办里指向它的条目并全部接进去——把「推」换成「拉」,拉取发生在**目标被创建的那一刻**,不依赖任何人记得住。
- **由来**:规划期先有待办再写执行说明,引用是顺手的;执行期执行说明早已写完才发现问题,**没人回头改**。指派方向与写作方向相反时,单向指针是默认结果,不是意外。

## 需求澄清

- 开工前若目标 / 边界 / 取舍尚未拍板,由**用户主动**发起澄清:AI **一次只问一个问题**、每问附带推荐答案、能从代码 / 文件查到的**事实自己查**(只把**决策**交用户),达成共识前不动手。
- 该模式**不可由 AI 自行触发**;澄清结论当场落 `CONTEXT.md`(术语)与对应设计文档(方案),不留在对话里。
- 澄清期的事实查证**限定在点名的文件 / 目录内**,不放任全库扫描。

## 核心硬约束（摘要 · 完整版见 `docs/ARCHITECTURE.md` §7）

- **引擎独立进程化**:aria2 / yt-dlp / ffmpeg 作为独立子进程调用,崩溃隔离、可独立更新;不自己重写下载 / 解析协议。
- **二进制内置 + yt-dlp 可热更新**:三引擎随安装包分发、开箱即用;yt-dlp 因站点改版频繁,必须支持脱离应用整体升级的独立热更新。
- **进程职责分离**:主进程是业务大脑(TaskManager / DownloadEngine / VideoResolver / MediaTool),渲染进程纯 UI,两者只经强类型 IPC bridge 通信。
- **数据安全**:SQLite 存的是**真实下载历史(源数据,非派生)**——损坏时备份旧库再新建,**绝不静默丢弃**;断点续传靠 aria2 的 `.aria2` 控制文件,业务元信息与字节级进度分离持久化。
- **合规与诚实**:工具中立;**不承诺规避网络封锁**(代理仅对接用户自带节点 / 系统代理);下载内容受版权与平台 ToS 约束,如实声明,不做虚假承诺。
- **实现纪律**:分步小验证、保持可运行;不一次性重构、不越界改无关模块;先更新对应版本 spec / plan,再实现功能。

---
> Source: [lie-dream/DownLord](https://github.com/lie-dream/DownLord) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-24 -->
