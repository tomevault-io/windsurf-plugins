---
trigger: always_on
description: - When a compiler feature or language semantic is missing, fix the underlying implementation correctly and immediately. Do not defer the real fix, add workaround paths, or accept "enough for the corpus" behavior unless the user explicitly asks for a temporary stopgap.
---

# Repository Instructions

- When a compiler feature or language semantic is missing, fix the underlying implementation correctly and immediately. Do not defer the real fix, add workaround paths, or accept "enough for the corpus" behavior unless the user explicitly asks for a temporary stopgap.

---
> Source: [withlang-dev/open-jai](https://github.com/withlang-dev/open-jai) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-12 -->
