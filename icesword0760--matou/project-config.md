---
trigger: always_on
description: - 所有面向用户的沟通，以产品、用户和具体使用场景的视角为主，优先说明问题、用户影响、适用场景、取舍和建议。
---

# Matou 项目协作规则

## 用户沟通

- 所有面向用户的沟通，以产品、用户和具体使用场景的视角为主，优先说明问题、用户影响、适用场景、取舍和建议。
- 技术架构、代码、协议、数据结构和测试等内容仅作为结论的辅助证据，不作为沟通主体。
- 输出必须达到产品经理可据此做出决策的标准：结论清楚、影响明确、边界可判断；存在多个选择时，说明各自的用户价值、代价、风险并给出推荐。
- 功能交付和验收说明优先描述用户能看到什么、能完成什么、异常时会发生什么，再补充实现与验证证据。
- 已确认的规格进入实现后连续推进，不为常规技术选择、阶段计划或中间检查重复请求确认；仅当实现与 PRD 或 reference product 既有产品行为产生实质冲突、需要产品取舍时再向用户确认。

## 产品与交互实现

- 后续功能开发和修改以用户体验为第一优先级，避免为了技术上的完整、抽象或小概率保护而引入用户可感知的等待、跳动、额外步骤或认知负担。
- 优先采用能直接闭合核心场景的最小方案；没有明确用户价值或尚未被真实场景验证的能力不提前设计。
- 功能完成不等于交付完成。验收必须同时覆盖交互友好性与交互性能，包括反馈时机、动画连续性、滚动、焦点、误触和真实终端负载下的响应。
- 已确认 Mockup 的用户感知效果是实现基准；底层技术允许不同，但反馈速度、状态变化和运动轨迹应保持一致。

## PRD 实现与验收基线

- 视觉与交互的 1:1 对照边界仅覆盖 reference product 的 CLI 模块本身，即截图中的黑色工作区域：黑色区域内部的工作区/事项侧栏、标签栏、终端及分屏区域、快捷操作区，以及由这些入口触发的菜单、弹窗和状态反馈。
- 这里的“外边侧栏”专指截图左侧白色的产品级导航（我的分身、Kode、协作、市场等）；它不属于 CLI 模块。黑色区域内左侧的工作区/事项侧栏仍属于 CLI，继续纳入 1:1 对照。
- reference product 黑色区域外的白色产品侧栏、顶部 Logo/品牌区域及其它非 CLI 产品外壳全部排除：不要求 Matou 实现，也不纳入截图、像素或交互差异判断。Matou 保留自己的窗口标题与应用级导航设计。
- 后续 reference product / Matou 视觉证据默认裁切到 CLI 黑色区域再比较；外部产品侧栏或品牌区即使出现在原始截图中，也只作为定位参照，不计入验收。
- 每个 PRD 在提交产品验收前，必须以原始 reference product 的可运行产品和现存代码逻辑为双重基线，完成该 PRD 范围内全部界面与交互的逐项比对。
- 比对范围覆盖：信息层级、布局、尺寸、间距、颜色、字体、图标、文案、默认状态、加载状态、空状态、悬停、焦点、选中、禁用、菜单、弹窗、键盘与鼠标操作、拖拽、窗口行为、异常反馈、恢复行为、持久化结果和操作后的状态迁移。
- reference product 已存在的同范围能力，Matou 的 UI 样式、入口位置、操作步骤、反馈时机、交互逻辑和最终结果均按 1:1 一致验收；工程实现方式允许不同，用户可感知行为保持一致。
- 每个 PRD 的验收材料必须附带「reference product 交互对照矩阵」，逐场景记录 reference product 基线证据、Matou 实际结果、截图或运行证据及差异结论。矩阵未闭合时，该 PRD 继续处于实现阶段。
- 发现 Matou 与 reference product 的差异时，优先对齐 reference product；若 reference product 行为与已确认 PRD 发生实质冲突，暂停对应冲突点并提交产品取舍，其余无冲突项继续推进。

## 品牌与命名卫生

- 源码、测试、资源路径、文档、注释和生成脚本统一使用 Matou 自有命名，或 `generic`、`legacy`、`reference` 等中性术语。
- 历史外部终端产品与参考产品的品牌标识均属于仓库禁用标识；功能迁移、兼容和对齐说明也使用中性名称。
- 每次提交前运行 `pnpm check:identifiers`；门禁覆盖受控文件与新增文件的路径及内容。

---
> Source: [icesword0760/matou](https://github.com/icesword0760/matou) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-03 -->
