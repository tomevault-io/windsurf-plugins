---
trigger: always_on
description: This file applies to the whole repository. Read it before changing code. If a nested directory later provides a closer `AGENTS.md`, follow the closer file for that subtree.
---

# Agent Instructions

This file applies to the whole repository. Read it before changing code. If a nested directory later provides a closer `AGENTS.md`, follow the closer file for that subtree.

## Language And Encoding

- `AGENTS.md` is intentionally written in English.
- User-facing product documentation under `docs/` is written in Simplified Chinese.
- WebUI text must go through i18n catalogs whenever the page is locale-enabled.
- Always read and write text files as UTF-8. On Windows PowerShell, set `[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()` before inspecting non-ASCII output.
- Before finishing documentation or locale work, scan touched files for obvious mojibake or replacement characters.

## Project Overview

- Twilight is an Emby / Jellyfin user-management panel.
- Backend: Go module `github.com/prejudice-studio/twilight`, entrypoint `cmd/twilight`.
- Frontend: `webui/`, Next.js App Router, TypeScript, Tailwind CSS, Radix/shadcn-style components, Zustand, and TanStack Query.
- Preferred deployment: Linux + systemd.
- Current Go source and `docs/guides/development.md` are authoritative. Do not reintroduce old Python backend entrypoints, uvicorn, `requirements.txt`, or historical multi-SQLite migration notes.

## Documentation Priority

Read these when work touches architecture, APIs, deployment, security, or feature behavior:

- `docs/guides/development.md`
- `docs/guides/modular-architecture.md`
- `docs/reference/backend.md`
- `docs/reference/api-index.md`
- `docs/reference/backend-api.md`
- `docs/reference/developer-js.md`
- `docs/guides/security.md`
- `README.md`

Update docs in the same change when behavior changes.

## Docker Warning

`docs/guides/docker.md`, `docker-compose.yml`, and `docker-compose.prod.yml` were generated by an LLM and are not the recommended deployment path. Keep a prominent warning at the top when editing Docker docs or compose files. Prefer Linux + systemd unless the user explicitly asks for Docker work.

## Directory Map

- `cmd/twilight`: CLI entry, supporting `api`, `all`, `scheduler`, `bot`, and `version`.
- `internal/api`: HTTP routes, auth, rate limits, response helpers, handlers, external clients, scheduler, admin operations, runtime APIs.
- `internal/api/routes.go`: centralized route registration.
- `internal/config`: `config.toml`, `config.local.toml`, and `TWILIGHT_*` environment loading.
- `internal/store`: single-state-document persistence, JSON or PostgreSQL.
- `internal/redis`: RESP client for shared sessions and rate limits.
- `internal/security`: tokens, password hashing, and secure random helpers.
- `webui/src/app`: Next.js App Router pages.
- `webui/src/lib/api-request.ts`: low-level request wrapper, credentials, timeout, and `ApiError`.
- `webui/src/lib/api.ts`: frontend API client. New backend routes usually need matching client methods and `api-types.ts` types.
- `webui/src/locales`: i18n catalogs.
- `deploy/`: systemd units and install scripts; units must point to `bin/twilight`.

## Feature Location Guide

| Domain | Backend | Store | Routes | Frontend | Docs |
| ---- | ---- | ---- | ---- | ---- | ---- |
| Setup wizard | `setup_handlers.go`, `config_admin.go` | `User`, `AuditLog` | `/setup/*` | `(auth)/setup` | `backend-api.md` |
| Auth/session/password reset | `auth_handlers.go`, `session.go` | login logs, sessions | `/auth/*` | `login`, `forgot-password` | `security.md` |
| User profile | `handlers.go`, `upload_handlers.go`, `safepath.go` | `User` | `/users/me/*` | `settings/*` | `background.md` |
| Email | `email_handlers.go`, `email_verify_service.go` | email verification records | `/users/me/email/*`, `/admin/email/*` | email components, `admin/email` | `email.md` |
| Emby binding/devices/IP audit | `emby*.go`, `emby_device_audit.go` | `User.EmbyID` | `/emby/*`, `/admin/emby/*` | `dashboard`, `admin/emby` | `backend-api.md` |
| Telegram Bot | `telegram*.go` | roster/rebind state | `/users/me/telegram/*`, `/admin/telegram/*` | `admin/telegram*` | `telegram-bot.md` |
| Registration codes | `regcode_handlers.go`, `code_use_handlers.go` | `RegCode` | `/admin/regcodes/*`, `/users/me/use-code` | `admin/regcodes` | `regcodes.md` |
| Invite tree | `invite_handlers.go`, `invite_admin_handlers.go` | invite codes and relations | `/invite/*`, `/admin/invite/*` | `invite`, `admin/invite` | `invite.md` |
| Media requests | `media_request_handlers.go`, `media_service.go` | `MediaRequest` | `/media/*` | `media`, `admin/requests` | `backend-api.md` |
| Sign-in/score | `signin_handlers.go` | `signin.go` | `/signin/*` | `score` | `backend-api.md` |
| Announcements | `announcement_handlers.go` | `Announcement` | `/announcements`, `/admin/announcements/*` | announcement board | `announcements.md` |
| Bangumi | `bangumi*.go` | Bangumi caches/logs | `/bangumi/*`, `/admin/bangumi/*` | `bangumi/*` | `bangumi.md` |
| API keys | `apikey_handlers.go` | `APIKey` | `/apikey/*`, `/users/me/apikeys` | `settings/apikey` | `api-key.md` |
| Scheduler | `scheduler*.go` | `SchedulerRun` | `/admin/scheduler/*` | `admin/scheduler` | `backend.md` |

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [Prejudice-Studio/Twilight](https://github.com/Prejudice-Studio/Twilight) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-22 -->
