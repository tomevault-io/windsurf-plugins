---
trigger: always_on
description: MCP Server for GenieACS — TR-069 device management, provisioning, and monitoring via MCP.
---

# AGENTS.md — GenieACS MCP

## Project Overview

MCP Server for GenieACS — TR-069 device management, provisioning, and monitoring via MCP.

- **Repo:** [GeiserX/genieacs-mcp](https://github.com/GeiserX/genieacs-mcp)
- **Language:** Go
- **License:** GPL-3.0
- **Parent project:** [GeiserX/genieacs-container](https://github.com/GeiserX/genieacs-container)

## Architecture

MCP (Model Context Protocol) server built in Go. Exposes tools and resources for AI assistants to interact with genieacs-container functionality.

### Key Files

- `cmd/server/main.go` — Entry point, MCP server initialization, tool/resource registration
- `client/acs.go` — HTTP client wrapping the GenieACS NBI REST API
- `config/config.go` — Environment variable loading (ACS_URL, ACS_USER, ACS_PASS, DEVICE_LIMIT)
- `internal/tools/` — 12 MCP tool handlers (parameter CRUD, presets, provisions, tags, search, tasks)
- `internal/resources/` — 7 MCP resource handlers (devices, files, tasks, presets, provisions, faults)
- `version/` — Build version metadata injected by GoReleaser ldflags
- `Dockerfile` — Multi-stage container build

## Development

```bash
go build -o genieacs-mcp ./cmd/server
```

## Security

> **Do not commit secrets to the repository.**
> Use environment variables or secure vaults for credentials.

---

*Generated by [LynxPrompt](https://lynxprompt.com) CLI*

---
> Source: [GeiserX/genieacs-mcp](https://github.com/GeiserX/genieacs-mcp) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-15 -->
