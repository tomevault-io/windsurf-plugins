---
trigger: always_on
description: - Quarto tends to output verbose information when rendering documents, which can be token-consuming. You should use `--quiet` option by default when you don't need to debug something.
---

- Quarto tends to output verbose information when rendering documents, which can be token-consuming. You should use `--quiet` option by default when you don't need to debug something.
- When writing Lua filters, make sure you actually know [Quarto's Lua filter API](https://quarto.org/docs/extensions/lua-api.html).

---
> Source: [sun123zxy/quarto-callouty-theorem](https://github.com/sun123zxy/quarto-callouty-theorem) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-31 -->
