---
trigger: always_on
description: > Auto-generated by The AI Brain. Edit .brain/brain.yaml and run `/the-ai-brain:brain-vault-align` to update.
---

# Claude Code Toolkit Brain — AI Brain

> Auto-generated by The AI Brain. Edit .brain/brain.yaml and run `/the-ai-brain:brain-vault-align` to update.
> Last generated: 2026-03-28

## About the Owner
- Name: cfircoo
- Role: Developer / AI tooling builder
- Domain: Claude Code skills, agents, hooks, commands

## Vault Structure
- `Human/` — Human-authored content (READ only unless explicitly asked)
  - `Human/Daily/` — Daily notes (YYYY-MM-DD.md)
  - `Human/Projects/` — Project files
  - `Human/Archive/` — Completed/archived items
- `Machine/` — AI-managed content (READ and WRITE freely)
  - `Machine/Session-Logs/` — Session continuity logs
  - `Machine/Memory/` — Persistent memory (entities, decisions, corrections)
  - `Machine/Rules/` — Self-updating behavioral rules
  - `Machine/Templates/` — Note/project templates
  - `Machine/Canvases/` — Obsidian JSON canvas visualizations

## Startup Protocol
Before doing ANY work:
1. Read `Machine/Rules/active-rules.md`
2. Read `Machine/Memory/context-cache.md`
3. Read the most recent file in `Machine/Session-Logs/`
4. Read today's daily note if it exists

## Obsidian CLI (active — v1.12.7)
On WSL2, `obsidian` is a wrapper to the Windows Obsidian.com CLI binary.
Prefer CLI commands over raw file reads:

| Task | CLI command | Fallback |
|------|------------|---------|
| Search vault | `obsidian search query=<text> format=json` | Grep files |
| Today's daily note | `obsidian daily` / `obsidian daily:read` | Read file |
| Incomplete tasks | `obsidian tasks todo format=json` | Grep `- [ ]` |
| Find orphans | `obsidian orphans format=json` | Manual link scan |
| Get backlinks | `obsidian backlinks file=<name> format=json` | Grep `[[name]]` |
| Open a note | `obsidian open path=<path>` | N/A |

## Formatting Conventions
- Use [[wikilinks]] for all internal references
- Include YAML frontmatter on every new file
- Date format: YYYY-MM-DD
- Tags in frontmatter arrays: `tags: [tag1, tag2]`

## Directory Permissions
- `Human/` — READ freely, WRITE only when explicitly asked
- `Machine/` — READ and WRITE freely
- `.brain/` — READ only (config, do not modify manually)

## Memory Protocol
- New person/project/tool encountered → log to `Machine/Memory/entities.md`
- Significant decision made → log to `Machine/Memory/decisions.md`
- User corrects behavior → log to `Machine/Memory/corrections.md`
- Update `Machine/Memory/context-cache.md` with current working context

## Session End Protocol
Before ending any session, write a session log to `Machine/Session-Logs/YYYY-MM-DD-{topic}.md` using the template at `Machine/Templates/session-log.md`.

## Self-Improvement Protocol
When the user corrects behavior:
1. Log the correction to `Machine/Memory/corrections.md`
2. Propose a new rule for `Machine/Rules/active-rules.md`
3. Wait for user approval before adding the rule
4. Log the rule change to `Machine/Rules/rule-changelog.md`

## Global Brain
At startup, also read `~/Brain/CLAUDE.md` for:
- Who I am (identity, role, global preferences)
- Global rules that apply to all projects
- Cross-project entities (people, tools, recurring patterns)
- Known projects registry

---
> Source: [cfircoo/claude-code-toolkit](https://github.com/cfircoo/claude-code-toolkit) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-02 -->
