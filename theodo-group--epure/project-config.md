---
trigger: always_on
description: Épure (package/CLI: `epure`) is a grid-snapped, orthogonal-routed architecture-diagram editor that
---

# Épure — repository guide for Claude

Épure (package/CLI: `epure`) is a grid-snapped, orthogonal-routed architecture-diagram editor that
reads/writes a tiny D2 subset as a git-reviewable file pair (`<name>.epr.d2` + `<name>.epr.layout.json`).

---
> Source: [theodo-group/epure](https://github.com/theodo-group/epure) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-20 -->
