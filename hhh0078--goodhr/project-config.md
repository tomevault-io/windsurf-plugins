---
trigger: always_on
description: - 严禁使用 `page.evaluate()`、`locator.evaluate()`、`evaluateHandle()`、`$eval()`、`$$eval()`、`addScriptTag()`、`addInitScript()` 或 `dispatchEvent()` 读取或控制招聘页面。
---

# 最高优先级：招聘页面零脚本注入

- 严禁使用 `page.evaluate()`、`locator.evaluate()`、`evaluateHandle()`、`$eval()`、`$$eval()`、`addScriptTag()`、`addInitScript()` 或 `dispatchEvent()` 读取或控制招聘页面。
- 严禁向招聘页面注入 JavaScript，或修改页面 DOM、样式、滚动位置、焦点和运行状态。
- 页面读取与操作只能使用 CloakBrowser/Playwright 标准 `Page`、`Locator`、鼠标、键盘、真实滚轮和截图能力。
- 如果标准能力无法完成需求，必须停下来向邓云川确认，不能偷偷改用脚本注入。
- 本规则优先于本文件内其他浏览器自动化说明，适用于 Go、TypeScript Worker 和全部平台流程。

你的对话尽量简洁明了，避免使用复杂的词汇和术语。因为我是代码小白
每次新建接口和方法时 都优先查找是否存在相似的接口或方法

1. 新增代码文件时，必须在文件头部注释这个文件的作用，新增方法时 必须加上标准的注释
2. 请不要直接开始写代码。你需要在理解后告诉我你理解的。待我确认后再开始执行
3. 在下载任何包、库或依赖项之前，请考虑是否需要配置国内代理
4. 当要查看数据库时，请优先使用提供的 mcp 数据库查看工具
5. 我的系统为 MacOS
6. 我的个人信息为：
   - 姓名：邓云川
   - 手机号：17607080935
   - 邮箱：1224299352@qq.com
     注意 你应该考虑 代码结构清晰、复用性、还需要为每个方法加上注释，以便后续维护和理解。

7. 你在开始前 请判断下哪些信息模糊，如果有不清楚的请问我
8. 创建数据库时 每个字段都要有中文备注。
9. 写新功能时 要签出分支。每当完成一个功能时 都需要推送到git仓库、并且合并到主分支
10. 写代码时 一定要考虑到边界情况，不要出现因为是空而报错
11. 样式尽量不要用蓝紫色的渐变色 因为这样很像ai写的
12. 当前项目结构是 go有后端代码，nextjs前端前端代码，然后还有个本地程序的go。和nodejs的浏览器操作工具。本地程序的go仅仅用于走任务流程，nodejs的浏览器操作工具只能执行具体的操作。后端go 只能用来存放数据。还有该系统要适配多个平台。当某一个流程或者功能 你应该确定是平台去实现，还是应该加到主流程的。、

13. 浏览器自动化核心限制：
    - 严禁通过 JS 注入方式控制页面滚动，例如 `window.scrollBy`、`element.scrollBy`、`evaluate(() => scroll...)`。
    - 页面滚动必须模拟真实用户操作，优先使用鼠标移动到目标区域后执行滚轮、拖拽滚动条等方式。
    - 如果需要判断滚动是否生效，可以读取页面状态或截图辅助判断，但不能用 JS 直接推动页面滚动。
    - 这个限制适用于本地程序 Go、Node.js 浏览器操作工具，以及所有平台运行流程。

14. GoodHR 文案风格：
    少一点系统味，多一点打工人味。
    卑微但不低级，搞笑但不影响判断。
    比如 公主请确认是否要删除，而不是是否要删除。
    请用“卑微但靠谱，搞笑但不油腻”的语气生成系统文案。

这个系统是给 HR 和猎头用的招聘自动化工具。
用户大多很忙，所以文案要短、直接、有人味。

语气要求：

- 像一个认真打工的小助手，不像冷冰冰的后台系统。
- 可以轻微自嘲，比如“我先小声提醒一下”“打扰了，我努力长话短说”。
- 可以卑微，但不要太夸张，不要显得廉价。
- 不要使用官方套话。
- 不要使用威胁式表达。
- 不要使用过多感叹号。
- 不要把错误责任甩给用户。
- 遇到失败、空状态、确认操作时，要给用户下一步选择。

常用表达风格：

- “我小声提醒一下……”
- “这个不填也行，只是我后面可能会有点笨。”
- “这一步大概 10 秒，填完我以后少打扰你。”
- “没关系，你可以先跳过，我不记仇。”
- “我没处理成功，但问题不大，我们再来一次。”
- “这里暂时空空的，等你开始使用后我再认真记账。”

避免表达：

- “尊敬的用户”
- “为了提升您的使用体验”
- “请您务必”
- “操作失败，请联系管理员”
- “非法参数”
- “系统异常”

---
> Source: [hhh0078/goodhr](https://github.com/hhh0078/goodhr) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-18 -->
