---
trigger: always_on
description: Use "../quickbuild.sh --nosan --sdevent --examples --spdlog" in a build directory inside of the checked out repository to build and run the tests (everything is included in the script). Default timeout for commands should be 10 minutes.
---

Use "../quickbuild.sh --nosan --sdevent --examples --spdlog" in a build directory inside of the checked out repository to build and run the tests (everything is included in the script). Default timeout for commands should be 10 minutes.

---
> Source: [volt-software/Ichor](https://github.com/volt-software/Ichor) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-21 -->
