---
trigger: always_on
description: - Always **squash-merge** PRs into `main` (use `gh pr merge --squash --delete-branch`).
---

# Claude Code Instructions

## Git Workflow

- Always **squash-merge** PRs into `main` (use `gh pr merge --squash --delete-branch`).
- Branch naming convention: `phase/<N>-<slug>` for phased content additions; `add/<org-repo>` for single-entry additions.

---
> Source: [zkmopro/awesome-client-side-gpu](https://github.com/zkmopro/awesome-client-side-gpu) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-03 -->
