---
trigger: always_on
description: > 项目代号：农智闭环（AgriLoop）
---

# 智慧农业项目统一规则

> 适用目录：`智慧农业/`
> 项目代号：农智闭环（AgriLoop）
> 当前交付周期：15 天
> 规则版本：v1.5（2026-08-27）

## 1. 项目范围（必须遵守）

本项目当前只交付三条软件主线及其联调：

```text
模拟数据传输 -> 智能体决策 -> 可视化呈现/操作
```

本期允许使用：模拟器、情景脚本、MQTT、Redis Streams、PostgreSQL、SSE/WebSocket、MaxKB/RAG、LLM 适配器、Vue/ECharts、虚拟执行器。

本期不承诺、不应在文档或答辩中表述为“已完成”：

- 真实传感器、真实水泵/阀门和现场网关；
- 鸿蒙端、开发板、GPIO、串口和真实设备驱动；
- 真实端-智-云联调；
- 视觉/语音模型的生产级接入；
- 未经本地测试的代码或配置。

真实硬件只能作为后续适配方向说明，不能作为本期验收证据。

## 2. 文档优先级与事实来源

发生冲突时按以下优先级处理：

1. 用户最新明确要求；
2. `01_智慧农业_基本功能清单.md`（基础需求基线）；
3. `02_智慧农业_功能架构.md`、`03_智慧农业_技术架构.md`、`04_智慧农业_大致路线与流程.md`（当前设计基线）；
4. `PROJECT_STATUS.md`（当前进度事实）；
5. `TASKS.md`（任务分工和排期）；
6. 代码、截图、演示录屏和个人口头约定。

修改需求或架构时，必须同步检查受影响的状态和任务；不能只改一份文档。

## 3. 架构不变量

### 3.1 三主线边界

- 数据主线负责生成、传输、校验、去重、质量评分、落库和事件发布。
- 智能体主线负责检索、诊断、试算、建议和受控工具调用。
- 可视化主线负责 REST 查询、实时事件订阅、图表、操作、时间轴和回放。
- 浏览器不直接连接 MQTT，不暴露消息代理凭据。
- 模拟器不能直接写入告警、Agent 结论或最终控制结果。

### 3.2 Crop Pack 规则

- 作物差异通过版本化 Crop Pack 配置注入，不在前端、规则或 Agent 中复制作物分支。
- P0 至少支持 2 个完整 Crop Pack；每个包必须有作物档案、生长阶段、指标、规则、知识、策略、情景和测试。
- 参数解析顺序固定为：系统默认 -> Crop Pack -> 农场 -> 地块。
- 指标使用统一编码，例如 `SOIL_MOISTURE`、`AIR_TEMPERATURE`、`LIGHT`、`CO2`、`PH`、`EC`、`WATER_LEVEL`。
- 每项指标必须声明 `SUPPORTED`、`SIMULATION_ONLY` 或 `UNAVAILABLE`；不可用指标不得由模型猜测。
- 每次建议必须记录 `cropPackVersion`、`ruleVersion`、`knowledgeVersion` 和 `agentVersion`。

### 3.3 核心八项与五项增强能力规则

- `CAP-01`～`CAP-08` 是“看见、理解、行动”的核心闭环能力；`CAP-09`～`CAP-13` 分别补齐“预测、学习、协同、经营、信任”。十三项能力均位于三条主线之上，不得按能力拆服务或复制现有任务、Agent、命令和审计体系。
- 核心闭环固定为：全周期计划 -> 今日农务 -> 传感器/田间核验 -> 根因诊断 -> 结构化处方 -> 安全确认 -> 虚拟执行 -> 效果验证 -> 计划实绩 -> 回放优化；增强能力在同一链路中补入短期预测/就绪度、受控案例学习、资源协同和经营价值，不建立平行闭环。
- 生命周期计划使用 Crop Pack `task_templates`、`crop_batch` 和统一 `work_order`；不得为每种作物建立独立计划模块。
- 今日农务是计划、告警、诊断和设备健康的聚合读模型，不建立第二套任务事实表。
- 人工观察必须保存人员、时间、来源、修订和关联任务，不能覆盖遥测；诊断必须同时展示支持、反对和缺失证据。
- 根因置信度、处方剂量、工作项优先级和效果评分由确定性、可测试逻辑计算；Agent 只能查询、组合、解释和发起受控申请。
- 数据质量/新鲜度不达标或诊断指向传感器漂移时，P0 不得生成可执行灌溉处方；`drought` 与 `sensor-drift` 必须产生可区分且可重复的处理分流。
- P0 农业处方先以扩展后的 `irrigation_plan` 实现；未实际支持通风、施肥等动作前，不建立空泛的通用执行承诺。
- 处方、命令、ACK、执行实际和效果评价必须可关联；成功、部分成功、失败和超时不得混写，评价条件不足时明确标记 `PENDING`、`PARTIAL` 或 `INCONCLUSIVE`。
- P0 最小 What-if 使用同一冻结快照和随机种子的 `EXECUTE/NO_ACTION` 双轨；完整对比报告仍为 P1，任何分支不得回写主状态。
- 15 天范围必须区分“目标架构、P0 必做、P1 增强、P2 演进”；新增五项增强能力进入完整目标设计，但不得在缺少代码和证据时表述为本期已完成。

### 3.4 预测、学习、协同、经营与信任规则

- 风险预测必须保存输入窗口、预测时点、预测范围、算法版本、假设和不确定性；样本、质量或阶段上下文不足时返回 `UNAVAILABLE`，不得由模型补造未来值或伪概率。
- “学习”只使用已完成效果评价且质量合格的案例；反馈先形成可审计的相似案例或 `strategy_candidate`，必须经过离线回放、测试和人工批准后才能发布新策略，禁止生产态静默自改阈值或处方规则。
- 多地块、多人员和多资源调度必须显式声明容量、时间窗、技能、优先级和冲突；任何分配不得超过可用水量、设备能力或人员工时，并需解释未满足需求。
- 经营价值必须区分计划、实际、基线和反事实，并统一标记 `OBSERVED`、`USER_PROVIDED`、`DERIVED`、`SIMULATED` 或 `ESTIMATED`；`OBSERVED` 还必须保存 `sourceMode=SIMULATION|REAL`，本期固定为 `SIMULATION`。没有产量、价格或因果证据时，不得宣称真实收益或避免损失。
- 决策就绪度与诊断置信度是两个概念。就绪度由数据新鲜度、覆盖率、冲突、设备健康、权限和安全门共同决定；状态固定为 `READY`、`NEEDS_EVIDENCE`、`HUMAN_REVIEW` 或 `UNAVAILABLE`。
- 系统发现证据不足时应给出缺失项和最小补证动作，可受控创建巡田、复测或设备检查任务；高风险或低就绪度动作必须拒绝或转人工，不得只显示一个模糊低置信度。

### 3.5 事件和控制规则

- 遥测事件必须带 `eventId`、`farmId`、`plotId`、`deviceId`、指标编码、值、单位、时间戳、质量和 `scenarioId`。
- 控制命令必须经过权限、风险、上限、冷却窗口和审批检查。
- 命令必须带幂等键；重复点击不能产生重复执行。
- 高风险动作必须人工确认；虚拟执行必须返回 ACK、结果和效果数据。
- 规则/安全策略与模型建议冲突时，以确定性策略为准并保存双方输入输出和否决原因；Agent 不得重试绕过。
- 告警必须有状态机、证据和处理人，不能只在页面显示一条临时提示。

## 4. AI、知识和安全规则

- 模型只能调用受控工具集合，工具入参必须经过 JSON Schema 校验。
- 模型不得直接拼接 SQL、MQTT topic、任意 HTTP 请求或发布控制命令。
- RAG 回答必须区分观测值、检索证据、模型建议、用户确认和模拟结果。
- 资料不足时按“地块 -> 地区/环境 -> 阶段 -> 作物 -> 通用知识”回退，并标明证据范围。
- LLM/RAG 不可用时切换到 `rules-only` 或 `mock`，前端必须显示降级状态，不能伪装成模型结果。
- 审计至少保存：原始输入、检索证据、工具输入输出、策略检查、审批人、命令 ACK 和最终结果。
- 决策护照必须能追溯观测、推导、模拟、估算和人工输入各自的来源，展示预测区间、就绪度、适用边界、版本和未满足条件。
- 导出和截图不得泄露真实个人敏感信息；演示数据使用虚拟身份和虚拟地块。

## 5. 开发与文档规范

- 所有 Markdown 使用 UTF-8；标题层级从 `#` 开始，不跳级；流程优先使用 Mermaid 或等宽文本。
- 功能、接口、状态和指标使用稳定编号；删除或改义时保留变更说明。
- 代码和配置中的字段命名优先使用英文统一编码，面向用户的页面文案使用中文。
- 15 天内优先采用模块化单体，禁止为了“看起来先进”引入未经验证的微服务复杂度。
- 每个功能变更必须同时说明：输入、处理、输出、失败/降级路径和验收证据，并区分目标设计、已实现状态与当前演示范围。
- 不把“调用了大模型”单独当作创新；必须能展示证据、工具链、风险和回放。

### 5.1 Git 提交原则

- 只要本轮对话产生了文件改动，必须在交付回复前完成本地 Git 提交。
- 用户明确指定提交备注时使用该备注；未指定时，根据本轮实际改动自动编写简洁、准确的提交说明。
- 默认只创建本地提交；推送远端或合并分支必须由用户明确要求。

### 5.2 Git 版本号规则

- 每次 Git 提交的备注都必须包含本项目版本号，格式为 `V主版本.次版本` 或 `V主版本.次版本.修订版本`。
- 用户明确指定版本号或提交备注时，以用户指定内容为准。
- 用户未明确说明是“大改动”时，一律按小改动处理，在最近一次明确版本的修订版本上递增 `0.0.1`；例如 `V5.7` 的下一次小改动为 `V5.7.1`，`V5.7.1` 的下一次小改动为 `V5.7.2`。
- 只有用户明确说明是“大改动”时，才递增 `0.1`；例如 `V5.7`（或 `V5.7.x`）升级为 `V5.8`，修订版本归零并省略。
- 不得为了补版本号而改写、合并或重置已有提交历史；新规则从当前后续提交开始执行。

## 6. 测试与完成标准

提交或标记“完成”前，至少完成对应层级测试：

1. 单元：风险/根因分、优先级、迟滞、冷却、灌溉处方、效果评价、短期预测、决策就绪度、资源约束、价值口径、权限、Crop Pack Schema；
2. 集成：MQTT -> Streams -> 数据库 -> SSE/WebSocket；
3. 场景：正常、干旱/传感器漂移分流、渐进缺水、暴雨、设备离线和恢复、人工核验一致/冲突、执行非成功、最小双轨、受限水源、重复案例和预测失准；
4. AI：引用、结构化输出、工具参数、越权请求、降级；
5. 回归：从一键启动到决策回放的固定演示脚本。

- 涉及界面的改动，在代码、自动化测试和前端构建全部完成后统一执行一次浏览器功能与视觉验收；可以使用截图，但不要在开发过程中反复截图。验收至少覆盖目标交互、关键页面结构/对齐和控制台健康，并向用户提供本轮功能清单、实际结果及必要截图。
- 浏览器验收优先复用并刷新已经打开的本地页面；只有当前没有本地开发页面时才打开一次，不重复新建标签。
- 打开网页不替代与改动相关的自动化测试和前端构建验证。

“完成”必须同时满足：功能可运行、异常路径有结果、文档已更新、证据可复现。只有写了代码或画了页面，不视为完成。

## 7. 进度更新规则

- 每天收工前更新 `PROJECT_STATUS.md` 的阶段、阻塞项和证据链接。
- 任务开始/完成/阻塞时更新 `TASKS.md`；任务状态只允许 `未开始`、`进行中`、`待验收`、`已完成`、`阻塞`。
- 状态为“已完成”必须写明验收方式；没有证据只能写“待验收”。
- 任何延期必须说明影响、替代方案和新的截止日。
- D5、D11、D14 是强制阶段门；阶段门未通过，不得把后续任务标记为完成。

## 8. 评审前检查清单

```text
[ ] 没有把硬件/鸿蒙/真实端-智-云写成本期已完成
[ ] 基线 B-01 至 B-10 仍可追踪
[ ] 至少两个 Crop Pack 的配置、版本和回归计划明确
[ ] 事件、命令、Agent 输出合同一致
[ ] CAP-01 至 CAP-08 可映射到既有模块、优先级和任务
[ ] CAP-09 至 CAP-13 已映射到既有模块，并清楚区分 P0/P1/P2 和实际实现状态

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [FlowerNeverFade/smart-agriculture](https://github.com/FlowerNeverFade/smart-agriculture) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-28 -->
