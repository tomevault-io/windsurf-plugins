---
trigger: always_on
description: - **Language**: TypeScript (`^5.0.0`)
---

## Development Environment

- **Language**: TypeScript (`^5.0.0`)
- **Framework**: TanStack Start
- **Routing**: TanStack Router (File-based)
- **Styling**: Tailwind CSS
- **Component Library**: shadcn/ui
- **Data Fetching**: React Query (TanStack)
- **Linting**: ESLint with `@typescript-eslint`
- **Formatting**: Prettier
- **Package Manager**: `pnpm` (preferred)

## Command Instructions

You do not need to cd before running commands.

- `pnpm install` - Install dependencies
- `pnpm dev` - Start development server on http://localhost:3000
- `pnpm lint` - Run linting without fixing
- `pnpm fix` - Run all code formatters and linters (ESLint + Prettier)

## shadcn Instructions

Use the latest version of Shadcn to install new components, like this command to add a button component:

```bash
pnpm dlx shadcn@latest add button
```

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/nilsbtr) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:windsurf_rules:2026-04-09 -->
