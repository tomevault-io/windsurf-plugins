---
trigger: always_on
description: <!-- buildr:required begin -->
---

# AGENTS.md

<!-- buildr:required begin -->
# Buildr 工作空间规则

Buildr 产品及其生成的工作空间（Workspace）共同遵守本规则。

## 智能体优先原则

本节指导智能体参与的工作协作；用于产品与架构设计时，适用于引入智能体交付产品结果的部分。软件引入智能体（Agent），参与理解用户目标、判断或执行工作并交付产品结果，就开始向智能体软件（Agentic Software）演进，不要求所有功能同时改造。仅使用智能体开发软件，不自动要求该产品采用智能体优先架构；尚未引入智能体的功能沿用适合自身目标的设计，既有业务规则、安全与授权边界保持有效。

智能体（Agent）是软件能力的主要执行者；人负责目标、约束、判断、授权和结果验收。

### 产品原则

1. 以目标驱动协作。人负责表达目标、约束、判断和授权；智能体在授权边界内理解目标并持续推进。不得把本可由智能体完成的过程重新拆成人工操作步骤。
2. 以真实工作现场为中心。对话用于表达和解释，界面用于认知和直接操作。真实工作现场由代码、文档、数据、环境和外部系统等工作对象的当前事实构成。智能体每次继续工作时，应重新核对当前事实，不能只依赖历史对话。
3. 以产物为核心组织协作。产物（Artifact）包括工作过程中形成或改变的中间成果与最终结果。产品应围绕这些成果组织交互、执行与验收，使其能够被独立查看、修改和接续，不依附于某次对话。不同入口的变化应可被其他参与者发现，人和智能体应依据当前成果继续工作；成果一致不等于目标完成，仍须按目标验收。

### 架构原则

4. 提供确定性能力，开放组合方式。软件能力应明确作用对象及其身份或版本、输入、输出、副作用、授权和失败边界。智能体根据现场动态组合这些能力；重复且稳定的组合再沉淀为确定性工作流（Workflow）。
5. 多种入口共享同一当前事实。人、智能体、界面和接口都可能改变工作现场。所有入口必须对齐同一对象身份和已观察版本；冲突必须显式处理，不得静默覆盖。各入口应能发现相关变化，继续判断或修改前核对当前事实，写入时校验已观察版本；人应能查看同一成果及必要变化，无需阅读完整执行过程。对话会话（Session）不能成为产品状态的唯一权威。不要求所有操作属于同一次执行（Run），也不要求每次变化立即唤醒智能体。
6. 约束局部化，失败影响隔离。每项能力只校验自身正确执行所必需的条件。局部缺陷只能阻止相关动作，不得扩大为整个目标或其他安全工作的阻塞；产品内部错误不得否定已经成立的事实，包括内部登记、派生证据或自动化信心不足，不得否定可从权威来源验证的事实。

### 性能原则

7. 以可信完成目标的效率衡量性能。首先保证目标正确、真实地完成，再减少完成目标所需的时间、步骤、词元（Token）、工具调用、重复尝试和人工介入。新增状态、门禁和流程必须证明其收益大于成本。

## 责任与治理

- Buildr 负责治理长期工作资产与少量确定性边界，不成为另一个智能体或通用许可层。
- 智能体（Agent）向用户说明目标、结果、影响与必要决定，不把 Buildr 内部术语和流程问题转交用户。
- 除非用户明确在开发 Buildr，发布版 Buildr 的用户不承担 Buildr 内部问题的诊断、修复或操作责任；只有涉及用户授权或业务判断时，才向用户请求决定。
- 用户明确指令优先于技能（Skill）的一般做法；业务约束、授权边界与实际权限继续有效。同一任务中已取得的授权，在对象、范围和副作用未改变且未被撤回时继续适用，不因换轮次重复确认。
- 只有继续推进会造成越权、错误对象写入、未经授权的外部或不可逆副作用、证据失真、完成误报或覆盖他人工作时，才关闭式失败。需要决定时，先完成不依赖该决定的已授权工作，再展示具体影响；若技能（Skill）要求暂停，链接对应 `SKILL.md` 并引用适用条款，区分明文要求与自身判断。

## 用户沟通

- 面向用户时，使用直接、简练、易于理解的表达；仅在准确表达所必需时使用专业术语。
- 人和智能体使用一致术语；专业术语每次出现都必须使用“中文（English Term）”，无稳定中文译名时使用“中文释义（English Term）”，不得单独使用中文或英文专业术语。
- 命令、代码标识、字段名、接口名、文件路径、错误原文和产品专名必须精确对应实现；作为专业概念说明时补充中文名称或中文释义。
- 输出环境支持时，关系、时序、分支或状态转换用文字不易准确理解，就使用 Mermaid，并用一句话说明结论；简单线性内容使用文字或表格。
- 完成当前工作、到达阶段节点或遇到阻塞时，说明明确、可执行的下一步；任务已经完整结束时明确说明完成，不机械追加无关建议。

## 工作资产职责

- 当前作用域（scope）继承组织根（Organization/Root）→ 项目（Project）→ 服务（Service）的 `AGENTS.md`：组织根承载组织资产；项目表示业务、产品线、系统或长期工作单元；服务表示代码仓、应用、模块或可执行资产。
- Buildr 管理长期源资产和可重建的运行时投射；运行时、本机状态、凭证、临时提示词（prompt）和一次性聊天不是源资产。
- 规则（Rule）只承载价值观、权威与授权边界、约束和结果不变量；组织根、项目或服务的 `AGENTS.md` 只能增加当前作用域的这些内容。
- 规则不承担技能路由、命令序列、生命周期、恢复或专业状态；可以命名唯一责任主体（owner）和禁止绕过的不变量，但不得复制流程或当前状态。
- 技能说明（Skill description）发现用户意图，技能正文（Skill body）承担任务流程，能力绑定（capability binding）选择提供者（provider），项目声明（Project declaration）说明适用性与证明范围，技能或应用（Application）维护状态与结果（Result）；规则不成为第二权威。
- 读取 `rules/manifest.yml`；启用、必需且已安装的规则必须读取；可选且已安装的规则只在语义相关时读取；禁用或卸载的规则不生效。智能体运行时（Agent runtime）只负责发现和投射，不替智能体判断语义。

## 不可绕过边界

- 智能体必须遵守当前作用域已选择且可用的能力提供者，不得猜测或绕过能力绑定。
- 组织资产、项目资产和服务代码仓的 Git 边界必须按实际仓库判断，不得凭目录名假设。
- Buildr 受管区块（Managed Block）内联核心规则，是工作空间必读入口；专业规则继续通过 `rules/manifest.yml` 登记并按需引用。
- Buildr 受管区块由 Buildr 管理，不得手工改写；损坏时由 Buildr 恢复，区块外用户内容必须保留。
<!-- buildr:required end -->

面向用户的回复和文档默认使用中文；术语表达遵循上方核心规则。

## Buildr 自举原则

本 workspace 是 Buildr 用来开发 Buildr 自身的自举 workspace。PATH 中的默认 `buildr` 属于 npm installation，不得由 development checkout 创建、覆盖或要求绑定当前源码。正式自举激活成功时，必须以 Environment retained Node 显式验证本次 delivered retained checkout 的 `projects/product/buildr`，且最终 workspace Doctor 必须 ready。正式 sync、Buildr Web Dev 安装、development entry identity 检查与最终 Doctor 或 Finish resume 只由 `buildr-self-bootstrap-sync` Skill 的唯一 runner 编排；Agent 不得自行拆分、补跑或替代其中步骤。

- Buildr 产品治理事实只在 `projects/product/` 维护；可执行产品实现分属两个 Service：`projects/product/services/buildr/` 负责 npm package、CLI、本机应用 HTTP/运行时（runtime）、`web-dist` 托管与打包，`projects/product/services/buildr-web/` 负责 Buildr Web React/Vite 权威前端源码与正式构建。当前 workspace 中由 Buildr 交付的资产只能通过当前 Product checkout 的 `update` / `sync` 更新，不直接编辑。

开发阶段执行 Buildr 命令时，使用本 workspace 内的产品 CLI 入口 `projects/product/buildr`，不要依赖本机 PATH 上安装的 `buildr`。

---
> Source: [BuildrAI/Buildr](https://github.com/BuildrAI/Buildr) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-08 -->
