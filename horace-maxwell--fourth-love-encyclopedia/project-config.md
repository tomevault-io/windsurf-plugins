---
trigger: always_on
description: 给接手会话/agent 的定向文档：规约回答「内容该长什么样」,本文件回答「怎么安全地动这个仓库」。
---

# 第四爱百科 · 维护手册(harness)

给接手会话/agent 的定向文档：规约回答「内容该长什么样」,本文件回答「怎么安全地动这个仓库」。
规约已写的这里只给链接不复述。同一件事写两处，必然漂移。

## 0. 先读什么

- 写内容 → [00-编纂规约/02-条目写作模板](00-编纂规约/02-条目写作模板.md) + [05-去AI化写作铁律](00-编纂规约/05-去AI化写作铁律.md)
- 动流程/发布 → [00-编纂规约/07-工作流程](00-编纂规约/07-工作流程.md)，顺序不变量、子代理协作五条纪律，均见于此。
- 碰尺度/引用 → [00-编纂规约/03-版权与内容红线](00-编纂规约/03-版权与内容红线.md) + [01-分级与预警体系](00-编纂规约/01-分级与预警体系.md)
- 优先级撞车时：**内容红线 > 分级与预警 > 去AI化铁律**（铁律 §四之二）

## 1. 仓库边界

- 报告、清单、临时脚本这些会话产物不能落进库内，一进来就会改变篇数，被 F/G/I/J 四类检查扫到，还会进公开仓库
- `10-藏书索引/`（私有，不入库）跟 `10-藏书索引-公开/`（入库）**是两个目录**，检查器和 preflight 靠目录名精确匹配来区分，**别重命名任何一个**
- fresh clone 上没有语料，也没有 `10-藏书索引/`，`sync_index` 会自动跳过派生统计自查。语料本身放在库外同级目录，本机私有
- 进仓库什么以 `.gitignore` 为准，具体理由见 [06-发布风险评估](00-编纂规约/06-发布风险评估.md)

## 2. 命令代价表

| 命令 | 读写 | 代价 | 什么时候跑 |
|---|---|---|---|
| `python3 _脚本/run.py doctor` | 只读 | 秒级 | 进场先体检 |
| `python3 _脚本/run.py check` | 只读 | 秒-分钟 | **写完任何内容** |
| `python3 _脚本/sync_index.py --check` | 只读 | 秒级 | 引用或修改数字前后 |
| `python3 _脚本/run.py rebuild` | **写**:00-总表、情景卡 JSON、全库数字、三种导出 | 分钟 | 改了正文之后 |
| `python3 _脚本/run.py mine` | **写**:10-藏书索引 + 13-语料挖掘 | **约 50 分钟**,需本机语料 | 只在加了新语料后，先问用户 |
| 单跑 `aggregate_corpus.py` / `render_*.py` | 写(同上两卷) | 秒级，只需本地 JSON | 改了渲染种子后 |
| `python3 _脚本/run.py publish` | 写+检查 | 分钟 | 推送前(= rebuild + check + preflight) |

## 3. 真值从哪来(想引用一个数，先跑命令，不默写)

| 要引什么 | 权威来源 |
|---|---|
| 篇数/汉字/表格数/术语总数/卡张数/桥段条数/套路数 | `sync_index.py --check` 的「当期真值」块 |
| HTML 内嵌篇数 | `index.html` 首行 `<!--gb-html-docs:N-->` |
| 桥段条数(总表口径) | `14-桥段库/00-总表.md` 首行 `<!--gb-vignette:N-->` |
| 语料口径按篇数/字数/独立作品 | `from aggregate_corpus import CANON`，别用文档里的旧数。两套口径之分见 [10-藏书索引-公开](10-藏书索引-公开/) |
| 节拍族数/共现门槛 | `13-语料挖掘/07-桥段分布.md` 卷首 + `mine_vignette.py` 的 MIN_SCENE |
| 缺口清单 | `python3 _脚本/find_gaps.py` 查词条级，`--vignette` 查桥段级 |

**手写数字是这个库的头号病，审计 24 条必修里 11 条是它。新写正文别放全库统计数，非放不可就先跑 `sync_index --check` 抄真值，它的 suspects 会终身盯防你写下的那处。**

## 4. 改动影响面清单(做 X 要同步动 Y)

| 改动 | 必须同步 |
|---|---|
| 新增一篇(任何卷) | INDEX.md 该卷表格加行 → `run.py rebuild`(篇数、HTML 自动归一) |
| 新增 14 卷桥段族 | 文件名编号顺延，条目标题必须 `## 一 · 名`(带「 · 」)、分级行 `` `T` ｜``、`**骨架**｜` 行——**总表生成器与 L 项按这些正则抽取**;写完 rebuild,总表/索引/条数全自动 |
| 桥段条目改名 | `build_vignette_index.py` 的 MOOD/TROPE/PERSONA 字典若引用了旧名，重跑时会打印「查无条目」警告——照警告改字典 |
| 加/改情景卡 | **只改 `_脚本/scene_cards_data/` 的数据模块**,然后 rebuild;JSON 是生成物，手改必被覆盖 |
| 批量生成情景卡 | 叙述层用第三人称(438 张老卡里只有 1 张用「我」);AI 扮女主的卡里，演的那个角色是「她」——转人称时模型会统一写成「他」,26 张卡因此通篇两个人都是他，而检查器不查这个，得自己比对 dir 字段 |
| 加角色卡 | `11-模板包/角色卡/` 落 JSON,跑 check(K 项管字段/体量/安全框架/元评论) |
| 新增整卷目录 | `build_exports.py` 的卷配置 + INDEX.md + 决定 `check_compliance.SKIP_DIRS` 收不收，每项要写理由注释 |
| 改 README/INDEX 版式 | **先查 `sync_index.py` 是不是靠那个格式定位的**，徽章行、`> 最后更新` 等都是锚点。改版式导致锚点失配=数字从此静默漂移，README 徽章漂过一次已修 |
| 改渲染脚本的中文字符串 | 跑 check(M 项扫 `_脚本/*.py` 的种子);清洗正则类的「药」加行尾 `# 例外:M` |
| 语料增删 | `run.py mine` 全套 → rebuild → `sync_index --check` 报出的派生统计漂移逐一修 |
| 加新检查规则 | 先想豁免面，引文/黑话/精读笔记「本篇」/「全书」指作品，FAIL 与 WARN 分开，警告不进返回码 |

## 5. 检查器速览(A–M,细节看 `check_compliance.py` 的 docstring 与行内注释)

三行头(A)/分级就高(B)/安全框架含 E 级(C)/写崩标题(D)/来源声明·警告级(E)/链接(F)/双链(G)/引用配额(H)/成句引用(I)/去AI化七条(J)/卡片与世界书(K)/桥段六栏目(L)/渲染种子(M)。
**警告通道不挡提交**;FAIL 才挡。行内豁免：该行加 `<!-- 例外:规则号 理由 -->`,报告末尾会列出全库豁免；省着用。
既定豁免与理由：引号内引文一字不动(红线 1.3);精读笔记「本篇」指被拆作品;「全书/全文+最」指被分析作品;`CLAUDE.md` 本身是提示词层，铁律 §文档≠提示词,J 项不扫。
逐部精读笔记(原 09 卷)已下架：本地保留、`.gitignore` 排除、不入库(理由见红线 1.4)。
H 项与 09 卷专属豁免(`_SELF_CARD`/`META_CARD`)都带存在性守卫，目录不在就跳过,fresh clone 上照常全绿。

## 5.5 去 AI 味：三层判据

规约 [05-去AI化写作铁律](00-编纂规约/05-去AI化写作铁律.md) 分两节写标准，第六节是统计层。
机器判据在 `_脚本/ai_taste.py`，检查器的 **N 项**，FAIL 级。

| 用法 | 命令 |
|---|---|
| 查全库 | `python3 _脚本/ai_taste.py --lib` |
| 查几篇 | `python3 _脚本/ai_taste.py <文件或目录>` |
| 查卡的开场白 | `python3 _脚本/ai_taste.py --cards` |
| 查库外的稿子 | `python3 _脚本/ai_taste.py --narrative 稿子.txt`(也收 .txt、也读管道) |
| 重算真人基线 | `python3 _脚本/ai_taste.py --baseline` |

阈值全部来自 100 篇真人小说实测，不是拍脑袋。**改阈值之前先去语料里量一遍。**

给创作者用的三份成品在 [11-模板包/去AI味/](11-模板包/去AI味/):写作预设(1,600 字，可直接粘)、
SillyTavern 世界书、自查清单。644 张卡的 system_prompt 由 `card_format.with_deai()` 统一注入,
只有一份来源。

**判据本身出过十来次错**,全部记在规约第六节。最贵的一次：剥 YAML 的正则没锚定开头,
吞掉正文整段，导致一整轮改写的验收标准失效。教训写在那一节末尾——
凡是靠工具验收的批量作业，先拿一份已知有病的样本喂给工具，确认它真的报得出来。

**批量改文风之前，先认清哪些字是机器在读的**:判据条的豁免标记 `<!-- gb-nonscene -->`、
安全声明里的「幻想装置／不是现实指南／SSC/RACK/PRICK」、三行头、分级行 `` `T` ｜ ``、
`**骨架**｜`、`#### 六栏目` 的字面、角色卡 description 开头的「扮演方向：」声明行。这些是接口不是文风——改一个近义词检查器就报错,
而报出来的错和你改的地方对不上号。改写脚本要把它们整块跳过。

引文剥离在 `_脚本/quote_strip.py`,两个检查器共用。它认嵌套引号
(「…『…』…」),原先各写各的正则会在嵌套处提前截断，把引文碎片当成本库的机器腔。

## 6. 提交纪律

- **用户明确说「推」才 commit/push**，「推送了嘛？」是问句。每轮默认只留工作树改动
- 推送前 `run.py publish`，pre-commit 三道闸（preflight → sync_index --check → check_compliance）会自动跑
- 提交信息：中文动宾短语一行，说做了什么（照 `git log` 风格）。`--no-verify` 仅限钩子自身故障，理由写进提交信息
- 交付纪律（[红线第五节](00-编纂规约/03-版权与内容红线.md)）：JSON 实测能导入、HTML 浏览器打开过、统计结论抽样核验过、自撰示例回搜过。**不交「应该能用」的东西**

## 6.5 语域：说人话之上还有一层

铁律第七条是 2026-08 那轮实测出来的：**书面语的标点手势**。
靶子取自库内引用的 276 条社群原话与藏书里随机 85 篇真人小说，两边一起量：

| | 本库改前 | 社群原话 | 真人小说 P90 |
|---|---:|---:|---:|
| 括号／千字 | 7.6 | 0.3 | 0.67 |
| 分号／千字 | 1.4 | 0.3 | 0.34 |

分号是公文标点，一律改句号或逗号。括号分两种，机器判据 `长括号/千字` 只查后一种：
名词短语的术语注解放行（`pegging（女方进入的核心行为）`），带主谓的行文旁白要查
（`（这已经是今晚第三次了，她自己都数不清）`）。引注、交叉引用、来源声明、分级说明整行不查。

**每收窄一格都回头拿故意写坏的样本复验过**，这条判据前后收窄过五次。
一份全是术语注解与引注的样本量出 0，另一份四段带括号旁白的样本量出 34／千字。
「让库通过」会在某一步悄悄替换掉「把库改好」，而且换得无声无息——不这么复验的话。

## 6.6 别拿模型判「像不像 AI 写的」

2026-08 实测否掉了这条路，细节在 [铁律第六节](00-编纂规约/05-去AI化写作铁律.md)。
绝对判断：真人 40% 被冤枉、AI 60% 混过去。两两对比：15 对挑对 0 对，
左右换位答案就翻。**分不开。**


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [Horace-Maxwell/fourth-love-encyclopedia](https://github.com/Horace-Maxwell/fourth-love-encyclopedia) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-19 -->
