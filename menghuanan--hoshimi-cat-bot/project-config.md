---
trigger: always_on
description: 每次进入本仓库执行任何任务前，必须先读取本文件，并在整个任务过程中严格遵守以下规则。
---

# Codex 执行规范

本文件适用于整个仓库。

## 读取要求

每次进入本仓库执行任何任务前，必须先读取本文件，并在整个任务过程中严格遵守以下规则。

## 文档阅读引导

本文件是仓库级主入口，优先级高于 `docs/AGENTS.md`。阅读者必须先确认并遵守本文件的强制规则，再按任务类型决定是否继续阅读 `docs/AGENTS.md` 及其下级文档。

如果本次任务涉及以下任一动作，必须继续阅读 `docs/AGENTS.md`：

1. 修改、新增或审查任何源码、测试、构建脚本、Docker、CI、配置或运行脚本。
2. 修改、新增或审查 `docs` 目录中的项目规范、架构、模块、运维、开发流程或上下文文档。
3. 调查 bug、性能问题、资源泄漏、平台适配、Skia 绘图、Tasker 生命周期、配置迁移或部署问题。
4. 需要判断某个变更是否违反项目红线、系统不变量、层边界或模块职责。
5. 需要为后续开发者或 AI 建立执行计划、变更说明、维护规范或长期上下文。

如果本次任务只是读取本文件、回答与仓库无关的普通问题，且不查看或改写项目文件，可以不继续阅读 `docs/AGENTS.md`。一旦任务范围扩大到项目文件或项目行为，必须立即回到上述规则并继续阅读。

## 强制规则

1. 查看文件、改写文件、新建文件时，必须使用 UTF-8 编码。
2. 如果所用工具默认不是 UTF-8，必须先显式切换到 UTF-8，或在命令中明确指定 UTF-8 编码后再操作。
3. 不得删除、减少、绕过或覆盖仓库中已有的注释，尤其不得删除现有注释。
4. 每新增一个函数、方法或新的代码逻辑块时，必须补充紧邻代码的注释，说明其用途、意图或关键约束。
5. 在修改代码时，必须优先保留并延续现有注释风格，新增注释不得写成空泛描述。
6. 修改已有函数签名（包括增删参数、修改返回类型）时，必须同步更新对应的 KDoc，确保文档与声明保持一致。
7. 引入新的设计模式或架构层时，必须在该层的入口类或接口上补充紧邻注释，说明架构意图、职责边界或关键约束。
8. 不得将项目内的文档，或文档中的部分字段、片段、内容，添加到 `src\test` 测试模块中。
9. 除非用户在当前对话中明确要求、所使用的 skill 明确要求查看文档，或本文件“文档阅读引导”已触发必须阅读 `docs/AGENTS.md`，否则不得主动读取 `docs` 目录中的任意文档文件。

## 执行说明

如果以上规则与临时实现习惯冲突，以本文件要求为准；除非用户在当前对话中明确要求例外处理。

## 项目文档体系现状

当前仓库的长期维护文档以 `docs/AGENTS.md` 为项目级索引，覆盖 Kotlin/JVM 主程序、OneBot11/NapCat/LlBot/QQ 官方平台适配、Skia/Skiko 绘图、Tasker 生命周期、WebUI 服务端、React WebUI 前端、构建发布和运维监控等子系统。

维护或补齐文档时，必须以当前源码入口为依据：后端源码位于 `src/main/kotlin/top/bilibili`，前端源码位于 `webui-frontend/src`，运行资源位于 `src/main/resources`，测试入口位于 `src/test/kotlin` 与 `webui-frontend` 的 Vitest/Playwright 测试。`docs/plans`、`docs/release`、`docs/规则` 和 `docs/过期文档` 只能按 `docs/AGENTS.md` 的分区边界使用，不得覆盖当前维护文档。

---
> Source: [menghuanan/Hoshimi-Cat-Bot](https://github.com/menghuanan/Hoshimi-Cat-Bot) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-06-17 -->
