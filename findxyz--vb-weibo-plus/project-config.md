---
trigger: always_on
description: **不要想当然。不要掩盖疑惑。明确说明权衡。**
---

## 1. 编码前先思考

**不要想当然。不要掩盖疑惑。明确说明权衡。**

在开始实现之前：

- 明确写出你的假设。如果不确定，就提问。
- 如果需求存在多种理解方式，全部列出来——不要默默自行选择。
- 如果有更简单的实现方案，要主动指出。
- 如果某些地方不清晰，就暂停。明确说出哪里不清楚，并提出问题。

------

## 2. 简单优先

**只写解决问题所需的最少代码。不要做额外设计。**

- 不添加未被要求的功能。
- 不为一次性代码做抽象。
- 不为了“未来灵活性”或“可配置性”提前设计。
- 不为不可能发生的情况编写错误处理。
- 如果你写了 200 行，而其实 50 行就够，重写它。

时刻问自己：

> “一个资深工程师会觉得这实现过度复杂吗？”

如果答案是“会”，就继续简化。

------

## 3. 外科手术式修改

**只改必须修改的部分。只清理自己造成的问题。**

修改已有代码时：

- 不要顺手“优化”旁边的代码、注释或格式。
- 不要重构没有问题的代码。
- 遵循现有代码风格，即使你个人不喜欢。
- 如果发现无关的死代码，可以提一下，但不要直接删除。

如果你的修改导致某些代码变成无用：

- 删除因为你的修改而产生的无用 import、变量、函数。
- 不要删除本来就存在的死代码，除非明确要求。

判断标准：

> 每一行修改，都应该能直接对应用户需求。

------

## 4. 以目标驱动执行

**先定义“成功标准”，然后循环验证直到达成。**

把任务转换成可验证的目标：

- “增加校验”
  → “先写非法输入测试，再让测试通过”
- “修复 bug”
  → “先写能复现问题的测试，再修复它”
- “重构 X”
  → “确保重构前后测试都通过”

对于多步骤任务，先给出简短计划：

1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]

强成功标准意味着：

- 你可以独立推进并持续验证。

弱成功标准（例如“把它弄好”）意味着：

- 你会不断需要澄清需求。

---

## 5. 格式规范

- 所有注释、打印输出、日志内容，必须遵循以下规则：

  - **标点符号**：有中文时，全部使用中文标点（如逗号“，”、句号“。”、冒号“：”、引号““ ””、括号“（）”等）；若内容为纯英文，则使用英文标点。
  - **中英文及数字间距**：中文与英文、中文与数字、英文与数字之间，必须加一个半角空格。
  - **日期格式**：日期中的连字符“-”前后不加空格，按标准格式书写。

  **正例**：

  - 知识库检索请求：query = {}，index = {}（有中文时标点均为中文，大括号内为占位符，与中文之间不加空格）
  - 创建百炼 Client 失败：credential =（中英文之间加空格，但中文符号内部不加）
  - 今天是 2026-06-26，今天天气不错。（中文与数字间加空格，逗号、句号为中文）
  - Today is 2026-06-26（纯英文，使用英文标点，英文与数字间加空格）

  **反例**：

  - 知识库检索失败,请稍后重试（有中文却用了英文逗号）
  - 知识库名称(可选)。不传则检索默认知识库。（有中文却用了英文括号）
  - 价格为6.3元,不便宜.（有中文标点未用中文，且数字与中文间无空格）
  - 当出现error后,抛出异常（中英文间没有空格，且逗号应为中文）

- 所有 `@Test` 方法统一使用 snake_case，例如 `list_returns_200`；测试辅助方法仍使用普通 Java camelCase 命名。
- 如非类名冲突，类的使用过程中不要出现全路径包名的类。

## Agent skills

### Issue tracker

Issues and PRDs are tracked as local Markdown files under `.scratch/`; external PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the five default triage label strings. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. See `docs/agents/domain.md`.

---
> Source: [findxyz/vb-weibo-plus](https://github.com/findxyz/vb-weibo-plus) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-06 -->
