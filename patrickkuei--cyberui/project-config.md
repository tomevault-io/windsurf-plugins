---
trigger: always_on
description: Claude Code configuration for **cyberui-2045** — a cyberpunk-themed React UI component library.
---

# CLAUDE.md — CyberUI

Claude Code configuration for **cyberui-2045** — a cyberpunk-themed React UI component library.

> For full agent guidance see [AGENT.md](./AGENT.md).

## Project at a Glance

- **npm package**: `cyberui-2045` (also called "CyberUI")
- **Stack**: React 19, TypeScript 5 (strict), Vite 7, Tailwind v4, Storybook 9, Vitest
- **Library entry**: `src/index.ts` → re-exports everything from `src/components/index.ts`
- **Public surface**: components in `src/components/`, hooks in `src/hooks/`, context in `src/contexts/`

## Commands

```bash
npm run dev           # Demo app (Vite dev server)
npm run storybook     # Storybook on :6006
npm run lint          # ESLint
npm run type-check    # tsc --noEmit (fast, no bundle)
npm run build         # Library build (typecheck + bundle → dist/)
npm test              # All tests (unit + Storybook/Playwright)
```

> `npm run build` runs in `--mode library` — it bundles to `dist/` and generates `.d.ts` files.
> Unit tests use jsdom; Storybook tests use Playwright/Chromium headless.

## Component Inventory (v2.2.0)

| Category    | Components |
|-------------|-----------|
| Forms       | Button, Input, Select, Toggle, Checkbox |
| Layout      | Card, Modal, Divider |
| Feedback    | Notification, Badge, Skeleton |
| Progress    | CircularProgress, LinearProgress, SegmentedProgress |
| Navigation  | TabNavigation, Carousel, Steps |
| Typography  | GradientText, SectionTitle |
| Display     | Timeline |
| Media       | Image |
| Hooks       | useCyberScrollbar, useCyberNotifications, useAnimatedProgress |
| Context     | CyberNotificationProvider |

## Key Conventions

- **Add component**: `src/components/ComponentName.tsx` → export from `src/components/index.ts` → add `*.stories.tsx`
- **Styling**: Tailwind v4 classes + CSS variables only; no new style systems
- **Responsiveness**: use `ResponsiveValue` / helpers from `src/utils/responsive.ts`
- **Types**: strict, no `any`, no `@ts-ignore` without justification
- **Tests**: `*.test.tsx` for unit (jsdom), Storybook stories auto-run via `@storybook/addon-vitest`
- **No new runtime deps** without approval

## Slash Commands

Custom commands live in `.claude/commands/`. Invoke them with `/command-name [args]`.

| Command | Usage | What it does |
|---------|-------|--------------|
| `/new-component` | `/new-component Tooltip` | Full scaffold: component + stories + tests + index export |
| `/new-hook` | `/new-hook useCyberTooltip` | Full scaffold: hook + types + stories + index export |
| `/component-review` | `/component-review Button` | Audit a component against all quality gates (TS, a11y, responsive, stories, tests) |
| `/validate` | `/validate` | Run lint → type-check → unit tests; report each step |
| `/release` | `/release 1.4.0` | Bump version, move CHANGELOG entries, build, pack dry-run, then commit + tag on confirm |
| `/changelog` | `/changelog added Tooltip component` | Add a typed entry to the `[Unreleased]` CHANGELOG section |

## Rules (enforced automatically)

Key rules from `.claude/settings.json` — Claude Code respects these in every session:

- Never install runtime deps without approval
- Never edit `dist/` directly (generated by build)
- Always verify new components are exported from `src/components/index.ts`
- All interactive components require keyboard handlers + ARIA attributes
- Styling: Tailwind v4 + CSS variable tokens only
- Responsive props must use `ResponsiveValue<T>` from `src/utils/responsive.ts`
- Run `lint && type-check` after every component/hook change
- When modifying an existing component: update its `*.stories.tsx` and `*.test.tsx` to cover the changed/new behaviour
- When adding or modifying a component's public API (props, variants, hooks): update `AGENT.md`'s component table and the matching entry in `bin/usage-content.js`
- Stories are required — Default + variants + sizes + disabled + AllVariants render story
- Use cyberpunk-flavored copy in stories (not generic placeholder text)

## Validation Before Every PR

```bash
npm run lint && npm run type-check && npm test -- --project=unit
```

---
> Source: [patrickkuei/CyberUI](https://github.com/patrickkuei/CyberUI) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-05-07 -->
