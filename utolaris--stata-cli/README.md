# AI instruction files for Stata-CLI

> Sourced from [Utolaris/Stata-CLI](https://github.com/Utolaris/Stata-CLI), graded against the public Tome Standard and kept consistent across every major platform by [TomeVault](https://tomevault.io)

Stata CLI for AI agents and coding agents (Codex, Claude Code, Cursor, OpenCode). Run Stata 18/19/StataNow locally, execute .do files, inspect .dta data, and return structured JSON via a native Rust engine — no Python or PyStata required.    开箱即用的AI原生Stata命令行工具，附带完整skill，支持最新版stata。完全由Rust构建，无需Python依赖。

## Windsurf Config

The `project-config.md` file in this directory is the project config converted for Windsurf.
Original source: `AGENTS.md` in [Utolaris/Stata-CLI](https://github.com/Utolaris/Stata-CLI).

## Also available for

- **Claude Code** — `CLAUDE.md`
- **GitHub Copilot** — `copilot-instructions.md`
- **Cursor** — `project-config.mdc`
- **Gemini CLI** — `GEMINI.md`
- **Windsurf** — `project-config.md`

Graded against the Tome Standard, TomeVault's public ruleset for whether AI instructions load, read clearly and stay safe. Source: [github.com/Utolaris/Stata-CLI](https://github.com/Utolaris/Stata-CLI)

---

Own this repo? Install the TomeVault Relay to keep every platform's copy in sync on every push: [https://tomevault.io/install](https://tomevault.io/install).

<!-- genome:a-c-q -->
