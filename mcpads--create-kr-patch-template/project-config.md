---
trigger: always_on
description: 기존 빌드·테스트·문서·자산 흐름을 존중한다. 아래는 새 프로젝트의 기본 소유 위치다. 같은 역할의 체계가 있으면 실제 위치로 안내를 바꾸고 각 사실을 한곳에서 관리한다. 필요한 문서만 현재 작업에 맞게 읽는다.
---

# Repository Guidelines

## 프로젝트에 적용

기존 빌드·테스트·문서·자산 흐름을 존중한다. 아래는 새 프로젝트의 기본 소유 위치다. 같은 역할의 체계가 있으면 실제 위치로 안내를 바꾸고 각 사실을 한곳에서 관리한다. 필요한 문서만 현재 작업에 맞게 읽는다.

| 위치 | 맡는 내용 |
|---|---|
| [docs/status.md](docs/status.md) | 현재 범위, 통합 상태, 미해결 조건과 다음 판단 |
| [docs/decisions.md](docs/decisions.md) | 사람의 제품 방향·품질·지원·투자 결정 |
| [config/](config/) | 채택한 대상 명세와 제품 설정 |
| [research/](research/) | 관찰, 가설, 판별 실험과 채택 근거 |
| [assets/translation/](assets/translation/) | 번역 입력, 검토 상태와 릴리스 승인 |
| [assets/fonts/](assets/fonts/) | 글꼴 원천, 변환과 문자 매핑 |
| [adapters/emucap/](adapters/emucap/) | emucap을 선택한 경우의 실행 증거 연결 |
| [adapters/retro-typed-isa/](adapters/retro-typed-isa/) | Rust 기계어 검증 연결 |

배포할 권한이 없는 원본·제3자 자산과 비공개 실행 자료는 로컬 입력·출력 경로에 두고 `.gitignore`로 제외한다. 공개할 자료는 배포 권한과 포함 내용을 확인한다.

## 작업 판단

선언한 제품 범위가 하나의 플레이 경험에서 함께 성립하도록 채택한 변경을 주 빌드에 통합한다. 완성을 가장 크게 좌우하는 미해결 조건에서 다음 작업을 고르고, 도구·언어·조사 순서는 판별할 질문에 맞춘다. 관측이 설명과 어긋나거나 반복 작업이 새 정보를 주지 않으면 기준선과 가설을 재검토한다.

사람은 제품 가치와 중요한 절충을 결정하고, 에이전트는 기술 근거와 선택지를 밝히며 채택한 의도를 구현한다. 알려진 범위 내 결함은 수정하거나 근거를 갖춰 범위·주장 변경을 논의한다. 해결되지 않은 결함은 릴리스 판단에 남긴다.

## 제품 빌드의 검증 조건

제품 빌드는 식별한 불변 원본, 추적 가능한 제품 입력과 [채택 명세](config/README.md)로 산출물을 재현한다. 다음 조건을 빌드에서 검사하고, 실패하면 정상 산출물로 내보내지 않는다.

- 원본의 크기와 강한 해시가 선언한 지원 원본과 일치한다.
- 모든 최종 변경은 Expected Write 또는 동등한 쓰기 계획에 등록되며, 원본 byte·범위·보호 영역·최종 크기와 전체 diff가 검증된다.
- 의도적으로 겹치는 변경은 하나의 결과로 합성하고, 각 변경 byte의 소유자가 명확하다.
- 기계어 변경은 기계어로 분류하며, 필요한 어셈블리 원천과 대상 ISA 프로필의 조립·연속 해석·재조립 검산을 갖춘다.
- 무변경을 선언한 경계는 전체 byte가 일치한다.

최종 쓰기 계획 전체를 불변 원본에 대조해 검증한 뒤 적용한다. 한 논리적 변경의 쓰기는 모두 성공할 때 함께 적용한다. 부분 실패가 정상 결과로 남지 않도록 출력도 함께 관리한다. 언어 중립 사례는 [reference/conformance/](reference/conformance/)에 있다.

## 증거와 완료 범위

번역·자산·배치에 따라 바뀌는 값은 빌드가 현재 입력에서 계산한다. 테스트는 통제된 입력으로 안정된 동작과 재현된 실패를 확인하고, 산출물 검증은 정확한 결과 파일을, 실행 검증은 그 파일의 실제 소비를 확인한다.

자동 판정은 확정된 기계적 조건에 적용한다. 언어 휴리스틱은 검토 후보를 찾는 데 쓰고, 의미·말투·가독성과 시각 품질은 사람 검토로 판단한다. 미완료 번역이 있는 개발 빌드와 릴리스 후보의 조건은 [번역 자산 규약](assets/translation/README.md)을 따른다.

선택 가능한 Rust 참고 구현의 검증 명령은 [README.md](README.md#참고-구현-확인)에 있다.

---
> Source: [mcpads/create-kr-patch-template](https://github.com/mcpads/create-kr-patch-template) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-09-23 -->
