---
trigger: always_on
description: Use `make test ARGS="<pytest args>"` to run tests, e.g. `make test ARGS="./tests/metrics/deepeval/"` to run only DeepEval metrics tests.
---

# Development tools

Use `make test ARGS="<pytest args>"` to run tests, e.g. `make test ARGS="./tests/metrics/deepeval/"` to run only DeepEval metrics tests.
Use `make lint` to run linters and `make format` to run code formatters.

If you need to read the code of a library already used in the project, prefer to look for it in .venv/Lib/site-packages.

---
> Source: [epam/ai-dial-admin-evaluation-metrics](https://github.com/epam/ai-dial-admin-evaluation-metrics) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-24 -->
