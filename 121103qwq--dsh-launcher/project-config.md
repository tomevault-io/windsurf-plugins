---
trigger: always_on
description: 本文件约束 AI Agent 在 DSH Launcher 仓库中的开发行为。
---

# DSH Launcher Agent Rules

本文件约束 AI Agent 在 DSH Launcher 仓库中的开发行为。

目标：**准确完成用户要求，以最小必要改动推进项目，不擅自扩大范围。**

## 子 agent

- 任务可以拆分且并行有明确收益时，在开始阶段调用子 agent；简单任务直接完成，不必调用子 agent。
- 普通独立子任务只允许使用 `gpt-5.6-luna`，并将推理强度显式设置为 `xhigh`。
- 只有当任务确实需要多个子 agent 彼此通信、分工协调或汇总协调时，才允许使用 `gpt-5.6-terra`，并将推理强度显式设置为 `high`；不得将 Terra 用于普通独立子任务。
- 允许使用已配置为 Luna `xhigh` 的 `luna_worker` 预设处理普通独立子任务。不得使用其他模型或推理强度组合；如果当前运行环境无法指定上述允许的组合，不调用其他子 agent 作为替代，由主 agent 完成或明确报告限制。

## 1. 优先级

发生冲突时按以下顺序处理：

1. 用户最新明确要求
2. 当前仓库实际代码和实际行为
3. `CURRENT_DESIGN.md`
4. 旧任务、旧计划、旧聊天内容

用户的新要求可以覆盖旧要求。

代码代表“现在实际是什么”，用户最新要求代表“接下来应该是什么”。

不要为了保持旧文档正确而修改本来已经正确工作的代码；应更新过时的文档。

---

## 2. 修改前先检查

修改任何已有功能前：

- 阅读相关代码。
- 找到真实调用链和数据流。
- 确认已有实现是否已经满足需求。
- 确认修改影响范围。

不要根据文件名、旧提示词或猜测直接重写。

进行任何 UI 设计、布局、导航或交互修改前，必须先查看 PCL2 的对应界面，以及该界面对应的功能和操作关系，确认信息层级、操作顺序和状态反馈后再设计 DSH Launcher。PCL2 只作为交互和信息架构参考，不复制其代码、素材或视觉资源。

如果仓库实际情况和任务描述不同，以实际代码为基础实现用户目标。

---

## 3. 最小修改原则

始终选择能够完成目标的最小实现。

禁止因为一个局部需求：

- 重写整个模块；
- 更换已有技术方案；
- 大规模调整目录；
- 修改无关接口；
- 重命名大量代码；
- 顺手清理正常工作的代码；
- 引入新的框架或大型依赖。

如果现有实现可以小改完成任务，就小改。

---

## 4. 不要擅自增加功能

只实现用户明确要求的功能，以及实现该功能不可缺少的部分。

未经用户明确要求，不要自行加入：

- 与当前目标无关的安全机制；
- 审计系统；
- 企业级权限体系；
- 复杂抽象层；
- 插件框架；
- 多后端兼容层；
- 遥测系统；
- 大型日志系统；
- 云端功能；
- 多用户功能；
- 复杂迁移机制；
- 为“未来可能需要”准备的架构。

不要以“以后可能有用”为理由增加复杂度。

---

## 5. 保留已有工作成果

已经正常工作的功能默认保留。

修改方向后：

1. 找到真正冲突的部分；
2. 修改冲突部分；
3. 删除已经确定无用的旧实现；
4. 保留其他正常代码。

不要因为设计变化重新实现整个功能。

---

## 6. CURRENT_DESIGN.md

`CURRENT_DESIGN.md` 是当前有效设计的简短记录。

它不是：

- PRD；
- 开发日志；
- changelog；
- 聊天摘要；
- 完整技术文档。

它应该记录：

- 当前真正采用的关键设计；
- 已确认的数据关系；
- 会影响后续开发的重要约束；
- 已确定但尚未完成的重要方向。

更新规则：

- 根据实际代码和用户最新要求更新；
- 旧方案被替代后直接删除；
- 不保留已经废弃的设计作为有效内容；
- 不把猜测写成事实；
- 不机械复制用户长提示词；
- 保持简短。

局部需求变化只更新受影响章节。

---

## 7. DSH Launcher 当前基本原则

除非用户以后明确修改：

- 产品名称为 `DSH Launcher`。
- 首要平台为 Windows。
- Manager 独立于现有 DeepSeek Desktop。
- UI 可以参考 PCL2 的信息架构和交互方式，但代码、视觉和素材独立实现。
- 支持管理 installed DSh 和 source DSh 项目。
- 不同实例的数据应按现有设计保持必要隔离。
- Manager 自身数据和 DSh 实例数据分开。
- Plugin、Skill、MCP、Workflow、Preset 可以统一展示，但底层不必强行采用相同实现。
- 一条 DSh 对话本质上是一个独立文件。
- Manager 负责对话管理，实例负责打开和继续对话。
- 对话可能存在启用或关闭上下文压缩的状态。
- 不要假设关闭压缩可以恢复已经被压缩的历史。
- 未明确要求前，不实现多个 DSh 进程同时写同一个对话文件。

具体实现细节以当前代码和 `CURRENT_DESIGN.md` 为准。

---

## 8. 编码原则

优先：

- 简单；
- 直接；
- 可读；
- 少代码；
- 少依赖；
- 少抽象。

避免：

```text
一个函数能解决的问题 → 创建五个模块
一个明确实现 → 提前设计通用框架
当前只有一种情况 → 为未知未来建立复杂抽象
```

只有出现真实重复或真实需求时才抽象。

---

## 9. 不要虚构仓库状态

没有检查过的内容，不要说：

- “已经实现”
- “项目目前使用……”
- “这里一定是……”
- “现有架构要求……”

应先读取代码确认。

如果只看到部分实现，就明确按照已经看到的代码继续分析。

---

## 10. 方向发生变化时

当用户说：

- 改成……
- 不再……
- 之前那个不要了……
- 用这个方案替代……
- 现在按……

视为设计变更。

执行：

```text
读取相关代码
↓
确认旧实现
↓
确定影响范围
↓
只修改冲突部分
↓
删除明确废弃代码
↓
验证功能
↓
更新 CURRENT_DESIGN.md
```

不要自动扩大修改范围。

---

## 11. Bug 修复

修 Bug 时首先定位根因。

不要用大量特殊判断掩盖问题。

优先：

```text
找到错误状态产生的位置
→ 修正源头
```

而不是：

```text
错误出现
→ 加一个 if
→ 又出现
→ 再加一个 if
```

如果最小修复可以解决问题，就不要顺便重构模块。

---

## 12. 测试

测试范围与修改范围匹配。

优先运行：

1. 修改模块相关测试；
2. typecheck；
3. 必要的 build；
4. 与该功能直接相关的运行验证。

不要为了一个很小的修改新增大量与需求无关的测试基础设施。

发现与本次任务无关的历史问题时，可以报告，但不要擅自修复。

每次完成任意测试版或发布版构建后，都必须将生成的产物复制一份到 `C:\Users\121103qwq\Desktop\DSH Launcher`。如果产物是文件夹，则复制整个文件夹；复制后必须确认目标文件或文件夹实际存在。

Computer Use 默认禁用，不作为常规测试或验收方式。只有用户明确要求使用 Computer Use 时，才可以通过它检验桌面界面或实际操作。

---

## 13. GitHub 发布

用户要求“提交 GitHub”时，默认同时包含代码提交、推送和 GitHub Release；不能只推送分支或只创建 PR。发布前必须完成与改动范围匹配的测试，核对 Release 标签、目标提交、产物文件名和 SHA-256。

- GitHub Release 中面向用户下载的 Windows 可执行附件必须固定命名为 `DSH.Launcher.exe`，不得随版本、功能或本地文件名改变，以保证公开自动路由链接 `https://github.com/121103qwq/DSH-Launcher/releases/latest/download/DSH.Launcher.exe` 始终有效。
- 仅用于测试、试验、原型，或尚待实机验证的改动，提交到独立分支并创建 PR，不直接进入 `main`。
- 用户已经明确要求修复、且不属于上述测试性改动的内容，在完成与修改范围匹配的验证后，直接提交并推送到 `main`，无需先创建 PR。
- 用户明确指定分支、PR 或合并方式时，以该次要求为准。

---

## 14. 完成任务后的输出

完成后简洁说明：

- 改了什么；
- 关键文件；
- 是否进行了测试；
- 是否还有与本任务直接相关的已知问题。

不要输出大量过程性描述。

不要把简单任务写成长篇总结。

---

## 15. 核心原则

始终遵循：

> 先理解现状，再修改。

> 最新需求决定目标，真实代码决定起点。

> 能改一处解决，就不要改十处。

> 能复用现有实现，就不要重新实现。

> 没有明确需求，就不要增加功能。

> 不为了未来假想需求增加今天的复杂度。

---
> Source: [121103qwq/DSH-Launcher](https://github.com/121103qwq/DSH-Launcher) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-27 -->
