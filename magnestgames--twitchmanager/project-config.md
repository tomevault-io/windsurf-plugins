---
trigger: always_on
description: このファイルはリポジトリ全体に適用する。Codex、Google Antigravity、Claude Codeその他のAIは、調査、変更、ブランチ作成、コミット、Push、Pull Request、Issue操作の前に本ファイルを確認する。
---

# AGENTS.md

## 適用範囲

このファイルはリポジトリ全体に適用する。Codex、Google Antigravity、Claude Codeその他のAIは、調査、変更、ブランチ作成、コミット、Push、Pull Request、Issue操作の前に本ファイルを確認する。

## 基本原則

- 現在のブランチ、未コミット変更、リモートの更新、関連IssueとPull Requestを作業前に確認する。
- 他者または別AIの変更を上書き、退避、破棄しない。
- 目的、対象範囲、対象外、完了条件が未確定のまま実装を開始しない。
- 1本の作業ブランチに複数目的の変更を混在させない。
- リポジトリ固有の既存構成、規約、テスト方法を優先する。
- 不明な仕様は「未確定」、推測は［推測］、仮説は［仮説］と記載する。

## Issue

`release_*`、`dev_*_beta`など、リポジトリで定義されたリリース用・統合用ブランチを除き、機能追加、不具合修正、調査は原則として1件のIssueと1本の作業ブランチを対応させる。

Issueには次を記載する。

```md
## 目的

## 対象範囲

## 対象外

## 開発ブランチ

- 作業ブランチ: `<branch-name>`
- 基点ブランチ: `<base-branch>`
- ブランチの役割: <このブランチで実施する内容>

## 完了条件

- [ ] 実装条件
- [ ] 検証条件
- [ ] 文書更新条件
```

- Issueを先に作成し、ブランチ作成直後に確定したブランチ名をIssueへ記載する。
- 作業範囲が変わる場合は、実装前にIssueを更新する。
- 複数Issueを1本のブランチで処理しない。例外が必要な場合は、各Issueに理由と統合先を記載する。

## ブランチ

リポジトリに既存規則がある場合は、その規則を優先する。規則がない場合は次を使用する。

```text
dev_<version>_Issue<issue-number>_<ShortName>
```

バージョン系列を使用しないリポジトリでは次を使用する。

```text
issue/<issue-number>-<short-name>
```

- Issue番号を省略しない。
- 個人名、AI名、端末名だけをブランチ目的名に使用しない。
- 対象バージョンの統合ブランチまたは既定ブランチの最新状態から作成する。
- 別の機能ブランチを基点にしない。依存がある場合はIssueに依存関係を記載する。
- `release_*`、`dev_*_beta`、既定ブランチへ機能実装を直接コミットしない。

## 作業開始

1. `git status`で未コミット変更と現在のブランチを確認する。
2. `git fetch --prune origin`でリモート情報を更新する。
3. 関連Issue、既存ブランチ、開いているPull Requestを確認する。
4. 同じIssueを別AIまたは別ブランチが処理していないことを確認する。
5. Issueの目的、対象範囲、対象外、完了条件を確認する。
6. 基点ブランチを更新してから作業ブランチを作成する。
7. 変更前の状態と検証方法を確認する。

## AI間の共有

- 作業開始時にIssue番号、ブランチ名、実施範囲をIssueまたはPull Requestへ記録する。
- 1本の作業ブランチを複数AIが同時に編集しない。
- 引き継ぎ時は、変更済みファイル、未完了項目、実行済み検証、未実行検証、既知の問題を記録する。
- 他AIのコミットを修正する場合は、追加コミットを基本とする。
- 利用者の明示承認なしにrebase、commit amend、`--force`、`--force-with-lease`を実行しない。

## コミット

- コミット前に差分全体を確認し、対象外のファイルを含めない。
- 1コミットを1つの説明可能な変更単位にする。
- 認証情報、ローカル設定、不要な一時ファイル、意図しない生成物を含めない。
- コミットメッセージには対応Issue番号を含める。

```text
<type>(<scope>): <summary> (#<issue-number>)
```

使用する`type`:

| type | 用途 |
|---|---|
| `feat` | 機能追加 |
| `fix` | 不具合修正 |
| `refactor` | 挙動を変えない構造変更 |
| `test` | テスト追加・修正 |
| `docs` | 文書変更 |
| `chore` | 保守作業 |

- 利用者がコミットまたはPushを許可していない場合は、ローカル変更までで停止する。

## Pull Requestと終了条件

- 作業ブランチから統合ブランチまたは既定ブランチへPull Requestを作成する。
- Pull Requestに目的、主な変更、対象外、検証結果、未検証項目、対応Issueを記載する。
- 完了条件を満たした場合は、Pull Request本文に`Closes #<issue-number>`を記載する。
- 完了条件を満たしていないIssueを閉じない。
- Pull Requestのマージを確認してから作業ブランチを削除する。
- 未マージで中止する場合は、Issueに中止理由と残作業を記載する。

## 承認が必要な操作

次の操作は利用者の明示承認なしに実行しない。

- 強制Pushまたは履歴の書き換え
- ブランチ削除
- Issueのクローズ
- Pull Requestのマージ
- リリース、公開、デプロイ
- 認証、権限、課金、外部サービス設定の変更
- データを失う可能性がある操作

## 検証と報告

- 変更内容に対応する最小限の検証を実行する。
- 静的確認、自動テスト、ビルド、実行確認、目視確認を区別して報告する。
- 未実行の検証を実行済みとして扱わない。
- 最終報告には変更内容、コミット、ブランチ、検証結果、未確定事項を記載する。

---
> Source: [MagnestGames/TwitchManager](https://github.com/MagnestGames/TwitchManager) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-20 -->
