---
trigger: always_on
description: Two firmwares for two keyboards that share one behaviour ("Dwerty"): the base layer types Dvorak, but holding or one-shotting Ctrl/Alt/Win sends the key in its Qwerty physical position, so shortcuts (Ctrl+C, Ctrl+V, etc.) stay in muscle memory. The OS stays on US Qwerty and Shift is excluded so shifted letters still type Dvorak.
---

# Copilot Instructions

## Project Overview

Two firmwares for two keyboards that share one behaviour ("Dwerty"): the base layer types Dvorak, but holding or one-shotting Ctrl/Alt/Win sends the key in its Qwerty physical position, so shortcuts (Ctrl+C, Ctrl+V, etc.) stay in muscle memory. The OS stays on US Qwerty and Shift is excluded so shifted letters still type Dvorak.

| Directory | Keyboard | Firmware | SoC |
|-----------|----------|----------|-----|
| `max/` | Keychron V6 Max | QMK | shipping |
| `ultra/` | Keychron V6 Ultra 8K | ZMK | Realtek RTL8762G (spike) |

Each directory is self-contained, with its own build scripts, tests and README. They share no toolchain.

## `max/` — QMK (Keychron V6 Max)

The keymap (`max/keymaps/keychron/v6_max/ansi_encoder/keymaps/dvorak_qwerty/keymap.c`) defines 5 layers:

- **MAC_QWERTY (0)** — stock Keychron Mac base, no remapping
- **MAC_DWERTY (1)** — Mac Dvorak typing with Qwerty shortcut interception
- **WIN_QWERTY (2)** — stock Keychron Win base, no remapping
- **WIN_DWERTY (3)** — Win Dvorak typing with Qwerty shortcut interception
- **FN (4)** — Fn-held overlay (F-keys, RGB, Bluetooth, Dwerty/Qwerty toggle)

The Mac/Win slide switch is vanilla Keychron: `v6_max.c`'s `dip_switch_update_kb` sets the default layer to the Mac half (0/1) or Win half (2/3). Within a half, `qwerty_shortcuts_layer_active()` returns `true` only on the DWERTY layers. Fn+Z (LAYOUT_SEL) toggles Dwerty↔Qwerty within the current half. All base layers use `MO(FN)`.

A 1-bit Dwerty/Qwerty mode is persisted in EEPROM user data; `default_layer_state_set_user` folds it onto whichever OS half the DIP selects, so it survives a switch flip and reboot. Default is Dwerty. (Pure Dvorak was dropped.)

Shortcut interception works by maintaining a `qwerty_shortcut_map[]` lookup table that maps Dvorak keycodes to their Qwerty equivalents. When a modifier (Ctrl/Alt/GUI) is held on a DWERTY layer, `process_record_user` intercepts the keypress and sends the Qwerty equivalent instead. The `qwerty_shortcut_active[]` matrix tracks which remapped key is physically held so the correct keycode is unregistered on release — this release path must run before the modifier check.

**Build and test (run from `max/`):**
```bash
./scripts/firmware.sh build    # build only (Podman container, see Containerfile)
./scripts/firmware.sh flash    # flash only
./scripts/firmware.sh          # build + flash (default)
./scripts/test.sh              # Python unittest suite
```

The build clones Keychron's QMK fork (`wireless_playground` branch) into `max/.cache/qmk_keychron/` and copies the keymap in before compiling.

**`max/` conventions:**
- The keymap directory mirrors the QMK keyboard path. The build script copies it into the QMK tree at compile time.
- When adding or removing keys from `qwerty_shortcut_map[]` in `keymap.c`, update the `EXPECTED` set in `max/tests/test_shortcuts_mapping.py` to match.
- `config.h` sets `DYNAMIC_KEYMAP_LAYER_COUNT 5` — update this if adding layers.
- `via/v6_max_ansi_encoder.json` is the VIA draft definition for live remapping.
- Shift is intentionally excluded from shortcut mod masks so shifted keys produce Dvorak characters, not Qwerty.

## `ultra/` — ZMK (Keychron V6 Ultra 8K)

The V6 Ultra runs ZMK on a Realtek RTL8762G. It mirrors `max/`'s 5-layer scheme: MAC_QWERTY(0), MAC_DWERTY(1), WIN_QWERTY(2), WIN_DWERTY(3), FN(4). The Dwerty behaviour is reproduced with 33 per-key `&mod_morph` (`&dq_*`) behaviours on both DWERTY layers: each differing key binds its Dvorak letter normally and its Qwerty-position letter when Ctrl/Alt/GUI is held, with `keep-mods` keeping the modifier. One-shot (`&sk`) modifiers trigger the morph identically. The keymap is generated by `scripts/gen_keymap.py` from the stock shield (Mac base, Win base, Win fn). The Mac/Win slide GPIO is read in C (the `keymap.c` patch) to drive the OS-half bit of the default layer at boot and on every edge (the stock momentary `&mo` could be missed by a marginal boot scan, leaving the board stuck on Mac, so the slide cell is `&none`); Fn+Z (`&to 0xFF` at the Z position on FN) flips the persisted Dvorak/Qwerty bit within the current OS half. Only that bit persists; the slide always owns Mac/Win. Pure Dvorak was dropped.

The fork is **pinned** by SHA in `config/west.yml` + `scripts/build.sh`; behaviour lives in `git apply` patches in `ultra/patches/` (the fork has none): `0001` persists the layout bit and drives the Mac/Win slide from its GPIO, `0002` adds the Fn+Z RGB flash (green=Dvorak/blue=Qwerty) via the PWM flush. `.github/workflows/ultra-fork-update.yml` bumps the pin, builds/tests, and opens a PR when the fork moves.

**Build (Keychron fork) vs test (upstream ZMK) — two separate toolchains:**
- **Build** the real firmware on Keychron's fork (`Keychron/zmk@rtl8762g`, pinned), board `keychron`, shield `keychron_v6_ultra_ansi`, container `zmk-build-arm:3.5`.

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [teh-hippo/dwerty](https://github.com/teh-hippo/dwerty) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-07 -->
