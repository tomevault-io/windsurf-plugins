---
trigger: always_on
description: - Every commit made in this repository must include a DCO signoff. Use `git commit --signoff` or `git commit -s`. If a commit is missing the signoff, fix it with `git commit --amend --no-edit --signoff` before pushing.
---

# Repository Instructions

- Every commit made in this repository must include a DCO signoff. Use `git commit --signoff` or `git commit -s`. If a commit is missing the signoff, fix it with `git commit --amend --no-edit --signoff` before pushing.

---
> Source: [runmedev/docs.runme.dev](https://github.com/runmedev/docs.runme.dev) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-16 -->
