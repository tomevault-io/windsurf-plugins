---
trigger: always_on
description: `docs/conventions.md` is how the tools behave, and beats any ticket that says
---

# CLAUDE.md

`docs/conventions.md` is how the tools behave, and beats any ticket that says
otherwise. This is how to work on them.

## Writing

Short, plain, human. No em dashes, emojis, AI buzzwords.
Anything the tool prints is man page style: imperative, impersonal, terse.
Commits are one line, `type(scope): 3-8 words`. No body.
Never sign the work: no co-author, no "generated by", no tool names, in
commits or PRs.

## Comments

Prefer none. Add one only where the code cannot say it itself, then keep it a short line.
Describe the code as it stands. Never what changed or why it changed.

## Code

Strict YAGNI. Only what was asked.
POSIX shell where it costs nothing, so this ports past Arch and bash.
Root `ptt` is a shim. Scripts in `src/`, shared libs in `src/lib/`, data in `share/`.
Each bash script sets `PTT_ROOT` from its own location. No hardcoded repo paths.

## Testing

Mic work is the user's. Never start a dictation session: `WTYPE` types into
whatever window has focus.

## Git

Read only. Ask before staging, committing, or rewriting.

---
> Source: [shalom2552/ptt](https://github.com/shalom2552/ptt) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-28 -->
