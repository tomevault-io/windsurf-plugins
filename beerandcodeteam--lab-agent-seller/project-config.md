---
trigger: always_on
description: <!-- Generated by /ai-context. Manual edits are overwritten on re-run. -->
---

# AGENTS.md

<!-- Generated by /ai-context. Manual edits are overwritten on re-run. -->

Multi-tenant Laravel SaaS. Companies connect Pipedrive CRM, `ScanCrmConnection` job imports pipelines/stages/custom fields/persons/deals, final clients log in passwordlessly via magic link (matched by CRM person email), then chat with a per-company AI sales agent (`SellerAgent`, pt-BR, `laravel/ai` streaming). Runs inside Laravel Sail (Docker).

## Sections

1. [Build, Lint, Test and Coverage](#1-build-lint-test-and-coverage)
2. [Code Style & Project Conventions](#2-code-style--project-conventions)
3. [Agent Communication & Behavioral Guidelines](#3-agent-communication--behavioral-guidelines)
4. [Setup, Troubleshooting and Tips](#4-setup-troubleshooting-and-tips)
5. [References](#5-references)
6. [Agent Documentation](#6-agent-documentation)

## 1. Build, Lint, Test and Coverage

All commands run through Laravel Sail. Prefix every PHP, Artisan, Composer, and Node command with `vendor/bin/sail`.

| Command | Purpose |
|---------|---------|
| `vendor/bin/sail up -d` | Start Sail services (PHP 8.5, Postgres 18, Redis, Mailpit) |
| `vendor/bin/sail stop` | Stop Sail services |
| `vendor/bin/sail npm run build` | Build frontend assets (Vite) |
| `vendor/bin/sail npm run dev` | Run Vite dev server |
| `vendor/bin/sail composer lint` | Format PHP via Laravel Pint (`pint --parallel`) |
| `vendor/bin/sail php artisan test` | Run Pest test suite |
| `vendor/bin/sail php artisan test --compact` | Run tests, compact output |
| `vendor/bin/sail php artisan test --compact --filter=testName` | Run one test |
| `vendor/bin/sail composer test` | Full gate: `config:clear` + `lint:check` + `types:check` + `php artisan test` |

## 2. Code Style & Project Conventions

- PHP `^8.3` (CI matrix 8.3/8.4/8.5; Sail runtime 8.5). Framework `laravel/framework` ^13, `livewire/livewire` ^4, `laravel/ai` ^0.9.
- Formatting: Laravel Pint, preset `laravel` (`pint.json`). Run `vendor/bin/sail bin pint --dirty --format agent` after editing any PHP file. Never run `pint --test` to fix; run `vendor/bin/sail bin pint --format agent`.
- Static analysis: Larastan/PHPStan level 7 (`phpstan.neon`), enforced by `composer test` → `types:check`.
- Editor defaults from `.editorconfig`.
- PHP: always use curly braces for control structures, even single-line bodies.
- PHP: use constructor property promotion (`public function __construct(public GitHub $github) {}`); no empty zero-param `__construct()` unless private.
- PHP: explicit return types and param type hints on all methods (`function isAccessible(User $user, ?string $path = null): bool`).
- TitleCase for Enum keys. Prefer PHPDoc blocks (with array shape types) over inline comments.
- Descriptive names (`isRegisteredForDiscounts`, not `discount()`). Check sibling files for structure/naming; reuse existing components before writing new ones.
- Create files via `vendor/bin/sail artisan make:` with `--no-interaction`; use `make:class` for plain PHP classes.
- Prefer named routes and `route()` for links. Livewire keeps state server-side; validate/authorize in actions.
- Full rules: `docs/agents/coding_guidelines.md`.

## 3. Agent Communication & Behavioral Guidelines

- Run `search-docs` (Laravel Boost MCP) before making code changes; prefer Boost tools (`database-query`, `database-schema`, `get-absolute-url`, `browser-logs`) over raw shell/SQL.
- Prefer Pest feature tests with model factories over tinker/verification scripts to prove behavior.
- Be concise; activate the relevant skill under `**/skills/**` when working in that domain.

### Never in this repository

- Never delete tests without approval (Pest suite is the correctness gate).
- Never add or change dependencies, create new base folders, or create documentation files without approval.
- Never run PHP/Artisan/Composer/Node commands outside `vendor/bin/sail`.
- Never leave PHP unformatted — Pint (`lint:check`) and PHPStan level 7 (`types:check`) block `composer test`.

## 4. Setup, Troubleshooting and Tips

Prereqs: Docker + Docker Compose. Services: PHP 8.5, Postgres 18, Redis, Mailpit (`compose.yaml`). MCP: `laravel-boost` (`.mcp.json`).

```sh
vendor/bin/sail up -d
vendor/bin/sail composer install
vendor/bin/sail npm install
vendor/bin/sail artisan migrate
vendor/bin/sail npm run build
```

| Symptom | Check |
|---------|-------|
| `Vite manifest` error / frontend change not visible | Run `vendor/bin/sail npm run build` (or `npm run dev` / `composer run dev`) |
| Artisan hangs on prompts | Pass `--no-interaction` |
| Outgoing mail (magic links) not delivered | Inspect Mailpit inbox |

## 5. References

| Doc | Purpose |
|-----|---------|
| `README.md` | Project readme |
| `docs/agents/` | Agent-facing project documentation (see section 6) |
| `compose.yaml` | Sail service definitions |
| `phpstan.neon` | Static analysis config |
| `pint.json` | Formatter config |

## 6. Agent Documentation

| File | Content |
|------|---------|
| `docs/agents/project_overview.md` | High-level product goal and flow |
| `docs/agents/architecture.md` | System architecture and layers |
| `docs/agents/tech_stack.md` | Languages, frameworks, versions, runtime |
| `docs/agents/coding_guidelines.md` | PHP/Laravel conventions and style rules |

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [beerandcodeteam/lab-agent-seller](https://github.com/beerandcodeteam/lab-agent-seller) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-11 -->
