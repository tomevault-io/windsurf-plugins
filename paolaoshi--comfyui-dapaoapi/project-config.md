---
trigger: always_on
description: - `🤖dapaoAPI/🍬大炮AI主力维护🍬` 和 `🤖dapaoAPI/🍬大炮API常用工具🍬` 是本项目默认的长期维护分组；用户没有特别指定时，新节点和现有节点修改默认归入这两个分组。
---

# ComfyUI-dapaoAPI 项目开发规则

## 长期维护分组与统一规范（强制）

- `🤖dapaoAPI/🍬大炮AI主力维护🍬` 和 `🤖dapaoAPI/🍬大炮API常用工具🍬` 是本项目默认的长期维护分组；用户没有特别指定时，新节点和现有节点修改默认归入这两个分组。
- `🤖dapaoAPI/🔮API通用工具🔮` 仅用于通用 API/协议测试类节点；`🤖dapaoAPI/🦄RH功能专区🦄` 保持现有独立渠道边界。不得重新创建或使用“旧版节点(不再维护)”分组。
- 默认中转站为 `https://api.dapaoai.com`。除非用户明确指定其他服务商、URL、模型路由或协议，新节点应沿用现有 dapaoAI 鉴权、错误处理、价格展示和结果返回规范。
- 节点显示名保留项目既有中文语气和 emoji 风格；UI 应沿用相邻维护节点的紧凑布局、模型/参数选择器、价格标签和中文状态信息，不要引入与现有风格冲突的独立界面。
- 需要注册入口的节点统一使用显眼的 `👉点此注册API密钥👈` 按钮，链接为 `https://api.dapaoai.com/sign-up?aff=vcOZ`。自定义按钮必须跟随节点当前尺寸重绘，不能缓存侧栏或画布的临时宽度；点击处理必须同时兼容 ComfyUI/LiteGraph 可能传入的 `pointerdown`、`mousedown` 和 `click` 事件。
- 图像和视频节点默认参考现有维护节点的素材输入、文生/图生自动识别、并发列表映射、价格计算、错误中文化和结果输出方式；特殊协议必须在用户明确说明后再偏离。
- 任何节点只要接收用户上传的 `IMAGE`，或要把用户图片上传/转发给 LLM、图像模型、视频模型或素材接口，必须在实际请求边界统一做图片预处理：每张图片独立按比例缩放，最长边不得超过 `2048px`（2K）；多图和批次输入必须逐张处理。默认使用高质量 Lanczos 缩放并尽量采用 PNG 无损编码，不能只在 UI 上提示而漏掉实际上传路径。除非用户明确要求更低规格，不得把 2K 上限继续降低。
- 新节点应复用项目的共享图片预处理工具，并在图片上传类 `429`/排队错误中明确提示用户检查每张图片是否超过 2K；不得把大图限流单纯描述成模型或余额问题。图层拆分、蒙版对齐等依赖原图尺寸的节点，必须使用压缩后的实际尺寸继续后处理，避免请求尺寸与本地合成尺寸错位。
- 任何需要 LLM 的节点都必须提供统一的项目 LLM 候选列表。以后新增或修改模型时，必须同步更新两个长期维护分组内所有需要 LLM 的节点、输入校验和相关前端候选，不能只改单个节点。
- LLM 模型 ID、API 地址、注册按钮、价格标签和通用错误提示应优先复用已有实现；不要让用户重复说明这些默认约定。
- `500`、`502`、`503`、`429` 等常见服务错误必须提供普通用户能理解的中文说明；image-2 和香蕉节点还要给出切换官方稳定模型的明确操作建议。

## 批量输入并发（强制）

- 新增或修改任何包含 API 请求、联网检索、文件上传、任务轮询或其他明显阻塞工作的节点时，必须支持 ComfyUI 对列表输入的并发映射。
- 节点注册在 `FUNCTION` 中的公开执行方法必须是异步方法；已有可靠的同步业务逻辑应保留为独立私有方法，并通过 `await asyncio.to_thread(...)` 调用。不得为了并发重写或改变已验证的请求协议。
- 不要给普通输入设置 `INPUT_IS_LIST` 来实现并发。普通 ComfyUI 列表应由框架逐项映射，这样每条提示词/每个任务成为独立协程；只有节点确实需要一次接收完整列表时才使用 `INPUT_IS_LIST`。
- 必须保持单条输入完全兼容：输入名称、输出类型与顺序、模型路由、价格计算、缓存/随机种、错误策略、素材处理及响应内容不得因并发改造而改变。
- 同一节点的“出图数量/任务数量”与上游列表并发是两件事。接口原生支持 `n` 时保留单次请求；需要多次请求时才使用有上限的线程池，并在界面中明确并发含义。
- 每个并发任务应创建自己的 API 客户端或请求状态；可变共享缓存、首次下载、数据库解压和临时资源初始化必须使用锁或原子操作保护。
- 付费的 POST/提交请求不得自动重试，以免重复扣费；只读查询可按原节点策略重试。
- 验收并发改造时必须使用模拟响应或拦截请求，不得调用真实付费 API。至少验证：公开执行方法是协程、单条输出结构不变、列表任务可以重叠执行、异常仍按原策略传播。
- 纯本地且瞬时完成的字符串整理、类型转换和引用标记节点可以保持同步；这类节点没有等待瓶颈，放入线程只会增加调度开销。

---
> Source: [paolaoshi/ComfyUI-dapaoAPI](https://github.com/paolaoshi/ComfyUI-dapaoAPI) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-30 -->
