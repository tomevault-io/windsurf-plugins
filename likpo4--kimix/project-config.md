---
trigger: always_on
description: > 通用长期开发规则。目标：小步、可验证、少废话、可交接。
---

# AGENTS.md

> 通用长期开发规则。目标：小步、可验证、少废话、可交接。

## 沟通

- 始终用中文，每轮第一句必须说“你好霖江路”
- 信息不足时只问会阻塞当前任务的关键问题。
- 没有测试、日志、构建或运行证据，不说“已修复/已完成”。
- 同一思路连续失败 2 次，停下分析根因；反复卡住时把选择权交给用户。

## 开工

- 先读项目规则、README、关键文件和当前状态，再动手。
- 以仓库实际代码为准，优先搜索确认，别凭记忆改。
- 默认每轮只做一个可验证的最小增量。
- 优先复用现有架构、命名和目录，不轻易新建并行体系。
- 发现与当前任务无关的问题，记录为后续事项，不扩大本轮范围。

## 修改

- 只改必要文件，不做无关重构、格式化、清理。
- 新依赖、新脚本、新配置必须说明理由和回滚方式。
- 删除、覆盖、批量迁移、发布、强推、重置、清理历史等不可逆操作前先征得用户确认。
- 实现与用户原话存在数值、范围或语义偏差时，必须说明。

## UI 留白防回归

> Kimix 已多次在队列卡片、标题栏、侧栏、代码块、输入区等位置出现“文字/按钮贴边、内容被边框挤压、独立模块上下连成一片”的问题。后续凡是新增或调整 UI 容器，必须先按此规则自查。

### 硬规则（必须遵守）

- 所有带边框、背景、圆角或选中态的容器，文字不得直接贴到边缘；左右内边距默认不小于 16px，顶部/底部至少保留 8px 视觉呼吸空间。
- 垂直堆叠的独立区块（卡片、状态块、输入框、按钮组、说明文案）之间，不得视觉相连；默认最小间距 14px，窄侧栏和紧凑弹窗里也不得低于 12px。
- 相邻兄弟元素要单独检查，不得只给父容器 padding 就算完成；尤其是 `input -> textarea`、`状态块 -> 按钮组`、`按钮组 -> 说明文案` 这种链路，必须显式检查 `marginTop` / `gap`。
- 容器右侧有状态文字、操作按钮、时间、计数等内容时，左右两端都要留白，不能只给左侧 padding；右侧文字和按钮不得贴边。
- 队列、列表、菜单、卡片头部这类横向信息密集区域，优先用 inline `style` 明确 `paddingLeft`/`paddingRight`，不要只依赖 Tailwind 的 `px-*`，避免 JIT 或旧缓存导致“看起来没生效”。
- 小浮层、紧凑弹窗、底部/顶部工具面板里的 `margin`、`padding`、`gap`、`top/bottom/left/right` 等关键 spacing，不使用 Tailwind scale 类（如 `mt-*`、`mb-*`、`gap-*`、`px-*`、`bottom-*`）作为唯一来源；必须用 inline style 或任意值语法明确数值，尤其是用户已经反馈“没生效”的区域。
- 圆角框和选中/焦点态优先用浅灰边框、浅灰背景或次要灰色文本；除真正的主操作按钮外，不使用黑色描边、黑色 focus ring 或黑色实线边框。
- 对话流里的折叠按钮、状态按钮、工具行按钮统一克制尺寸：普通文字按钮高度 32-34px，图标 14-16px，左右 padding 12-14px；不要做成大胶囊或大面积灰底。默认态优先透明背景 + 次要灰色文字，hover/active 才出现浅灰底。
- 图标+文字按钮不要每处临时写 `gap-1 px-2` 这类紧凑样式；优先复用 `.kimix-icon-text-button`，默认最小高度 34px、gap 8px、左右 padding 14px，紧凑场景也至少 32px 高、左右 12px。
- 右侧按钮、数字胶囊、删除按钮等与左侧文本同排时，要把“整行高度”和“两列布局”同时定死：优先 `gridTemplateColumns: "minmax(0, 1fr) auto"` 或固定操作列，按钮/胶囊用固定 `height/minWidth`，必要时用 1px 视觉补偿；不能只写 `items-center` 就认为完成。

### 小浮层/列表专项规则

- 小浮层内的内容必须先拆成明确层级：浮层外壳、头部、分区卡、分区头、列表容器、列表项。不要把“分区标题”裸放在两张卡片之间；如果标题属于某组内容，就放进该组分区卡内部。
- 浮层里的独立分区卡之间必须有显式 inline `marginTop` 或父级 inline `gap`，默认 14-16px；相邻分区卡视觉相连时，先检查 spacing 是否真的由 inline style 生效。
- 分区卡内部也要显式分层：标题行到内容区默认 10-12px；列表项之间默认 8-10px。多项列表必须有专门的列表容器（如 `display: flex; flexDirection: "column"; gap: 8`），不要让 `map()` 直接输出一串相邻卡片。
- 同一个 UI 必须同时验收空态、单项态、多项态。只看空态或单项态会漏掉列表项贴在一起、计数胶囊错位、删除按钮不齐等问题。
- 如果用户连续两次反馈同一浮层/卡片“没居中、没缝隙、没生效”，先停下找结构根因：检查元素是不是在错误层级、标题是不是漂在两个区块之间、spacing 是否依赖 Tailwind scale 类、是否只修了子元素内部而没有修父级布局。不要继续叠加背景、阴影、标签条来遮盖问题。
- 图标+文字主按钮在两行说明文字右侧时，不能只按几何中心对齐；要用截图检查文字基线和按钮上下留白，必要时在固定高度基础上做 1px 视觉补偿，并说明这是视觉补偿而不是布局主逻辑。

### 高风险区域（每次必查）

- 右侧窄栏：长程任务状态栏、设置侧栏、上下堆叠的状态卡。
- 弹窗表单：项目卡、创建卡、输入框、textarea、底部按钮区。
- 顶部 banner 和消息上方信息块：标题、状态、步骤、操作按钮同排时。
- 小浮层 / onboarding / 下拉菜单：按钮组、说明文案、代码块、检测结果连续堆叠时。
- 底部状态栏弹出的配置浮层：项目/工作目录/套餐用量这类既有头部按钮、分区卡、列表项、计数胶囊的面板，必须按“小浮层/列表专项规则”检查空态、单项态、多项态。
- 对话流里的圆角框、折叠行、状态胶囊、TodoList/队列面板：必须同时检查父容器和子行，不能只给父容器 padding 后让子行贴边。

### 提交前留白验收清单

- Agent 不必自行截图验收；截图/视觉验收由用户负责回传。Agent 只需完成代码自查、构建/运行证据和必要的可复现说明，等待用户截图反馈后再判断是否继续微调、提交或发布。
- 检查标题/头部文字、列表行文字、右侧辅助文字、图标按钮、边框四角附近是否有挤压。
- 检查截图里是否出现“字贴边”“按钮贴边”“上下背景连成一块”“两个独立块之间看不出缝隙”；一旦出现，先按留白问题处理。
- 检查窄侧栏和紧凑弹窗里，是否存在 `卡片贴卡片`、`按钮贴输入框`、`输入框贴说明文案` 的情况。
- 检查按钮热区是否足够：顶部菜单文字触发区（如“文件/编辑/查看/窗口/帮助”）必须是明确按钮热区，不能让 hover 背景/阴影刚好贴着文字；默认最小宽度约 52px、高度约 34px、左右 padding 14px，并保留轻微 hover 阴影。
- 检查小浮层/下拉菜单即使内容少，也不能压缩成“标题、进度条、说明文字贴在一起”的状态；外层 padding 建议 20-22px，标题区和内容区至少 16px 间距，重复条目之间至少 14px 间距，条目内部标题、进度条、说明文字各自保留 8px 左右的垂直呼吸。
- 检查动态列表至少两项时是否仍有缝隙；如果列表项由 `map()` 生成，必须确认外层有显式 `gap` 或每项有显式 `marginTop`。
- 检查横向头部右侧按钮/计数胶囊是否在整行空间中视觉居中，而不是只在自身背景里居中；必要时用截图确认上/下留白。

## 视觉改动方法论（Kimix 项目血泪经验）

> 场景：调整 UI 间距/留白/边框等数值，用户反复反馈"没生效"时的标准流程。

1. **改动不生效时，逐项排除**：
   - 旧 Electron 进程残留：`taskkill /F /IM electron.exe /T & taskkill /F /IM node.exe /T`。
     Kimix 场景反复出现旧窗口占用、dev 新窗口没真正打开的情况。
   - 构建缓存：`rmdir /S /Q out` + `rmdir /S /Q node_modules\.vite` + `rmdir /S /Q node_modules\.cache`。
   - 发图系统：用户截图系统偶发延迟。看到截图上的版本号和期望不一致时，**立即要求用户重发**，不要基于错图推理。
   - **版本号锚定**：每一轮改动必须 bump 版本号（`package.json` + `Sidebar` + `SettingsPanel` 三处同步），差距要大（v0.1.7 → v1.0.0），方便一眼判断画面是不是最新。
   - Tailwind JIT 动态类名：Kimix 当前配置下，某些 spacing 类（如 `px-16`、`pb-8`、`pt-10`、`mt-4`、`gap-2`、`bottom-10`）会出现**不生成或旧缓存表现不一致**的情况。涉及 padding/margin/gap/定位数值调整时，**优先用 inline `style={{ paddingLeft: 20, marginTop: 14, gap: 8 }}` 或任意值语法 `[padding-left:20px]`**，不要用 `px-*`/`mt-*`/`gap-*`/`bottom-*` 这类 spacing scale 类作为唯一依据。
   - 连续两次只改子元素仍被反馈“没居中/没缝隙”时，立刻检查父级结构和 sibling 关系：是不是缺少列表容器、分区标题是不是不属于任何卡片、是不是只在按钮内部居中但按钮所在行没有居中。

2. **改完后的标准验证流程**：
   - 杀掉所有 `electron.exe`、`node.exe` 旧进程。
   - 清 `out/` + `.vite` + `.cache`。
   - `pnpm build`（观察新 hash）→ `pnpm dev`（后台启动）。
   - 版本号同步 bump，三处一起改。
   - 不要求 agent 自行截图验收；由用户截图验收并回传。版本号对上后，agent 再根据用户截图反馈判断内容问题。

3. **默认 shell 的 PATH 问题**：
   本机 cmd 默认 PATH 不含 Node/pnpm，执行构建前必须前置：
   `set "PATH=C:\Program Files\nodejs;C:\Users\lijialin08\AppData\Roaming\npm;%PATH%"`

## Git

- 每轮有实际改动后必须提交一次 git；即使改动很小，也要先提交再继续下一步，避免丢失内容。
- 提交前必须查看 `git status` 和相关 `git diff`，确认只包含本轮改动。
- 只 stage 本轮相关文件，避免 `git add .` 收进无关改动。
- 未通过验收、分支不明确或风险明显时不提交/不推送。
- 不提交密钥、缓存、构建产物、临时文件和个人笔记。
- **提交信息必须使用中文**（标题与正文均中文）。格式优先：`类型: 简明说明`，类型可用 `修复` / `功能` / `性能` / `文档` / `重构` / `测试` 等；需要时可加一行正文补充原因或影响范围。不要用英文 Conventional Commit 作为默认（例如不要默认写 `fix: ...`）。

## 验收

- 有实际改动后，优先运行项目已有的测试、构建、lint、typecheck 或局部验证命令。
- 无法验证时说明原因，并告诉用户需要回传什么。
- 输出结论必须区分：已验证、未验证、部分验证。

## 每轮输出

每轮结束按这个格式，保持简短：

1. 本轮目标（如修复问题则为出现问题原因和修复方式）
2. 变更文件
3. 验收结果
4. 风险与回滚
5. 下一步最小行动

## 根因快照流程

> 处理“工具调用/子代理结束后助手正文缺失”“消息归属错误”“状态机异常”等需要区分 SDK/模型行为与 Kimix 渲染/归属逻辑的问题时，必须先抓快照，再写兜底。

### 强制顺序


<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [LiKPO4/kimix](https://github.com/LiKPO4/kimix) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-07-24 -->
