---
trigger: always_on
description: BOB — Box of Boardgames（桌游沙盒）：网页桌游沙盒，上传 PDF 生成桌游，浏览器里拖拽操作，规则由玩家自己遵守。
---

# AGENTS.md

BOB — Box of Boardgames（桌游沙盒）：网页桌游沙盒，上传 PDF 生成桌游，浏览器里拖拽操作，规则由玩家自己遵守。

给 AI 代理与协作者的常驻命令。每条规则 1~3 行，详细内容链接到其"家"文档。

## 工作流程

- **先讨论，再写代码**。方向 > 速度：任何非平凡变更先讨论方案，确认后再动手（见 `docs/practices.md`）
- **需求三字段格式**，写完即可开工，不用再追问：
  - **做什么**：具体技术动作，列出关键字段名、文件路径、函数名
  - **验收标准**：客观可判断的完成条件，含具体数值 / 颜色 / 位置 / 文件路径
  - **关联**：依赖哪些前置需求、被哪些后续需求依赖
- **垂直切片优先**：每轮做一个完整功能增量，做完就能在浏览器中验证

## 角色检查

输出需求前切换视角检查：

| 角色 | 检查问题 |
|------|----------|
| PM | 这条需求和哪个切片对应？前后依赖对不对？ |
| Tech Lead | 字段、类型、文件路径有没有过度设计？ |
| Developer | 做什么写清楚了吗？可以直接敲代码了吗？ |
| QA | 验收标准客观可验证吗？有数值 / 颜色 / 位置吗？ |
| UX | 组件尺寸 / 间距 / 颜色 / 边框定义了吗？ |
| DevOps | 需要新装依赖吗？（默认答案应为"否"） |

实现完成后，**对照该需求的验收标准逐条验证**，展示验收明细后再报告完成。

## 硬性规则

- **禁止自动 commit**（包括 amend）。必须等用户明确说"提交"、"commit"或"amend"
- **禁止未经许可修改 / 创建 / 删除文件**。每次文件操作前先说明改什么、为什么，等确认后执行；诊断性只读操作不受此限
- **不做过度设计**。当前不需要的抽象不引入；用"这个有必要吗"筛查每个提案
- **决策必须落盘**：每次非平凡变更写 / 更新一条 note（见 `docs/notes/README.md`）；不建中央索引；写当前状态不写历史
- **回复优先**：用户的回复只要包含问句（包括反问），就不要修改代码，先回答问题（包括反问）
- **禁止 `>nul` / `2>nul` 重定向**：Git Bash 在 Windows 下会把 `nul` 当普通文件名创建真实文件（`nul` 是保留设备名，会让 Turbopack 编译 panic）。丢弃输出一律用 `> /dev/null`
- **发现 `nul` 文件立即处理**：项目根出现 `nul` 时删除并提示用户重启 dev（`rm -f nul`）；删不掉时用 `Remove-Item -LiteralPath '\\?\<绝对路径>\nul'`

## 文档地图

| 文档 | 内容 | 什么时候读 |
|------|------|-----------|
| `README.md` | 项目介绍、快速开始、Done/TODO 进度 | 第一次接触；规划需求时看 TODO |
| `docs/architecture.md` | 架构地图：分层与关键机制 | 改代码前 |
| `docs/practices.md` | 协作实践：讨论循环、角色方法 | 新 session 开始 |
| `docs/notes/` | 决策记录（proposed / implemented / archived 目录 + rejected.md） | 做决策前，先查是否已有结论 |

---
> Source: [LemonLiquor/bob](https://github.com/LemonLiquor/bob) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-28 -->
