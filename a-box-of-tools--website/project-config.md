---
trigger: always_on
description: Orientation for an agent. [README.md](README.md) is the real documentation and
---

# Working in this repository

Orientation for an agent. [README.md](README.md) is the real documentation and
this does not replace it — this is the short list of things that are not
obvious from reading the tree, and where to look for the rest.

## What this is

A static site generated by `python build.py`. Every tool runs entirely in the
visitor's browser: nothing is uploaded, there is no server, and no page has a
network step. That promise is the product, so a change that adds a `fetch` to a
tool is a change to what the site claims about itself, not a detail.

No dependencies, no lockfile, no build step for the JavaScript. What is in
`tools/<slug>/src/` is byte for byte what the browser runs.

## Commands

```bash
python build.py                                   # readable build into dist/
python build.py --out _plain --clean --no-minify  # what CI runs
python -m unittest discover -t . -s tests/python  # the generator
node --test "tests/js/*.test.js"                  # what the browser runs
```

Both suites need nothing installed. Node 21+ needs the glob **quoted**; on 20
and earlier a directory argument works and a glob does not.

`--clean` matters. Building several branches into one output directory leaves
pages from all of them, and `check_links` then reports dozens of broken links
that do not exist on the branch under test. It looks exactly like "main is
broken".

`--check` diffs against the deployed `dist` branch, which tracks `main`
exactly. On any feature branch it exits 1 by construction. CI does not run it.

## Layout

| Path | What it is |
|---|---|
| `build.py` | the generator; one function per kind of page |
| `buildlib/` | the parts it is made of — templating, minifying, i18n, imports |
| `config/site.toml` | everything true of every page, the CSP included |
| `config/planned.toml` | the roadmap list |
| `tools/<slug>/` | one tool: `tool.toml`, `body.html`, `styles.css`, `src/`, `og.png`, `README.md` |
| `shared/` | the frame, and the CSS and JS parts a tool can ask for |
| `locales/<lang>/` | one language |
| `pages/` | the guides and the legal pages |

## Things that will bite

**Every text file is LF**, working tree included — see `.gitattributes`. The
build copies `shared/` and each tool's `src/` byte for byte, so a CRLF makes
`--check` report a difference that is not real. Python's `write_text()`
silently writes CRLF on Windows: use `write_bytes()` on anything in this repo.

**Never `git add -A`.** A CI-style build writes `_plain/`, and a single
`git add -A` once swept 3,816 generated files into `main` (#61). Stage explicit
paths and check `git diff --cached --name-only | wc -l` first — a tool change is
around 20 files, a locale around 66.

**`og-image.ps1` without `-Only <slug>` redraws every share card**, and the
redraws are not byte-identical because it rasterises the logo through headless
Edge and that flakes. Name the tool you changed.

**A shared module cannot be imported by a unit-tested leaf module.**
`js_parts` copies `shared/js/x.js` into a tool at `src/shared/x.js` *at build
time*; that path does not exist in the source tree, and the JavaScript tests
import tool modules straight off the disk. So `./shared/` imports belong in
`main.js`, which no test loads. `buildlib/imports.py` fails the build if an
import does not land on a file the tool ships.

**Some modules are deliberately copied, and must stay in step.** The MP4
reader is in five tools and the writer in two, because the rule above blocks
sharing them. `tests/python/test_duplicates.py` declares which copies must
agree and fails if they drift, comparing tokens so each copy keeps its own
comments. Fix one copy and it will tell you about the others; add a new copy
and it will make you declare it.

## Adding a tool

The checklist is [README.md → Adding a tool](README.md#adding-a-tool). Two
steps that section does not spell out, and that every real tool change has
needed:

- `.\og-image.ps1 -Only <slug>`, to draw `tools/<slug>/og.png`. The build
  refuses a tool without one;
- if the tool was on the roadmap, drop its line from `config/planned.toml`
  **and** from every `locales/*/planned.toml`. Those lists are matched
  positionally; a locale list longer than English fails the build.

You do not have to touch the footer, the sitemap, or `tools/README.md` — all
three are generated.

## Languages

Ten locales. Publishing is judged **per page**: a page that is not translated
is still built and readable, and is kept out of the sitemap, the hreflang sets
and the switcher. `complete = true` claims only that the frame is translated,
so shipping a tool does not break the finished languages.

Lists whose English entries carry an `id` are merged **by id**; lists of plain
strings are still positional. That distinction is the whole reason
`locales/*/planned.toml` needs care and `[[hub.categories]]` no longer does.

## Conventions

Prose in this repository — commit messages, comments, `tool.toml` — explains
**why**, in full sentences, and assumes the reader can see the code. Match it.
Comments that restate the line below them do not belong; the ones here earn
their space by recording a decision or a trap.

---
> Source: [A-Box-of-Tools/website](https://github.com/A-Box-of-Tools/website) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-23 -->
