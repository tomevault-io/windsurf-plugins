---
trigger: always_on
description: - commit、issue、PR 评论和代码中不使用 emoji
---

# 开发规则

## 对话风格

- 回答简短、精炼
- commit、issue、PR 评论和代码中不使用 emoji
- 不写废话或过度热情的填充语（例如写 "Thanks @user" 而不是 "Thanks so much @user!"）
- 只用技术性表达，直接了当
- 使用简洁、清晰、易懂的语言。无法避免的专业术语要先定义。
- 解释非平凡的设计与问题时，按「问题 → 具体示例或简短调用链 → 解决方案」的顺序。说明为什么该方案必要，并区分它与可选复杂度。
- 优先用具体行为和小示例，而非抽象总结、密集术语或未解释的变更列表。
- 用户提问时，先回答问题，再执行编辑或运行命令。
- 回应用户反馈或分析时，先明确表态同意或不同意，再说你改了什么。

## ASu 求职输出规范

ASu的求职输出面向中文技术岗位，目标是让 HR 快速理解候选人的定位、项目全景、个人贡献和可验证结果。处理 `/asu`、简历包装或 HR 自我介绍时，遵循以下规则：

- 先给出岗位定位，再组织经历；定位至少包含目标岗位、方向、核心能力和可用的城市/到岗信息。
- 简历顶部优先写“岗位身份 · 领域方向 · 代表性能力”，再放 GitHub、论文、开源、竞赛或实习等可信背书；不写空泛的“学习能力强、责任心强”。
- 每段经历按“背景目标 → 个人边界 → 关键动作 → 系统能力 → 业务价值 → 结果证据”展开。复杂项目补充架构/流程、关键难点、落地结果和可追问证据。
- 技术关键词必须绑定个人动作和结果。只有用户确实承担过相应职责时，才能使用 Owner、项目负责人、核心作者、0-1、架构升级等强标签；“参与”不能直接升级为“主导”。
- 数字、排名、Star、用户量、性能提升、成本下降和影响力必须来自用户材料或公开证据。没有可靠数字时写可核验的定性结果，并标记“待补证据”，禁止编造。
- HR 开场白先说身份和目标方向，中间放 1—2 个最强真实证据，结尾说明岗位/城市/到岗时间（若已知）并邀请继续交流。短版约 80—160 字，完整版本约 180—280 字，不复制整份简历。
- 交付顺序默认是：岗位定位 → 简历顶部摘要 → 主要经历/项目改写 → 2—4 条可粘贴 bullet → HR 开场白短版与完整版 → 证据清单和可能追问。
- 参考图片只用于学习结构和表达密度，不能把图片中他人的公司、项目、技术、成绩或个人信息写入用户简历。

## 代码质量

- 大范围改动、编辑未完整查看的文件、或被要求调查/审计前，要完整阅读文件。不要依赖搜索片段做广泛改动。
- 除非绝对必要，不用 `any`。
- 仅有一处调用点的单行 helper 直接内联。
- 查看 node_modules 里的外部 API 类型；不要猜测。
- **禁止行内 import**（`await import()`、`import("pkg").Type`、动态类型导入）。只用顶层 import。
- 永远不要为了修复过期依赖导致的类型错误而删除或降级代码；应该升级依赖。
- 在根配置检查的代码中（`packages/*/src`、`packages/*/test`、`packages/coding-agent/examples`）只使用可擦除的 TypeScript 语法（Node strip-only 模式）：不用参数属性、`enum`、`namespace`/`module`、`import =`、`export =` 或其他需要特殊 JS 输出的构造。使用显式字段加构造函数赋值。
- 删除看似有意图的功能或代码前，必须先询问用户。
- 除非用户要求，否则不保留向后兼容。
- 不要硬编码按键检查（例如 `matchesKey(keyData, "ctrl+x")`）。把默认值加到 `DEFAULT_EDITOR_KEYBINDINGS` 或 `DEFAULT_APP_KEYBINDINGS`，以保持可配置。
- 不要直接修改 `packages/ai/src/models.generated.ts`；应更新 `packages/ai/scripts/generate-models.ts` 后再重新生成。包含重新生成的 `models.generated.ts` diff 总是可以接受的，即使其中包含无关的上游模型元数据变更。

## 命令

- 代码变更后（非文档）：运行 `npm run check`（输出看完整，不要 tail）。在提交前修复所有 error、warning、info。不运行测试。
- 除非用户要求，否则不运行 `npm run build` 或 `npm test`。
- 不要直接运行完整 vitest 套件：它包含在存在端点/认证环境变量时自动激活的 e2e 测试。所有非 e2e 测试从仓库根运行 `./test.sh`。否则从包根运行指定测试：
  - Vitest：`node "$(git rev-parse --show-toplevel)/node_modules/vitest/dist/cli.js" --run test/specific.test.ts`
  - `packages/tui`（`node:test`）：`node --test test/specific.test.ts`
- 如果创建或修改了测试文件，运行它并迭代到通过。
- 对于 `packages/coding-agent/test/suite/`，使用 `test/suite/harness.ts` + faux provider。不调用真实 provider API、key 或付费 token。
- issue 专项回归测试放在 `packages/coding-agent/test/suite/regressions/`，命名为 `<issue-number>-<short-slug>.test.ts`。
- 临时脚本用 `write` 写到临时文件（如 `/tmp`），运行、按需编辑、结束后删除。不要把多行脚本嵌入 `bash` 命令。
- 除非用户要求，否则不提交。

## 依赖与安装安全

- 将 npm 依赖与 lockfile 变更视为需要 Review 的代码。直接外部依赖保持固定精确版本。
- 更新 `undici` 时，必须阅读目标版本的 changelog/release notes，评估是否会影响功能后再应用。
- 本地更新用 `npm install --ignore-scripts`；干净/CI 风格用 `npm ci --ignore-scripts`。除非用户要求，否则不运行生命周期脚本。
- 如果依赖元数据变更，用 `npm install --package-lock-only --ignore-scripts` 刷新 `package-lock.json`。
- 如果 `packages/coding-agent/npm-shrinkwrap.json` 需要重新生成，运行 `node scripts/generate-coding-agent-shrinkwrap.mjs`（用 `--check` 或 `npm run check` 验证）。新增带生命周期脚本的依赖需要 Review 并在该脚本中显式加入白名单；不要静默添加。
- 预提交默认阻止 lockfile 提交，除非设置 `PI_ALLOW_LOCKFILE_CHANGE=1`。除非用户希望提交 lockfile 变更，否则不要绕过。

## Git

同一工作目录下可能同时运行多个 ASu Agent 会话，各自修改不同文件。触及你自己变更之外的未暂存、已暂存或未跟踪文件的 Git 操作会破坏其他会话的工作。遵循以下规则：

提交：

- 只提交**当前会话中你自己改动**的文件。
- 使用显式路径暂存（`git add <path1> <path2>`）；禁止 `git add -A` / `git add .`。
- 提交前运行 `git status`，确认只暂存了你的文件。
- `packages/ai/src/models.generated.ts` 可以始终与你的文件一起包含。
- 提交信息格式：`{feat,fix,docs}[(ai,tui,agent,coding-agent)]: <commit message>（可多行）`。信息要简洁、有意义。

永远不要运行（会破坏其他 agent 工作或绕过检查）：

- `git reset --hard`、`git checkout .`、`git clean -fd`、`git stash`、`git add -A`、`git add .`、`git commit --no-verify`。

如果发生 rebase 冲突：

- 只解决你修改过的文件中的冲突。
- 如果冲突发生在你未修改的文件中，abort 并询问用户。
- 不要 force push。

## Issues 与 PRs

贡献者门槛见 `CONTRIBUTING.md`（自动关闭工作流、`lgtm`/`lgtmi`、质量门槛）。

Review PR 时：

- 除非用户明确要求，否则不要运行 `gh pr checkout`、`git switch` 或把 worktree 切到 PR 分支。
- 使用 `gh pr view`、`gh pr diff`、`gh api` 以及针对已拉取引用的本地 `git show`/`git diff` 来检查 PR 元数据、commit 和 patch，不切换分支。
- 如果需要 PR 文件内容，把它们取到临时文件，或使用 `git show <ref>:<path>` 而不切换分支。

创建 issue 时：

- 为受影响的包添加 `pkg:*` 标签（`pkg:agent`、`pkg:ai`、`pkg:coding-agent`、`pkg:tui`）；适用的都加上。

发布 issue/PR 评论时：

- 把评论写到临时文件，然后用 `gh issue/pr comment --body-file` 发布（不要通过 `--body` 传多行 markdown）。
- 保持简洁、技术性、使用用户的语气。
- 每条 AI 发布的评论结尾加上原始 prompt 指定的 AI 生成声明行（例如 `This comment is AI-generated by `/wr``）。

通过 commit 关闭 issue 时：

- 在提交信息中包含 `fixes #<number>` 或 `closes #<number>`，合并后会自动关闭 issue。多个 issue 要重复关键词（`closes #1, closes #2`）；共享关键词（`closes #1, #2`）只会关闭第一个。

## 用 tmux 测试 ASu Agent 交互模式

在受控终端中运行 TUI（从仓库根目录）：

```bash
tmux new-session -d -s asu-test -x 80 -y 24
tmux send-keys -t asu-test "./asu-test.sh" Enter
sleep 3 && tmux capture-pane -t asu-test -p     # 启动后捕获画面
tmux send-keys -t asu-test "your prompt here" Enter
tmux send-keys -t asu-test Escape               # 特殊按键（C-o 表示 ctrl+o，等）
tmux kill-session -t asu-test
```





## 发布

**Lockstep 版本控制**：所有包共享一个版本；每次发布一起更新。`patch` = 修复 + 小功能，`minor` = 破坏性变更。没有 major 发布。

1. **更新 CHANGELOG**：询问用户是否已经在 `main` 最新 commit 上运行过 `/cl` prompt。如果没有，他们必须先运行 `/cl` 审计并更新每个包的 `[Unreleased]` 分区，然后再发布。

2. **本地冒烟测试**：在仓库外构建未发布 release 并做冒烟测试（这样它无法解析 workspace 文件）：
   ```bash
   npm run release:local -- --out /tmp/asu-local-release --force
   cd /tmp

   # Node 包安装冒烟测试

<!-- Content truncated to meet Windsurf 6KB limit -->

---
> Source: [Hisn00w/Asu](https://github.com/Hisn00w/Asu) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:windsurf_rules:2026-08-15 -->
